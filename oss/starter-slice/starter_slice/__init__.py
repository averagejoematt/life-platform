"""starter-slice — one source, one bucket, one table, one chart.

A teaching slice of the architecture behind averagejoematt.com. Standard library
only; `boto3` is imported lazily and is needed only for the AWS backend.
"""

__version__ = "1.0.0"
