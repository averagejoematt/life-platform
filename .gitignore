# Lambda deployment zips and tarballs (regenerated by deploy scripts)
*.zip
*.tar.gz

# Personal health data exports — never commit to GitHub
datadrops/
memory_export/

# Lambda backups — git is the backup now
lambdas/backup_*/

# Dashboard generated data files (contain personal health data)
lambdas/dashboard/data.json
lambdas/dashboard/clinical.json

# Lambda deployment artifacts left in lambdas/
lambdas/*.py.backup*
lambdas/*.py.broken

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Local output / scratch files
output.json
ingest/ingest.log
ingest/*.log

# Throwaway analysis scratchpad (may hold personal food/health data — never commit)
scratch/


# Virtual environments
venv/
.venv/
env/

# CDK build artifacts
cdk/cdk.out/
cdk/.venv/
cdk.out/
layer-build/
cdk/layer-build/

# Node
node_modules/
**/node_modules/

# Tool caches (regenerated; each writes its own internal .gitignore, but pin here too)
.mypy_cache/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Secrets (never commit these)
*.env
.env
secrets.json
token*.json
.config.json

# Visual QA artifacts (regenerated per run; auth cookie is sensitive)
qa-screenshots/

# Stale committed QA report snapshot (regenerated, unreferenced — untracked 2026-06-08)
deploy/qa_report/

# Experiment-restart pipeline run-reports + scratch grep dumps (regenerated on EVERY
# reset run — the scripts write here by design, RUNBOOK §restart). Tracking them was
# pure churn: a dry-run dirtied 9 of them. Keep the DIRECTORY and the hand-written
# RESET_*/RESTART_*/README docs + leadin_backups/; ignore the generated _*.txt outputs.
# (untracked 2026-07-13; same rationale as deploy/qa_report/ above)
docs/restart/_*.txt
docs/restart/_*.log

# Local scratch / superseded harnesses (PDF builder venv, pre-v4 page captures)
show_and_tell/
captures/

# Personal backup snapshots — not project code, may contain sensitive material
archive/051726_downloadsfolder_backup/

# Backup files (use git instead)
*.bak*
*.sw[po]

# Session briefs (ephemeral, regenerated per session)
CLAUDE_CODE_BRIEF*.md
docs/CLAUDE_CODE_BRIEF*.md
cdk/_mcp_staging/

# pytest-cov local artifacts
.coverage
.coverage.*
coverage.xml
htmlcov/

# ER-06: personal PII denylist — real literals (partner name, employer, …) never
# committed to this PUBLIC repo. Only config/pii_denylist.example.json is tracked.
config/pii_denylist.local.json
cdk/_bundle_staging/
