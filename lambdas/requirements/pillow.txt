# pillow-layer — binary dependency layer (python3.12, x86_64)
#
# GENERATED — do not hand-edit. Regenerate with:
#   python3 deploy/build_lambda_layer.py --promote pillow --from-build <build.json> --layer-version <N>
# Spec: deploy/build_lambda_layer.py::LAYERS['pillow'] · drift gate: --check-manifest (#2099)
#
# Pillow — PNG/WebP share-card rendering and book-cover processing.
# Attached to: og-image-generator, reading-cover-pipeline (CDK stack: LifePlatformOperational).
# Layer ARN: cdk/stacks/constants.py::PILLOW_LAYER_ARN.
#
# WHY THIS FILE EXISTS (#1336): third-party code reaches a running Lambda ONLY via
# these binary layers — no deploy path pip-installs from lambdas/requirements/. So
# this manifest is the sole surface pip-audit can scan for the layer's contents, and
# editing it does NOT change what is deployed (#1778/#1780). A real upgrade is:
#   build_lambda_layer.py build pillow -> publish-layer-version -> bump
#   PILLOW_LAYER_VERSION -> cdk deploy LifePlatformOperational -> --promote back to here.
#
# ── BUILD TARGET — top-level pins the next build installs (spec, hand-edited there)
#   layer-build-target: Pillow==12.3.0
#   layer-build-platform: manylinux2014_x86_64 manylinux_2_28_x86_64 · cp312
#
# Pillow 12.x publishes manylinux_2_27/_2_28 wheels only — a manylinux2014-only
# build resolves nothing. Both tags are listed; glibc 2.34 on the AL2023-based
# python3.12 runtime satisfies manylinux_2_28.
#
# ── DEPLOYED — the full resolved contents of the LIVE layer version below.
#   pillow-layer:2, measured 2026-08-08 read-only via
#   `aws lambda get-layer-version-by-arn` + inspection of the returned zip's
#   *.dist-info. Ground truth, not an estimate. Transitive packages are listed
#   explicitly so pip-audit sees the WHOLE layer, not just its top-level pins.

pillow==12.3.0
