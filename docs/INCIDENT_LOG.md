# Life Platform — Incident Log

> **Status:** log · **Owner:** Matthew · **Verified:** 2026-05-19

Last updated: 2026-08-31 Session O launch session (#1332 gate; +2 rows — the launch-eve rollback that reverted wanted prose over an /api red born of the reset's own-path deploy gap (#3395/#3396), and the double #2899-class docs-only red). Prior: 2026-08-31 financial-diligence session (#1332 gate; +1 row — the compute-pipeline-stale 24h flap that fired and cleared between wraps, caught by the #2912 flap detector at the wrap, not by observation; not root-caused — diligence session, review+design scope). Prior: 2026-08-31 Session N (#1332 gate; +2 rows — a production-gate lease stranded 4.9h by the session's own exiting lease loop, caught by deploy-wedge-watch dispatching the remediation agent, fixed with a persistent session steward; and Session M's wrap line (`CLAUDE.md:213: $200`) redding every PR's full suite while main's badge stayed green behind a path-filter skip, fixed by PR #3359 + update-branch ×4). Prior: 2026-08-31 Session M (+2 rows — the 7-second asset race in the site sync, PR #3349) · previously Session L ambitious-drain session (#1332 gate; **+1 new row** — the remediation-role trust policy widened to any-branch for ~6 min by applying a grant through a stale shell twin of the checked-in IAM JSON, caught by `verify_oidc_iam.py`, restored from the JSON, structural fix #3336). Prior: 2026-08-27 Session G cost-cliff + class-fix session (#1332 gate; **+4 new rows** — a site auto-rollback that reverted Session F's own published build beat over a DynamoDB-sourced narrative defect it was structurally incapable of fixing, reporting success on every step while the defect stayed live (and the `config/` half of the same asymmetry is worse, because `config/` IS repo-versioned and WAS shipped by that workflow); main red at boot while the outgoing handover declared it green, all 10 full-suite failures tracing to one wrap-prose line, and unclearable by its own docs-only fix because of #1908's paths-filter trap — which a lane then reintroduced hours later and #1908's guard caught; two production deploy leases rejected as stale, the second OLDER than the manual deploys already live so approving it would have regressed production; and the deploy-wedge watcher's first true positive, which measured that its declared `*/15` cadence has been delivered 0 times in 99 and the 17-minute safety property its own comment asserts has held 1%). Prior: 2026-08-27 Session F overnight net-negative drain (#1332 gate; **+5 new rows** — a production deploy lease stranded 7.5h whose approval would have ROLLED BACK two fixes deployed live the same session (#1901 class, rejected with decode); main red twice on the same census literal while the auto-reconcile job that owns it reported success both times, root-caused to a missing PyYAML in that job's env — #3156's second home, fixed by #3234 and proven live within the hour; issue #3222 falsely auto-closed by a stray `Fixes` that arrived via a SHARED scratchpad path between two concurrent agent lanes, not a mistyped PR number; PR #3231 shipping half-broken with all 12 of its tests green, its only symptom one line in a CI durations block; and a live inflated stale-source SNS alert from a Pacific `DATE#` day anchored at UTC midnight, a defect #3196's own sweep introduced and both matchers are structurally blind to). Prior: 2026-08-27 Session E drain-and-diagnose (#1332 gate; **+3 new rows** — main red ~1.5h on Session D's own #3206 timezone fix, whose fixtures stayed on a UTC clock and therefore fail 7 hours of every 24, green only because #3206's CI ran outside that window (#3222/#3223); the budget tier escalating to 2 and pausing reader coach narratives, which blocks verification of the #3202 fix shipped the same session; and five GitHub event swallows in one session against five in the whole program before it, with the head_sha-instrument folklore corrected on #3219). Prior: 2026-08-26 Session D harvest-and-drain (#1332 gate; **+4 new rows** — the dated wire box that FALSIFIED its own theory (truncation dead, 2 coaches still held on a grounding defect it was masking, #3202; the qa-smoke citation closed EARLY on measurement rather than left to expire); #3200's classified verdict shipping verdict-closed and NEVER FIRING, caught by dogfooding it within the hour (#3209, live-proved exit 4); the third phrase-matched-suppressor instance in one session gating main on a false positive (#3208, fixed structurally); and the near-miss where remediation advised applying a never-applied ruleset that would have wedged the reconcile push on every merge (#3207)). Prior: 2026-08-26 Session C landing-and-drain (#1332 gate; **+2 new rows** — the #3198 site auto-rollback that reverted its own fix on a reader-truth judge flake over a TRUE claim (ground-truthed; #3199 owns the demotion rules, both instances baselined, fresh dispatch green); and Session B's wrap push event-swallowed with ZERO runs, landing stale derived Patterns blocks that only the first Session C PR suite surfaced (fixed on main + 5 branches; swallow-check-every-push is now the reflex)). Prior: 2026-08-25 Session B overnight drain (#1332 gate; **+3 new rows** — the ~13h GitHub synchronize-event swallow that degraded the per-PR merge machinery (recovery vocabulary: close/reopen mints, supersede-PR for stale heads; drained via the 14-lane integration train #3184); a 16.4h stranded production lease whose approval would have REGRESSED the fleet (rejected with decode; the train-sha lease approved as idempotent re-apply); and the mid-train main red at 2217507 (counter transient + Plan-red, superseded) folding in the boot-time Docs CI red (the UTC/PT stamp divergence, killed structurally)). Prior: 2026-08-25 Session A D3+foundation session (#1332 gate; **+2 new rows** — the 20:43Z merge-train site auto-rollback on ONE novel truth high while the statics ran ahead of the not-yet-deployed site-api (the documented API-before-frontend mixed-state class; full rerun green at 23:22Z once the API deployed, now with #3102's confirm-before-gate in the lane); and the Collect-lane 10m job-timeout episode — GitHub renders a timeout kill as `cancelled`, so 3 PRs' lanes were re-run into the same wall for ~1.5h before the annotation was read; ceiling re-derived 10m→15m from 10 measured runs, #3141). Prior: 2026-08-24 D2 truth-manifest + drain session (#1332 gate; **+3 new rows** — two merge-train site auto-rollbacks each on one novel truth high (a #2957 member baselined + a real UTC-week front-end bug fixed, PRs #3087/#3095; the single-run-verdict exposure filed as #3102); the deploy-critical lane red ~1.5h on the recurring undeclared-PyYAML collection class (PRs #3100/#3105); and a ~15.5h production-lease chain from the overnight merge train, both leases rejected with decode at the wrap). Prior: 2026-08-23 D1 control-boundaries session (#1332 gate; **+1 new row** — the wrap-beats site auto-rollback on a TRANSIENT data-state AA-contrast violation at `/method/board/`: the coach payload regenerated 6 min after the sweep and the same check passed; class filed as #3057 (data-supplied coach colors carry no AA clamp), full rerun green). Prior: 2026-08-23 D0 P0-truth session (#1332 gate; **+1 new row** — the receipts-re-deploy auto-rollback: two novel oracle highs on genuinely-unlabeled cross-phase surfaces, baselined as #2957 debt via the sanctioned path, next dispatch green end-to-end). Prior: 2026-08-23 campaign→diligence session (#1332 gate; **+1 new row** — the #3038 site auto-rollback: the #2959 non-stationary oracle tail raised a novel WEEK-1-chronicle high on /story/, deploy content (receipts caption) orthogonal, healthy prior build served; aggravated by the visual-qa CI role's missing ssm:GetParameter grant that darkens the cycle ground-truth feed in the gating path — both fold to epic #3042). Prior: 2026-08-23 fable-week council-plan session (#1332 gate; **+2 new rows** — the 3x oracle-blocked publish path (2 auto-rollbacks; rubric-instructed class + #2959 non-stationarity, both structurally answered same-session by #3028/#3029), and the #3030 one-clock QA race (Day-6 screenshots judged against a Day-7 phase across midnight PT, no rollback, fixed by #3031)). Prior: 2026-08-23 fable-week Lane A session (#1332 gate; **+1 new row** — the main-red chain from two queue-surfaced structural gaps, both guarded permanently in-session: the reconcile whitelist had the writer but not the write (#3017), and a real red rode into a merge behind the expected doc-literal category (#3016), with #3013's full-suite-only unit tail (#3020)). Prior: 2026-08-22 opus max-paydown session (#1332 gate; **+4 new rows** — a documented signal silently dropped at the source for 6 days (the 15 BodyScan segmental fields were never written; the fixture was not the wire); a post-deploy visual-QA red on two NEW reader-truth findings the deploy did not cause; the reader-truth `report.json` storing its evidence truncated; and a production-approval lease found waiting 1.3h by the wrap gate rather than by anyone watching). Prior: 2026-08-22 opus machinery-first session (#1332 gate; **+2 new rows** — post-deploy visual-QA red on two NEW reader-truth findings the deploy did not cause, with `/method/board/` among the 91 passes; and a ~5 min Docs CI red from a `test_count` stamp dropped during a rebase and healed unattended by the reconcile bot). Prior: 2026-08-22 opus plan-then-execute session (#1332 gate; **+2 new rows** — main red 1h58m on two independent stale-derived-artifact defects, the second landing on an innocent commit (lane-placement defect filed as #2975); and two production-approval leases rejected as superseded after the deploy was done manually and verified). Prior: 2026-08-22 opus autonomous drain session (#1332 gate; **+3 new rows** — three consecutive site auto-rollbacks from an un-baselined reader-truth gate, fixed structurally by #2956/#2970; main red on the wrap's own INCIDENT_LOG edit; and the 08-21 wrap commit re-closing #2921/#2578 by narrating the accident that closed them). Prior: 2026-08-21 opus epic-tails session + post-wrap (#1332 gate; **+4 new rows** — the site auto-rollback where the gate was RIGHT (the newly-armed reader-truth check caught a live wrong Day number on its first run, #2941); two stranded deploy-approval gates auto-filed as #2937 and cleared by rejection; both AI gates on the deploy path structurally dark while the job reported success (#2938); and a live impossible `light_pct: 106.7` on `/api/sleep_detail` (#2939)). Prior: 2026-08-19 opus backlog-drain session (#1332 gate; **+2 new rows** — main red 29m on an `aws_cdk` import CI does not install, a green-local/red-CI collection abort fixed by #2907; and a 6h22m production-deploy wedge behind a stranded approval gate, auto-detected by the platform as #2901 and cleared by rejecting two superseded leases). Prior: 2026-08-19 opus September-base session (#1332 gate; **+1 new row** — main red 1h02m on the $150 ceiling merge, four deselected stale-literal test failures, deploy gate rejected rather than approved, fixed by #2896). Prior: 2026-08-18 opus deploy-unblock session (#1332 gate; **+0 new rows — no incident-class event this session**; amended the 2026-08-18 site-auto-rollback row's resolution: it was closed the same day by #2879 WITHOUT a weigh-in, because the day-number proxy — not the missing Withings row — was the defect). Prior: 2026-08-18 opus observability session (+2 rows: the wrap-beat site auto-rollback — a TRUE positive about the data and a FALSE positive about the deploy, the vitals smoke carve-out keys on day_n not on whether a weigh-in happened, so every site deploy auto-rolls-back until one lands (#2878, owner-gated on a weigh-in); and a merge to main that produced ZERO CI runs — the swallowed-push class, second documented occurrence, recovered by manual dispatch). Prior: 2026-08-17 system-model session (+2 rows). Prior: 2026-08-17 conformance-guard session (+2 rows). Prior: 2026-08-17 charter/cycle-14 session (+1 row). Prior: 2026-08-16 overnight session (see history below).

> Tracks operational incidents, outages, and bugs that affected data flow or system behavior.
> For full details on any incident, check the corresponding CHANGELOG entry or handover file.

---

## Severity Levels

| Level | Definition |
|-------|------------|
| **P1 — Critical** | System broken, no data flowing or MCP completely down |
| **P2 — High** | Major feature broken, data loss risk, or multi-day data gap |
| **P3 — Medium** | Single source affected, degraded but functional |
| **P4 — Low** | Cosmetic, minor data quality, or transient error |

---

## Incident History

| Date | Severity | Summary | Root Cause | TTD* | TTR* | Data Loss? |
|------|----------|---------|------------|------|------|------------|
| 2026-08-31 | **P3** (site auto-rollback on a false-for-site trigger; wanted content reverted ~40 min) | **The launch's D2 site-deploy (run 33459065966) failed ONE smoke check — `/api/vitals` weight arbitration — and the auto-rollback reverted PR #3392's two-line prose binding, reporting success; visual QA had PASSED.** | Two stacked causes, both filed: the #2878 predicate read "in-cycle weigh-in exists" because `/api/source_freshness` was still serving genesis 2026-08-17 — the cycle-15 reset's `cdk deploy --all` does not ship site-api's OWN-PATH bundle (#3396), so the public API ran old constants while vitals ran new; and the rollback's SMOKE leg carries no reachability scope (#3352 fixed only the visual leg), so an /api-class red reverts site content it cannot fix (#3395). | ~15 min — the failed run read directly at the wrap of the merge train. | ~40 min — site-api redeployed via its sanctioned path (postflight sha green), attended `sync_site_to_s3.sh` re-landed the merged tree, full smoke 246/246; the beat's final site-deploy then passed every gate incl. the previously-failing check. | No — content only; the reverted two lines were re-shipped within the hour. |
| 2026-08-31 | **P3** (main badge red ~2.5h; every PR's suite red ~1h — two #2899-class specimens in one day) | **Main's CI/CD badge red from 16:48Z (a doc-facts budget line) while two docs-only fix commits path-filter-skipped the suite; independently, every open PR failed `test_operating_knowledge_ledger_2848` because the previous wraps added two memory-ledger rows without the snapshot/coverage update — main never ran the test (path filter), so main read green while all PRs were red.** | The #2899 class both times: a tree-evaluated gate tripped by docs-only commits that never run main's suite. Aggravated by the rerun trap: `gh run rerun --failed` re-tests the run's ORIGINAL merge commit, so post-fix reruns failed identically until `gh pr update-branch` minted fresh merge refs (memory: reference_rerun_reuses_the_original_merge_commit). | ~30 min — boot pre-flight named the badge; the PR-wide red surfaced on the first wave-1 suites. | ~1h — ledger snapshot reconciled on main (42f4dfc22), branches updated, badge re-minted green by the first train merge. | No. |
| 2026-08-31 | **P4** (single 24h stale-compute episode, self-cleared before it was seen) | **`compute-pipeline-stale` flapped: one ALARM episode 2026-08-30 15:57Z → 2026-08-31 15:57Z, fired AND cleared between wraps — surfaced only by the (e10) flap detector (#2912), never by observation.** | Not root-caused this session (financial-diligence session, review+design scope): one daily compute cycle read stale and the next cycle cleared it; episode window read from `describe-alarm-history`. | n/a — alarm fired at stale-threshold as designed. | ~24h — self-cleared on the next compute cycle; no intervention. | None observed (alarm cleared on fresh compute output). |
| 2026-08-31 | **P3** (ops — a held deploy slot, ~4.9h; nothing shipped wrong) | **A production-gate lease stranded for 4.9h by the driver's own tooling, caught by the platform's dead-man, not by the operator.** Run 33374549427 (`9507d01f7`, the #3360 merge) reached the `production` gate ~09:05Z and sat there until `deploy-wedge-watch` fired at 13:53Z (run 33399438265), auto-filed #3363 and dispatched the remediation agent (run 33399467194, `kind: stranded-approval`, the 13:58Z "12 need you" email). | The session's hand-rolled lease loop had been written to EXIT once the run at main's tip was waiting (so the driver could approve by hand); it approved `7edaa94cf` at 08:15Z and stopped. The next merge minted a new lease nothing watched, and the session was idle for hours (background waiters returned late). The sanctioned `watch_deploy_gate.sh` was not the answer either — it approves every young waiting run and would have approved an ANCESTOR after the tip. | ~4.9h — by the platform (`deploy-wedge-watch` → remediation dispatch → #3363), read by the driver at 14:2xZ. | ~2 min — the lease was REJECTED with decode (its sha was by then an ancestor of the tip, #2467 rule); a persistent session steward (reject ancestors / approve the tip, `Monitor persistent`) replaced the exiting loop; #3363 closed by hand — `deploy-wedge-watch` has no recovery leg (not #1447-wired), named as the residual there. Memory: `reference_lease_steward_must_outlive_the_tip`. | No. Nothing deployed from the stale run; the tip had been approved and was live. |
| 2026-08-31 | **P4** (CI — every PR's full suite red ~1h; main's badge green throughout) | **Session M's wrap block in `CLAUDE.md` tripped `test_doc_facts_budget_2899::test_live_tree_states_no_retired_ceiling_after_the_window_reverts` (`CLAUDE.md:213: $200`) on every PR opened at Session N's boot (#3355, #3356, #3357 — one identical red each) while `check_main_green` reported GREEN.** | The test evaluates the live tree *as of 2026-09-02* on every run — deterministic, not clock-dependent — and the wrap line named the retired August ceiling as current truth. The wrap commit `47f960b42` was docs-only, so ci-cd.yml's `paths:` filter declined it and main's own CI never ran the test on that tree (#1908 class): a red-in-waiting that every PR's merge ref inherited. | ~15 min — first PR watcher verdict read against the failing test name; the shared single cause across three unrelated PRs was the tell. | ~25 min — PR #3359 reworded the line (no bare retired figure), then `gh pr update-branch` on the four open PRs so their merge refs picked it up (one fresh PR-checks cycle each). Reflex added to memory: run the doc-facts gate on the FINAL wrap tree — the wrap block is exactly the text no CI runs on. | No. |
| 2026-08-31 | **P1** (reader-facing, ~22 min) | **The entire `/data/*` door (20 reader pages) served as `application/json` after the #3350 site deploy (run 33357672930, 04:44Z) — and again after the auto-rollback re-ran the same script.** The visual-QA gate read 20 pages with no `<title>`/`lang`/viewport, `js_bytes 0`; the rollback "succeeded" and re-broke it identically. Restored at 05:1xZ by an in-place `aws s3 cp --metadata-directive REPLACE --content-type text/html` over `site/data/**/*.html` + a `/data/*` invalidation. | PR #3349 (the same night's assets-first reorder) moved the "Data JSON" sync — `aws s3 sync $BUILD_DIR/data/ … --content-type application/json`, no `*.html` exclude — AHEAD of the HTML sync. `site/data/` also holds the door's `index.html` pages; the JSON step now uploaded the 20 changed pages as JSON and the HTML sync skipped them as unchanged. The overlap was latent for months, masked only by the old order. Self-inflicted by the driver; the reorder was tested for ORDER, not for what each step's include set covers. | ~20 min — the gate caught it on the first probe; the driver read the failure reasons ~15 min later. | ~2 min — in-place content-type re-copy + invalidation; script fix in the follow-up PR (`--exclude "*.html"` on the JSON sync, mutation-proven guard). **The second half — the rollback re-running the defect — is #3352:** the visual-QA job now exports a machine-readable surface verdict and `rollback-site-on-failure` scope-checks it, so this exact shape (page shell not `text/html`) classifies `deploy-script`, the rollback DECLINES by name instead of re-syncing, and a tracked issue is filed. | No. HTML bytes were intact throughout; only the Content-Type metadata was wrong. The #3350 CSS fix was reverted by the rollback and re-lands with the next site deploy. |
| 2026-08-31 | **P3** (false red, deploy correct) | **43 pages read red in the ci-cd Visual + AI-vision QA (run 33353122904) with `Refused to execute script … motion.15f49da6.js … MIME type text/html` — a cached 404 for the new hashed asset, reproduced on the 15-second confirm re-probe.** The #3277 site deploy (run 33354711931) had uploaded the HTML at 03:43:18 and the hashed JS it referenced at 03:43:25; the edge cached the 404 for requests inside that window and the QA probed inside it. The site was correct seven seconds after the HTML landed (`X-Cache: Hit` on the same URL minutes later). | `deploy/sync_site_to_s3.sh` Phase 2 synced HTML BEFORE the hashed CSS/JS it references — #2978 shape (a), the asset race, by construction; the ci-cd QA path carries no convergence check (`VISUAL_QA_EXPECT_BUILD` unset, "un-raced-checked" by design). | ~30 min — the run's failure was read at the next lease check; the 43 identical reasons clustered in one grep. | ~25 min — root-caused from the deploy log timestamps; PR #3349 reorders Phase 2 (assets → … → HTML last) with a mutation-proven order guard in `tests/test_deploy_convergence_2978.py`. Datum posted on #2978. | No. Nothing rolled back (rollback's `needs` excludes the visual-QA job); the deploy was correct. |
| 2026-08-30 | **P2** (security, ~6 min exposure) | **Applying a one-line additive IAM grant through `deploy/setup_remediation_role.sh` — a hand-maintained TWIN of `infra/iam/github-actions-remediation-role.{permissions,trust}.json` — regressed 4 grants and WIDENED the role's trust policy from `ref:refs/heads/main` to `repo:…:*` (any branch/PR could assume the remediation role: Bedrock + read-only diagnosis + scoped S3 writes).** The #3278 lane staged the grant in the JSON and named the script as the apply path; the script applied its own stale inline documents instead. | Two copies of a security-bearing document with no derivation guard between them; the copy that RUNS was the stale one, and `tests/test_security_log_retention_3278.py:461` pinned the grant string INSIDE the twin, cementing it. | ~6 min — `deploy/verify_oidc_iam.py` run immediately after the apply named both drifts. | ~2 min — both documents re-applied from the JSON (`put-role-policy` + `update-assume-role-policy`); verify CLEAN. Structural answer → #3336 (P1: script applies the JSON verbatim, contract test bans inline policy literals, CloudTrail check of the window). | No. No `AssumeRoleWithWebIdentity` from a non-main ref was observed in the window at wrap (driver's read; #3336 confirms via CloudTrail). |
| 2026-08-30 | **P2** | **A production deploy stalled ~9 hours behind a stranded lease the session could not see — two CI/CD runs minted 8 seconds apart at a merge train, the OLDER reached the `production` gate first and sat `waiting` holding the deploy-group lease, while the newer (union) run's Deploy job sat `pending` behind it, structurally unable to mint its own approval request.** The session's gate-watch polled `pending_deployments` on the NEWER run — an empty list, forever — and its until-loop was silent by construction, so 'blocked' was indistinguishable from 'still waiting'. Found only when the owner asked why running items showed 9 hours. Disposed in minutes once seen: older lease rejected with decode (strict ancestor of the union sha), union approved, deploy+smoke+visual-QA green same hour. | Two gaps stacked: (1) the land ritual said dispose 'the' lease, singular — nothing instructed enumerating ALL non-completed CI/CD runs at merge time, and a train that pushes N squashes seconds apart mints up to N runs; (2) **the #3021 lease janitor — built, running, and the exact machine fix for this case — has been dark on its rejection path since it shipped**: every attempted rejection 4xxes for want of `DEPLOY_GATE_JANITOR_TOKEN` (a fine-grained PAT of the required reviewer, Actions read/write), loudly redding its own job each sweep. Detection existed twice over; disposal existed zero times. | ~9h (owner-initiated: 'are we stalled?'). | Same hour once seen. Class fixes same session: land skill §3 rewritten to enumerate-ALL-runs-after-every-merge (structural — a run count, not a phrase); janitor token surfaced to the owner with click-path while they were already in the PAT UI; watch-hygiene rule recorded (no silent until-loops on external conditions — bounded timeout that escalates). | No. |
| 2026-08-27 | **P2** | **A site auto-rollback fired, reported success, deleted a wanted content change, and could not possibly have fixed the failure that triggered it.** Site deploy `33079187092` on Session F's wrap commit `da1f9dd5`. Deploy succeeded; the commit touched exactly ONE file under `site/` — `site/story/build/beats.json`, +31 lines, Session F's own published build beat. Visual+AI QA then failed on one REPRODUCED high: `/coaching/by-coach/` showed Dr. Max Reyes saying "I'm ten days into this restart with you — Day 10 as of today" on Day 11. Auto-rollback ran `git checkout <ref> -- site/` + `sync_site_to_s3.sh`, reported success on every step, and reverted the build beat. Live `/version.json` read `1d5513b` for ~2.5h. The failing narrative is served from **DynamoDB** via `/api/coach_analysis`; the rollback's entire mutation surface is `site/` objects in S3. Disjoint. Re-pulled after the rollback completed: still `regeneration_paused: true`, still `generated_at 2026-08-26T17:02:28Z`, still Day 10 — **the defect was live throughout.** | ADR-125 tier 2 pauses narrative regeneration, and the frozen text carries an ABSOLUTE day number plus the words "as of today", so it goes stale by one more day for every day the pause holds — a self-worsening ADR-104 violation. The rollback's scope is `site/` by construction, so for any failure sourced from `/api/*` it is structurally incapable of remediation while still reporting success. Worse and unfixed: the deploy ALSO syncs bucket-root `config/` (`config_twin_sync.py --apply --strict`), which the site-api reads at runtime, and the rollback does not cover it — so a bad `config/` twin survives a "successful" rollback, and unlike the DynamoDB case `config/` IS repo-versioned and WAS shipped by that same workflow. | ~14h (the gate itself; nobody was looking for the underlying defect). | Trigger fixed same session in PR #3243 (merged `fe698334e`) by datelining the paused read in the frame its own prose uses — the pause untouched, nothing regenerated, zero Bedrock spend. Build beat confirmed back on the live site. Rollback-scope class folded to #2799 as two checkboxes; OPEN. | No — content only, self-healed on the next deploy from newer main. |
| 2026-08-27 | **P3** | **Main was red at session boot while the outgoing handover declared it green, and the docs-only fix could not clear it.** The latest completed CI/CD run on `main` had FAILED at the coverage-gate step; all **10** full-suite failures traced to ONE root cause — `check_doc_facts` exiting 1 because Session F's own wrap prose in `CLAUDE.md:238` read "#3231 shipped half-broken with all 12 tests green", which the gate parsed as a live claim that the suite holds 12 tests (truth: 17,664). A second, independent residual rode along: the INCIDENT_LOG Patterns block was stale at 184 dated / 149 post-June while F's gate line claimed the derived block had been regenerated. | Two Session-F wrap residuals. The reason it persisted is the more useful half: **#1908's trap, live.** The fix is by definition a `docs/**` edit, `docs/**` is not in `ci-cd.yml`'s `paths:` filter, so the docs-only fix at `25205ce5` did not re-run CI/CD and the stale red stood until diagnosed by hand. Hours later a lane's first attempt at the #3234 class fix reintroduced that exact trap by adding a doc gate to the reconcile job, and #1908's own guard (`test_docs_ci_owns_doc_gates.py`) caught it. | ~6h (found by the boot gate pre-flight, not by a page). | ~15 min: prose reworded to drop the bare number, Patterns block regenerated via `scripts/incident_log_patterns.py --apply`; Docs CI green on the wire at `25205ce5`. | No. |
| 2026-08-27 | **P3** | **Two production deploy leases rejected as stale; approving the second would have REGRESSED live Lambdas.** Lease 1 (`33094047251`, sha `d2c8ce27`) sat 6 commits behind main and would have shipped a tree without #3239's CGM absence semantics. Lease 2 (`33094295362`, sha `69a35909`) was found by the wrap's own (e2) gate at 1.3h, approaching the #1901 stranded class — and was **older than the manual deploys already live at `e06d88dc`**, so approving it would have shipped an older tree over current code. Both rejected with a written decode; a third lease at current main (`6a6ef3a1f`) was APPROVED rather than left waiting. | The same #1901 class as Session F's 7.5h stranded lease, twice in one session. Root shape: a lease is minted per merge, so a session that merges 8 PRs and deploys manually in between accumulates leases that are stale by construction — and the default posture (leave it waiting) is the one that strands them. Standing rule reaffirmed: approve or REJECT on sight, never leave waiting, and always decode the sha against what is actually live. | Lease 1 immediately; lease 2 at 1.3h by the wrap gate. | Same session — both rejected with decodes naming the sha delta and the regression risk. DEPLOY_GATE_JANITOR_TOKEN (#3021) remains the standing fix and is still unissued. | No — rejection is the safe direction. |
| 2026-08-27 | **P3** | **The deploy-wedge watcher went 10 hours without a scheduled run against a declared 15-minute cadence, and its stated safety property has held 1% of the time.** #3213's cron-freshness watcher — shipped the previous session — fired its first true positive: `deploy-wedge-watch.yml` had no `event: schedule` run inside its 8.25h deadline (cadence 0.25h + grace 8.0h), and auto-filed advisory tracker **#3237**. Measured every consecutive scheduled gap, **n = 99**, window 08-23 → 08-27: min 0.26h, median 0.71h, mean 0.83h, p90 1.18h, max 6.99h. Gaps at or under the declared 15 minutes: **0 of 99 (0.0%)**. Gaps at or under the 17-minute blind window the workflow's own comment claims to bound: **1 of 99 (1.0%)**. By session end the gap had grown to ~10h. | GitHub does not deliver `*/15` on this repo and never has. The workflow's comment states a safety property derived from a real incident — "the 2026-08-02 recurrence was cancelled at 17 minutes holding a stale sha; 15 minutes bounds the blind window below that" — and that property is false: the blind window is a median of 43 minutes with a p90 of 71 and an observed max of 7 hours. Every consumer reasoning about wedge-detection latency from that comment is reasoning from a number the platform has never achieved. The 8.0h grace in `scripts/scheduled_workflow_registry.py` was tuned just above the then-observed max and today's delivery cleared it. | First made visible by #3213's watcher, on its first live opportunity — declared-vs-delivered had never been measurable before. | Open — owner decision on #3237: make the declaration honest, or move detection to an event-driven trigger (a wedge is CREATED by a deploy, so it need not wait on cron delivery at all) and let cron be the backstop it is good at. | No. |
| 2026-08-27 | **P2** | **Main red ~1.5h on Session D's OWN timezone fix — a test that fails 7 hours of every 24 and shipped green because its CI ran outside that window.** Three tests failed at `81662a9d` (`test_data_reconciliation_behavior` x2, `test_permanence_lambda_1400::test_the_run_emits_the_heartbeat_metric`). The CODE was correct; the FIXTURES were on the wrong clock. #3206 (`5ecc3a1f`) moved `permanence_lambda.py:219` and `data_reconciliation_lambda.py:221` to `pacific_today()`; `_days_ago` and `_all_seven` stayed on `datetime.now(timezone.utc).date()`. Between 17:00 PT and PT midnight the UTC date has rolled and the Pacific one has not, so every expectation sat one day ahead of the handler: `days_silent` came back one short and a whole coverage week read YELLOW. Filed **#3222**, fixed in PR #3223 (merged `678a0598`); the 14-file sweep + re-entry guard stay OPEN on #3222, which deliberately does NOT carry `Fixes`. | A fixture computing relative to *now* but in the wrong FRAME. `_days_ago`'s own docstring explains at length why a hard-coded date is a time bomb — and then reads the wrong clock, the same bomb with a shorter fuse. The reason it survived review is the finding: #3206's CI ran ~13:00 PT, OUTSIDE the 17:00-PT-to-midnight failure window, so it was green and stayed green; the first main run to cross 00:00Z after it went red 24h later on unrelated work. A time-dependent gate exercised only outside its own failure window is not a gate. | ~24h (the next main run to cross 00:00Z). | ~50 min, same session — fixtures moved to the handler's own `pacific_today()` helper; a third slack assertion (`end < today` vs a rolled UTC today) tightened in the same pass. | No. |
| 2026-08-27 | **P3** | **Budget tier escalated 0 -> 2 at 17:00 PT, pausing reader coach narratives platform-wide — and blocking the verification of the fix shipped the same session.** New alarm `life-platform-budget-tier-escalation` (5 lit total). ADR-125 band 2 pauses `coach_narrative`, so #3202's fix (merged `81662a9d`, fleet-deployed, bundle-verified) CANNOT be proven at the 17:00Z brief while the tier holds — its last acceptance box is deliberately left unchecked. Arithmetic verified correct, not a false alarm: MTD $146.07 over 26 days projects ~$174 against August's temporary $200 ceiling = 87.1%, just past the 87% band edge. | Driven by an inflated month-to-date average, not the current rate: the 08-23 external-diligence day cost $11.15 and 08-24 $6.98, while the last two days were $3.98 and $2.63. NOT overridden — the guard computed correctly and forcing it to 0 would override a working control. Aggravating for anyone planning the override: `cost_governor` runs `cron(0 0/8 * * ? *)`, so a value set outside 16:00-17:00Z is silently rewritten before the brief. | Same session (surfaced by the wrap alarm sweep, not by a page). | Open — owner decision. The Sept 1 ceiling revert $200 -> $150 is the related cliff. | No. |
| 2026-08-27 | **P3** | **Five GitHub event swallows in one ~3h session, against five in the entire program before it.** PRs #3215 (x2), #3214, #3218 (x2): zero workflow runs at the full 40-char head sha while `gh pr checks` reported "no checks reported", each recovered by `close` -> `reopen` with runs minted 2-70s later. `pr-checks.yml` has no path filter, so a legitimate skip is ruled out. ~25 min of driver time on a mechanical ladder step. | GitHub-side event delivery, not a repo defect. Compounding on OUR side: `wait_pr_green` polls `0/N green` for the full 1800s timeout without ever naming a condition it already documents in its own header — filed **#3219** with the census and a recurrence test. Second-order: an implementer talked itself OUT of a correct swallow call on the false theory that `head_sha` queries cannot see `pull_request` runs (measured false — the query returns 5/10/5 on those shas; #3103 forbids the SHORT sha, not the full one). That correction is pinned on #3219 so the folklore does not outlive the night. | Per event: ~5 min of polling before a manual out-of-band check. | Each recovered in-session; the rate itself deliberately NOT filed (one session is not a trend, and it may be transient GitHub degradation) — census + recurrence test recorded on #3219 instead. | No. |
| 2026-08-26 | **P2** | **A dated wire box matured and FALSIFIED its own fix theory: the coach-v2 truncation was real and is dead (13-14 WARNs/brief -> 0), but 2 of 7 coaches still hold — on a DIFFERENT defect the truncation was masking.** The 17:00Z brief measured zero truncation WARNs and the gate pass-rate recovering 1/7 (08-24, 08-25) -> 5/7. `nutrition_coach` and `mind_coach` still fail BOTH attempts at score 28 / `number_grounding=ungrounded` on full untruncated text (2338/2267 chars inside the raised 1000 cap) — filed **#3202**. Consequence chain measured end-to-end: Dr. Marcus Webb IS the nutrition coach, so the gate holding his narrative is why the stale "321.0 lb" never regenerates, why qa-smoke `cross_surface:weight` stays red, and why `qa-smoke-failures` stays lit. The alarm's dated self-clearing window was therefore closed EARLY on measured evidence (re-cited to #3202 + #3204) rather than left to expire 08-27T19:00Z on a premise already disproved. | Two defects stacked on one symptom: a token-budget bug (#3190, fixed) sitting on top of a grounding bug (#3202, open). Removing the confound was the only way to see the second. Aggravating: the gate logs violation COUNTS but never `number_grounding_violations[].detail`, so WHICH number offended is invisible in CloudWatch — which is why this hid under the truncation story for three cycles. | 1 cycle — the box was dated to exactly this brief. | Measured + filed same session; #3202 owns the fix, #3083 owns the hold-vs-fail-open policy and now has unconfounded evidence. | No. |
| 2026-08-26 | **P2** | **#3200's classified verdict shipped, was verdict-closed, and NEVER FIRED — caught within the hour by dogfooding it on this session's own queue.** Run against PR #3206 (the textbook case: all 6 load-bearing checks green, sole red the Wiki-drift gate with `lambdas/web/platform_counts.py` as the only ❌ path), `wait_pr_green.sh` returned `VERDICT FAIL` (true exit 1) instead of `GREEN-WITH-RECONCILE-OWNED-RED` (exit 4). Diagnosed to ONE of six fail-closed branches: `gh run view --job <id> --log` returns a log containing every step EXCEPT the failing `Literal-drift gate` — reproducible across refetches, and inconsistent job-to-job (job 98275389042, same workflow/step/attempt an hour earlier, DOES include it — and that is the job #3200 was ground-truthed against). `gh api repos/{}/actions/jobs/{id}/logs` returns it reliably and the existing extractor parses that shape unmodified. Filed **#3209**, fixed in PR #3211, live-proved from merged main (`TRUE_EXIT=4`), then dogfooded for real — #3206 was merged ON that verdict. | Testing the pure classifier with fixtures and treating the `gh`/network plumbing as "just glue". #3200's own closing verdict named that path as the single untested one, and it broke on first contact. A feature can pass every unit test and never work. Compounding: the enrichment is fail-closed BY DESIGN, so a broken feature degrades silently to the pre-feature behaviour — there is no error, no alarm, and a broken fail-closed path looks identical to a working one. | ~1h — the deliberate dogfood, not an alarm. | Same session, with a live verdict + exit code as the acceptance. | No. |
| 2026-08-26 | **P3** (false positive) | **Post-deploy visual-qa gated main on ONE false positive: `/method/intelligence/` showing "DAYS OF DATA … 9/10" on Day 10** — honest (Days 1-9 complete, Day 10 in progress), and already covered by #2959's day-counter-bound ruling. The identical finding DEMOTED correctly on a live re-run of the same scoped sweep; only the oracle's wording differed ("States … and …" vs "Page states … with prose …"). `_DAY_BOUND_RE` demanded an explicit "only/at most/a maximum of N days" scaffold. Deploy, smoke and post-deploy integration were all green and auto-rollback correctly did not fire. The real note text was recovered from the run ARTIFACT (`report.json`), never the truncated log line. Filed **#3208**, fixed in PR #3210 by making the predicate structural (day number + a matching data-quantity from either the lexical bound OR an `N/M` fraction whose denominator matches the day ±1); driver independently re-proved 5/5 cases incl. an unrelated ratio and a wrong-category note. | **Third instance of one class in a single session.** #3199 fixed it twice hours earlier in the same file — its first `is_sparsity_objection` was measured failing live against a rephrasing with no shared keywords and was rewritten as a numeric predicate. Its own closing note named the residual: "a suppressor written against one observed string fires once." `_DAY_BOUND_RE` was that suppressor. | Immediate — the gating job. | Same session; no baseline entry added, `truth_baseline.json` stays at zero where #3199 left it. | No — the deployed fleet was healthy throughout; the red was the gate, on a false positive. |
| 2026-08-26 | **P4** (near-miss) | **The remediation sweep advised applying a ruleset that would have wedged the reconcile push on every merge — and it was nearly followed.** It reported `main-required-fast-lane` "absent … critical safety gap. Operator action: run `apply_branch_protection.py --apply` to restore." The detection is correct (live differs from declared posture); the framing and the action are both wrong. The ruleset was NEVER applied — it is declared desired posture, gated on A-Grade D0.6 and the owner's `RECONCILE_PUSH_TOKEN` PAT. Its only bypass actor is a User actor (#2198), and ci-cd.yml's reconcile job authenticates as that user ONLY with the PAT — it currently falls back to `GITHUB_TOKEN`. Applying it first would fail every post-merge reconcile push against its own required-checks rule, the exact failure ci-cd.yml's own error text documents. Caught by reading `MANAGED_WHERE_LEDGER.md` before acting. Filed **#3207**. | `deploy/github_posture.json` can only express DESIRED state — it has no way to say "declared, deliberately not yet applied, blocked on X". So a declared-vs-live comparison reports permanent drift and regenerates the same dangerous action on every Mon/Wed/Fri sweep. The caveat lives only as prose in a markdown ledger no automated consumer reads. | Immediate — read the ledger before acting. | Not applied; #3207 owns the machine-readable fix. | No — nothing was applied. |
| 2026-08-26 | **P3** (false positive) | **The #3198 site deploy auto-rolled-back HEALTHY on a reader-truth judge flake — reverting the very fix the deploy carried.** The run's visual-qa red was Marsh's `/method/board/` "active logging went silent" claim judged HIGH temporal_contradiction; ground-truthed against DDB: TRUE (macrofactor/hevy/journal 0 rows since genesis, habitify 0 check-ins). The same finding had been NON-REPRODUCED (#3102) on the run 90 min earlier and demoted by #3003 on a third pass — a coin-flip judge on a true claim. Rollback restored the pre-#3198 build, un-labeling the start-weight provenance the deploy existed to add. | The #2959 shape-(b) oracle class, now with a rollback attached: the judge grades true, honestly-labeled claims (sparsity, active-vs-passive) as HIGH and flakes between passes. #3199 owns the deterministic demotion rules; both instances interim-baselined with that ref via the sanctioned path. | Immediate (the gating job reds + the watcher read it). | ~35 min — fold into #3199, baseline both pairs, fresh dispatch green end-to-end (deploy+smoke+visual-qa all green, build ed9efba live). | No — prior build served throughout; the fix landed on the redeploy. |
| 2026-08-25 | **P4** | **Session B's own wrap push (0a3823bf7) was event-swallowed — ZERO workflow runs minted — so its 3 new INCIDENT_LOG rows landed with STALE derived Patterns blocks and no gate ever saw them.** Docs CI's `incident_log_patterns --check` is correctly wired for exactly this, but a swallowed push runs nothing; the red surfaced only when the FIRST full PR suite of Session C (#3188) inherited it, plus a dependabot rebase's Wiki-drift red. Fixed on main (`--apply`, 175 rows/140 post-June) and all 5 in-flight branches updated. | The GitHub event-swallow class (B's ~13h episode, same night) reaching the wrap commit itself: a guard wired on push cannot see a push that mints no runs. Session C's reflex: swallow-check EVERY push by counting check-runs at the sha (three more swallows caught live this session: #3180's fix push, recovered by close/reopen). | ~7h (B's wrap ~17:30Z → C's first PR suite red 00:01Z decode). | ~10 min once decoded (regenerate + commit + update branches). | No. |
| 2026-08-25 | **P3** | **GitHub dropped `synchronize` events repo-wide for ~13h — force-pushes to PR branches minted ZERO workflow runs, one PR's head pointer went stale against its own pushed branch, one closed PR refused to reopen.** Broke the per-PR merge discipline mid-train: 7 green PRs re-verified into swallowed heads; merge_train.sh's live run dropped 7 of 8 on watch-timeouts against runless shas. Recovery vocabulary established: close/reopen mints runs for the head the PR believes in; a stale head pointer or an unreopenable PR takes a SUPERSEDE (fresh PR from the same branch — #3165→#3183, #3148→#3185); per-PR recovery measured ~25 min × 14 → the 14-lane integration branch #3184, each lane green-verified at its own head first, stacked validation catching 3 real cross-lane frictions. githubstatus.com read all-operational throughout. | GitHub-side event delivery (unconfirmed by status page); the repo's merge machinery had no degraded-mode play until tonight. | ~30 min (a watcher's zero-checks NOTE). | ~13h of degraded mode; full queue drained via the integration train. | No. |
| 2026-08-25 | **P4** | **A 16.4h stranded production lease (run 32801924278, sha d657b973, the D5 merge) jammed the whole CI/CD queue — and its approval would have REGRESSED the fleet**: the parked sha predates the train-2 integration that was hand-deployed (105/0 postflight) hours later. REJECTED with decode per the lease rule; the next queued lease (the train sha itself) APPROVED as an idempotent re-apply whose smoke pass doubles as independent verification. Wedge watch auto-filed #3175 (closed with verdict once cleared). | Same class as the 08-24 row: rapid merges outrun lease discipline and nothing sweeps the gate (#3021 janitor still unarmed pending its token — owner batch). The NEW sharp edge: a stale lease is not merely clutter, it is a pending regression. | ~2h (wedge watch). | Cleared at 17:1xZ (reject + approve). | No. |
| 2026-08-25 | **P4** | **Main red at 2217507 (the #3155 squash) for the mid-train window: Unit Tests red on the single-writer `test_count` transient + Plan red (R8-ST6 shape).** Expected churn of the counter design under a 9-PR train — the reconcile bot heals main's literal but each in-flight run samples a moving value; superseded by the train sha's run. Also of record: main was RED at Session B boot — Docs CI failed 30s after the A-wrap's own commit on the UTC/PT catalog-stamp divergence (fixed structurally, sync stamps both lines; then the fix collided with sync_doc_metadata.py sitting at EXACTLY its 1253-line baseline → the `--refresh-secrets` extraction). | The stamp class: one fact, two writers, two clocks (UTC stamp vs PT session) — killed 2026-08-25. The counter transient: inherent to single-writer-literal + concurrent merges; merge_train.sh (#3104) now bounds it. | Boot-time (the boot green-check). | Stamp class ~40 min; counter transient self-healed per-merge. | No. |
| 2026-08-24 | **P4** | **Two site auto-rollbacks in the D2 merge train (runs 32675418444 ~00:02Z, 32672013881 ~23:11Z), each on ONE novel reader-truth high.** First: `/method/verify/`'s Whoop-vs-Garmin table (30 rows, Apr–Jun) unlabeled on a Day-7 page — another #2957 member, baselined citing it (PR #3087). Second: `/story/agents/` claimed "THIS WEEK'S ACTIVITY: Aug 24–Aug 30" at 17:20 PT Sunday — `agents.js` seeded its week from bare `new Date()` (UTC Monday), the #2506 Pacific-day class on a front-end producer, fixed at both seeds (PR #3095). Both auto-rollbacks restored healthy builds; the post-fix full deploy went green end-to-end. | One real producer bug (UTC week seed) + one real framing gap (#2957 class); the deploys' own content was orthogonal both times. The single-run-verdict exposure is now filed as #3102 (confirm-before-gate for NEW truth highs). | Immediate — the gating job reds. | ~1h each to the green redeploy (diagnose + fix/baseline + rerun). | No — prior builds served throughout. |
| 2026-08-24 | **P4** | **Deploy-critical lane red on main ~1.5h (runs on 5de78251d → 0f6397eb5) — the 2026-08-08 undeclared-PyYAML class recurring.** `tests/grant_enumeration.py` imports `yaml` inside `ci_jobs()`, which the #2824 test module called at MODULE SCOPE — the package-less deploy-critical lane broke at collection for its whole 1,588-test selection. Fixed by guarding the module-scope sweep (ImportError → sentinel) with the six CI-half tests skipping LOUDLY (PRs #3100, #3105 — the first fix unmasked one more consumer, caught on the next run). | A collection-time import in a test module runs in EVERY lane, including ones that never select the tests; the loud-skip guard is the pattern. The class's first member (2026-08-08) triple-redded main the same way. | ~10 min (the lane names the import). | ~1.5h across the two-step fix. | No. |
| 2026-08-24 | **P4** | **Production-approval lease chain stalled the gated deploy path ~15.5h — two runs (32686522005 sha 11d94442, 32690759936 sha 448da59f) parked `waiting` unnoticed during the overnight merge train, every later run queued pending behind them.** Auto-detected by the wedge watch (auto-filed #3098, #3096); both leases REJECTED with decode at the wrap (superseded — HEAD's fleet deploy of 105 lambdas + 6 cdk stacks + hand deploys carried everything, postflight-verified). No reader impact; the site auto-deploy lane was unaffected. | Rapid serial merges outran the lease discipline: mid-train runs reach the gate while attention is on PR checks, and nothing sweeps them (the #3021 janitor is unarmed pending its token — owner batch). | ~2.3h (the wedge watch fired + auto-filed). | Cleared at the wrap (reject × 2). | No. |
| 2026-08-23 | **P4** | **Site auto-rollback on the D0 wrap-beats deploy (run 32657693554, 18:34Z) — a NEW serious axe color-contrast violation at `/method/board/` on a TRANSIENT data state.** `button[data-coach="sleep"] > div > h3` failed AA, reproduced on the #2978 15s re-probe (so not probe-transient) — then the coach payload regenerated at 18:40:37Z and the identical sweep passed locally against live in both color schemes. The deploy carried only beats.json + docs; the site tree was innocent. Full rerun (per the rollback-recovery rule) went green end-to-end and published the D0 build dispatch. | `.coach-name { color: var(--coach, var(--ink)) }` renders reader text in a DATA-supplied color with no AA clamp anywhere between the roster payload and the pixel — the second member of the data-driven a11y dark-state class (first: `wu-carried`, 2026-08-10). Filed **#3057**: clamp the coach-accent text SET (`.coach-name/.rd-name/.th-name/.cv-name` + is-active backgrounds), mutation-proved. | ~2 min (the gate names the node). | ~35 min to the green rerun. | No — statics only; the correct prior build served throughout. |
| 2026-08-23 | **P4** | **Site auto-rollback on the receipts-caption re-deploy (run 32653124659, ~17:10Z) — the oracle tail again, TWO new highs on pages the banner-misread ruling didn't cover.** The D0-boot re-deploy carried the new `is_position_banner_misread` ruling; the WEEK-1 shape did NOT recur (10 findings dropped/demoted correctly, first sweep with the cycle ground-truth sentence present after the SSM grant), but the sweep raised novel highs on `/coaching/lab-notes/` (a 2026-07-26 diary reaction, dated but unlabeled prior-cycle) and `/method/wrong/` (validator catches from May/June with no cycle labels). Auto-rollback restored the prior build; no reader impact. Both pairs baselined via the sanctioned `--update-truth-baseline` sweeps as #2957 debt (cross-phase-unlabeled class — the producer fix is that issue's scope); a queued doomed re-sweep was cancelled pre-gate rather than paying a third rollback; the next dispatch (32654357546) went **green end-to-end** and the receipts caption + D0.5 truth corrections are live. | The #2959 non-stationary tail: structural rulings answer known shapes; novel shapes on new pages need the (page,category) ledger. The two flagged surfaces genuinely lack cycle labels on cross-phase content — a real producer gap owned by #2957, not an oracle hallucination. | Immediate — the gating job reds. | ~1h to the green deploy (baseline + fresh dispatch). | No. |
| 2026-08-23 | **P4** | **Site auto-rollback on #3038's deploy (run 32650063358, ~16:12Z) — the #2959 non-stationary oracle tail, deploy content orthogonal.** The #2959 ground-truth-feed PR deployed its receipts-caption change; the full-surface reader-truth sweep then raised a NEW high on `/story/` + `/story/chronicle/` (a chronicle piece correctly dated 'WEEK 1 · 2026-08-18' read as a temporal contradiction) — a page that passed prior sweeps. `smoke=success visual-qa=failure` auto-rolled the site back to the healthy prior build (3ad9572); all surfaces 200, no reader impact. The PR's own fix machinery worked — every DOCUMENTED class dropped/demoted correctly on the same sweep. | The exact non-stationary tail #2959 tracks (one novel high per sweep). Aggravated by a NEW finding: the visual-qa CI role lacks `ssm:GetParameter` on `/life-platform/experiment-cycle`, so the cycle ground-truth sentence was omitted from the gating prompt (`AccessDeniedException` logged) — the fix is dark in exactly the deploy-gating path. Both fold to epic #3042. | Immediate — the gating job reds. | Not yet — the receipts-caption re-deploy + the two residuals carry into the A-Grade Program's oracle lane (#3042); the site is healthy on the prior build. | No. |
| 2026-08-23 | **P3** | **Main-red chain 00:45–03:30Z during the 11-PR fable-week merge queue — two queue-surfaced gaps, both repaired same session with permanent guards.** (1) The reconcile job regenerated `model/platform_model.json` (#3012's new alarm) and refused to commit it — `generate_platform_model.py` was in `run_generators()` but its output was never whitelisted, so the CI/CD run died at job 1 and Deploy skipped. Manually reconciled per the job's own fallback; #3017 added the whitelist entry + a mutation-proved registry test (every `reconciled=True` artifact's outputs must match `ALLOWED`). (2) #3012 merged with its `Collect + deploy-critical + format` lane red on an unregistered tree-sweeping guard (#2372 class) — the driver merged on the agent's pre-declared red-reason without reading the lane; #3016 registered it. A third tail: #3013's honesty upgrades red the full unit suite only (string-probe test, whole-dict assert, pillow parity, fixture PNG that died at the new IHDR sniff) — #3020. | Two structural gaps: the reconcile lane had the writer but not the write, and an expected-red category (doc-literal treadmill) invited lumping a real red into it. The unit-lane tail is the PR-lane-deselection class (~11k tests). | ~20 min per defect (each surfaced by the next run in the queue). | ~2.5h for the chain end-to-end; publish path confirmed clear by the fleet run's 93/93 sweep. | No — deploys held or were superseded; readers served the previous build throughout. |
| 2026-08-22 | **P2** | **A documented signal was silently dropped at the source for 6 days: the 15 BodyScan segmental fields (`{fat_free_mass,fat_mass,muscle_mass}_{torso,left_arm,right_arm,left_leg,right_leg}_kg`) were never written to any row.** PR #2794 (2026-08-16) added `SEGMENTAL_TYPES` 173/174/175 plus the position-aware parsing branch, and left `_fetch_range`'s `getmeas` request built from `MEAS_TYPES` alone — which contains none of the three. Measured: the last weigh-in's raw payload (`fetched_at 2026-08-17T05:05:45Z`, **after** #2794 merged) carries types `1,5,6,8,11,54,76,77,88,91,130,155,168,169,170,196,197,198,226,227` and its DDB row had 34 fields, zero segmental. `docs/SCHEMA.md` documented all 15 as shipped throughout. Fixed by #2994 / PR #2995 (request derived from both handler tables + a SET-equality guard + an AST derivation catching a third table), deployed 23:15:18Z, and the 2026-08-16 row repaired by `date_override` re-fetch: 34 → 49 fields, five type-173 values summing to 92.08 = the scalar `fat_free_mass_kg`. | **The fixture was not the wire.** `test_withings_bodyscan2_scalars_and_segments` fed a fixture containing 173/174/175 inline and passed continuously — it mirrored the spike's *unfiltered exploratory* `getmeas`, not the filtered request production issues. A transform can be correct and unreachable, and nothing compared the set the transform parses against the set the fetch asks for. | 6 days — found 2026-08-22 while verifying epic #2797's last folded finding, not by any alarm. **Silent**: no error, no gap signal, the row simply carried fewer fields. | ~2h from discovery to deployed + verified + past date repaired. | **Recovered, not lost** — Withings retains history, so the affected date was repairable by backfill; nothing downstream consumed the fields yet. |
| 2026-08-22 | **P3** | **Post-deploy `Visual + AI-vision QA` red on run `32601989142` — two NEW high reader-truth findings the deploy did not cause, and #2992's `/method/` finding did not recur.** `91 passed, 2 failed` across 93 pages: `/story/timeline/` (`temporal_contradiction` — the oracle's own arithmetic does not establish one; its objection resolves to the phrase being *"vague"*) and `/data/habits/` (flagged for the `90-DAY HISTORY PREDATES THE CUT` banner, i.e. the page's own honest cross-phase disclosure). Neither baselined — filed as **#3003** pending a render check, so the publish path is held for a named reason rather than cleared by assumption. | The oracle's non-stationary finding population, third consecutive session: the high set MOVES rather than shrinking, because it batches 4–6 surfaces per call and samples a different subset of a 93-page latent population each run. Both new highs look like the sanctioned-content-shape class #2959 owns. | Immediate — the gating job. | Unresolved at wrap; #3003 owns it. | No — the site serves the previous healthy build; the cost is a blocked publish path. |
| 2026-08-22 | **P4** | **A third finding inside the instrument: `report.json` stores the reader-truth finding note TRUNCATED.** Both #3003 notes end mid-word (`vague abou`, `habit data wit`) **in the artifact**, not merely in the console — so triage necessarily works from a partial sentence, and reaching even that much required downloading a 68 MB `visual-qa-screenshots` artifact. | The known "never diagnose from a truncated log line" trap, built into the instrument's own record: the note is truncated at STORE time rather than at print time. | Same session, while triaging the two highs above. | Unresolved at wrap; carried on #3003's acceptance. | No — instrument plane only. |
| 2026-08-22 | **P4** | **A production-approval lease sat `waiting` 1.3h and was found by the wrap gate, not by anyone watching.** Run `32602488214` (sha `a057c47d`, the #2996 merge) parked at the `production` environment gate; `check_main_green.py` classified it at step (e2) with `becomes the #1901 stranded class at 2h`. Its plan was **0 Lambdas** — the fleet path had triggered on a `site_api_common.py` doc-literal line. Approved with the reasoning recorded on the run. | Not a fault — the gate behaved correctly. The cost is the lease: a gated run holds the deploy concurrency group. Two merges in one session means two gates, and the second is easy to miss while watching the first. | 1.3h — and only because a wrap gate enumerates them; nothing pages on a waiting lease before 2h. | ~2 min once seen. | No. |
| 2026-08-22 | **P2** | **Three consecutive site auto-rollbacks un-shipped a healthy fix — the newly-armed reader-truth gate had no debt baseline.** #2940 armed the gate; its first run caught a real bug (#2941, the client day-stamp). The deploy carrying that FIX then failed on **16 standing content findings that predate any deploy** (run `32545820852`, `77 passed / 16 failed`), rolled back, and two further rounds failed on *different* pages (3, then 2 findings) because the oracle batches 4–6 surfaces per call and samples a different subset of a 93-page latent population each run. Fixed structurally by #2956 (a triaged debt ledger: NEW findings gate, baselined ones warn with their issue ref) plus #2970 (the deterministic half — `_payload_is_empty` did not recognise the engine's `available:false` + `reason` declared-absence contract, so `/data/autonomic/` FAILed on honest genesis-week emptiness on cycle Day 5 of a 7-day minimum). | Arming a semantic gate against an un-baselined surface. The a11y gate solved this in #1433 with a debt ledger; the reader-truth gate shipped without one, so "this deploy broke truth" and "the site carries standing truth debt" were the same signal. | Immediate — the first armed run. | ~7h across 5 deploy rounds; findings per round 77 → 16 → 3 → 2 → **0**. | No — the live site served the healthy rollback target throughout; the cost was a blocked publish path and the #2941 fix reaching readers hours late. |
| 2026-08-22 | **P3** | **Main red 1h58m (14:35Z→16:33Z) on TWO independent stale-derived-artifact defects, and the second one landed on an innocent commit.** (a) `docs/PROPORTIONALITY.md:84`'s `Period=86400` — a CloudWatch period in the #2912 flap-detector row — was read by `check_doc_facts`'s alarm-inventory rule as a claim of 86400 alarms against the real 108, redding `Docs CI` on the wrap commit itself. (b) The same 08-21 wrap added 3 incident rows here without re-running `scripts/incident_log_patterns.py`, so `test_incident_log_patterns_2840` (4 assertions) redded `test / Unit Tests` — **not on the wrap that staled it, but on the next unrelated push that touched `tests/**`**. Fixed by `caa7e6ae2` (matcher: `=` added to the lookbehind, mutation-proved on 3 shapes × 4 controls) and `3eb9f2583` (regeneration; the silence sub-table's mean/parseable/exceeded cells are not emitted by the script and were recomputed through its own `parse_rows()` rather than estimated — the >1day ratio moved 1.7×→1.9× and was restated). | Two instruments describing the repo to itself, both stale-by-construction. (b) is a lane-placement defect filed as **#2975**: the guard runs only in CI/CD's Unit Tests, but the change that stales it (`docs/**`) triggers only Docs CI, which runs no pytest — so the red is always misattributed to a later commit. | ~20 min (first red run inspected at session boot). | 1h58m. | No — instrument plane only; nothing user-facing was wrong. |
| 2026-08-22 | **P4** | **Two production-approval leases rejected as superseded — the deploy they gated had already been done manually and verified.** Runs `32585879582` (sha `cbbc08ce`) and `32587130309` (sha `a2e02465`) both sat `waiting` at the production gate. `cbbc08ce` is an **ancestor** of the live sha, so approving it would have deployed backwards and `verify_bundle_ancestry.sh` preflight would have refused it; `a2e02465` was already live (site-api 17:52:06Z, mcp 17:53:31Z, both ancestry-gated preflight+postflight). All test jobs on both runs passed. | Not a fault — the gate behaved correctly. The cost was the lease: a gated run holds the deploy concurrency group and reaches the #1901 stranded class at ~2h, so it must be approved or REJECTED, never left waiting. Manual deploys during a session make the pipeline's own run redundant by construction. | Real-time — `check_main_green.py` classified both at the wrap gate. | ~5 min each (rejected with the reasoning recorded on the run). | No — nothing shipped stale; the fleet was already on the newer sha. |
| 2026-08-22 | **P4** | **Main red on the full Unit Tests lane: the 08-21 wrap commits dropped the `#1332` marker from INCIDENT_LOG's `Last updated:` line and inserted 4 incident rows into the Severity Levels legend table.** `test_incident_gate_1332` caught the first defect (run `32545821044`, 20,102 passed / 1 failed); the misplaced rows were found while fixing it. | A wrap edit rewrote a gated line to a bare date. The gate did its job; the wrap that wrote the row was the thing that broke it. | ~1 CI cycle. | ~15 min, fixed directly on main (`baf3cd0e`). | No. |
| 2026-08-22 | **P3** | **The 2026-08-21 wrap commit re-closed #2921 and #2578 by *describing* the accident that closed them the first time.** Its body reads `"closed #2921 by accident writing 'Does NOT close #2921' … and closed #2578 overclaiming"`. GitHub parses the literal phrase `closed #2921` as a closing keyword regardless of surrounding prose, so both issues closed again ~1 min after the push (01:27Z). Both reopened this session. | A second variant of the #2921 trap, one layer up: the known rule is "a negated closing keyword still closes"; this is "**writing about a closure in past tense also closes**". Post-mortem prose about issue handling is itself parsed. | ~11h (found at the next wrap). **Silent** — no signal at all. | Same session; both reopened with the diagnosis. | No — issue-state only, but both were live work misfiled as done. |
| 2026-08-21 | **P2** | **Site auto-rollback fired, and the gate was RIGHT — the newly-armed reader-truth check caught a live wrong Day number on its first run.** #2938 installed `boto3` so `visual-qa`'s `--reader-truth` could actually execute (dark since 2026-06-05). Its first armed run (`32541686196`) reported `Visual QA: 16 passed, 77 failed across 93 pages`, flagging `/method/ask/` and `/method/kitchen/` for `DAY 6 · WEEK 1, SINCE AUGUST 17 2026`. Ground truth at 18:32 PT: Pacific day = **5**, UTC day = **6**. `site/assets/js/coach_popover.js:24-28` computes `Math.floor((Date.now() - GENESIS.getTime()) / 86400000) + 1` — `Date.now()` is a UTC instant while `GENESIS` is parsed as browser-LOCAL midnight, so a Pacific reader sees the right number and everyone east of Pacific sees a day ahead. 77 pages share the stamp. Filed #2941. | Two clocks in one expression, on the client. The #2675 class ("a UTC gate blessed the wrong day every PT evening") moved to the browser. It rendered for months without a red because the only check that would catch it was the AI gate that could not run. | ~3 months latent; **~4 minutes** once the gate was armed — its first live run. | Not fixed at wrap; #2941 filed, site deploy path blocked until then. | No — the live site is healthy (all 200s) serving the rollback target `8b0940d`; auto-rollback did exactly its job. The cost is that every `site/**` merge now deploys, fails visual-QA and rolls back until #2941 lands. |
| 2026-08-21 | **P3** | **Production deploys wedged behind two stranded approval gates (~6h and ~2.5h) — the platform auto-filed it before a human noticed, for the THIRD session running.** Run `32508603771` (sha `bd9efa9e`) sat `waiting` at the production gate from 17:31Z; `ci-cd.yml` is `cancel-in-progress: false`, so the next run queued behind it as 0-job pending. The remediation agent auto-filed #2937 at the 2h threshold. On inspection the parked sha was **5 commits behind main** — approving would have deployed a tree missing #2935 and #2936, the 2026-08-19 class that `deploy/reject_deployment.sh` exists to warn about. **Rejected**, not approved; the queued run reached the gate within 45s of the slot freeing and deployed 104/104 Lambdas. A second and third gate were actioned before wrap rather than left overnight. | A gated run is a deploy LEASE and must be approved or rejected, never left waiting. The detector is working; the recurring failure is the human half. | 2h28m by the platform's own auto-filer (#2937), consistent with #2931 the session before. | ~6h for the first gate; ~2.5h for the second. | No — nothing stale deployed; the rejection concluded its run without executing Deploy. |
| 2026-08-21 | **P2** | **Both AI gates on the deploy-gating `visual-qa` job were structurally dark, and the job reported SUCCESS.** Observed in run `32509917798` / job `96909117231` during a real fleet deploy: `AI-vision QA` and `Reader-truth QA` each printed `⚠ AI-QA unavailable — could not import bedrock_client: No module named 'boto3'` and the job concluded `success`. `boto3` was never installed in ANY of the three visual-qa workflow copies (`ci-cd.yml`, `site-deploy.yml`, `visual-qa.yml`), while CLAUDE.md and ADR-076 describe these as gating since 2026-06-05 ("a deterministic FAIL or AI 'high' verdict blocks the pipeline"). Not credentials — OIDC is configured and the import fails first. Fixed by #2940: boto3 installed in all three (pinned via `ci_pins.py`) AND `run_sweep` now returns `failed == 0 and not ai_gate_failures`. Filed #2938. | The missing dependency was the trigger; the defect was `⚠` + exit 0 — the exit code read page failures only, so an `unavailable` AI status had no effect on it. Compounds #1927, which moved these same gates out of budget band 1 for being "dark 26 of 30 days while still reporting green". | ~3 months (gating claimed since 2026-06-05; found 2026-08-21 while verifying an unrelated deploy). **Silent.** | Same session. | No — the deterministic half of the job (Playwright sweep, leak-token sweep) was armed and working throughout; only the two AI gates were dark. |
| 2026-08-21 | **P3** | **`/api/sleep_detail` served an impossible percentage to the public site: `light_pct: 106.7` (deep 11.1 + rem 31.1 + light 106.7 = 148.9%).** The stored Eight Sleep row for 2026-08-20 has stage hours summing to 2.01h against a TST of 1.35h — the vendor's stages carried the 0.61h of awake time, TST excluded it, and `compute_derived_fields` divided by TST anyway. All three percentages were wrong, not just the visible one. Fixed at the writer (#2939, omit-as-a-set with `stage_pct_omitted_reason`, ADR-104 absence over a clamped figure), in the archive (1 row corrected; 44 skewed-but-possible rows deliberately untouched), and verified live. | `tests/accuracy_audit.py::impossible_values` had exactly the right rule (`_pct` in [0,100]) but scanned two top-level blocks of ONE document (`public_stats.json`). The bad value sat inside a LIST on an endpoint it never fetched — a correct rule with a denominator narrower than the live surface (#2652's class). The scan is now recursive and sweeps the endpoint set derived from `qa_manifest`'s `api_deps`. | Unknown — surfaced only because a reader-truth oracle raised a DIFFERENT (false) finding on the same payload and the investigation read the rows. **Silent.** | Same session. | No — a wrong published figure, not lost data; the raw stage hours were never touched, so the correction is reversible. |
| 2026-08-19 | **P4** | **Main red 29m on the social-channels merge (#2905 / `a66ce369`) — the PR was green, the structural-guard set was green, and CI still failed at COLLECTION.** `Deploy-critical tests` and `test / Unit Tests` both died on one line: `ModuleNotFoundError: No module named 'aws_cdk'` importing `tests/test_social_channels_registry_2806.py`. The new derivation-guard test did `import stacks.ingestion_stack` to read `SOCIAL_CHANNELS_ENV`, which pulls in `aws_cdk` — **not installed in either of those CI jobs**. It passed locally because a dev environment has it. Because it fails at collection it aborts the whole job rather than reddening one test. Fixed by #2907, which reads the CDK fact by `ast.parse` instead — the established in-repo idiom (`test_heartbeat_completeness.py`, `check_doc_facts.py`) — and is a STRONGER guard: it asserts the assignment is an `ast.Call` into `social_channel_source_ids`, so a hand-typed literal now fails on SHAPE before any value comparison. | A test reached for a CDK fact by importing the stack. The green-here/red-there asymmetry is invisible to every local check, including the five-suite structural set (`drift_sentinel`, `gate_registry_1349`, `gate_census_2578`, `module_size_guard`, `conformance_guard_2844`) which I ran on the branch and which passed 168/168. | ~2 min — the post-merge run check, not a notification. | 29m (21:04:03Z red → 21:33:02Z green). | No — the failure was collection-side; nothing deployed on red (the run never reached Plan or Deploy). |
| 2026-08-19 | **P3** | **Production deploys wedged 6h22m behind a stranded approval gate (#1901 class) — and the platform detected it before I did.** Run `32263995111` (sha `68022d1f`, #2887) sat `waiting` at the production gate from 14:26:10Z; because `ci-cd.yml` is `cancel-in-progress: false`, every later CI/CD run queued behind it as 0-job `pending`. The sha was already an ancestor of main and its only failing check was `test_gate_steps_cannot_swallow_status_2746` — the swallowed-exit-status guard, whose defect #2900 had **already fixed on a later commit** — so approving would have deployed a tree missing every subsequent merge. **Rejected** (not approved) at 20:48:51Z per `reject_deployment.sh`'s own rule. A second gate of the same shape (run `32300377555`, sha `2424ddd7`, 9 commits behind main, 0-Lambda plan) was rejected at 22:09:27Z. **The detector worked:** the remediation agent auto-filed #2901 ("CI/CD deploy wedge — production deploys are stalled") at 16:54:42Z, 2h28m into the wedge, and it auto-closed at 20:55:00Z — 4 minutes after the rejection cleared it. | A gated run is a deploy LEASE: it must be approved or rejected, never left waiting. Two runs were left waiting by prior sessions. The concurrency setting turns one unactioned gate into a fleet-wide deploy freeze. | 2h28m by the platform's own auto-filer (#2901); the session found it via `check_main_green.py` at boot+~1h. | 6h22m for the first gate; 1h23m for the second. | No — nothing stale deployed; both rejections concluded their runs without executing Deploy. |
| 2026-08-18 | **P4** | **Main red 1h02m on the \$150 ceiling merge (#2895 / `71607f78`) — the PR lane was 9/9 green and had deselected ~11,482 tests.** `test / Unit Tests` failed 4 of 19,365 on the push lane: three stale `\$85`/`85.0` literal assertions in `test_inference_receipt_ceiling.py` and one `"85" in reason` in `test_coach_sim_scoreboard_2539.py`. **No behaviour defect** — one failure message read `The \$150 base ceiling (\$100 in effect …)` which looks like a stale surge literal but is the test's own fixture; `site_api_budget.py:352` formats it correctly (verified before changing anything). The production deploy gate was **rejected** rather than approved, so nothing shipped on red; `cost-governor`/`site-api` were already live from the manual deploys at the same sha. Fixed by #2896, which also converted one of the four to derive from `MONTHLY_CEILING` instead of re-pinning the number. | The base ceiling is hand-maintained in 5 code sites + ~18 prose sites (#2898), so the sweep had a long tail; the pre-merge lane deselects the tests that hold that tail (#2692). My own process miss compounded it: the broad grep-driven sweep I ran pre-merge silently failed on a zsh unquoted-expansion quirk and I proceeded on a narrower set instead of re-running it. | ~19 min — the by-`head_sha` gate check after merge, not a notification. | 1h02m (03:49:08Z red → 04:51:47Z green). | No — the deployed code was functionally correct throughout; the failures were test-side assertions only. |
| 2026-08-18 | **P3** | **Site auto-rollback on the wrap-beat deploy (run 32172620543) — a TRUE POSITIVE about the data and a FALSE POSITIVE about the deploy; the site is un-deployable until a weigh-in.** Deploy + Visual/AI-QA green; smoke failed 1 of 243: `/api/vitals: missing weight_lbs`. Rollback reverted `0773e90c6` → `9e3659a1e`, un-shipping the build beat. `/api/vitals` is **correct** — Withings' latest row is `DATE#2026-08-16` (321.01) and cycle 14's genesis is 2026-08-17, so there is no in-cycle weigh-in and the API honestly returns `weight_lbs: None` (ADR-104 absence, the `genesis_week_present_none` class). The gate's carve-out keys on `day_n <= 1`; live `day_n = 2`, so it lapsed while the predicate it actually means ('before the first weigh-in') is still true. **Consequence: every `site/**` merge auto-deploys (#750, no approval gate), fails smoke, and auto-rolls-back until a human weighs in** — the deploy can neither cause nor cure the condition, and the rollback target carries it identically. Filed #2878. | Genesis carve-out implemented as a day-number proxy for 'has a weigh-in happened'; a same-day reset with a future-dated genesis and no Day-0/1 weigh-in breaks the equivalence. | Real-time (deploy watcher on the wrap push). | **Resolved same day (2026-08-18 ~19:33Z) by #2879 — WITHOUT a weigh-in, which was the point.** The owner-gate framing above was wrong: the missing Withings row was the trigger, not the defect. The defect was the day-number proxy. The carve-out now reads the real predicate (an in-cycle weigh-in, via `/api/source_freshness`'s un-genesis-clamped withings `last_update` against `experiment.genesis` — a different module and query from `/api/vitals`, so the two must agree). Mutation-proven both directions on the block extracted verbatim: a weigh-in present in DDB but absent from the payload still FAILs; unreadable freshness fails closed. Site deploy on `0bffc1e` then went green on deploy + smoke + Visual/AI-QA with rollback skipped, `/version.json` == HEAD, and the un-shipped beat is served (113 → 114). | No — the site served the prior healthy state throughout; the only loss is one build-beat entry not yet public. |
| 2026-08-18 | **P4** | **A merge to `main` produced ZERO CI runs — not a failure, an absence (the swallowed-push class, second documented occurrence).** After #2875 merged as `cdb0a7f6b`, no workflow fired at all: no CI/CD, no Docs CI, nothing — despite the commit touching `lambdas/**` and `tests/**`, which both match `ci-cd.yml`'s path filters, and `docs/**` which matches `docs-ci.yml`. Queried by `head_sha`, `total_count` was **0** seven minutes after the merge. Recovered with `gh workflow run ci-cd.yml --ref main`; the dispatched run went fully green including Deploy, Smoke and Visual+AI QA. **The failure mode is that nothing is red** — a `gh run list` glance shows the previous sha's green runs and reads as healthy, so 'is main green?' answers yes while the merged sha was never validated and its code never deployed. | GitHub did not dispatch the push event (no run object was ever created). Path filters were verified as matching, so this is the documented swallowed-push class, not a config gap. | ~7 min — the by-`head_sha` check at the merge checkpoint, not a notification (there was nothing to notify). | ~3 min to dispatch; ~18 min to full green incl. deploy. | No — the prior sha stayed deployed and healthy throughout; the window's only exposure was an unvalidated, undeployed merge. |
| 2026-08-17 | Low | **Site auto-rollback fired on the system-model wrap-beat deploy (run 32099132375) — false positive, plus a recovery trap.** Deploy + smoke green; visual QA failed ONE page, `/data/autonomic/`: "All `[data-readout]` empty/placeholder" — but the beat change (beats.json only) cannot touch it, `/api/autonomic_balance` probed healthy (200, 86ms, honest `available:false — 0 of 7 days`, the genesis+Whoop-latch window), and a local single-page sweep PASSED (the honest empty-state text renders once the fetch completes — a transient fetch during the CI sweep left the skeleton). **The trap: the first recovery attempt used `gh run rerun --failed`, which re-ran only the QA job — it validated the ROLLED-BACK site, went green, and shipped nothing** (live beats.json still ended at yesterday's beat; caught by checking the served artifact, not the run verdict). Full rerun re-executed Deploy; beat live-verified. | Transient API fetch during the Playwright sweep (the known QA-transient rollback class) + a rerun semantics gap: after a rollback, only a FULL rerun re-ships. | Real-time (workflow watcher). | ~25 min end-to-end incl. the trap; beat live-verified served. | No — content-plane only; the site served the prior state throughout. |
| 2026-08-17 | **P4** | **Main's Unit Tests job red ~2.5h across three consecutive runs (65e9d0090 / 5d9a4ba11 / cc22c0971) on `test_public_write_hardening` vote tests asserting 503 — a latent test-order dependence, no code defect on main.** The tests injected `_challenge_catalog_cache` but not its `_cache_at` TTL stamp; an earlier test's real load left a monotonic stamp, the long suite crossed the config TTL, and `_catalog_cached()` ignored the fixture and re-fetched from S3 — 503 under CI's FAKE creds, but under local real creds it silently served the REAL production catalog and passed by coincidence (the fixture-must-be-the-wire class). The evening's ~30 new tests (#2865/#2867) shifted suite composition past the TTL edge. Deploy, smoke, and visual-QA were green on every red run. | Test fixture injected the cache but not the pin stamp the module documents for test injection; local-vs-CI credential asymmetry hid it for months. | ~10 min (run-completion watcher on the first deploy). | ~40 min — root cause reproduced both directions (leaky-stale-stamp harness + FAKE creds: 3 failed unfixed, 8 passed fixed), #2869 merged; final run 32096134923 fully green. | No — test/CI plane only; deploys stayed healthy throughout. |
| 2026-08-17 | Low | **Site auto-rollback fired on the wrap-beat deploy (run 32081254650) — false positive.** Deploy + smoke green; visual QA failed ONE page, `/method/biology/`: "All `[data-readout]` empty" — but the page's own build-beat change never touched it, and `/api/genome_risks` probed healthy + fully populated minutes later (200, 158ms, full payload). A transient fetch during the Playwright sweep left the loading skeleton in place and the deterministic check read it as a dead page. Rollback restored the pre-beat site (which binds the same endpoint — it reverted nothing relevant). | Transient API fetch during the post-deploy sweep — the known smoke/QA-transient rollback class (see 2026-06 smoke exit-28 row); not a content or code defect. | Real-time (workflow failure notification). | Same-session `gh run rerun` — beat live on the re-sweep. | No — content-plane only; the site served the prior state throughout. |
| 2026-08-17 | **P3** | **Whoop data-endpoint 401s freshly-minted tokens; auth breaker latched 12:00:37Z — cycle-14 Day 1 opens with no Whoop sleep/recovery (night-of-08-16 missing), and `/api/vitals` served night-of-08-15, which qa-smoke's #2613 rule correctly FAILed as a pre-cycle measurement.** Same signature as the 08-01→07 incident (#1934): token *exchange* succeeds and rotates, data calls 401 twice → latch. The #1934-built machinery all fired: `ingest-auth-unhealthy-whoop` OK→ALARM 12:01Z → urgent `repository_dispatch` remediation runs (12:01Z, 15:58Z). | Whoop-side auth rejection of valid tokens (#1934 class — last time only owner OAuth re-grant fixed it); not a pipeline defect. | Real-time — the per-source alarm #1934's acceptance added. | **Open** — owner re-auth requested (session owner-ask #2); after re-auth the #2085 latch-clear runs, then gap-aware backfill recovers the missing days. | Expected No (Whoop retains; backfill on recovery) — gap open until re-auth. |
| 2026-08-16 | Low | August budget ceiling raised $115/$135 → $200/$235 (ADR-133 3rd amendment) after `budget-tier-sustained-7d` sat lit ~4d and the projection hit 171% of ceiling — owner accepted the overrun explicitly (#2734); tier stays 1 by projection, September revert unchanged | Dev-heavy August spend + a base calibrated for near-zero traffic (Sept re-derivation on the ledger, #2836) | ~4d (alarm→decision, forced by the /wrap citation gate) | same-day (decision+deploy) | No |
| 2026-08-15 | **P3** | **The code-deploy pipeline was stranded for ~14h by a gate working correctly — every deploy that night went around it.** A `sqs:SendMessage` grant added for the #2655 canary DLQ tripped CI/CD's **R8-ST6 IAM-review gate**: `Plan deployments` failed, `Deploy` was SKIPPED, and lint/tests stayed green — the #1901 stranded shape, not an ordinary red. Consequence: seven merges landed on main with no pipeline deploy behind them; each was deployed individually via `deploy/deploy_lambda.sh` / `deploy_site_api.sh` and verified live. | **Not a fault — the gate is a deliberate human checkpoint on IAM, and it fired on exactly the change it exists to catch.** The cost was the duration, not the trigger: the grant landed at 02:52Z and the owner-run `cdk deploy LifePlatformOperational` cleared it at 16:33Z. A second contributor: the 16:25Z run computed its diff at 16:27:59Z, ~5 min BEFORE the deploy finished, so it read as still-stranded on stale data and needed re-reading rather than re-fixing. | Real-time — `check_main_green.py` classified it as `#1901 stranded` on the first wrap-gate run. | Owner-run `cdk deploy LifePlatformOperational` (UPDATE_COMPLETE, 27/27, 41.8s), after CDK refused to self-approve without a TTY. Verified live rather than from the CloudFormation green: `DeadLetterConfig` attached AND `iam simulate-principal-policy` → `allowed` for `sqs:SendMessage`, and the deployed canary bundle re-checked to confirm the restaged asset had not rolled back the per-function code deploy. `cdk diff` then showed **zero** remaining IAM changes. **Structural answer → #2834** (2026-08-30, ADR-065 amendment): the R8-ST6 grep is now `deploy/iam_additive_gate.py`, a statement-level template-diff gate. This grant's shape — a new `sqs:SendMessage` Allow on a `life-platform-*` queue — is `ALLOW-ADDITIVE` and ships through CI; the PR *as merged* (a new DLQ resource + `DeadLetterConfig` riding along) would still read `OWNER-REQUIRED`, by design — only the pure IAM half moved. | No — code-plane only; site served normally throughout (smoke 241/0 at both ends of the session). |
| 2026-08-15 | **P3** | **BACKFILLED 2026-08-21 (#2840) — the Day-N grounding gate ran on UTC and blessed the WRONG DAY every Pacific evening.** `cycle_gate_params` defaulted to a UTC clock, so between 17:00 PT and midnight PT the gate computed tomorrow's day number and evaluated grounding against a day that had not happened yet. Live wrong output on an AI surface, not a test artifact. Fixed by #2675 (`10c33c67c`) — the gate now defaults to Pacific, the clock the rest of the platform already used. | A wallclock default chosen per-module rather than inherited from the platform's one clock; the divergence is invisible for 17 hours a day. | Unknown — found by inspection during the timezone sweep, not by an alarm. **Silent.** | Fixed same day as discovery. | No — grounding verdicts only; no stored row was written against the wrong day. |
| 2026-08-16 | **P3** | **BACKFILLED 2026-08-21 (#2840) — the `dashboard:date` QA check did not know about the pre-compute morning window and failed against a correct system.** The check asserted a freshness that the daily pre-compute has not yet produced in the early-morning window, so a healthy platform read as stale. Fixed by #2670 (`b88563f89`), which taught the check the producer's actual schedule. | A QA expectation hand-mirrored from a producer cron instead of derived from it — the #2818 class. | Same-day (QA smoke). | Same-day. | No — a false negative on a check; nothing user-facing was wrong. |
| 2026-08-14 | **P2** | **A security fix disabled the control it was fixing: rate limiting went unenforced on the public write surface for ~40 min.** PR #2689 (the #1221 Lambda half) stopped deriving the rate-limit identity from the client-controllable `X-Forwarded-For` and fell back to `requestContext.sourceIp`, on the reasoning that it is coarser but un-forgeable. Deployed at 22:29Z. Re-measured live against `/api/submit_finding` (limit 3/hour): `400 429 400 400 400 400` over six requests where a working limiter gives `400 400 400 429 429`. Every IP-gated write (votes, follows, nudges, checkins, board_ask, subscribe) was effectively unmetered in that window. | **`sourceIp` is the CloudFront *edge* address and is not stable per viewer**, so nearly every request minted a fresh rate-limit bucket. The evidence was already in #1221's own body — `site_api_ai_lambda`, keying on raw `sourceIp`, produced *17 consecutive POSTs -> 0 x HTTP 429* — and was misread as "that limiter is broken" rather than "`sourceIp` is not an identity". Stability was never tested before shipping, on the mistaken belief that testing it required Bedrock spend; `/api/submit_finding` tests it for free and was already in use. **Neither the 18,421-test suite nor any CI gate could see it** — the defect lives in the relationship between the code and the edge, which every fixture asserts rather than observes (the CONVENTIONS §9a class this same session had just written up). | ~40 min — caught only by re-running the live mechanism test post-deploy, as a matter of habit rather than by any alarm. | PR #2691 restored `X-Forwarded-For` as the middle fallback (order: `CloudFront-Viewer-Address` -> XFF last hop -> `sourceIp`), deployed via run `31850478638`. Verified live: fixed forged XFF and ordinary traffic both back to `400 400 400 429 429`. A test now pins the ORDER so a future "hardening" cannot silently re-disable enforcement. **Not a full fix for #1221** — rotating the header still buys a fresh bucket, which is the known-open bypass until the origin request policy lands. | No — no data touched; the exposure was unmetered write *capacity*, and no abuse was observed. |
| 2026-08-14 | **P1** | **The privacy screen on outbound email has been a silent no-op, and the AI-quality canary has failed 100% of runs since 2026-08-10 — both for the same missing IAM grant.** Found by the 2026-08-14 bug bash (station 6). `between-chronicle`'s `_scrub()` runs `privacy_guard.scrub` over every narrative field of the mail it sends; that resolves through `content_filter_channel.blocked_keywords(require=True)`, deliberately **fail-closed** so a screen can never scan with an empty vocabulary and report a pass (#2203's class). The role holds **zero S3 statements**, `CONTENT_FILTER_JSON` is unset, and the local vocabulary file is gitignored and absent from the bundle — so all three channels are dead, the fail-closed design raises, `_scrub`'s bare `except Exception` returns the **unscrubbed** text, and the send logs `1 ok, 0 failed`. `EXTERNAL_EMAILS_ENABLED` is set on that function. Separately, `life-platform-ai-quality-canary` has only `s3:PutObject` to its own audit prefix and no `GetObject` anywhere, so every scheduled run since 2026-08-10 raised `ContentFilterUnavailable` before the first probe. Filed #2654 / #2655. | **#2503 (2026-08-09) moved the ER-06 vocabulary off-repo** behind a three-source channel (env → gitignored local file → S3) and two consumer roles never got the S3 leg. Neither failed loudly: `_from_s3_boto` swallows the AccessDenied so the canary reported "no source available" rather than "permission denied", and the canary has **no DLQ**, so its three exhausted retries vanished. The scrub's fail-open `except Exception` converted a deliberately fail-closed contract back into a fail-open one at the call site — the exact class the `require=True` flag exists to prevent, one layer up. This is the **third and fourth** casualty of the same change; #2644 (the CI scan never receives the secret, because a reusable workflow does not inherit secrets) was the first, found at the previous wrap. | 5 days for the canary (dead 08-10, found 08-14). The `ai-canary-heartbeat` alarm fired 2026-08-13 and was not acted on; `Errors` on `between-chronicle` reads 0.0 throughout, so no metric-based check could have seen the scrub half at all. | Not yet resolved — PR #2684 grants both roles `s3:GetObject` scoped to `config/content_filter.json`, and **requires an owner-run `cd cdk && npx cdk deploy LifePlatformEmail LifePlatformOperational`** (CI's code-deploy cannot ship IAM, and `role_policies*` is on the auto-merge DENYLIST per ADR-065/#2611). The fail-open `except Exception` in `_scrub` is not yet narrowed — tracked on #2654. **Structural answer → #2834** (2026-08-30, ADR-065 amendment): the grant #2684 needed — `s3:GetObject` on `config/content_filter.json` for two existing roles — is exactly the `ALLOW-ADDITIVE` shape, so under the additive-IAM gate it deploys through CI on merge instead of waiting for an owner-run `cdk deploy`; the five-day gap between "fix merged" and "fix live" is the class that gate removes. | **Unknown, and deliberately recorded as unknown.** No fabrication or corruption occurred; the question is whether any `between-chronicle` send since 2026-08-09 carried content the vocabulary would have redacted. That cannot be answered from logs — the scrub leaves no trace when it no-ops — so the honest verdict is that the screen was absent, not that it was breached. |
| 2026-08-13 | **P2** | **Predict-the-week went dark for the whole cycle-13 genesis week — the SAME class as the cycle-11 dark week (2026-08-02, below), on the very next reset that used the fixed tooling.** `/api/predict_week` returned `{"active": false}` for 2026-08-10 → 08-13 while the flagship reader-participation hook sat empty through the cycle's most-read week. Measured, not inferred: `head-object s3://matthew-life-platform/site/config/current_challenge.json` → **404, the object does not exist**, and `build_genesis_predict_week.py` (dry-run) derives a complete, freeze-verified subject for `2026-W33` on demand — so the surface was dark for want of one upload, not for want of data. Filed #2612. | **The reset pipeline reliably creates a hole it does not close.** `restart_pipeline.py` step 2d (`clear_predict_week_subject`, #1198) **deletes** `current_challenge.json` on every `--apply` — correctly, so the outgoing cycle's closed week stops taking bets — and **no lambda and no wipe ever rewrites it**. Re-seeding was an attended step printed in the closing "REQUIRED NEXT COMMANDS" block, and detection of the gap was *also* manual (`restart_verify.py` check 13, the post-genesis Monday run). Automated destroy + manual restore + manual verify ⇒ the failure recurs on **every** cycle reset, and it did: cycle 11 seeded the wrong `week_id` (#1952), cycle 13 never seeded at all. The only automatic detector, the nightly qa-smoke `predict_week:freshness` check, worked exactly as designed — WARN day 1, FAIL days 2 and 3 — but content-truth findings are deliberately non-gating (#1921), so it reported into a warn list rather than stopping anything. | 1 day to WARN, 2 days to FAIL (the check fired correctly from day 1); 3 days to a filed issue. | The re-seed is now post-verify hook (d) of `restart_pipeline.py`, default-ON, ordered after the pre-registration seed it derives from, and carrying a new `--if-frozen` flag so a reset whose freeze is not yet re-landed SKIPs loudly (exit 0) instead of aborting the pipeline's last hooks. Recorded — not quietly changed — in the pipeline's DELIBERATELY-NOT-FOLDED block: the exclusion came off the attended list because it was the one attended step whose absence the pipeline itself causes. This instance still needs one hand-run seed (`build_genesis_predict_week.py --apply`). | No — nothing written and no votes lost. Zero predictions were solicited during the opening week, exactly as in the cycle-11 instance: reader participation forgone, not corrupted. |
| 2026-08-11 | **P2** | **Semantic recall has never indexed a single journal entry — the corpus is chronicle-only by silent data loss, not by design.** Discovered while shipping #2349; independently corroborated by a second agent measuring the same corpus for #2348, and confirmed a third time during the wrap. Live corpus: **19 rows, all `kind=chronicle`, zero `kind=journal`** — and of those 19, only **3** carry a published link, so the reader-visible slice is n=3. Filed #2569. | `deploy/backfill_recall_embeddings.py::gather_journal` reads a journal entry's text from `content` / `body` / `text`. **Live rows store it in `raw_text`.** Verified directly against DynamoDB: the first `#journal#` row's attribute set contains `raw_text` (49 chars) and none of the three names the reader looks for, so every journal row fails the `if not date or not text: continue` guard and is skipped as empty. A reader/writer field-name mismatch with no test that used a realistically-shaped row — a fixture carrying `content` would pass against the broken code. | ~unknown, latent since the indexer shipped; found by measuring the corpus rather than trusting it. | Not yet fixed — filed #2569 (P1) with the mismatch, the live attribute list, and an acceptance item requiring the field set be **derived from the writer** rather than a second hardcoded guess. | No data lost, but **every measurement of recall quality to date was taken against a corpus missing an entire source**, and #2347's "decide the corpus scope" question rests on it — reframed in that issue. |
| 2026-08-11 | **P3** | **The BLOCKING coach quality gate passes fabricated numbers — all three fabricated-number canaries scored 92 / 92 / 82 and PASSED.** Surfaced by calibrating the judge for the first time (#1374 / PR #2567), across 3 independent Bedrock runs with identical cells. Sensitivity **0.900** [0.744, 0.965] on n=30; specificity **0.400** [0.118, 0.769] on n=5. Filed #2573. | Not judge weakness — **rubric scope**. Only 1 of the 5 negatives falls inside the judge's four-criterion rubric at all; it has **no rule about invented numbers**, so it is never asked to look. ADR-108 promoted this gate advisory → blocking, so a brief that fabricates a figure passes a blocking gate. Second finding from the same runs: **4 of 5 blocking decisions came from the model's own `passed=false` while scoring ≥ 60**, meaning `PASS_SCORE_THRESHOLD` is not the operative control and any past tuning of it was aimed at the wrong variable. | Latent since the gate was promoted to blocking; found the first time anyone measured it against labelled data. | Not yet fixed — #2573 (P1). The deterministic grounding gate does catch this class every time, so the exposure is the blocking LLM layer, not the platform's only defence. | No — no fabrication is known to have reached a reader through this path; the deterministic gate sits in front of it. |
| 2026-08-11 | **P4** | **The semantic-recall corpus had rotted under a link-scheme change — one indexed installment carried a stored link that no longer matched the derived reader link (2026-08-02), so retrieval would have cited a URL a reader cannot open.** Caught by the nightly qa-smoke `recall:corpus_freshness` content-truth check and repaired the same session. | A link-scheme change updated how reader links are derived without refreshing the stored link metadata on already-embedded docs. Newly material because **#2348 shipped this session**: `/api/ask` now retrieves from this corpus, so a stale link becomes a reader-facing dead citation rather than dormant metadata. | Same-session — the check names the doc and the repair command. | ~2 min: `python3 deploy/backfill_recall_embeddings.py --apply --kinds chronicle` — 1 embedded, 2 metadata-refreshed, 16 unchanged, 19 scanned, ≈$0.0000 (no re-embed needed for the metadata half). | No — metadata only; no content changed and nothing was published in the interim. |
| 2026-08-10 | **P2** | **The coaching surface could not acknowledge anything Matthew told it that contained a number — live, on the first real morning after the humanity pass shipped.** He reported what he'd done that morning in a sentence carrying a distance; the reply held **twice** on `fabricated_number` and fell back to the deferral string, promising to "check" and telling him to ask again in a minute. He had not asked a question, and nothing in the system would have had an answer in a minute. He stopped texting. Generalises to every "I ran 3 miles" / "I weighed 214" / "I slept 5 hours". Filed #2517. | **The wiring, not the gate.** `telegram_worker_lambda.lambda_handler` called `run_turn(inbound=text, ..., grounder=_grounder_for(a))`. `_assemble()` runs BEFORE the turn, so `a["thread"]` held only PRIOR turns — the current message reached the **model** but never the **gate**, leaving any number in it unsupported by the allow-list. The gate was correct that the figure was absent from platform facts (nothing had ingested the activity); it should never have been asked to adjudicate the user's own words. The sibling referral path already passed its conversation `tail` (`_grounder_for(a, tail)`) — that asymmetry between two callers of the same helper was the tell. | ~9h (shipped ~06:00Z, detected 14:46Z by reading the live thread — **no alarm exists for a held reply**; the only trace was one `WARNING` log line). | ~30 min from detection to deployed fix (PR #2518, merged `5e187d87`, `deploy_lambda.sh telegram-coach-worker`, symbol-verified live 15:15Z). Fixed by adding the inbound message as an evidence source. Guarded by three tests — the property, **its complement** (an unsourced number must still fail, so the fix is not a grounding bypass), and a night-class test proving #2343's class stays armed — plus an AST pin at the call site, because a gate-level test passes with the bug still present. | No — nothing written; replies were withheld, not falsified. The gate never let a fabrication through. |
| 2026-08-10 | **P3** | **Near-miss: the #1970 phase-stamp backfill would have marked Matthew's entire coach conversation history for deletion — and the platform's own nightly QA was instructing the operator to run it.** Caught in dry-run during #2379 triage; **`--apply` was never run and no row was written.** Measured live: all 21 rows in scope would have been mis-stamped — 15 `cross_phase` (`CHAT#`, `CHAT#summary`) and 6 `system_state` (`DEDUPE#`); **not one was genuinely `experiment_scoped`.** Filed #2520. | `deploy/backfill_coach_ensemble_phase_stamps.py::query_unstamped` sweeps **entire** `COACH#*`/`ENSEMBLE#*` partitions and stamps every row lacking `phase`, with **no call to `phase_taxonomy.classify()`**. Sound when written (#1970), when those partitions held only `PREDICTION#`/`BRIEF#` rows. **ADR-153 then added `CHAT#`/`RELATIONSHIP#` rows to the same partitions**, classified `CROSS_PHASE` ("NEVER tagged") precisely so conversation history survives a reset. A sweep scoped by **container** rots silently when the container gains new row classes. Two compounding failures: `qa_smoke_lambda` printed a literal `Run ... --apply` remediation, and its coverage check counted the correctly-unstamped `cross_phase` rows as gaps — so the count **grew every time he texted a coach** and could never reach zero, a standing feeder of the `qa-smoke-failures` saturation (#2379). | ~6 days latent (ADR-153's chat rows began landing 08-04); found by classifying the dry-run output rather than trusting the tool's own recommendation. | Not run. Fix in PR #2525: both the backfill and the nightly audit derive from `phase_taxonomy.should_phase_stamp()`, protected rows are reported as **visible skips** rather than silent omissions, and the `--apply` string is emitted only when every listed row is genuinely safe. Mutation-proved both directions at both surfaces. Live dry run now reports `0 to stamp, 21 protected`. | No — averted. The stamp writes under `attribute_not_exists(phase)`, so it would **not** have been reversible by re-running. |
| 2026-08-10 | **P4** | **Site auto-rollback on the `/legacy` privacy-scrub deploy — visual-QA read every `/protocols/discoveries/` readout as an empty placeholder and gated (`site-deploy` 31353488934).** The rollback reverted a merged privacy fix (#2500) over a page that was, in fact, healthy. | Two causes compounding. (1) The all-empty check cannot distinguish an honest genesis dark state from a broken bind — a cycle-13 Day-1 page whose collections were wiped renders placeholders legitimately. (2) The actual trigger was a **transient origin hang during the concurrent fleet deploys**: `getJSON` had no timeout, so a fetch that never *settles* slid past `tryJSON`'s catch-only guard and stranded the pre-rendered skeleton — a hazard shared by ~15 renderers across all four doors. Diagnosed with Playwright against live + a route-mocked matrix of 8 payload shapes; the page rendered correctly in all of them. | ~2 min (the gate names the selector). | ~35 min — #2511 (probe the page's own `api_deps`; affirmatively-empty payload downgrades to a warning, unknown/non-200/populated still gates) and #2514 (`AbortSignal.timeout(10000)` at the shared chokepoint). | No — statics only; the API served correct data throughout. |
| 2026-08-10 | **P3** | **Main's reconcile lane died on three consecutive runs (`01b0b518`, `abad6cc7`, `9ff9512b`), stopping doc-literal reconciliation platform-wide.** Every merge after the #2503 privacy scrub failed at `Reconcile derived artifacts`. | #2503 made the content-filter vocabulary fail CLOSED (raise) everywhere — correct for publishing, but one of the six reconcile generators (`v4_build_game_explained.py` → `privacy_guard.assert_clean`) runs inside a `bash -e` script in CI, which has no `CONTENT_FILTER_JSON` secret until the owner injects it. One raise killed the whole lane. | ~6 min (first red run inspected). | ~12 min — #2510 scopes the fail-closed posture to the ARTIFACT: with no vocabulary the page does not regenerate (nothing unscreened ships; the committed artifact was screened when written) and `main()` exits 0 with a loud HOLD line. | No — CI-plane only. |
| 2026-08-10 | **P4** | **Main's Lint job red across three runs (`619c7b33`, `b50de4c3`) on five flake8 violations that had landed silently.** | The #2503 scrub's own main run died at the then-broken reconcile step (row above) **before the Lint job could report**, so F841 (an unused exception binding) and four E225s (missing operator whitespace inside f-string conditionals) reached main unseen. A second, self-inflicted instance followed: a file formatted with the pinned black 26.3.1 was rejected by local black 25.9.0 in the pre-commit hook — the known both-ways pin skew. | ~4 min. | ~25 min across #2512 and #2514; whole-repo `black --check` (pinned) + `ruff` over all six dirs verified clean after. | No — CI-plane only. |
| 2026-08-10 | **P4** | **A merge to main minted ZERO workflow runs — GitHub swallowed the push event (`1a7d66c4`, the #2505 merge).** The commit touches `lambdas/**`, `cdk/**` and `config/**`, all ci-cd trigger paths, yet no run exists for that sha in any workflow. | Dropped push event, not a lease problem: `check_deploy_wedge.py` reported no wedge, and querying by `head_sha` returned an empty run set (distinct from the 'pending, 0 jobs' phantom-queue class). Cause is GitHub-side and unattributable from here. | ~10 min (noticed when the expected run never appeared during the wrap sweep). | Not recovered by dispatch **by deliberate choice** — the run would red at Plan anyway on #2505's own IAM diff (R8-ST6), and the owner's required `cdk_deploy.sh LifePlatformServe` ships that Lambda code, the schedule and the IAM together. State documented in the handover instead. | No — the merge is safe on main; only its deploy is pending. |
| 2026-08-10 | **P4** | **Site auto-rollback on the coaching-roster merge's deploy — visual-QA axe AA contrast failure on `.rd-badge.wu-carried` (/method/pipeline/).** The rollback fired correctly and the site briefly served pre-roster statics against the already-deployed v2 API (tolerable: the board-ask legacy map covered the old JS). | Data-driven dark state: the badge style shipped weeks earlier, but its FIRST live render arrived with the cycle-13 carried baseline at the genesis flip — `opacity:0.85` sank the badge text below AA only once a carried metric existed to render. | ~2 min (the gate names the node). | ~40 min (PR #2476: dashed-border-only; next deploy passed the full sweep). | No — statics only, APIs unaffected. |
| 2026-08-09 | **P4** | **The cycle-13 reset's CI fleet deploy red on ONE function — a concurrent-deploy ResourceConflictException race.** The approved ci-cd Deploy (run 31337080891, fde52829) ran while `restart_pipeline --apply`'s own re-run was updating the same fleet; `coach-panel-podcast` hit `UpdateFunctionCode` mid-update and the job failed 103/104 (smoke/QA skipped, rollback correctly did NOT fire). Same code both paths, so no drift — but the run read red and its gated deploy became a parked lease. | Two same-code deploy paths (CI deploy_all + the reset pipeline's stack deploys) allowed to overlap by the driver; Lambda serializes UpdateFunctionCode. Recovered by rejecting the superseded gate (the #2467 no-parked-zombies discipline) and letting the tip run (076d348b) go green; the missed function re-covered by the pipeline's own deploy + manual redeploys. | Immediate (watched run). | ~55 min to green tip run. | No — one function briefly on N-1 code identical in behavior; fleet verified at tip via ancestry postflight. |
| 2026-08-09 | **P3** | **The deploy path dark ~6.5h across the overnight burn — merge-storm supersession then the #2052 phantom wedge, TWICE.** ~40 merges at ~20/hour meant every ci-cd Deploy was superseded (safe — nothing stale shipped, but nothing shipped at all); the survivor run (`31306913970`) then sat run-level "pending, 0 jobs" from 09:51Z — the phantom-wedge class: GitHub evaluates concurrency BEFORE the environment rule, so `pending_deployments` stays empty, the approval watcher correctly never fires, and the UI's "waiting for approval" is a lie. The first recovery dispatch then failed unit tests (the union row below) and ITSELF phantom-wedged. Compounded by two session harness stalls (~10:00–12:16Z, ~12:45–16:00Z) during which the driver could not act. | The #2052 class, recurring at merge-storm scale; the standing `Deploy wedge watch` workflow caught it (hourly loud failures + a remediation urgent_alarm) and its own escape hatch recovered it: `python3 scripts/check_deploy_wedge.py --recover` (cancel + `deploy_all=true` dispatch). | Wedge-watch: ~34 min to first loud failure; driver: at the 12:16Z stall recovery. | First recovery 12:17Z; final deploy-of-record dispatch at the wrap (~17:4xZ). | No — nothing stale shipped; the fleet simply stayed on the prior day's code all session. |
| 2026-08-09 | **P4** | **`main` unit-tests red on the first full-UNION suite run of the burn — a new module × a derived contract scan neither PR executed.** The recovery dispatch's full suite failed `test_todoist_reader_writer_contract`: #2438's new `health/pillar_absence.py` (merged green) queries the todoist partition, so the DERIVED reader scan flagged it — unregistered in READER_INVENTORY, and its `"habitify"` source-name token read as an unwritten record field. Each contributing PR was green alone; the contract file was in neither PR's executed set. | The union class (#2258's family) one layer over: a derived tree-sweeping scan meets a module born after its inventory. Fixed forward same hour — PR #2463 registers the reader + a NON_RECORD_KEYS exemption with the stated reason. | ~5 min (the assertion names the module and the cure). | ~30 min to merged fix; green proven by the wrap's deploy-of-record dispatch. | No — docs/test-plane only; the fleet was unchanged throughout. |
| 2026-08-09 | **P4** | **`main` unit-tests red ~3h total — the post-merge suite SERIALLY caught the new Telegram surface on two set guards.** Run `f7f56184` failed the #946 singleton-tombstone guard (the chat worker's unguarded `RELATIONSHIP#state` read); its fix's run then failed the serve-stack Throttles-alarm guard (both new Lambdas alarm-less, #1328's rule). Every run's Deploy + smoke were green, and the deployed worker was functionally fine TODAY (no tombstones exist mid-cycle; no throttles occurred). | Both guards live only in the post-merge full suite — the PR fast lane structurally could not have shown either, so a new multi-Lambda surface pays one full-suite round-trip per missed registry. Fixed forward with `singleton_visible()` (PR #2403) and the two alarms + regenerated count literals (PR #2404, 39s stack update, alarms verified live in CloudWatch). | ~10 min each (job logs name the call site / the set). | ~3h to the fully-green run `c1eb156d` (each fix merged within the hour; the window is run-cadence-bound). | No — both latent-only. |
| 2026-08-09 | **P4** | **`main` unit-tests red ~65 min on a stamped doc literal after a `deploy/`-only merge — no reconcile bot came.** PR #2397's ARCHITECTURE.md secrets-table row made the inventory 26 against a stamped count of 25; the #1957 checker redded the Unit Tests job on run `59e6e370` (04:52Z). The same run's **Deploy job succeeded** — the fleet was on the tip throughout. | The stamp lives in `deploy/sync_doc_metadata.py`, and the fix (#2399) was a `deploy/`-only merge — a path that triggers **neither ci-cd (so no fresh run to prove green) nor its reconcile job (so the four dependent doc literals also went stale, #2400)**. | ~5 min (the checker names its own fix). | ~35 min to merged fix; the visible-red window ran longer because no run existed to re-read. | No — docs-plane only; deploys healthy throughout. |
| 2026-08-09 | **P4** | **`main` red ~35 min on a visual-QA failure I caused by my own deploy timing — a false positive.** The gating `visual-qa` job reported `/data/zone2/` with all `[data-readout]` blocks empty/placeholder, 92 passed / 1 failed. | **A cold-Lambda window, not a defect.** The job sampled the live page at 22:45Z, minutes after I redeployed `life-platform-site-api` (22:36Z) and `life-platform-site-api-ai` (22:38Z) by hand; the page's readout waits on `/api/zone2`, whose 1800s cache had just been invalidated by the deploy. Re-running the same check warm passed, and the origin answered in **25–29 ms** across three samples. The #1911 "measure at the ORIGIN" class, recurring on the render side rather than the perf budget. | ~10 min (the next completed run read green). | None needed — subsequent runs green. Recorded rather than dismissed because a transient that looks exactly like a real empty-readout regression is the thing that trains an operator to ignore this gate. Root avoidance is to stop hand-deploying into a visual-QA window, not to loosen the check. | No — the API was healthy and honest throughout (`available: true`, `zone_2_minutes: 0.0` on one Zone-1 activity in a 5-day-old cycle). |
| 2026-08-09 | **P3** | **A CI deploy raced over a manual one and shipped a `site-api` bundle missing a merged PR.** `verify_deployed_symbol.sh` found `logged_days` **absent** from `web/site_api_meals.py` in the deployed zip, on a function whose `LastModified` (22:14Z) was newer than my own deploy. | Seven CI/CD runs were in flight concurrently, each deploying the fleet at **its own SHA**. An older run's deploy landed after my manual deploy of a newer tree — the documented "CI deploy race overwrites manual deploy" class, with the twist that the timestamp looked *newer*, not older. | Immediate — but only because the bundle was checked. A `LastModified` comparison would have read as a successful deploy. | Redeployed from current `main` and re-verified: `logged_days` present 3x, plus `_num` (18x) and `_envelope` (18x) for the two sibling PRs in the same bundle. | No — reader-visible for ~6 min; the missing code was an absence-honesty improvement, so the pre-fix behaviour was the old (wrong-but-stable) output, not an error. |
| 2026-08-08 | **P4** | **`main` red ~70 min — two merges each crossed the 1,200-line module-size ceiling on the same afternoon.** #2293 took `monthly_digest_lambda.py` 1099 → 1316 and #2285 took `wednesday_chronicle_lambda.py` 1156 → 1250; `tests/test_module_size_guard.py::test_no_new_oversize_module` went red on main. | **Both additions were correct code.** Each file sat just under the ceiling, so the next legitimate change to either was always going to cross it. The guard's `BASELINE` registry is documented as shrink-only and its `# module-size-exception:` escape is scoped to generated/registry files, so neither escape hatch applied — the fix had to be a real split. **This was only visible at all because `set -o pipefail` landed hours earlier (#2259);** before that the coverage step swallowed its own exit code and this would have reported green. | Immediate once the gate could fail. | Two splits merged the same session: PR #2294 (`monthly_digest_render.py`, 1316 → 938) and PR #2295 (`chronicle_personas.py`, 1250 → 1080). No BASELINE entry added, no exception comment used, no public contract changed. | No — structural guard only; no production defect shipped. |
| 2026-08-08 | **P4** | **`main` red ~30 min — the sibling 2,000-line god-module gate, twice.** #2284 took `site_api_ai_lambda.py` 1988 → 2099; then #2292's six-line ADR-104 guard took `ai_expert_analyzer_lambda.py` 1999 → 2005. | The repo has **two independent size guards** with different limits and different registries (`test_lambda_size_gate.py` at 2000 for `*_lambda.py`, `test_module_size_guard.py` at 1200 for everything). A file can slip one and trip the other, and `ai_expert_analyzer_lambda.py` sat at **1999** — one line of headroom, so any correct change to it was going to red main. | Immediate. | Split rather than grandfathered, since grandfathering a file grown the same day is precisely what the gate exists to prevent: `web/site_api_ai_prompt.py` (2099 → 1991) and `intelligence/expert_personas.py` (2005 → 1886). The first split then needed the phase-context census **and** the grounding-surface registry repointed — both guards caught it on the first full-suite run. | No — structural guards only. |
| 2026-08-08 | **P4** | **`main` red ~25 min — a union breach between a coverage tranche and a privacy gate.** #2208 added `tests/test_site_api_meals_behavior.py` asserting the **ungated** `/api/food_delivery_overview`; #2210 then gated that endpoint default-OFF (#2209, a P1). 15 tests failed with `TypeError: 'NoneType' object is not subscriptable`. | **Each PR was green on its own.** The PR fast lane runs *collect + deploy-critical*, so `*_behavior.py` only ever executes in the post-merge Unit Tests job — the two changes could not be seen to disagree until both were on main. | Immediate (post-merge job). | Fixed forward `520a7c12`: the 15 tests opt into the flag via a `delivery_public` fixture rather than the gate being weakened, two tests that were only *vacuously* passing behind the gate restored to tracking their real defects, and the gate's own xfail flipped to a mutation-proven assertion. | No — test-only; the shipped gate was correct throughout and nothing reached a reader. |
| 2026-08-08 | **P4** | **`main` red ~50 min — the identical union-breach class, one layer over.** #2229 (tranche 3) added `tests/test_daily_brief_behavior.py` + `tests/test_weekly_digest_behavior.py` pinning the **ungated** `get_food_delivery_{brief_signal,digest_line}`; #2251 then gated both default-OFF (#2233, the live-publication P1). 7 tests failed across three CI/CD runs (`31248982320`, `31249147069`, `31249572646`). | Same mechanism as the row above — and the same *two change types* colliding: a coverage tranche that pins current behaviour, and a privacy gate that turns that behaviour off by default. Both are routine work here, so the collision recurs by construction. **Driver error compounded it**: having fixed the identical collision ~4h earlier, I did not check whether the tranche pinned those two functions before merging the gate. | Immediate (post-merge job). | Fixed forward `023874f0`: 11 tests opt in via a fixture — **6 of them were still passing and asserting absence vacuously**, i.e. they would have passed against a function stubbed to `return None`. Also flipped the xfail that originally *discovered* #2233 into a real assertion, mutation-verified. Root cause filed as **#2258** (the pre-merge lane is a strict subset of the post-merge one — three reds in 24h), coupled to **#2259**. | No — test-only; no production defect shipped. |
| 2026-08-08 | **P4** | **Main red ~63min: #2200's `test_branch_protection_spec.py` needs PyYAML, which was declared NOWHERE — not in `requirements-dev.txt`, not in any workflow.** The PR was green because the fast lane only COLLECTS those tests and `import yaml` sits inside `preflight_contexts()`, not at module scope; only the post-merge full Unit Tests job executes them. Fixed forward `e731ebef` (pin added to `requirements-dev.txt` + `ci-test.yml`, registered in `_GATED_TOOLS`). | An in-function import of an undeclared dep — a lane that collects but does not execute is structurally blind to it (same family as the PR-checks-lack-the-mypy-gate class). | ~1h (gate monitor surfaced the failed run). | ~63min. | No. |
| 2026-08-08 | **P4** | **Unresolved `<<<<<<<`/`=======`/`>>>>>>>` conflict markers merged to main in `docs/TESTING.md` via the driver's own #2200 reconcile — and EVERY gate passed.** The rebase conflicted on two doc-sync literal files; only one got `checkout --theirs`, then `sync_doc_metadata.py --apply` rewrote the literal on BOTH sides of the still-conflicted block, making them byte-identical. `--check` passed (the literal it greps for was present and correct), lint/black/mypy never read markdown prose, and `check_main_green.py` reported GREEN. Fixed forward `367896b4` + `tests/test_no_conflict_markers.py` (set derived from `git ls-files`, self-mutation-proven, verified to fail against the real broken content). | Partial conflict resolution + a tool that writes *inside* a conflict block; the failure mode is specifically "a conflict whose two sides are identical", which is the doc-literal class this repo hits on nearly every concurrent PR. | ~40min (spotted by a subagent reviewing an unrelated PR, not by any gate). | ~15min once identified. | No. |
| 2026-08-08 | **P4** | **Main red ~45min: #2161's Bluesky/Mastodon sources tripled every post in 4 of #2168's tests — the `_FakeTable` answered every partition pk with the same rows, so the membrane loop's 3 sources returned each post 3×.** Fixed forward `4e0346a2` (fake keys rows to ONE source partition, like real data). | pk-blind test fake + a source set that grew — guard-the-SET recurrence in test-harness form. | Minutes (gate monitor). | ~45min. | No. |
| 2026-08-08 | **P4** | **Main red ~35min after the owner layer rebuild: `garminconnect 0.2.40→0.3.8` flipped deployed truth under a test that HARDCODED the old pin, the pillow drift fixture's 11.3.0→12.3.0 hand-edit became a no-op when reality caught up, and #2162's gate-registry cell exceeded the 120-char pointer cap.** Fixed forward `0428b2ea` (test derives from the deployed record; fixture injects synthetic 99.0.0; cell trimmed). | Version literals in tests vs a moving deployed truth — the #2099 gate itself was green throughout. | Minutes. | ~35min. | No. |
| 2026-08-07 | **P4** | **Main red ~2.2h: #2182's `garmin_client.connectapi` imported `garminconnect` (layer-shipped, absent in CI) at method top — the happy path itself required the wheel, so `test_adapter_passes_results_and_kwargs_through` redded the merge run (31201902796) and two follower runs while the noise of interleaved green runs made it look transient.** Fixed forward `67622779` (import moved inside the error-translation branch, guarded like `_is_garth_http_error`; absent wheel → re-raise untranslated). Deploys unaffected (failed runs skip Deploy; the fix run deployed the fleet green). | Call-time lazy import of a layer-shipped dep + the implementer env had the real wheels installed, so its full local suite was green by construction (new face of the layer-dep test class — memory rule extended). | Minutes (gate monitor flagged the failed run). | ~2.2h to green main (fix authored <30 min after diagnosis). | No. |
| 2026-08-06 | **P3** | **GitHub Actions platform incident spanning ~14h of the overnight session, three presentations: (1) a ~9h runner-starvation stall — the 05:49Z-approved deploy sat `queued` until 14:43Z (an approved gate + queued Deploy is indistinguishable from a wedge; the wedge checker correctly said "progressing"); (2) "Failed to resolve action download info" 500/503 Set-up-job reds across ~6 runs; (3) swallowed events into the evening — pushes minting no CI/CD runs and one PR branch (#2160) minting no check suites through empty commits, the rerequest API, close/reopen AND a fresh-branch swap (#2167).** Recovered via `gh run rerun --failed` + two `deploy_all=true` dispatches (final: run 31136765457 full-green) + PR swap. | GitHub-side platform incident (public repo, billing fine — the third distinct presentation of the [[push-CI silent death]] class). | ~15 min (deploy-wedge-watch red overnight) but ~8h to correct diagnosis (queue stall ≠ stranded gate). | Rolling; final full-green pipeline ~01:30Z 08-07. | No — delays only; every merged PR deployed green by wrap. |
| 2026-08-06 | **P4** | **Main red on #2157's merge: `digest_utils.py:131` mypy `var-annotated` failed ci-cd's lint job — the PR's own checks don't run the mypy tier-2 gate, so a touched clean-set file red-mained only after merge.** Fixed forward same hour (`bfb980c0`, one-line annotation). | PR-check surface lacks the mypy gate (memory rule + reconcile reflex saved: mypy changed clean-set files before merging). | Minutes (gate monitor). | <1h fix-forward. | No. |
| 2026-08-06 | **P4** | **Main red on the dependabot garmin-layer manifest bumps (#2102/#2103 urllib3/idna): `test_live_tree_has_no_manifest_drift` (#2099) failed — the manifest is asserted EQUAL to the deployed layer, so a version bump without a promoted rebuild is a codified lie, not merely inert.** Both bumps reverted (`36438d39`); they ride the owner's layer rebuild instead. | Driver judgment error: merged manifest bumps ahead of the rebuild despite the dependabot-blind memory rule (rule now strengthened: never merge ahead). | ~1h (found decoding the deploy_all red). | ~30 min revert + resync. | No. |
| 2026-08-06 | **P3** | **Whoop's THIRD credential loss in six days: auth breaker latched 2026-08-04T00:00Z on "refresh 400 with unchanged refresh_token — genuine auth failure"; no DDB rows past DATE#2026-08-03.** Found via the qa-smoke freshness FAIL during the citation check; OPEN at wrap — owner re-auth + latch-marker delete (#2085) is owner-list item 1. | Credential revoked upstream again (classifier now correct post-#2076; root cause of the repeated losses unestablished — a 4th occurrence warrants its own issue). | ~1 day (scheduled qa-smoke). | OPEN (owner-gated). | Yes — whoop gap 08-04→ongoing until re-auth backfill. |
| 2026-08-05 | **P3** | **The deploy pipeline frozen ~21h behind a stranded production approval — run 30962630878 sat at the OPEN gate holding the deploy concurrency group while 13 merged PRs queued undeployed behind it.** The session driver deliberately left the gated run unapproved expecting supersession; gated runs are never superseded — a gated run is a LEASE on the deploy group. `deploy-wedge-watch` went red within ~15 min and stayed red across six runs, but its loudness channel is passive (a red workflow tab) and the session's own watchers polled the queued run's gate, which could never materialize. Unfrozen by `check_deploy_wedge.py`'s printed recovery (approve the holder); the 13-PR union then deployed green. | Driver process error (unapproved gated run) + narrow per-run watchers + no active alert on a confirmed wedge (#2149 filed; memory rule saved). | 20.7h (operator noticed 'nothing is moving'; the detector was red at ~15 min with no consumer). | ~10 min from wedge-checker run to unblocked. | No — nothing was lost, only delayed; every PR deployed green after. |
| 2026-08-04 | **P4** | **Two union-breach main reds in one evening, each green-alone/red-in-union:** (1) #2121's tightened CSS-token sweep x #2122's new raw `font-size: 0.62rem` (~20 min red; fixed forward with a reasoned fs-ok sanction mirroring the tokens.css .provenance kit); (2) #2129's gloss helper pushing `weekly_correlation_compute_lambda.py` to 1217 lines over the #1665 1200-line ceiling (~40 min red across three merge-commit runs; fixed forward by extracting `correlation_gloss.py`). Deploy path green throughout, no rollback fired. | Concurrent-PR union effects invisible to per-PR checks (the documented #1863-era union class, third+fourth occurrences). | Minutes (gate monitor announced each failing run). | ~20–40 min each, fix-forward on main. | No. |
| 2026-08-04 | **P4** | **Visual-QA false positive during the #2121 deploy: the sweep ran mid-site-deploy and every page referenced the new hashed `evidence.css` before the asset landed (MIME text/html on the CSS URL), failing the run's Visual+AI-vision job.** The asset served 200 text/css minutes later once #2122's site-deploy completed; Deploy/smoke green, no rollback (visual-QA is excluded from rollback's needs). Auto-filed advisory #2147 closed with this row as its record. | Mid-deploy asset/HTML window (the #1526 smoke-vs-invalidation race class, on the visual-QA oracle). | Minutes. | Self-resolved with the completing deploy; verified by direct asset probe. | No. |
| 2026-08-04 | **P4** | **Main's full suite red ~55 min (three push runs' Unit Tests job) — PR #2091 moved `config/challenges_catalog.json`/`content_filter.json` out of seeds/ and `test_privacy_guard::test_vice_keywords_superset_of_content_filter` still opened the seeds/ path (FileNotFoundError).** The PR fast lane doesn't run that test, so the break surfaced only on main's push run; the deploy path (Deploy, smoke, integration, visual QA) stayed green on every affected run and no rollback fired. Under ADR-148's fast-lane-required-checks posture this class lands by design; the bar is a fast fix. | The implementer's targeted + `-k` test sweeps missed one full-suite-only consumer of the moved file. | Minutes (deploy-per-merge run watch). | ~55 min (one-line PR #2095, merged same hour; next push run fully green). | No. |
| 2026-08-03 | **P3** | **Whoop dark again ~12:00Z→20:00Z — one day after the owner's re-auth.** A 502 on the token endpoint at 12:00:27Z got zero retry, losing the server-side refresh-token rotation; every hourly run after got a 400 the breaker's classifier (401/403-only) never recognized, so no latch signal fired while the 12:00Z EventBridge event aged into the ingestion DLQ (the DLQ alarms DID fire and page — the purpose-built `ingest-auth-unhealthy-24h` stayed OK). Recovery needed the owner's re-auth PLUS a hand-delete of the still-latched `AUTH_FAILURE` marker (re-auth does not clear it — #2085); the next run gap-filled all 3 days, 4 records, 0 errors. | Rotation write not persisted before post-refresh work + a status-code-only auth classifier; durable fixes shipped same day (PR #2076: retry on the token endpoint, persist-immediately, call-site auth marking). | ~6h (found by an implementer's live investigation, not an alarm). | ~8h to full data recovery; code fixes same session. | No — gap-aware backfill recovered every day. |
| 2026-08-03 | **P4** | **The #2064 site-deploy auto-rollback fired on `LCP 10516ms > 2500ms` for `/story/attempts/` — a mid-deploy CDN-cold measurement, not a content defect** (the #1526/#1911 transient class on a new metric: perf budget instead of timeout). Deploy + smoke were green; the rollback reverted cleanly and the deploy-time proof-rebake meant the live content converged anyway. A clean workflow_dispatch redeploy went fully green. | Perf budgets measured through an invalidating CDN during the deploy window; the LCP gate has no origin-side or warm-retry discipline yet. | Minutes (watched run). | ~15 min (redeploy). | No. |
| 2026-08-03 | **P4** | **`LifePlatformMonitoring` failed its stack update and rolled back — #2067's kill-switch metric filters carried `dimensions` on plain-term filter patterns, which CloudWatch Logs rejects server-side (400) though `cdk synth` validates clean.** The rollback was clean; no alarm coverage was lost (the filters had never existed). | A CDK-synth-invisible service-side validation rule; fixed same hour with per-sender metric names (PR #2073), redeployed UPDATE_COMPLETE with all 3 alarms + the whoop auth alarm. | Immediate (deploy output). | ~45 min incl. the fix PR through CI. | No. |
| 2026-08-02 | **P4** | **The #2040 site-deploy failed smoke and auto-rolled back — the PR registered its new `/api/content_cadence` route as a hard smoke `api_dep` while the route's Lambda deploy sat behind the stranded owner-gated fleet queue.** The rollback fired correctly (site restored to pre-#2040 bytes); left unfixed, EVERY subsequent site-deploy would have failed the same way, re-blocking the site path behind the fleet deploy. The front-end is fail-soft on the 404, so the dependency was never page-breaking. | The #1704 API-before-frontend landing-order class, gate-registration flavor: honest surface registration (per #1454) arrived before the surface could exist. | Minutes — the driver was watching the run. | ~40 min: PR #2050 deferred the smoke `api_dep` + downgraded the visual-QA 404 to a dated pending-deploy warning (the #1404 precedent); redeploy went green. Re-arm rides the numbered ask's post-deploy schema capture. | No — rollback clean, site never broken. |
| 2026-08-02 | **P4** | **The next site-deploy dispatch crashed mid-job on `ModuleNotFoundError: boto3` in #2049's new config-twin `--apply` step — AFTER site files had synced, so the site went live with smoke + visual-QA SKIPPED and no rollback armed.** Verified green by local HTTP smoke (232/232) within minutes; the crash also pre-empted an un-reviewed first `--apply` that would have overwritten the 7 drifted `config/` objects without the owner read the PR itself called for. | The deploy job's runner env installs no Python packages (setup-ci is env-only) and the new step's import was never exercised in CI; separately, wiring first-run `--apply` into an automatic workflow contradicted the PR's own owner-gated-first-run posture. | Minutes (watched run). | ~25 min: PR #2054 adds the boto3 install to both new workflows and demotes the site-deploy step to check-only-with-warning until the owner's first reviewed sync (dated re-arm marker); its self-triggered run went fully green (deploy + smoke + visual QA). | No — site content was correct (verified); config objects untouched. |
| 2026-08-02 | **P4** | **A worktree-implementer pasted a live subscriber row's DDB key (hashed-email sort key + creation timestamp) into public PR #2012's body** as a documented merge-time delete command. Flagged by the security monitor at agent completion; body redacted ~minutes later and the recovery path replaced with a read-only discovery query. Residue: GitHub keeps public edit history on PR bodies — purging the revision needs the owner's UI action (PR #2012 → edited → delete revision). Value was a SHA-256 of an email (not directly reversible). | The implementer brief said to document the operator step but did not say "discovery query, never the concrete production key" — and a PR body on a public repo is a publication. | Minutes (agent-completion security flag). | ~5 min to redact + comment; revision purge pending owner UI action. | No — exposure of a hashed identifier, briefly, now redacted from the current body. |
| 2026-08-02 | **P2** | **`/api/genome_risks` served 111 personal genome variants — 116 dbSNP identifiers and 93 gene names, each with a per-locus risk classification — to any unauthenticated caller, cached 24h at the CloudFront edge.** Found by the Fable delta review's security lens, which graded that lens **F** on it alone (the 2026-07-16 anchor makes a public genome identifier an automatic F). Independently reproduced before any change. The platform had already written the opposite rule **four times** — `_GENETIC_TEXT_RE` in the SAME file ~900 lines away, `tests/test_labs_privacy.py`, `DATA_GOVERNANCE.md` Tier 2 owner-only, and closed #920/#945 — every one scoped to `/api/labs`. Duration is unbounded backwards: the route predates the #920 fix. | Guard the instance, not the set — **fifth documented recurrence**. When #920 found a genotype leak on `/api/labs`, the fix was scoped to that one handler; `handle_genome_risks` lives in the same module, reads the same owner-only tier, and was never enumerated as in scope. Its docstring ("No raw genotypes exposed") narrowly denied the literal form while the payload carried the identifying form. | Months (only when a never-before-run lens looked). | ~40 min — payload made aggregate-only (PR #1943), deployed, CloudFront-invalidated, verified live: 0 rsIDs / 0 gene names / 0 genotype tokens. Guard is now a SET scan over every captured public API schema. Whether ANY per-variant detail may be published is PRE-13, still deferred, still Matthew's. | No data lost — but personal genome data was publicly readable for an extended period. Exposure, not loss. |
| 2026-08-02 | **P3** | **~370 experiment-scoped rows permanently escaped the cycle-11 reset archive** and are feeding current-cycle coach reads. The wipe ran `2026-07-26T16:08:48Z`; genesis was `2026-07-27` (a sanctioned future-genesis reset, #931/#939). The 17:00 UTC daily pipeline still ran in the 15-hour gap, and every row it wrote carries `phase="experiment"`, no cycle, no tombstone — 336 across the 8 `COACH#` partitions plus 34 un-stamped `INSIGHT#` rows. Because they carry the *current* phase value, the read filter admits them. | The wipe is a point-in-time snapshot with nothing pausing or re-sweeping the daily writers between the wipe run and the genesis boundary — a gap that only exists for future-genesis resets, which are themselves sanctioned. | ~6 days (the delta review's data-architect lens, the first ever run at Day-6 distance from a reset). | Filed from the review; the fix is a post-genesis verification step deriving the partition set from `phase_taxonomy` rather than a hand list. | No loss — the rows exist; they are mis-attributed to the current cycle rather than archived to the closing one. |
| 2026-08-02 | **P3** | **The subscriber weekly email has been kill-switched since 2026-04-23 while `/subscribe/` keeps promising "The Measured Life every Wednesday"** — and the confirmation email repeats the promise. `EXTERNAL_EMAILS_ENABLED="false"` is pinned on chronicle-email-sender, weekly-signal and between-chronicle (commit `0e7abd03`, privacy mode) and was never lifted when the site went public. CloudWatch 2026-07-29: `[kill-switch] EXTERNAL_EMAILS_ENABLED=false — skipping Chronicle subscriber send`. | A temporary posture with no expiry, no re-enable trigger, and nothing coupling it to the live promise on the page — the **pause-that-never-lifts** class ADR-147/#1927 named for budget pauses, one layer out. The weekly skip logs at INFO where nothing watches. | ~3 months (the delta review's growth lens). | Filed; the fix couples the promise to the switch (fail when the page solicits, subscribers exist, and sends are disabled). Flipping the switch is Matthew's call. | No data loss — but confirmed subscribers received nothing they were promised for ~3 months. |
| 2026-08-02 | **P4** | **Predict-the-week was dark for all six opening days of cycle 11, and qa-smoke greened it every night.** `/api/predict_week` returned `active:false` because `current_challenge.json` carried `week_id 2026-W30` while the genesis week is W31 — `build_genesis_predict_week.py` stamps the week from wall-clock run time, so a natural pre-genesis prep run produces a challenge the API can never serve (the fail-closed behaviour itself is correct, #1198). `check_predict_week_freshness` returns `ok` on `active:false` unconditionally, so 6 dark days produced 6 green nightlies. | Two independent halves: a wall-clock-derived `week_id` in a tool that exists to seed a specific week, and a check written to catch only the inverse regression (stale bets solicited). | 6 days. | Filed as two issues (the seeder and the check). | No loss — zero predictions were solicited during the cycle's most-read week. |
| 2026-07-28 | **P3** | **A stranded `production` approval blocked EVERY CI/CD run on main for ~15h — and `check_main_green.py` read straight past it, so main reported green throughout.** Run `30324990970` (sha `8d9a05cc`) reached the deploy approval gate at 03:06Z and was never actioned. `ci-cd.yml` sets `concurrency: cancel-in-progress: false`, so every later run queued behind it: the #1653 merge's run (17:44Z) sat `pending` with **0 jobs** and never started, as did a dispatched recovery run. No alarm, no digest line, no red badge — the blocked deploy path was discovered only because a session went looking for its own run. Compounding it, the symptom (`pending`, 0 jobs) is byte-identical to the known phantom-concurrency-queue class, so it was initially mis-diagnosed and a healthy run was cancelled before the real blocker was found. | Not a fault in `cancel-in-progress: false` (a mid-flight deploy SHOULD complete). Two gaps: nothing reports a deployment awaiting approval past a threshold, and `check_main_green.py` reads the latest **COMPLETED** non-cancelled run — a run stuck in `waiting` is invisible to it by construction, so "main is green" and "main can deploy" silently diverged. | ~15h (only when a session looked for its own run). | ~10 min once identified — cancel the stranded run, re-dispatch with `deploy_all=true` (a `workflow_dispatch` has no push diff for change detection). Filed **#1901** for the detection gap; the distinguishing tell (phantom = no other run in the group; stranded = an older run in `waiting`) recorded in `reference_push_ci_silent_death`. | No — nothing deployed, nothing reverted; the fleet ran its existing bundle throughout. |
| 2026-07-27 | **P4** | **Budget tier crossed 1→2 under the ADR-133 July temp ceiling ($115) — ensemble/internal AI already paused at tier 1; tier 2 adds the reader narratives (coach commentary, State of Matthew, chronicle).** Observed at the day session's bootstrap (SSM `/life-platform/budget-tier` = 2, was 1 at the prior wrap). Live behavioral evidence same day: the ensemble digest logged "AI paused by budget tier — using fallback" and wrote the deterministic digest; journal conversational enrichment honestly reported `paused_by_budget`. | Not a fault: the cost governor's projection crossed the 87% band of the effective July ceiling — the ADR-063/ADR-133 design doing its job in the temp window's final week. Auto-reverts 2026-08-01 with the window expiry; the $85 AWS Budgets backstop deliberately still signals. | Same-session (bootstrap tier check). | Self-resolves 08-01 (window expiry); no operator action sanctioned mid-window. | No — pausing is the designed protection; daily brief protected longest by design. |
| 2026-07-27 | **P3** | **The Lambda rollback net's FIRST true mass firing — and it worked: 98/99 functions cleanly reverted through the freshly-seeded #1848 artifacts (16:16–16:34Z, Day 1 morning).** The signal was a false positive: qa-smoke's `dashboard/matthew/data.json` staleness check used a 4h max-age against a file whose ONLY writer is the 1x-daily 17:00 UTC brief — stale-by-design ~20h/day, historically green only because the nightly qa-smoke cron lands a few hours post-brief; a `deploy_all`'s smoke at 16:15Z inherited a guaranteed FAIL. The revert put the fleet back on the 01:26Z bundle (pre-waves-1/2) for ~45 min; the sole revert failure was `email-subscriber` (us-east-1 — rollback_lambda.sh is us-west-2-hardcoded, function simply kept the new code). This firing closes the #1345 "never fired a true positive" gap with real evidence: detection→tally→per-function reverts→notification all behaved as designed. | The freshness window encoded the CHECKER's cron position, not the WRITER's cadence (same honest-signal-wrong-threshold family as the 07-26 weight-null). | Immediate (deploy-watch). | ~35 min — window fixed 4h→26h (`b79469ff`), dashboard freshened via dry-run brief, fleet restored via `deploy_all` on tip. Residual: us-east-1 rollback support gap noted on #1848. | No — reverted functions ran the older-but-functional 01:26Z bundle; no records touched. |
| 2026-07-27 | **P2** | **RECORD CORRECTION + incident: the 2026-07-26 auto-rollback (row below, filed as "rollback non-fire, fleet stayed on wanted code") actually attempted all 99 functions and REVERTED 50 of them** — including life-platform-mcp, withings-data-ingestion, notion-journal-ingestion, wednesday-chronicle, cost-governor. The fleet ran MIXED-VERSION from 00:12Z to the repair fleet deploy at ~01:33Z (~1h20m) on genesis eve; the wrap's "0 succeeded, 1 failed" was the FIRST function's per-invocation summary — the job's final tally read `50 succeeded, 49 failed`, 99 interleaved summaries above it. The 49 "failed" were the no-artifact class (benignly KEPT fleet code). Caught next session during #1848 evidence-gathering; corroborated by 4 live-AWS integration failures + LastModified stamps in the 00:01–00:12Z window. | Two compounding gaps: fleet deploys never wrote per-function `previous.zip` (so "previous" meant an arbitrarily stale individual deploy), and the rollback job's per-function summaries interleave so the first one reads as the total. | ~1h (next session's bootstrap evidence pass) | ~20 min once found — full fleet redeploy (fc18f0c2) restored uniformity AND seeded latest/previous for every function (deploy_fleet.sh fix); rollback now tallies reverted/no-artifact/failed distinctly with a mixed-version ::warning (#1848). Memory: `reference_rollback_partial_fires_mixed_fleet`. | No — reverted functions ran older-but-functional code; gap-aware ingestion backfilled; no records lost. |
| 2026-07-26 | **P3** | **A driver `git add -A` reconcile commit swept a concurrent agent's mid-work edits into pushed main (`d26f2a74`)** — 39 unreviewed lines in dispute_docket.py + conversation_enrichment.py rode a "doc-sync literals" commit. A worktree-implementer had briefly violated isolation and edited the shared checkout while the driver staged with `-A`; the agent honestly reported the overlap in its PR (#1836), which is the only reason it was caught. | `git add -A` stages whatever is in the tree at that instant; during a fan-out the driver cannot assume sole ownership of the shared checkout — the worktree rule's occasional failure plus `-A` turns a stray edit into pushed history. | Agent's own PR report (~2h after the push) | ~15 min — both files restored to `d26f2a74^`, the clean tested PR #1836 merged instead; driver staging switched to explicit path lists (memory: `reference_git_add_a_sweeps_concurrent_agent_edits`). | No — the swept code was additive and replaced by the reviewed version same session. |
| 2026-07-26 | **P4** (false positive) | **The rebuilt smoke oracle's FIRST live fire red the `deploy_all` smoke leg and the auto-rollback then failed 0/97** — qa-smoke returned `{"failed":1}` ("weight is null — expected data") inside a 200 body, which the #1831-hardened oracle now correctly reads as FAIL; but the failing check was the CHECKER being wrong for pre-genesis (a null weight the night before genesis IS the honest state, ADR-104). Rollback found no `previous.zip` for the first function and stopped — fleet stayed on the new (wanted) code. **[SUPERSEDED — see the 2026-07-27 P2 correction row above: the rollback did NOT stop; it reverted 50 of 99 functions.]** | Two latent gaps exposed by the new strictness: qa-smoke's weight sanity check had no pre-start concept, and `deploy_all` runs never seeded rollback artifacts. | Immediate (deploy-watch on the run) | ~30 min — qa-smoke got a pre-start/Day-1 weight-null grace (`39f01e88`, hot-deployed); rollback-artifact seeding filed #1848; suite-runtime blowup observed en route filed #1847. | No — deploy was healthy (integration + visual QA green); nothing actually rolled back. |
| 2026-07-26 | **P3** | **The cycle-11 prereg publish silently OVERWROTE the "Night Before Everything" journal page live — both genesis−1 posts computed the same `week-02` page key.** Promoting the eve article into PRELAUNCH_CALENDAR at days_before=1 put two chronicle installments on the same date; the journal renderers indexed seq by DATE ONLY (`all_dates.index(date)`), so the prereg chapter's in-process re-render wrote its page over week-02 and the manifest pointed both entries at the same URL (~40 min reader-facing, one article unreachable). | Date-only seq/label indexing across three parity implementations (restart_leadin_pages, journal_post_ref, publish_to_journal closures) predates the first legal date tie — the tie only became possible when the calendar gained a second genesis−1 entry alongside the prereg chapter. | Spotted in the publish output (the same `week-02/*` invalidation path appearing twice) during the eve sequence | ~40 min — `(date, sk)` composite keys in all three implementations (`ac753774`), live pages re-rendered Part I/II/III distinct, regression tests on both sides of the parity. | No — the overwritten page was regenerated from its DDB record; DDB was never touched. |
| | 2026-07-26 | **P3** (false positive) | **A spurious post-deploy smoke failure auto-rolled-back a HEALTHY `deploy_all` — 50 of 97 Lambdas reverted before it was caught.** The `deploy_all` deployed the wave fleet successfully and the qa-smoke Lambda returned `{"failed":0}` (checks all green), but the smoke-test job's step then died with `python3: can't open file '.../deploy/lib/smoke_oracle_decision.py'` → exit 2 → the auto-rollback trigger (`smoke==failure && deploy==success`) fired and reverted 50 functions (the other 47 failed the revert with `No previous artifact found at s3://…/deploys/<fn>/previous.zip` — newer functions with no prior deploy), leaving a mixed but functional fleet (wave changes were additive). | #1345 extracted the smoke/canary oracle to `deploy/lib/smoke_oracle_decision.py`, but the `smoke-test` job has **no `actions/checkout`** (the old inline Python needed none) — the script was absent at runtime, so a CI-tooling error read as a real smoke failure (`reference_ci_extracted_script_needs_checkout`). Review miss on the #1345 PR. | Deploy-monitor saw `Deploy=success Smoke=failure rollback=in_progress` | ~25 min — added the reconciled-tree checkout to the smoke-test job (`44e7bc5e`), re-`deploy_all` deployed all 97 fresh (Smoke green, rollback skipped), settling the mix. | No — the deploy was healthy; the reverted half ran prior working code; re-deploy restored the wave. |
| 2026-07-25 | **P4** | **Wave-2 `deploy_all` failed at the ENFORCED ruff lint gate — a new `tests/` file's import block tripped I001, unseen by a local `ruff check lambdas/ mcp/`.** #1381 Theme River's test `import theme_river` (after a `sys.path.insert`) had an un-sorted import block; CI runs ruff over all 6 dirs (`lambdas/ mcp/ cdk/ tests/ scripts/ deploy/`), the narrow local/agent run only 2 — so it passed locally + full `pytest` but failed CI at **lint, before the deploy job**; nothing shipped. | The CI ruff gate covers 6 dirs; a narrow local run misses `tests/`/`scripts/` I001. Combined-tree pytest + black + a 2-dir ruff ≠ the CI lint gate (`reference_ruff_full_dir_set`). | Deploy-gate monitor saw the run complete `failure` at lint instead of reaching its gate | ~15 min — `ruff check --fix` (whitespace-only), `54ac4a9a` tail commit, re-fired `deploy_all`. | No — caught at lint pre-deploy; behavior-identical whitespace fix. |
| 2026-07-25 | **P4** | **`/story/theme-river/` served 404 for a brief window after the wave-2 merges — the newer page's S3 object was removed by an older site-deploy's `--delete` sync.** #1377 (parent, no theme-river) and #1381 (adds it) each triggered a site-deploy; the older #1377 deploy re-synced its tree LAST with `--delete`, deleting the just-uploaded theme-river object. The page file was in git; only the S3 object was gone. | Concurrent site-only merges race: an older merge's site-deploy re-syncs its pre-page tree last and `--delete` strips the newer merge's page (`reference_ci_deploy_race_manual_overwrite`). | Post-deploy curl: `/story/theme-river/` 404 while the S3 key was absent | ~10 min — `bash deploy/sync_site_to_s3.sh` from main re-uploaded + invalidated; verified 200. | No — one page 404'd briefly; others unaffected. |
| 2026-07-25 | **P4** | **`main` CI/CD red ~35 min on the #1654 chronicle merge (`46747b4d`) — the ENFORCED content-policy scan (a CI-only ci-lint gate, NOT run by `pytest`) failed on blocked-term matches in the new `chronicle_prompt.py`.** The chronicle god-module split moved the Elena system-prompt — whose substance-privacy guardrail literally enumerates the blocked vice terms to instruct the model to NEVER name them — out of the allowlisted `wednesday_chronicle_lambda.py` into `chronicle_prompt.py`, which wasn't on `ALLOWLIST_FILES`. The full suite was green pre-merge (the scan isn't a pytest test), so it surfaced only in CI, and the run failed at **lint, before its deploy job** — no bad code shipped. | Path-keyed CI exemptions don't follow moved code: `scripts/content_policy_scan.py`'s `ALLOWLIST_FILES` is keyed on file path, so a refactor that relocates allowlisted content to a new file breaks the exemption. The scan is a CI-only ENFORCED ci-lint step (#354) not covered by `pytest tests/`. Same class the remaining god-module/email-lambda splits will re-hit (`reference_content_policy_allowlist_follows_path`). | ~ (deploy-gate monitor saw the chronicle run complete `failure` instead of reaching its gate) | ~35 min — hotfix #1731 allowlisted `chronicle_prompt.py` (same pattern as `panelcast_scripts.py`, #1185); a `scripts/`-only hotfix does NOT trigger CI/CD (not in the trigger paths), so cleared the red with a `deploy_all=true` `workflow_dispatch` that also deployed the stranded chronicle-email + reconciled the fleet. | No — CI-lint only; the deployed chronicle lambda kept running the behavior-identical monolith until `deploy_all`; no reader impact. |
| 2026-07-24 | **P3** | **CI/CD on `main` could not dispatch for >2h — a phantom stuck `concurrency`-queue entry wedged every run at `pending` / 0 jobs.** Carried over from the prior session's wrap; three fresh runs (push + two `workflow_dispatch`) each sat pending with a frozen timestamp, surviving cancellation of every visible member AND a verifiably empty group. Merges couldn't get CI validation. Initially mis-suspected as billing until diagnosed. | NOT billing — the repo is PUBLIC (unlimited Actions minutes). The `CI/CD` workflow's `concurrency: group: CI/CD-refs/heads/main, cancel-in-progress: false` acquired a ghost queue entry (a known GitHub bug); every new run queued behind nothing, forever. PR- and schedule-triggered workflows (different/no group) ran fine; githubstatus green → isolated to that one group. | ~ (spanned two sessions; decoded this session in ~10 min once PR/schedule workflows were confirmed healthy) | ~20 min — salted the concurrency group name to `…-${{ github.ref }}-v2` (`60d6652c`), routing new runs to a fresh queue; the path-matching edit's own run dispatched green, proving the fix. Also deployed `LifePlatformOperational` to clear a pre-existing R8-ST6 IAM Plan-gate red. | No — CI-infra only; site stayed up, no data touched, no user impact. |
| 2026-07-23 | **P3** | **Site auto-rollback on the #1704 Broadcast-feed merge — a recurrence of the 2026-07-09 API-before-frontend class.** PR #1704 added both `/api/broadcast` (site-api) AND `/story/broadcast/` (site) in one PR. The on-merge `site-deploy.yml` smoked `/api/broadcast` → 404 (site-api not yet deployed) → failed smoke + visual-QA → auto-rolled-back the whole site. Recovered same session: deployed site-api, confirmed the endpoint live 200, re-synced the site (smoke 217/0), and re-ran the deploy for a clean record. | Same class as the 2026-07-09 #750 rollbacks: site deploys are automatic-on-merge while site-api deploys are manual, so a single PR touching both `lambdas/web/` and `site/` creates a merge-order dependency the pipeline can't see — and here the new endpoint's own consuming page tripped it. Prevention now sharper: the API must be live AT merge time (a new endpoint+page in one PR can't be sequenced post-merge because the site deploy fires immediately). See `reference_api_before_frontend_autodeploy_race`. | Real-time (site-deploy smoke + visual-QA, one firing) | Same session — ~15 min (site-api deploy + endpoint verify + site re-sync + workflow re-run). | No — self-healed same session; the rollback protected the reader from a broken page; no data impact. |
| 2026-07-21 | **P4** | **Budget tier escalated to 2 with month-end projected over the hard ceiling — reader narratives auto-paused.** The governor projects **$96.09 against the $85 ceiling (113%)** on $63.26 month-to-date, holding tier 2: coach commentary, State of Matthew and the chronicle are paused for the rest of the month. Surfaced the same day the #1397 receipts page went live — the page is what made it legible, and the live over-ceiling state is the case #1617's breach copy was written for. Owner has accepted the overrun for this cycle. | Not a fault: the budget guard behaving exactly as ADR-063/ADR-133 specify. Spend rate (ai_daily $1.84 + non-AI $1.34) against an $85 base ceiling; surge mode inactive (728 uniques vs the 900 threshold), so no ceiling float. Accepting the overrun does NOT un-pause anything — tier derives from projection ÷ ceiling, so only a `MONTHLY_CEILING_USD` raise (with an ADR-133 amendment) restores tier 1. | Same-day — first render of /method/receipts/ against live data. | Owner-gated decision: leave tier 2 to ride out the month, or amend ADR-133 and bump the ceiling (~$110 → tier 1). | No — pausing is the designed protection; the daily brief is protected longest by design. |
| 2026-07-20 | **P3** | **macOS TCC revoked ~/Documents access for the whole Claude Code process tree mid-session — every local repo read EPERM (Bash, Read/Edit, git, pytest).** Directory listings still worked; file reads died with `Operation not permitted` (`git: Unable to read current working directory`). The max-drain session re-based onto `gh repo clone`s in /private/tmp (git/pytest/black/aws/cdk/gh all work there — ~/.aws, ~/.config/gh, ~/.claude are not TCC-protected) and completed 16 merges + 4 deploys + the wrap from clones. | macOS TCC per-app privacy grant dropped at runtime (the `reference_launchd_tcc_documents` class, first time against a LIVE interactive session); trigger unknown (no owner action observed). | ~2 min (first failing git call decoded) | Session-side workaround ~15 min; REAL fix owner-gated: re-grant Documents in System Settings → Privacy & Security (or app restart). | No — repo state lives in git; no data touched. |
| 2026-07-20 | **P4** | **Concurrent-agent tree contamination, two vectors in one session:** (1) an agent's Bash cwd silently reverted to the shared main checkout (case-twin path) and its banned `git stash -u` briefly captured the driver's uncommitted edits (popped back clean, agent self-reported); (2) three agents raced one shared scratchpad `repo/` clone — branch switched underfoot, edits clobbered; each recovered via a fresh uniquely-named clone and pushed exactly its own diff (verified per-PR). | Shared mutable trees + generic clone dir names in agent briefs; the stash ban needs to be verbatim in every brief and clone dirs unique (`repo-<issue>`). Memory rules updated (`git_stash_shared_across_worktrees`). | Same-session (agent self-reports + driver diff checks) | Same-session (restores verified; no foreign bytes landed in any merged PR) | No. |
| 2026-07-20 | **P3** | **GitHub Actions refusing jobs on billing — CI dead for the session's second half (and the confirmed root cause of the 2026-07-19 #1544 run-stoppage).** Runs queued but every job concluded `failure` with no logs; the check-run annotation states: "The job was not started because recent account payments have failed or your spending limit needs to be increased." Intermittent at first (some jobs started), then total. All 13 of the session's merges were carried on local full suites (no `-x`) + manual deploys per the standing outage protocol; the site synced attended with version.json verified. | Owner-side GitHub billing (failed payment / exhausted Actions spending limit) — external, not code. Evidence on #1544. | Detected within one merge cycle (queued-run reflex) | Owner-gated — Billing & plans fix; until then local-verify + manual-deploy protocol stands. | No reader impact; CI safety net absent until fixed. |
| 2026-07-20 | **P4** | **Withings 03:05 UTC cron invoke died into the DLQ mid-`cdk deploy --all`** (the cycle-9 reset's fleet deploy was updating the function as its schedule fired; async retries exhausted → 1 message). Found by test_i9 + the #1559 harness ops leg same hour; function proven healthy post-deploy (manual invoke: 6-day gap-aware sweep, honest no_data ×6), message drained, depth 0. A later harness probe hit the documented transient invalid-refresh-token 503 burst (`reference_withings_transient_refresh`) which self-recovered on the next invoke — token healthy for the Monday weigh-in. | Deploy-window race: hourly ingestion crons vs a ~25-min fleet deploy — any cron firing mid-update can DLQ once. Known-benign class; gap-aware backfill makes it lossless. | ~30 min (suite + harness) | ~5 min (verify + drain) | No — no data existed in the window (device-side dark). |
| 2026-07-19 | **P4** | **Superseded-skip stale-deploy window (~35 min): the constellation-promote merge's site-deploy reported success with a SKIPPED deploy step** — four site-deploys queued in ~20 min from the Day-2 merge queue; live served the prior sha (dee9bf4) while the run for cab9b70a showed green. Caught at live-verify by the stale-version.json tell; healed with the standing manual `sync_site_to_s3.sh` reflex; no rollback fired, no data impact. | The documented superseded-skip class (`reference_site_deploy_superseded_skip`) — fast merge queues make it MORE likely; the deploy job's supersede logic can skip while a sibling run is current-enough. | ~10 min (deploy checkpoint's live-verify) | ~5 min (manual sync + invalidation wait + re-verify) | No — home served a one-commit-older healthy tree. |
| 2026-07-19 | **P4** | **Wrap-deploy auto-rollback — a TRUE POSITIVE: /method/registry/ overflowed 390px by 31px** (the #1413 SCED entry's 45-char `module::function` source token can't break; `.mr-source code` had no overflow-wrap). The gating visual-QA caught it on the wrap deploy — the FIRST pipeline run to see the page, because the #1544 outage meant #1538 itself deployed manually with smoke-only checks. Rollback target carried the same overflow, so live overflowed either way until the fix. | Long unbreakable mono token + no `overflow-wrap:anywhere` on the source line; the outage window let it reach live un-QA'd. | Same session (the rollback firing) | ~25 min — `overflow-wrap:anywhere` in v4_build_methods.py, page regenerated, local 390px verify, PR #1546 merged, deploy green, live scrollWidth==390 verified. | No — desktop/tablet unaffected; mobile had horizontal scroll on one page for ~3h. |
| 2026-07-19 | **P3** | **Sunday weekly-digest crashed on the first post-genesis run (16:00 UTC cron) — 10 retries into the ingestion DLQ, no digest email sent.** `data["this"]["withings"]` existed WITH value None (the whole prior week phase-hidden by the cycle-8 reset) and the gate-telemetry line's `.get("withings", {})` doesn't guard an existing-None key → AttributeError every attempt. Reset-recurrence class (same family as the genesis-eve `_clamp_today` 500): fires the first Sunday after every reset. | The `d.get(k, {})` idiom guards a MISSING key, not a present-None one; the genesis-week empty read is the one week that produces present-None. Found via the i16/i9 live integration tests during an unrelated full-suite run — no DLQ-depth alarm had escalated yet. | ~2.5h (16:00 crash → caught by test_i9 in-session) | Same session — gate telemetry extracted to a pure `_gate_telemetry` helper with `or {}` guards + 3 regression tests (guard proven red on pre-fix tree); redeployed, digest re-invoked, DLQ drained. | No — the digest email was late, not lost; re-sent same morning. |
| 2026-07-19 | **P4** | **First #1527 deploy REGRESSED /api/predictions + /api/calibration to 12–16s at origin (~15 min window, ~07:07–07:25 UTC)** — the "parallelized" fetch built a boto3 Session per worker thread; Session construction is GIL-serialized pure-Python setup, and at 256MB (~1/6 vCPU) nine of them per request dwarfed the queries. Local pre-deploy timing (fast laptop CPU, parity-identical output) masked it. | Perf change validated only on dev-machine CPU; Lambda CPU is memory-fractional and the per-request executor rebuilt every thread-local each invocation. | Same session (~2 min — post-deploy origin samples were part of the deploy checkpoint) | ~15 min — hotfix PR #1532 (shared table handle, thread-safe botocore client underneath) → ~2.4s; then serve_stack 256→1024MB → p50 ~0.6s, 3/3 clean gating samples. | No — read-only endpoints; CloudFront's 300s TTL shielded most readers. |
| 2026-07-19 | **P4** | **Main CI Unit Tests red on genesis Sunday — `test_evening_ritual_nudge_links` ×3 are wall-clock-dependent**: they fed `pacific_today()` into `_check_evening_ritual`, and #1409's Sunday-only felt-probe metrics joined the missing-set the first Sunday after merge (genesis day), redding both post-merge CI/CD runs. Same golden-test wall-clock class as `reference_golden_tests_wallclock`. | Tests pinned behavior to "today" instead of a fixed date; the probe feature is date-conditional. First fireable day was also the first day it fired. | Same session (post-merge CI red spotted at deploy checkpoint) | Same session — dates pinned (Saturday for weekday cases, explicit Sunday probe test added); rode along: the `len<3` detail-copy branch mislabeled a fully-logged core + open probe as "Not logged yet", fixed. | No — CI-only; the nudge lambda itself behaved correctly. |
| 2026-07-19 | **P4** | **Second same-night auto-rollback of a healthy site deploy (05:40 UTC) — smoke raced the CloudFront invalidation.** The #1395 static cores + the smoke guard asserting them shipped in one deploy; smoke fetched /coaching/ before the invalidation propagated, saw the cached pre-deploy page, failed the brand-new static-core check, and reverted a healthy deploy. | Content assertion added in the same deploy as the content, with no invalidation-propagation wait in the smoke path (distinct from the #1331 harness-noise class, already fixed). | Same session (~10 min — live version.json showed the rollback build) | ~15 min — manual `sync_site_to_s3.sh` redeploy, propagation wait, 121/121 smoke green at HEAD. Hardening filed as #1526. | No — pre-deploy content served during the window. |
| 2026-07-19 | **P3** | **/api/fulfillment_ritual 500'd for ~4h on genesis eve (00:00–~04:00 UTC).** DynamoDB `ValidationException: BETWEEN upper < lower` — every request to the endpoint failed; the page's ritual card fail-softed. Found by the #1436 schema-snapshot capture run (an agent sweep), confirmed in site-api logs. | `_clamp_today()` clamped genesis-derived query lower bounds to **UTC** today while handlers build their upper bound from **PT** today — on genesis eve UTC rolls onto genesis (2026-07-19) ~7h before PT, so lower(genesis) > upper(PT yesterday) for the whole window. Genesis-eve-only, but recurs every reset staged with a future genesis. | ~4h (no per-route 5xx alarm caught it; the schema-capture sweep did). **Recurrence caught by `site-api-handled-5xx`** (#2819, 2026-08-18) — the handled-5xx EMF metric emitted from the error envelope in `site_api_common`, alarmed on the dimensionless aggregate at ≥5/15min with the per-`Route` dimension naming the failing door. This exact incident is the shape it was built for: a handled 500 that leaves AWS/Lambda `Errors` at 0. | ~30 min — `_clamp_today` now clamps in PACIFIC (PT date ≤ UTC date always, safe for both bound kinds) + regression tests with a fixed-date seam; fleet-deployed same night. | No — read-only endpoint; the card rendered its honest empty state. |
| 2026-07-19 | **P3** | **Site auto-rollback ~03:45 UTC — smoke asserted a not-yet-live `/api/character_calibration` route, rolling back a healthy code deploy.** The `site/**` merge (front-end panel calling the new route) went through automatic `site-deploy.yml`; its site-api lambda counterpart was blocked from deploying by the R8-ST6 IAM-review Plan gate (an IAM/`role_policies` PR reds "Plan deployments" by design until Matthew's own `cdk deploy` clears it) — so the smoke check 404'd against the route and the site rolled back to the prior tree. | The **deploy-API-before-frontend class** (see the 2026-07-09 row + `reference_deploy_api_before_frontend`) recurring under a NEW blocker shape: previously the API lambda deploy was simply "not yet run manually"; here it was structurally *held* behind an IAM-gate approval, which the automatic site-deploy pipeline has no visibility into. | Same night (site-deploy job) | Same night — site-api deployed once the IAM gate cleared; `site-deploy.yml` re-run and landed the healthy tree. | No — the rolled-back tree was itself healthy (prior release); no data loss, brief availability of the older (correct) version only. |
| 2026-07-18 | **P2** | **`Unit Tests` + `Deploy-critical tests` collected ZERO tests and failed outright** — `tests/test_og_card_coverage.py` did `from web import og_image_lambda`, which imports Pillow (`PIL`) at module top. Pytest raised `ModuleNotFoundError` at **collection**, so the whole offline suite exited 2 (`Interrupted: 1 error during collection`) — reads like a mass failure but was one bad import (#1237/#1290). | Pillow lives only in a Lambda **dependency layer**, not `requirements-dev.txt` — the PR's author ran the test locally where PIL happened to be installed, so it "passed" pre-merge and only reded CI. | Discovered same session via `pytest --collect-only` after the merge | Same session — fixed by PR #1297: parse the needed `PAGES` constant out of the source with `ast` instead of importing the (PIL-dependent) module; ADR-added reflex: `--collect-only` before trusting any agent's "tests pass locally" claim. | No — CI-only; no live/data impact. |
| 2026-07-17 | **P3** | **Signed `progress_pct = -1.2` flagged "impossible" by the accuracy gate → visual-QA FAIL → auto-rollback of a healthy deploy** (mid-#1193 merge, during the /fullreview wrap). The value was honest: 316 lb vs the 314 lb cycle-6 baseline on Day 5 (ADR-104 down-weeks-shown). | `tests/accuracy_audit.py::impossible_values` validated `progress_pct` in `[0, 100]` — every other `_pct` field is legitimately non-negative, but `progress_pct` can go negative on a genuine down-week and the range check didn't carve out that one signed field. | Same session (accuracy gate ran as part of visual-QA on the #1193 merge) | Same session — range widened to `[-100, 100]` for `progress_pct` only, + 5 regression tests (`tests/test_accuracy_audit_ranges.py`); re-deploy dispatched, verified live. See `reference_accuracy_gate_signed_metrics`. | No — read-only journey stat; site self-healed same session. |
| 2026-07-16 | **P4** | **Artifact-storage quota exhaustion threatened a spurious auto-rollback — PR #1193's merge was deliberately HELD to avoid it.** GitHub Actions' account-wide artifact-storage quota was exhausted; `site-deploy.yml`'s `visual-qa` job uploads screenshots with `if: always()` and no `continue-on-error`, so a real-gate-pass + failed-upload still marks the job (and therefore the deploy) red, firing the auto-rollback on a HEALTHY deploy. | GH Actions artifact quota is account-wide and recalculates only every ~6–12h; the upload-artifact step has no `continue-on-error`, so it's load-bearing for job status even though it's purely diagnostic. | Same session (recognized before merging, from the #1158/other quota reds pattern) | N/A — no rollback fired; merge held ~1 day until the quota reset, then merged clean. Structural fix filed as a reflex (add `continue-on-error` to diagnostic upload steps) — see `reference_ci_artifact_quota_rollback`. | No — averted; cost was investigation + merge-hold time only. |
| 2026-07-12 | **P3** | **Two consecutive `site-deploy.yml` auto-rollbacks of a healthy `/api/coach/eli_marsh` lambda deploy (#1158).** CloudFront caches API 404s for ~300s (error-caching on `/api/*`); a request to the new route landed BEFORE the site-api lambda deploy settled, caching a 404 at the edge. The site smoke then saw 404 (even though the lambda was already correct by the time smoke ran) and rolled back twice in a row. | New `/api/` route merged+deployed without a same-session CloudFront invalidation of its viewer path — the deploy-API-before-frontend reflex (2026-07-09 row) covers deploy ORDER but not edge-cache invalidation, a second, independent trap on the same class of route. | Real-time (site-deploy job failures, twice) | ~15 min each — `aws cloudfront create-invalidation` on the route's viewer path + `gh run rerun` once it served 200. Reflex added: invalidate the new route's viewer path immediately after any new-route deploy. See `reference_cloudfront_404_cache_smoke`. | No — self-healed same session; no data impact. |
| 2026-07-11 → 2026-07-12 | **P3** | **`ci-cd.yml` Unit Tests red for ~26h, unnoticed — a wrap declared "all gates green" over it.** PR #1049 archived `deploy/void_legacy_predictions_726.py` to `deploy/archive/onetime/`, but `tests/test_predictions_one_store_726.py` still did `sys.path.insert(0, deploy/)` + import → `ModuleNotFoundError` on every full-suite run. `site-deploy.yml`/canary/doc-sync all stayed green throughout (separate workflow), which is what let the red go unnoticed for a full day. | `ci-cd.yml` and `site-deploy.yml` are independent workflows — a session can merge+deploy site changes all-green while `ci-cd.yml`'s own Unit Tests run is red, and nothing surfaces that at wrap time unless it's checked explicitly. | ~26h (discovered next session, not by any automated escalation) | Same-session once found — PR #1074 fixed the stale sys.path import; `/wrap` gained the (e2) green-main gate (`scripts/check_main_green.py`, #1327) specifically to close this class going forward. | No — CI-only red; the live site was unaffected throughout. |
| 2026-07-11 | **P3** | **Auto-rollback correctly caught a real +255px overflow on `/data/vitals/` under REAL data — a true positive (#1008).** The render gate mocks all APIs EMPTY, so it never saw the data-driven overflow; only the live visual + AI-vision QA gate (against real production data) caught it, and `site-deploy.yml` rolled back the healthy-looking (gate-passing) deploy correctly. | A `.table-scroll`/labs-polish change used `width:auto` where real (non-empty) data pushed a wide element past the viewport; the render gate's empty-API mocking structurally can't exercise data-driven width. | Same session (live visual-AI QA gate) | Same session — fixed forward via PR #1034 (`width:100%` block-scroll instead of `width:auto`, restored `.rd-sec{overflow-x:auto}` for wide non-table content); re-verified live. | No — self-healed same session; correctly-functioning safety net, not a defect in the gate itself. |
| 2026-07-10 | **P3** | **`site-deploy.yml`'s smoke + visual-QA gates auto-rolled-back the cycle-5 pre-start countdown site TWICE** — both gates assumed a running experiment (non-empty feeds) and didn't recognize the deliberate pre-genesis countdown state as valid. | Gates had no `pre_start`-aware branch; empty manual-source feeds during the countdown window read as failures rather than the expected pre-launch state. | Real-time (site-deploy job failures, twice, during the cycle-5 reset) | Same night — `smoke_test_site.sh` made `pre_start`-aware; honest placeholder text wired into `[data-dx-read]` in both `dispatches.js` and `coaching.js` (same bug, two modules) for the empty-feed case. | No — self-healed same night as part of the cycle-5 reset execution. |
| 2026-07-29 | **P3** | **Spurious site auto-rollback re-exposed a real clinician's name on a live public page for ~15 min.** #1891/PR #1903 removed "owner Dr. Peter Attia" from `/method/game/`; the deploy synced cleanly and was live-verified, then the post-deploy smoke failed and `rollback-site-on-failure` reverted `site/` to `HEAD~1` — putting the name back. **False positive:** the smoke died on `exit 28` (curl timeout), not a content check; a local run of the identical script against the same live site passed 233/233. | `deploy/smoke_test_site.sh` runs each manifest api_dep with `--max-time 10`; the request after `/api/horizons` — `/api/inference_receipt` — exceeded it. ~1.6s warm from a dev machine but **~15× its neighbours' 0.11s**, so it sits close enough to the bound that a cold post-deploy container clears it. A single timed-out request is currently sufficient to auto-revert a merged, fully-gated change. | Real-time (site-deploy job failure) — but MIS-DIAGNOSED as the known transient class, so the deterministic pattern was missed on the first firing | ~15 min — `gh run rerun` of the same run redeployed the correct tree; smoke passed on the re-run. #1911 filed (retry-on-28 + name the failing dep in the log). | No — content only; the API and data were untouched. |
| 2026-07-30 | **P4** | **Second spurious site auto-rollback, same cause, 24h later** — reverted #1895/PR #1910's front-end belts (the what_changed recency gate + the constellation empty-state copy). Comparing the two firings is what settled the diagnosis: both died at the SAME point (last `✅ /api/horizons`, then `exit 28` exactly 10s later), which is not a random flake. **My earlier "transient" label was wrong and is corrected in the record.** | Identical to the 2026-07-29 row — `/api/inference_receipt` against the smoke's 10s bound. Corroborating: the recovery re-run passed against a site-api deployed ~20 min earlier and warmed by diagnostic probes, consistent with cold-start dominating (circumstantial — the origin-side CloudWatch check in #1911 would settle it). | Real-time (site-deploy job failure) | ~20 min — redispatched after the Lambda deploy landed; smoke + visual QA green, rollback skipped. Blast radius smaller than the 07-29 firing: `HEAD~1` still contained the #1891 fix, so only the belts were lost. | No — front-end only. |
| 2026-08-02 | **P3** | **`/api/supplements` kept serving the 20 fabricated citations for ~13h AFTER the fix merged — the S3 root `config/` prefix has no deploy path.** PR #1939 (merged 03:38Z) rewrote both registry twins and site-deploy went green, but the site-api Lambda reads `s3://…/config/supplement_registry.json` (root prefix, canonical per its own docstring) — that object was dated **2026-07-18**; `config/experiment_library.json` was equally stale. Three stacked caches hid it: no sync path for root `config/`, a no-TTL warm-container cache, CloudFront 3600s. The CloudFront-served `/config/*.json` copy WAS clean, which made the merge look live. Caught by this session's Track-B verification sweep (grep live payloads for the 20 forbidden PMIDs). | The site-deploy pipeline syncs `site/` but nothing syncs the repo `config/` twins to the bucket-root prefix the Lambda reads; PR #1939's own deploy note said the manual step was required and it fell through — manual deploy notes on merged PRs have no owner. | ~13h (merge → sweep) | Remediated same session: explicit `aws s3 cp` of the two twins (never a prefix sync — root `config/` also holds Lambda-written runtime state), description-only touch of site-api to recycle warm containers, CloudFront invalidation of the three affected API paths; verified live 0/20 forbidden PMIDs. Structural fix filed as **#2019** (P1, Now). | No — stale-but-plausible content served; no data loss. |
| 2026-08-02 | **P4** | **Main's Unit Tests red ~1.5h — the Wave-2 merge union breached the #1665 module-size ceiling while every PR was green alone.** #2025 + #2026 + #2027 each passed their own CI; merged together they put `qa_smoke_lambda.py` at 1296 and `restart_pipeline.py` at 1202 against the 1200-line gate. Per-branch CI structurally cannot see a union property. | Three implementers each added 40–90 lines to the same near-ceiling shared files in one concurrent wave; nothing checks headroom across a wave. | ~20 min (first red run → diagnosis) | Fixed on main same session (`30b3e5e2`): the five AWS-surface sweeps extracted to `qa_check_outputs.py` (the established re-export split idiom, 64 targeted tests green incl. the chronic-site AST registry), `restart_pipeline` epilogue condensed. Final union run: Unit Tests green. Memory rule filed (check shared-file headroom before a wave; re-run the size guard on main after). | No — code-plane only. |
| 2026-08-02 | **P2** | **Whoop ingestion has been dead since 2026-08-01 12:00Z — OAuth 401, auth circuit-breaker latched.** `whoop-data-ingestion` runs hourly and every run since has short-circuited with `auth_breaker_active source=whoop marked_at=2026-08-01T12:00:28Z error=HTTP Error 401: Unauthorized`. Latest stored record is `DATE#2026-07-31`; 2026-08-01 is missing. Whoop is the platform's **only fully passive** daily source and is `qa_required` — it feeds recovery, HRV, RHR and sleep, which in turn feed the readiness score, the daily brief and `/api/vitals`. | Whoop refresh tokens rotate on every refresh; a lost refresh response permanently invalidates the stored token — the same class as the 2026-06 outage, recurring here as a **401 + breaker latch** rather than the earlier 400. Recovery requires a fresh browser authorization and therefore **the owner** — it cannot be automated. | ~13.5h — found by `tests/test_integration_aws.py::test_i16` during the wrap, NOT by an alarm. `qa-smoke` checks *PT-yesterday*, which was still 07-31 all session, so it correctly passed and would only have gone red on the next PT day-roll. | **Not resolved — owner action required:** `python3 setup/setup_whoop_auth.py --backfill` (registered redirect `http://localhost:3000/callback`; the callback page will not load — paste the full `?code=…` URL back, codes expire in ~30–60s). Left flagged in the handover and in `[[project_whoop_reauth]]`, whose recovery path was also **stale** (said `deploy/`, the script lives at `setup/`) and is corrected. | **Yes — a data gap.** One day of recovery/HRV/RHR/sleep missing so far, growing daily until re-auth. `--backfill` should recover the window. |
| 2026-08-02 | **P2** | **A 12-day-old synthetic test row auto-reverted a correct fleet deploy — and while it existed, no deploy on this platform could survive.** The #2010 fleet deploy succeeded and was byte-verified (#1896/#1897 markers present in `coach-narrative-orchestrator`, `daily-brief`, `state-of-matthew`, `coach-quality-gate`). Smoke then failed on `life-platform-canary` → `statusCode 500`, and `rollback-on-smoke-failure` stripped the deploy back out (markers re-verified **absent** post-rollback). The canary's own breakdown: `dynamodb` OK · `s3` OK · `mcp` OK (76 tools) · `anthropic` OK · **`subscribe` FAIL — "1 synthetic canary row(s) survived cleanup in the subscriber partition (#1954)"**. Every piece of live infrastructure was healthy. | The stray row was created **2026-07-21T22:17:32Z** — twelve days before the deploy it killed — by the silent cleanup failure `canary_lambda.py:76-79` already documents. `smoke_oracle_decision.py` treats ANY canary failure as gating, so a **data-residue postcondition** (which describes stored state and which a rollback cannot fix) sat on the same unconditionally-gating path as the **infrastructure round-trip** (which a rollback can fix). This is exactly the distinction #1921 already drew for content-truth findings — *"content findings describe published state, not the code that just shipped"* — one path over. Blast radius was platform-wide, not session-scoped: any merge by any session would have deployed and reverted identically. | 12 days for the row itself (found only because it broke an unrelated deploy); ~5 min for the rollback once smoke failed. | Row deleted under a `source = canary` **condition expression** so it could not have touched a real subscriber; residue re-counted **0**; canary re-invoked → `all_pass: True`, 0 failures, all five checks green. Fleet re-shipped via `cdk_deploy.sh LifePlatformCompute LifePlatformEmail`. **Not resolved at source — #2051 filed** to split the infra and residue lanes and to give the residue condition its own signal on the day it appears. *Side effect:* the row carried `status: pending_confirmation`, so the "3 pending subscribers" figure in #1951 was really 2. | No — code-plane only; the revert was clean (no mixed fleet) and the site served normally throughout. |
| 2026-08-03 | **P4** | **Fifth phantom concurrency wedge — now in the deploy group, exactly where the v4 redesign predicted it would land.** `workflow_dispatch` run `30769861255` (`deploy_all=true`) ran Reconcile ✅ lint ✅ deploy-critical ✅ Unit Tests ✅ **Plan ✅** and then sat with `Deploy` queued 12+ minutes; `pending_deployments` returned **empty the whole time**, so the production approval gate never opened and there was nothing to approve. | The workflow-level group is per-`run_id` since the 2026-08-02 v4 redesign and behaved correctly — every validation job ran. The `deploy` job's own group has `cancel-in-progress: false`, so a phantom entry there parks the deploy indefinitely instead of cancelling it. Trigger matches all four prior recurrences: several supersede-cancellations in quick succession (runs `30769190998`, `30769284130`, `30769564973`, `30769668905` were all cancelled by the newer dispatch within ~18 min). `ci-cd.yml`'s own v4 comment called this shot: *"if a phantom EVER appears again after this, it can only be in the deploy group."* | ~12 min to recognise it as a wedge rather than a pending approval. | Shipped via `cdk_deploy.sh` instead, bypassing Actions entirely; the wedged run was cancelled once it held a stale sha. **#2052 filed.** The v4 comment's consolation — that a stuck Deploy is more legible than no CI at all — is **wrong in practice and recorded as such**: five green jobs above a `pending` run reads as *"waiting for a human"*, which is strictly **less** legible than the old `0 jobs` tell. | No — code-plane only; the fleet was already correct from the CDK path. |
| 2026-08-02 | **P3** | **The cutoff-1 AI band — including two CI gates — had been off for 26 consecutive days, and it looked identical to being on.** Budget tier sat at ≥1 from 2026-07-06 → 08-01 (SSM `/life-platform/budget-tier` history; broken only by two manual flips totalling ~22s on 07-12). Everything in `budget_guard`'s cutoff-1 band was paused throughout: `ensemble`, `chronicle_editor`, `coherence_semantic`, `reader_truth_qa`, `visual_ai_qa`, `eyeball_estimate`, `conversation_enrichment` — **two of which are CI gates that report green when paused**. Band 2 (reader-facing narrative: coach commentary, State of Matthew, chronicle, daily debrief) was additionally paused 07-27 08:00Z → 08-01 00:00Z, i.e. **Days 1–5 of cycle 11** — the new cycle's first five days ran with reader narrative off. | Two causes compounding. (1) Spend: July closed at **$98.35** against a ceiling that was temporarily $115, on four months of growth ($20 → $35 → $48 → $80 → $98) — so tier ≥1 is now the platform's default state, not an exception. (2) Invisibility: `Check.pause()` set `passed=True` and printed nothing, so a paused check and a passing one were byte-identical in every recorded signal; #1440's `QAPausedByBudget` alarm *did* fire, ~26 days running, and read as background. | ~26 days — found only because #1920 went looking for 30 days of verdicts and found 4 days of executions. | Partially resolved. Shipped: paused checks are now named + logged (#1921); `budget-tier-sustained-7d` alarm deployed (Minimum over 21×8h = 604800s, all datapoints — fires only when the tier never returns to 0 for a full week; replayed against the real series it would have fired **2026-07-12**, 19 days early). **Not resolved at source** — the ceiling-vs-burn decision is #1927 and is the owner's; at $85 the simulation puts tier 3 (all Bedrock off) around **2026-08-21**. | No — degraded AI features and dark QA gates; no data touched, site never down. |
| 2026-08-02 | **P4** | **Third phantom concurrency-queue wedge blocked the deploy path, and a merged change sat undeployed behind it.** ci-cd run `30725124678` (main @ `58db6c9c`, the #1923 merge) sat `pending` with **0 jobs for 13+ minutes** with nothing else queued or running. Cancelling it and re-running would not have helped; the group itself was wedged. Consequence: #1923's site-api change was merged but **not deployed** — confirmed by downloading the live zip (`night_of_for` absent; `LastModified` 23:02Z predated the 00:23Z merge). | The phantom stuck-queue entry already documented in `ci-cd.yml` for the **v2 (2026-07-23)** and **v3 (2026-07-27)** groups, with the same trigger all three times: several supersede-cancellations earlier in the session. Ruled out an Actions-wide fault by measurement — Docs CI (`30725124605`) and CodeQL (`30725124603`) ran on the **same sha in the same second** and both succeeded; only this workflow's group was stuck. Not billing (public repo). | ~13 min to notice the wedge; the undeployed change was caught only by inspecting deployed bytes after a run reported `Deploy: skipped` **and** `success`. | Salted the group to **v4** (PR #1933, `80d567d5`) — new runs picked up immediately (`queued`→`in_progress`, real jobs). #1923 then deployed via `deploy/deploy_site_api.sh` from clean main and verified three ways (deployed zip, live payload, `qa-smoke failed_deploy_health: 0`). **Symptom-level fix by design** — the behaviour is GitHub-side and salting is the only known remedy; a fourth recurrence should replace the hand-bumped counter with a per-run-unique component. | No — code-plane only; site served normally throughout. |
| 2026-08-01 | **P4** | **Gating visual-QA job red on a transport blip, not a content defect (false positive).** ci-cd run `30720561888` (the #1909 deploy) concluded `failure`: Deploy ✅ and Smoke ✅ passed, but `Visual + AI-vision QA` failed on `[fetch error] … Connection reset by peer` for three adjacent `/method/*` pages. Main's latest CI/CD badge therefore read red for ~2h until the next successful run. | `tests/leak_token_sweep.sweep()` records a **transport failure as a finding**: `fetch()` returns `status = 0` on a connection reset and the code appends `hits: [("HTTP error", ["0"])]` — the same field a genuinely leaked token lands in. The sweep cannot distinguish "I read the page and it was clean" from "I never read the page", and its summary ("91 passed, 1 failed") concealed that 3 pages were never checked. | Real-time (job failure). | Measured at the edge immediately after: all three pages returned **200 on four consecutive requests each**, ~0.1s, full 34 KB body — nothing was wrong and nothing leaked. No rollback fired (`rollback-on-smoke-failure`'s `needs` excludes visual-qa), so blast radius was a red badge and a blocked pipeline. **#1931 filed** to separate unreachable from read-and-checked. | No — false positive; site healthy throughout. |
| 2026-08-01 | **P3** | **Auto-rollback reverted 100 Lambdas on a pre-existing `qa-smoke` failure, not on anything the deploy did.** The #1915 fleet deploy (CI machinery for #1907/#1908/#1911) succeeded, then the Smoke Test job read `qa-smoke` → `failed=1` and `rollback-on-smoke-failure` reverted the whole fleet (**100 reverted, 0 no-artifact, 0 failed** — a clean full revert, no mixed fleet). The failing check was **Reader Truth**: `/api/vitals` publishes `hrv_30d_avg` (n=5) and `weight_delta_30d` labelled 30-day on **Day 5** of cycle 11. Practical impact was negligible (the deploy's only Lambda delta was a `test_count` literal) and the live site stayed healthy throughout. | **Pre-existing and unrelated to the deploy.** `LifePlatform/QaSmoke` `FailCount` was `0.0` at 2026-07-31 11:19 PDT and `1.0` at **16:19 PDT** — the scheduled run ~48 min BEFORE the deploy started (17:07 PDT). Any merge landing after 16:19 would have been reverted identically. Same shape as #1911 (a signal unrelated to deploy health reverting good work) but on a **different oracle** — qa-smoke rather than the site smoke. | ~5 min (rollback job fired in-run) | Rollback completed cleanly in ~19 min; live site verified 200 across `/`, `/cockpit/`, `/data/`, `/api/vitals`, `/api/character`, `/api/inference_receipt`, `/api/status`. **Not resolved at source** — #1917 filed P1; the pipeline stays blocked until it lands, and the pending deploy on `7408e326` was deliberately left unapproved so it cannot roll back over the #1911 fix. | No — code-plane only; no data touched, site never down. |
| 2026-07-09 | **P3** | **First hours of the new `site-deploy.yml` CI (#750) — TWO auto-rollbacks of a healthy site.** PR #900 added `/api/vitals_depth` + the front-end panel calling it; the site (automatic on merge) deployed before the site-api lambda (still manual) shipped the new route. Visual-QA caught `404 /api/vitals_depth` on `/data/vitals/` and rolled back the whole site — twice (the second rollback raced a manual site-api deploy by minutes). | Site deploys became automatic-on-merge (#750) the same day site-api deploys remained manual (`deploy_site_api.sh`) — any PR touching both `lambdas/web/` and `site/` creates a merge-order dependency the pipeline has no way to see. | Real-time (site-deploy job failures, twice) | Same session — deployed site-api, re-ran `site-deploy.yml`; reflex codified: deploy site-api FIRST for any PR touching both `lambdas/web/` and `site/`. See `reference_deploy_api_before_frontend`. | No — self-healed same session; no data impact. |
| 2026-07-10 | **DR drill** | **First verifiably-exercised DDB PITR restore + S3 versioned restore (#755).** Prior to this, `DISASTER_RECOVERY.md` documented the restore procedures but no DDB PITR restore had ever been run (only S3 versioning, via the 2026-03-16 accident). Exercised both into isolated targets: `restore-table-to-point-in-time --use-latest-restorable-time` → `life-platform-dr-drill` (ACTIVE ~10 min, 32,152 items; spot-checked `whoop / DATE#2026-07-01` matched prod exactly), and a prior version of `generated/public_stats.json` copied into `backups/dr-drill/` (live object untouched). Both artifacts torn down after verification. | N/A — planned drill, not an incident. | N/A | ~15 min end-to-end (restore wait + verify + teardown). Gaps: the doc's "Snapshot the layer" pre-emptive step referenced the retired shared-utils layer (#781) — fixed. Restore procedures themselves had no functional gaps. | No — isolated targets; prod untouched. |
| 2026-07-03 | **P3** | **"Frozen page" — many v5 pages loaded as a static shell only (JS-populated content blank); hard reload fixed it; reproducible after a browser restart.** Reported by the operator with a `/data/` screenshot: the header/title/footer rendered but the tabs, sidebar, and readout (all JS-populated) stayed empty. | `sync_site_to_s3.sh` content-hashed CSS/JS (immutable/1yr) but rewrote references **only in `*.html`** — the ES-module `import ... from "/assets/js/charts.js"` statements *inside* the modules kept pointing at the unhashed, mutable, `max-age=86400` URL. When a deploy changed an entry module AND a dependency together (#260), a returning browser paired a fresh hashed entry module with a stale cached dependency; the ES module graph throws atomically on a mismatched import → nothing executed → only the static shell rendered. Hard reload bypassed the HTTP cache; the stale copy survived a restart (≤24h TTL) so it reproduced. Same *class* as the 2026-03-10 CSS-cache issue ADR-039 addressed, but for the intra-module layer ADR-039's HTML-only rewrite never reached. | Operator report (interactive; freshness/QA checks are server-side and never saw it) | ~2 h — `deploy/hash_site_assets.py` now hashes the **full module graph** (leaves-first) and rewrites HTML + intra-module imports + CSS; deployed + live-verified (all imports hashed/immutable, 0 unhashed refs). ADR-098. | No — cosmetic/availability only; served data unaffected. Self-heals for stuck visitors within ~5 min (their cached HTML expires and points at the all-new hashed graph). |
| 2026-06-28 | **P3** | **coherence-sentinel silently broken — ran "green" off a stale artifact while throwing ImportModuleError.** A `cdk deploy` shipped the Sentinel a Lambda zip of only 2 entries (`operational/*`, **no root `lambdas/*.py`**), so it failed at cold start with `No module named 'coherence_invariants'`. The invoke still returned StatusCode 200 (handler re-raises → FunctionError payload with `errorMessage`, no `body`), and the last good `coherence-log/latest.json` made it look healthy. Detected when the new postflight asset-completeness check (#258) flagged it. **Reproducible** — recurred twice on routine redeploys this session. | CDK skips re-uploading an asset whose content-hash key already exists in the S3 assets bucket, so a corrupt `<hash>.zip` poisons every Lambda referencing that hash. `cdk deploy` reports "(no changes)" and `--force` won't re-upload; `rm -rf cdk.out` alone may resynthesize the same hash. Same *class* as the 2026-03-09 P2 (`Code.from_asset` subdir/ImportModuleError), different mechanism (S3 hash-skip caching). | Same-session (postflight check caught it; a 200 invoke had masked it) | ~5 min — pushed the correct asset via `aws lambda update-function-code --s3-bucket cdk-hnb659fds-assets-… --s3-key <hash>.zip` (a re-publish from another stack on the same hash had already overwritten the corrupt S3 object). | No — detection-only Lambda; the served data was unaffected, but coherence monitoring was blind for ~40 min. **Prevention:** `session_postflight.check_asset_completeness()` now downloads each bundled-asset canary's zip and asserts its imported root modules are present. See `reference_cdk_asset_staging_glitch`. |
| 2026-04-05 → 2026-05-19 | **P2** | **Garmin OAuth outage — ~44-day data gap.** Garmin OAuth1 token expired ~April 5 (pre-existing 30-day lifetime) during a platform-low-activity window; cron retries hit Garmin rate limits (429s) and the `auth_breaker` (added P2.4) tripped, halting attempts. Discovered during V2 audit when `life-platform-garmin-data-ingestion-errors` was found in ALARM. | OAuth1 expiry compounded by lack of pre-silence rule-disable. The auth_breaker correctly prevented log/quota spam during the outage, but no escalation surfaced the failure beyond CloudWatch ALARM. | ~44 days (alarm fired continuously, no inbox follow-through) | ~30 min (re-auth via `setup/setup_garmin_browser_auth.py` Playwright/Chromium flow; manual `clear_failure()` on auth_breaker DDB marker; first successful invocation 2026-05-19). Gap-fill will recover all DDB records over next 24h. | Data loss only during silence window; raw Garmin data not retained server-side beyond device sync history. Daily steps/sleep/HR for the gap may be partially recoverable from Garmin Connect UI export. |
| 2026-06-08 | **P2** | **BACKFILLED 2026-08-21 (#2840) — Whoop ingestion DLQ'd on a refresh-token race, and the alarm that fired was a FALSE positive about the cause.** EventBridge's at-least-once delivery double-fired `whoop-data-ingestion`; the second invocation reused the already-rotated single-use refresh token, got `HTTP 400`, and landed in the DLQ — presenting as a credential failure when the credential was fine. Fixed in `f858aa070` (2026-06-08): on a 400, re-read the secret fresh with a brief retry rather than trusting the in-flight copy. | EventBridge at-least-once delivery against a SINGLE-USE rotating refresh token — the second delivery is guaranteed to present a spent credential. | Same-day (DLQ depth alarm). | Same-day. | No — the missing day was re-fetched by the gap-aware backfill. |
| 2026-05-17 | **P2** | **V2 SES IAM regression — daily-brief delivery briefly failed AccessDenied.** During V2 P2 IAM tightening, `ses:SendEmail` was restricted to the identity ARN but the configuration-set ARN was dropped from the policy. SES requires permission on BOTH ARNs when emails are sent through a config set. Daily-brief delivery failed with `AccessDenied`. | Incomplete IAM resource list in `role_policies.py` for SES — single-ARN pattern doesn't suffice when a configuration set is involved (event tracking / dimension tagging requires the config-set resource grant). | Manual smoke check during V2 P2 verification (~minutes) | ~10 min (added config-set ARN to policy Resource list, redeployed EmailStack). Daily-brief resent successfully. | No — same-day fix; one delivery deferred. |
| 2026-03-16 | **P1** | **S3 bucket wipe — 35,188 objects deleted across all prefixes.** Deploy script `deploy_v3756_restore_signal_homepage.sh` ran `aws s3 sync --delete` from 17-file website dir to bucket root, deleting entire raw data archive (34,221 files, 2009–2026), config (24), deploys (25), dashboard (56), CloudTrail (753), exports (24), uploads/macrofactor (26), and 7 other prefixes. DynamoDB untouched. | One-off deploy script synced to `s3://$BUCKET/` (bucket root) instead of `s3://$BUCKET/site/`. The `--delete` flag treated all non-website objects as orphans. Canonical `sync_site_to_s3.sh` correctly uses `S3_PREFIX="site"` — the one-off script bypassed this. | Immediate (operator noticed deletions streaming in terminal) | ~2 hours. Full recovery via S3 versioning — delete markers removed with batch Python script. All 35,273 objects confirmed restored. | **No — full recovery.** S3 versioning was enabled pre-incident. All objects recovered by removing delete markers. Verified: `raw/` = 34,222, all other prefixes match forensic counts. |
| 2026-03-19 | **P2** | Eight Sleep data ingestion down for 10 days | `logger.set_date` crash — Lambda had stale bundled `platform_logger.py` missing `set_date()` method. Same class of bug as 2026-03-09 P2 incident but on a Lambda not redeployed in v3.3.8 batch fix. | 10 days (discovered during pipeline validation session) | ~30 min (hasattr guard + redeploy + re-auth after password change). 7 days backfilled. | No — backfill recovered missing data |
| 2026-03-19 | **P2** | Dropbox secret deleted — MacroFactor nutrition chain silently broken since Mar 10 | Secret `life-platform/ingestion-keys` scheduled for deletion (7-day recovery window expired). Dropbox poll Lambda couldn't read credentials → MacroFactor CSV never downloaded → nutrition data stopped. Undetected 9 days. | 9 days (discovered during pipeline validation session) | ~15 min (restored secret from deletion recovery) | No — MacroFactor data backfilled after restore |
| 2026-03-19 | **P3** | Notion secret deleted — journal ingestion silently broken | Same pattern as Dropbox: secret scheduled for deletion and not caught. Notion journal entries stopped ingesting. | Days (discovered during pipeline validation) | ~10 min (restored secret) | No — re-ingested after restore |
| 2026-03-19 | **P3** | Health Auto Export webhook Lambda crash | `logger.set_date` bug — same root cause as Eight Sleep. Lambda redeployed with stale platform_logger.py. | Days (discovered during pipeline validation) | ~15 min (redeploy with current code) | No — webhook data in S3, reprocessed |
| 2026-03-19 | **P3** | Garmin ingestion broken — missing modules + expired tokens | garth/garminconnect modules missing from Lambda package. OAuth tokens expired. | Days (discovered during pipeline validation) | Resolved 2026-04-05: garth-layer attached via CDK, re-authed, 4 days backfilled. Added OAuth resilience (proactive refresh, circuit breaker). | No — gap-fill recovered all data |
| 2026-03-12 | **P3** | Mar 12 alarm storm — 20+ ALARM/OK emails in 24h across todoist, daily-insight-compute, failure-pattern-compute, monday-compass, DLQ, freshness | CDK drift: `TodoistIngestionRole` missing `s3:PutObject` on `raw/todoist/*`. Policy correct in `role_policies.py` but never applied to AWS (likely stale from COST-B bundling refactor). Todoist Lambda threw `AccessDenied` on every invocation → cascading staleness alarms. | Alarm emails (real-time) | ~1 day (detected next session) — `cdk deploy LifePlatformIngestion` (54s) | No — Todoist data gap Mar 12 only. No backfill attempted (single day, non-critical). |
| 2026-03-12 | **P4** | `freshness_checker_lambda.py` duplicate sick-day suppression block silently breaking sick-day alert suppression | Copy-paste bug: sick-day block duplicated, second copy reset `_sick_suppress = False` after first set it `True`. Suppression never fired on sick days. | Code review during incident investigation | Fixed in v3.7.10 — awaiting deploy |
| 2026-02-28 | **P1** | 5 of 6 API ingestion Lambdas failing after engineering hardening (v2.43.0) | Handler mismatches (4 Lambdas had `lambda_function.py` but handlers pointed to `X_lambda.lambda_handler`), Garmin missing deps + IAM, Withings cascading OAuth expiry | ~hours (next scheduled run) | ~2 hr (sequential fixes) | No — gap-aware backfill self-healed all missing data. Full PIR: `docs/PIR-2026-02-28-ingestion-outage.md` |
| 2026-03-04 | P3 | character-sheet-compute failing with AccessDenied on S3 + DynamoDB | IAM role missing s3:GetObject on config bucket and dynamodb:PutItem permission. Lambda silently failing since deployment | ~1 day | 30 min | No (compute re-run via backfill) |
| 2026-02-25 | P4 | Day grade zero-score — journal and hydration dragging grades down | `score_journal` returned 0 instead of None when no entries; hydration noise <118ml scored | 1 day | 20 min | No (grades recalculated) |
| 2026-02-25 | P3 | Strava multi-device duplicate activities inflating movement score | WHOOP + Garmin recording same walk → duplicate in Strava | ~days | 30 min | No (dedup applied in brief; raw data retained) |
| 2026-03-10 | **P2** | All three web URLs (dash/blog/buddy) showing TLS cert error — `ERR_CERT_COMMON_NAME_INVALID` | `web_stack.py` had `CERT_ARN_* = None` placeholders — CDK deployed distributions without `viewer_certificate`, causing CloudFront to serve default `*.cloudfront.net` cert. Introduced during PROD-1 (v3.3.5). | Hours (noticed by user) | 15 min (v3.4.9) | No (data unaffected; all URLs inaccessible via HTTPS) |
| 2026-03-08 | **P3** | `todoist-data-ingestion` failing since 2026-03-06 | Stale `SECRET_NAME` env var (`life-platform/api-keys`) set on the Lambda — when api-keys was soft-deleted as part of secrets decomposition, the env var override started producing `ResourceNotFoundException`. Code default was correct but env var took precedence. DLQ consumer caught accumulated failures at 9:15 AM on 2026-03-08. | ~2 days | 15 min (env var removed + Lambda redeployed) | No — Todoist ingestion gap 2026-03-06 to 2026-03-08. Gap-aware backfill (7-day lookback) self-healed all missing task records on next run. |
| 2026-03-08 | **Info** | `data-reconciliation` first run reported RED: 17 gaps across 6 sources | Bootstrap noise, not real failures. First run has no prior reference point — all "gaps" were expected coldstart artifacts (MacroFactor real data only from 2026-02-22, habit gap 2025-11-10→2026-02-22, etc.). | First run | No action needed — monitor next 3 runs for convergence to GREEN | No |
| 2026-03-09 | **P2** | All 23 CDK-managed Lambdas broken after first CDK deploy (PROD-1, v3.3.5) | `Code.from_asset("..")` bundles files at `lambdas/X.py` inside a subdirectory, but Lambda expects `X.py` at zip root — causing `ImportModuleError` on every invocation. Affected: 7 Compute + 8 Email + 1 MCP + 7 Operational Lambdas. | Next scheduled run post-deploy | ~1 hr (`deploy/redeploy_all_cdk_lambdas.sh` redeployed all 23 via `deploy_lambda.sh`) | No — gap-aware backfill + DLQ drained. Permanent fix: update `lambda_helpers.py` to `Code.from_asset("../lambdas")` (tracked as TODO) |
| 2026-03-10 | **P1** | CDK IAM bulk migration — Lambda execution role gap during v3.4.0 deploy | CDK deleted 39 old IAM roles before confirming CDK-managed replacement roles were fully propagated and attached. Two email Lambdas (`wednesday-chronicle`, `nutrition-review`) had no execution role for ~5 min during the migration window, causing invocation failures on any warmup or invocation in that window. Root fix: `cdk deploy` sequencing — always verify role attachment before deleting old roles. *Identified retroactively during Architecture Review #4.* | Deploy logs (real-time) | ~15 min (CDK re-apply with `--force`) | No — no scheduled runs in migration window |
| 2026-03-10 | **P2** | CoreStack SQS DLQ ARN changed on CDK-managed recreation — DLQ send failures across all async Lambdas | CoreStack created a new CDK-managed DLQ (`life-platform-ingestion-dlq`) with a different ARN than the manually-created original. CDK-deployed Lambda env vars referenced the new ARN, but 3 Lambdas that had the old ARN cached in env var overrides (`SECRET_NAME`-style pattern) continued sending to the deleted queue. Result: DLQ send failures and silent dead-letter drop for ~30 min. *Identified retroactively during Architecture Review #4.* | CloudWatch errors (~30 min lag) | CDK update pushed correct ARN to all Lambda configs | Possible: some DLQ messages lost during gap window |
| 2026-03-10 | **P3** | EB rule recreation gap: 2 ingestion Lambdas missed scheduled morning runs during v3.4.0 migration | Old EventBridge rules deleted first; CDK replacements deployed after. 2 ingestion Lambdas (`withings-data-ingestion`, `eightsleep-data-ingestion`) missed their 7:15 AM / 8:00 AM PT windows during ~10 min gap between deletion and CDK rule creation. *Identified retroactively during Architecture Review #4.* | Freshness checker alert (10:45 AM) | Gap-aware backfill self-healed on next scheduled run | No — backfill recovered all missing data |
| 2026-03-10 | **P3** | Orphan Lambda adoption: `failure-pattern-compute` Sunday EB rule not included in CDK Compute stack definition | When 3 orphan Lambdas were adopted into CDK (v3.4.0), the `failure-pattern-compute` Sunday 9:50 AM EventBridge rule was omitted from the Compute stack definition. Lambda did not execute for ~1 week (one missed Sunday run). *Identified retroactively during Architecture Review #4.* | Architecture Review #4 inspection | EB rule added to CDK Compute stack | No — failure pattern memory records simply not generated for that week |
| 2026-03-10 | **P4** | Duplicate CloudWatch alarms after CDK Monitoring stack adoption of orphan Lambdas | CDK Monitoring stack created new alarms for 3 newly-adopted Lambdas (`failure-pattern-compute`, `partner-email`, `sick-day-checker`) that already had manually-created alarms — resulting in 9 duplicate alarms with overlapping SNS notifications and alert fatigue. *Identified retroactively during Architecture Review #4.* | Architecture Review #4 alarm audit | Manual alarms deleted; CDK alarms authoritative | No |
| 2026-03-09 | **P2** | All 13 ingestion Lambdas failing with `AttributeError: 'Logger' object has no attribute 'set_date'` | After `platform_logger.py` added `set_date()` to support OBS-1 structured logging, ingestion Lambdas had stale bundled copies of `platform_logger.py` missing the new method. 14 DLQ messages accumulated. Affected: whoop, eightsleep, withings, strava, todoist, macrofactor, garmin, habitify, notion, journal-enrichment, dropbox-poll, weather, activity-enrichment. | DLQ depth alarm + CloudWatch errors | ~30 min (`deploy/redeploy_ingestion_with_logger.sh` redeployed all 13 with `--extra-files lambdas/common/platform_logger.py`). DLQ purged in v3.3.8. | No — gap-aware backfill recovered all ingestion gaps. |
| 2026-02-25 | P4 | Daily brief IAM — day grade PutItem AccessDeniedException | `lambda-weekly-digest-role` missing `dynamodb:PutItem` | Since v2.20.0 | 10 min | Grades not persisted until fixed |
| 2026-02-24 | P2 | Apple Health data not flowing — 2+ day gap | Investigated wrong Lambda (`apple-health-ingestion` vs `health-auto-export-webhook`) + deployment timing | ~2 days | 4 hr investigation, 15 min actual fix | No (S3 archives preserved, backfill recovered) |
| 2026-02-24 | P3 | Garmin Lambda pydantic_core binary mismatch | Wrong platform binary in deployment package | 1 day | 30 min | No |
| 2026-02-24 | P3 | Garmin data gap (Jan 19 – Feb 23) | Garmin app sync issue (Battery Saver mode suspected) | ~5 weeks | Backfill script | Partial (gap backfilled from Feb 23 forward) |
| 2026-02-23 | P4 | Habitify alarm in ALARM state | Transient Lambda networking error ("Cannot assign requested address") | Hours | Manual alarm reset | No (re-invoked successfully) |
| 2026-02-23 | P4 | DynamoDB TTL field name mismatch | Cache using `ttl_epoch` but TTL configured on `ttl` attribute | ~1 day | 5 min | No (cache items never expired, just accumulated) |
| 2026-02-23 | P4 | Weight projection sign error in weekly digest | Delta calculation reversed (showing gain as loss) | 1 day | 5 min | No |
| 2026-02-23 | P4 | MacroFactor hit rate denominator off | Division denominator using wrong field | 1 day | 5 min | No |
| 2026-03-11 | **P2** | Partner email failing on all deploys since v3.5.1 | Two compounding bugs: (1) `deploy_obs1_ai3_apikeys.sh` used inline `zip` with path prefix — Lambda package contained `lambdas/partner_email_lambda.py` at a subdirectory rather than root, causing `ImportModuleError` on every invocation; (2) `EmailStack` in CDK had no layer reference — all 8 email Lambdas silently running on `life-platform-shared-utils:2` (missing `set_date` method added in v4). Root principle violation: deploy scripts must always delegate to `deploy_lambda.sh` (which strips path via temp dir); never inline zip logic. | Manual test during v3.5.4 session | ~30 min (v3.5.5): fixed zip via `deploy_lambda.sh` re-deploy; added `SHARED_LAYER_ARN` + layer reference to all 8 email Lambdas in `email_stack.py`; `npx cdk deploy LifePlatformEmail` to apply | No — no Partner emails sent since initial deploy; email content unaffected once fixed |
| 2026-03-11 | P3 | All 8 email Lambdas on stale layer v2 (missing `set_date`) since EmailStack CDK migration | EmailStack created in PROD-1 (v3.3.5) with no `layers=` parameter — all email Lambdas referenced zero layers and fell back to stale bundled copies of shared modules. `set_date()` method (added in platform_logger v2 for OBS-1 structured logging) was unavailable, causing silent `AttributeError` risk on any email Lambda that called it. No confirmed runtime failures because email Lambdas that bundled their own logger copy used the older API. Discovered during Partner email debug. | Discovered during v3.5.5 investigation | Fixed in v3.5.5 via EmailStack CDK layer patch | No confirmed impact — no `set_date` calls confirmed in email Lambdas prior to v3.5.5 fix |
| 2026-08-15 | **P4** | **A newly-added test skipped the `Deploy` job — self-inflicted, caught by the deploy run rather than by its author.** `tests/test_gate_census_error_bars_2639.py` (landed minutes earlier in #2726) builds the gate census at MODULE-IMPORT time. `gate_census.discover_ci_gates` imports PyYAML lazily — deliberately, so the module stays importable without it — and a module-level build defeats that. The `deploy-critical` CI lane installs a minimal dependency set and has no yaml, so the whole module raised `ModuleNotFoundError`, that lane failed, and `Plan deployments` + `Deploy` were skipped on the `deploy_all=true` run `31905168578`. | A test that imports its subject at collection time inherits that subject's FULL dependency graph, including imports the subject itself was careful to defer. Lazy imports in the module under test are invisible to a test that triggers them at import. | ~7 min (the run's own failure). | Hotfixed in #2732 with `pytest.importorskip('yaml')` — and verified the gate still RUNS in the full `test / Unit Tests` lane, where PyYAML is installed, because a guard that skips in every lane is the 'cannot fail' pattern that very file exists to measure. Three feature branches carrying the broken file were rebased onto the fix. | No — code-plane only; nothing deployed, nothing reverted, site healthy throughout. |
| 2026-08-15 | **P4** | **A module-size regression merged past a pre-merge gate that had already caught it — self-inflicted, by a polling loop that could not tell "all green" from "no checks exist".** #2745's annotation work took `lambdas/health/character_engine.py` from 2117 to 2127 lines against a 2117 baseline. The pre-merge lane DID catch it (run `31915495286`, sha `6da3af8f`, `Collect + deploy-critical + format` → FAILURE on `test_baselined_files_do_not_grow` + `test_headroom_census_is_derived_from_source`). It was merged anyway, so `main` carried a red module-size guard until #2747 restored 2117/2117. A second, unrelated red followed on the fix commit itself: `test_mypy_disable_cost_2638.py` pinned the disable list as a literal four-code set and failed the moment the tranche enabled `return-value` — a guard titled "Guard the SET" failing on the progress it exists to encourage (#2749). | The wait loop was `n=$(gh pr checks <N> \| grep -cE "fail\|pending"); [ "$n" = "0" ] && break`. `gh pr checks` prints `no checks reported on the '<branch>' branch` in the window after a push while checks re-register, and `grep -c` returns **0** for that too — so the loop read "no checks exist yet" as "every check passed". That is the measurement-aborts-reports-zero class, in the operator's own tooling, during the same session that fixed two instances of it in the platform's (#2736, #2746). The second red compounded it: the local sweep was `pytest -k "mypy_clean"`, which does not match `test_mypy_disable_cost_2638.py`. | ~25 min (the wrap's own green-main gate). | Size paid for in-file rather than by raising the baseline (#1665) — a two-line comment became inline and a seven-line docstring returned to one; the rationale already lived in `mypy.ini`'s #2638 block. The disable-list guard was rewritten as a ratchet (`declared <= _ORIGINAL_DISABLED`) so a NEW unpriced code still fails while a landed tranche does not. Every merge afterwards used a rollup assertion requiring `total_checks > 0 AND 0 not-green` — an empty check list is NOT READY, never READY. The first, wrong explanation ("the size guard isn't in the pre-merge lane") was publicly corrected on PR #2747, because a wrong explanation of a CI gap reads as a known lane limitation rather than an operator error. | No — code-plane only; nothing deployed from either red sha, site healthy throughout. |
| 2026-08-16 | **P4** | **Gating visual-QA failed a green fleet deploy on a false positive.** Run 31926388856's post-deploy sweep FAILed `/data/autonomic/` ("All [data-readout] empty"), reading as a broken page — the page was healthy, rendering its honest pre-data state ("Need at least 7 days of recovery data — 6 so far", Day 6 of cycle 13), and the REAL harness passed the same page minutes later. | The sweep raced the wrap commit's site-deploy sync/invalidation (the known smoke-vs-invalidation race class) and fetched a transitional mix. Rollback correctly did not fire (visual-QA is excluded from its `needs`). | ~15 min. | Decoded same-session; the identical sweep passed on the next fleet run (31929446983). No code change — page and check are both correct; the race window is the standing class residual. | No — Deploy + Smoke were green; the fleet deploy stood. |
| 2026-08-16 | **P4** | **A scripted batch-rebase committed CONFLICT MARKERS into two branches** (`fix/2754-breaching-alarms`, `fix/2705-coverage-gap`): a 2-commit branch conflicts twice during rebase; the loop resolved round one, and its blind `git add -A` staged round two's still-conflicted files — `<<<<<<<` landed in `site_api_common.py` and `docs/TESTING.md` as committed content. | Automation that resolves conflicts without verifying resolution: `add -A` cannot tell staged-resolved from staged-conflicted, and every stderr was silenced with `2>/dev/null`. | ~20 min (the branch lane's bundle-boot gate: "SyntaxError: invalid decimal literal" × 44 modules). | Both branches repaired by restoring the two GENERATED files from origin/main and re-running `sync_doc_metadata --apply` — the generator is the merge tool for generated content, never `checkout --theirs` mid-sequence. Marker sweep across every active worktree + main came back clean; the recipe is in the handover. | No — branch-plane only; the gate held it out of main. |
| 2026-08-17 | **P4** | **Main's completed verdict red ~18h across the merge-queue night (22:01Z 08-16 → the cycle-14 wrap): first a flaky concurrency-timing test (`test_calibration_fetches_partitions_concurrently`, 0.78s vs a 0.7s threshold on a shared runner) at the #2851 merge, then a GitHub platform incident (429/502/503 downloading actions in setup-ci) failing three more runs — no code defect on main at any point.** All six PR merges rode green pre-merge rollups; the fleet shipped via the reset pipeline's `cdk deploy --all`, and four interim deploy leases were rejected with reasons (the wedge watcher auto-filed #2856/#2857 on them, both auto-resolved). | External: shared-runner timing variance + GitHub's CDN incident; the timing threshold itself is tight (0.7s) and worth widening if it recurs. | Minutes per failure (terminal-state watchers after the mid-session fix; the first two failures sat ~1–2h under the old green-only watchers — see the session's watcher lesson). | Reruns + riding the queue to the reset HEAD; wrap-HEAD run approved to restore a green completed verdict. | No — test/CI plane only; site and data healthy throughout. |
| 2026-08-16 | **P4** | **Main red ~40 min after a coach-routing change — self-inflicted: the change swept THE guard file I knew about while a second file pinned the same state.** #2769 gave the `board` Telegram route to the lead (owner decision, #2719) and updated `test_telegram_route_provisioning_2677.py` — but `test_board_route_refuses_2719.py` (from the #2677 session) pinned the PRE-resolution state ('no persona claims board') as its precondition, and the run for `1b767c8c` (31923975892) failed Unit Tests on it. The approved fleet deploy on that run was unaffected (Deploy ✅ Smoke ✅ — tests red only). | Targeted suites (`route_provisioning`, `transport`, `persona_registry`) all passed; nothing swept the repo for OTHER tests referencing the changed state, and the full lane — which catches exactly this — was not run for that one PR, in the same session that ran it for two others. The guard-the-set scar, from the operator's side. | ~25 min (the next run's red). | #2772: repo-wide sweep for board-route state pins (two files total), the refusal tests re-pointed at a registry snapshot WITHOUT the alias so the mechanism keeps full coverage, the precondition flipped to pin the owner's resolution, board added to the reaches-assembly parametrization — shipped after a full lane (19,237 passed, 0 failed, exit code read unpiped). | No — code-plane only; the fleet deploy on the red run had already passed Deploy + Smoke; site healthy throughout. |

*TTD = Time to Detect, TTR = Time to Resolve

| 2026-08-22 | **P4** | **Post-deploy visual-QA red on `main` (~1h40m and counting at wrap) on two NEW reader-truth findings the deploy did not cause.** Run `32594947913` on `564efa0c`: Deploy, smoke and the I1/I2/I5 integration checks all SUCCESS, auto-rollback SKIPPED — then the full-surface sweep returned `91 passed, 2 failed across 93 pages` on `/story/chronicle/` and `/story/build/agent-review/`. Both checked against the LIVE pages rather than the CI line (which truncates the note at ~96 chars): the chronicle lists a 2026-08-18 post titled *Day One Actually Happened*, which is ordinary Day-2 retrospective publishing, and the agent-review page states its own span (`2026-05-29 → 2026-08-17`) and never claims to be current. | The oracle's non-stationary finding population (#2959's class), not a deploy defect. `/method/board/` — the finding that held the publish path all session — was among the 91 PASSES, so #2981's fix and its baselined entry held; two different pages had picked up highs in the meantime. Baselined via the deliberate path in #2990 (27 → 29 entries). | Immediate — the gating job reports it. | ~25 min to the baselining PR merged; the follow-on run's own sweep is the confirmation. | No — the deploy itself was healthy and stayed live. |
| 2026-08-22 | **P4** | **Docs CI red on `main` for ~5 min from a `test_count` stamp dropped during a rebase.** `#2983` was rebased onto main after `#2982` merged; its stamp commit conflicted, was skipped (the correct resolution — replay substantive files, let main's automation regenerate), and no replacement stamp was added before merge. `sync_doc_metadata --check` then failed on `9d7a99e0`. | The doc-sync literal treadmill, taken deliberately. The reconcile bot in `run_generators()` regenerated it unattended (`778bb7a2`) — which is exactly §4c's division of labour working, and the reason `#2982` was safe to ship. Docs CI is green on every later commit. | Immediate — Docs CI reds on the push. | ~5 min, unattended. | No. |
| 2026-08-25 | **P4** | **Merge-train site auto-rollback (run 32784460571-era, 20:43Z): 1 novel truth high on 93 pages while the night's statics deployed ahead of the not-yet-deployed site-api.** Rollback restored the prior site; no reader impact. Full workflow_dispatch rerun at 23:22Z — green end-to-end (92→93 pass), with the sweep itself flagging two baseline entries as shrink candidates (fixes verified live). | The API-before-frontend mixed-state class during a 25-PR merge train: site/** auto-deploy fired on merge while the lambda half of the same changes awaited the end-of-train manual deploy. Known class, documented recovery (full rerun, never --failed). #3102's confirm-before-gate merged the same night narrows the single-run-judge half of this class. | Immediate — the gating job reds. | ~2.5h to the green rerun (deliberately sequenced after the site-api deploy). | No. |
| 2026-08-25 | **P4** | **The pre-merge Collect lane timed out at its 10m job ceiling on 3 PRs and GitHub reported each kill as `cancelled` — ~1.5h of reruns misdiagnosed as runner flakes.** No reader impact; PR-lane only. | The session added ~300 tests; the lane crossed 10m (9m27s pass earlier the same evening). GitHub renders a `timeout-minutes` kill as conclusion `cancelled`; the only truth marker is the job annotation. Ceiling re-derived from 10 measured runs (observed max x ~1.23 -> 15m), landed as #3141 with the misread documented in-file. | ~1.5h (annotation finally read). | Same session — #3141 merged + all 3 lanes green under 15m. | No. |
| 2026-08-23 | **P3** | **The site publish path was blocked THREE times in ~90 min by the reader-truth oracle (runs 32616299944, 32618360726, 32620189500) — two full site auto-rollbacks, one dispatch cancelled pre-flight; the wrap's build dispatch stayed unpublished ~2.5h.** Each sweep produced a NEW (page,category) high on a page that had passed the sweep before: the build log vs the day-counter chrome, a 7-day HRV average on Day 6 (two pages), and two self-refuting notes emitted at gating severity. No reader impact — rollback restored the prior site each time. | Two causes, both structural: the rubric's own final DO-NOT-FLAG bullet INSTRUCTED flagging any window longer than days elapsed (the model obeyed, not hallucinated — fixed in #3028 with the cross-phase clause), and the oracle's non-stationary finding population (#2959) inventing one novel high per sweep (two deterministic suppressors + 6 triaged baselines, #3028/#3029). Deploy #4 green same night; the dispatch published. | Immediate — the gating job reds. | ~2.5h to the green deploy; each block's fix landed within ~40 min of its diagnosis. | No. |
| 2026-08-23 | **P4** | **Two post-deploy QA jobs red on GREEN deploys (runs 32622594057, 32623497239) — the harness judged Day-6 screenshots against a Day-7 phase.** Both sweeps straddled midnight PT: capture at ~23:5x PT correctly rendered the DAY 6 chrome (client-JS, PT-exact per #2941); the assessor computed its phase truth after 00:00 PT and flagged 8+ pages for disagreeing with a day that was not true when the pixels were made. No rollback (rollback is smoke-gated), no reader impact — red QA verdicts on healthy deploys. | One run, two clocks: assess_reader_truth never pinned a sweep clock; assess_prose accepted today_iso all along and nothing passed it. Fixed same night (#3030/#3031): pacific_today() pinned once before the first capture, threaded through, spy-test proved; stored in report.json. The new #3025 full-suite lane caught the fix's own stub break pre-merge. | Immediate — the QA job reds. | Same session (~2h from first red to the fix merged green). | No. |
---

## Patterns & Observations

> **DERIVED, not hand-tallied (#2840).** Everything in this subsection comes from
> `python3 scripts/incident_log_patterns.py`, which reads the rows above.
> `tests/test_incident_log_patterns_2840.py` fails when the numbers here disagree with
> what the table actually says. **Re-run it, do not hand-edit it.**
>
> This section previously listed nine root causes tallied from ~32 Feb–Mar rows and stamped
> *Verified: 2026-05-19*. It then never moved for three months while the corpus grew to 150
> rows, 115 of them post-June — so the three classes that dominate the recent record
> appeared in it nowhere, and only the "Last updated" line changed per session. A section
> that looks maintained and is three months stale is worse than one that is obviously old.

<!-- INCIDENT-PATTERNS:DISTRIBUTION:START (generated by scripts/incident_log_patterns.py — do not hand-edit) -->
**Distribution — 197 dated rows, 162 post-June** (newest row 2026-08-31):

| month | rows |
|---|---|
| 2026-02 | 11 |
| 2026-03 | 21 |
| 2026-04 | **0** |
| 2026-05 | 1 |
| 2026-06 | 2 |
| 2026-07 | 36 |
| 2026-08 | 126 |

**By severity:** P1 5 · P2 29 · P3 70 · P4 88 · Low 3 · Info 1 · DR drill 1.

**By root-cause class** (keyword-derived over Summary + Root Cause; a row may match more
than one, and 23 match none):

| n | class |
|---|---|
| 125 | deployment error |
| 52 | stale config / literal drift |
| 42 | QA-oracle false positive |
| 39 | QA false positive — deploy-race (#2978) |
| 30 | lane-subset / union-breach main red |
| 28 | deploy-plane wedge / strand / race |
| 27 | secret / credential |
| 18 | QA false positive — semantic oracle (#2959) |
| 16 | IAM / permission |
| 16 | timezone / wallclock |
| 6 | data quality / scoring |
| 23 | *(unclassified)* |
<!-- INCIDENT-PATTERNS:DISTRIBUTION:END -->

> **The QA-oracle false-positive rows have an owner: #2978** (the #2841 decision — umbrella,
> not acceptance). Do not read the five closed per-symptom issues (#1526, #1931, #1917,
> #2051, #1911) as class closure; that misreading is what #2841 was filed to end, and the
> rule is CONVENTIONS §8b. Note the keyword class spans **two** sub-shapes: the deploy-race
> shape (smoke/visual-QA measuring the edge before an invalidation lands, or a cold Lambda)
> which #2978 owns, and the newer semantic-oracle shape (the reader-truth oracle raising
> sanctioned content shapes) which #2959 and the #2956 debt ledger own. Measured
> 2026-08-22: 14 rows in July (1 per 2.2 days), 15 in 2026-08-01→22 (1 per 1.5 days).

<!-- INCIDENT-PATTERNS:TOPCLASSES:START (generated by scripts/incident_log_patterns.py — do not hand-edit) -->
The three classes the old list omitted entirely — **QA-oracle false positives (42)**, **lane-subset/union-breach main reds (30)** and **deploy-plane wedges/strands/races (28)** — are now the 3rd, 5th and 6th largest. They are the shape of this platform's failures *today*; **deployment error** remains the largest single class but is increasingly a co-tag on those three rather than a category of its own.
<!-- INCIDENT-PATTERNS:TOPCLASSES:END -->

### Silence is an axis, not a class

A row is **silent** when it failed without announcing itself — nothing paged, nothing
reddened, and someone found it by looking. That cuts across every class above, so it is
scored orthogonally (loud/silent × class) rather than as a tenth category.

**What the corpus actually supports, stated with its limits.** The effect is *directional
but modest*, and materially weaker than this axis was described as when filed:

<!-- INCIDENT-PATTERNS:SILENCE:START (generated by scripts/incident_log_patterns.py — do not hand-edit) -->
**52 of 197 rows are silent.**

| | silent | loud |
|---|---|---|
| rows | 52 | 145 |
| TTD parseable | 39 | 93 |
| median TTD | **28 min** | 12 min |
| mean TTD | 1,823 min | 1,477 min |
| exceeded 1 day | 6 (15% of parsed) | 7 (8% of parsed) |

Silent rows take **~2.3× longer to detect at the median** and are **~1.9× more likely to run past a day**. But the *means* are only 19% apart, and the "days-scale TTD for silent vs minutes for loud" framing does **not** reproduce over the population — it comes from reading the worst handful of silent rows, and the loud set has its own long tail (3 rows past a week, vs 2 silent). **Two caveats that bound all of this:** the classifier is keyword-based over free prose, and **65 of 197 TTD cells (33%) state no parseable duration** — they are excluded rather than counted as zero.
<!-- INCIDENT-PATTERNS:SILENCE:END -->

The durable finding is not the multiplier. It is that **38 failures in this corpus
announced nothing**, and every mitigation that has actually moved the numbers — the
freshness checker, the DLQ consumer, the canary, the handled-5xx detector (#2819) — works
by making a silent class loud.

### Pre-July frequencies are FLOORS, not counts

<!-- INCIDENT-PATTERNS:FLOORS:START (generated by scripts/incident_log_patterns.py — do not hand-edit) -->
**April has zero rows, May has one and June has two**, against 36 in July and 126 in August. The platform was not stable in those months — it was under-logged. Two proofs: the 2026-08-02 Whoop row cites *"the same class as the 2026-06 outage"* and no June Whoop row existed until #2840 backfilled it, and two shipped timezone fixes (#2675, #2670) left no rows at all. Never compare a pre-July class frequency against a post-July one and call the difference a trend; the denominator is not the same instrument.
<!-- INCIDENT-PATTERNS:FLOORS:END -->

### Row-inclusion rule (extends #1332)

#1332 mandated a row for **auto-rollback firings**. That scope is too narrow — it is why
the timezone class reads as absent while two live wrong-behaviour wallclock defects
shipped fixes. Extended rule: **an issue-tracked defect that produced live wrong output,
a red main, or a wedged deploy plane gets a row**, whether or not a rollback fired.

**Legacy Feb–Mar tally**, kept for continuity — it describes the 32 rows that existed on
2026-05-19 and nothing since: deployment errors 9 · CDK drift 3 · stale config/env
override 3 · silent secret deletion 2 · stale Lambda module caches 3 · wrong component
investigated 3 · missing infrastructure 3 · data quality/scoring 4 · one-off deploy
scripts 2.

**S3 sync --delete watch-out (ADR-032/033, v3.7.57):** `aws s3 sync --delete` to the bucket root will delete every object not in the source directory. This is the most destructive single command in the platform. **Hardening applied:**
1. S3 bucket policy denies `s3:DeleteObject` on `raw/`, `config/`, `uploads/`, `dashboard/`, `exports/`, `deploys/`, `cloudtrail/`, `imports/` for `matthew-admin` — deploy scripts physically cannot delete data files.
2. `deploy/lib/safe_sync.sh` wrapper blocks syncs to bucket root and aborts if dryrun shows >100 deletions.
3. S3 versioning enabled — delete markers are recoverable.
4. One-off deploy scripts are prohibited. All site deploys use `sync_site_to_s3.sh` with `S3_PREFIX="site"`.

**One-off deploy script watch-out (new pattern as of v3.7.57):** One-off scripts (`deploy_vXXXX_do_thing.sh`) bypass the safety patterns built into canonical tooling. Two separate P1/P2 incidents traced to one-off scripts that didn't use `deploy_lambda.sh` or `sync_site_to_s3.sh`. **Rule: no one-off deploy scripts.** Use canonical scripts with flags/arguments, or modify the canonical script temporarily.

**CDK drift watch-out (new pattern as of v3.7.10):** IAM policy changes in `role_policies.py` only take effect when the relevant stack is deployed. After any refactor touching role policies (secrets consolidation, prefix changes, etc.), always redeploy the affected stack immediately and verify with a smoke invoke. Do not assume CDK state matches AWS state without a deploy.

**MCP Lambda deploy watch-out (ADR-031, v3.7.47):** `deploy_lambda.sh life-platform-mcp` strips the `mcp/` package — the Lambda boots clean but routes everything through the bridge handler (401 on all requests). Always use the full zip build for MCP:
```bash
ZIP=/tmp/mcp_deploy.zip && rm -f $ZIP
zip -j $ZIP mcp_server.py mcp_bridge.py && zip -r $ZIP mcp/ -x 'mcp/__pycache__/*' 'mcp/*.pyc'
aws lambda update-function-code --function-name life-platform-mcp --zip-file fileb://$ZIP --region us-west-2
```
`deploy_lambda.sh` now hard-rejects `life-platform-mcp` with a clear error. Symptom: `{"error": "Unauthorized"}` from OAuth endpoints; Lambda logs show clean START/END with no errors (misleading).

**CDK packaging watch-out:** `Code.from_asset("..")` bundles source files one directory deep in the zip — Lambda can't find the handler. Always use `Code.from_asset("../lambdas")` (points at the lambdas directory directly). When CDK-managing Lambdas for the first time, verify a sample function works before assuming all 23 are healthy. `deploy_lambda.sh` is immune to this bug.

**Stale lambda module caches:** When a shared module (like `platform_logger.py`) adds new methods, all Lambdas that bundle their own copy of that file need to be redeployed. CDK packaging re-bundles from source automatically; `deploy_lambda.sh --extra-files` is the manual equivalent for Lambdas not yet on CDK.

**Secrets consolidation watch-out:** When consolidating Secrets Manager entries, Lambdas with `SECRET_NAME` (or similar) set as explicit env vars will override code defaults and continue pointing at the deleted secret. Always audit Lambda env vars — not just code — when retiring secrets. Also verify key naming conventions match between old and new secret schemas.

**Key lesson (from RCA):** When data isn't flowing, check YOUR pipeline first (CloudWatch logs for the receiving Lambda), not the external dependency. Document the full request path so you investigate the right component.

---

## Open Monitoring Gaps

| Gap | Risk | Mitigation |
|-----|------|------------|
| No end-to-end data flow dashboard | Slow detection of silent failures | Freshness checker provides daily coverage |
| DLQ coverage: MCP + webhook excluded | Request/response pattern — DLQ not applicable | CloudWatch error alarms cover both |
| No webhook health check endpoint | Can't externally monitor webhook availability | CloudWatch alarm on zero invocations/24h |
| ~~No duration/throttle alarms~~ | ~~Timeouts without errors go undetected~~ | **Resolved v3.7.36** — duration alarms deployed for all Lambdas |
| ~~No CDK drift detection~~ | ~~IAM policy changes in code may not be applied to AWS~~ | **Resolved v3.7.36** — `cdk diff` step added to ci-cd.yml; post-refactor deploy + smoke verify documented in RUNBOOK.md |

**Resolved gaps (v2.75.0):** All 29 Lambdas now have CloudWatch error alarms. 10 log groups now have 30-day retention. Deployment zip filename bug eliminated by `deploy_lambda.sh` auto-reading handler config from AWS.

**Resolved gaps (v3.1.x):** DLQ consumer Lambda (`dlq-consumer`) now drains and logs failures from `life-platform-ingestion-dlq` on a schedule — silent DLQ accumulation is now caught proactively. Canary Lambda (`life-platform-canary`) runs synthetic DDB+S3+MCP round-trip every 30 min with 4 CloudWatch alarms — end-to-end health check is now automated. `item_size_guard.py` monitors 400KB DDB write limits before they cause failures.

**Open gap (V2 audit 2026-05-19):** Long-running ALARM-state alarms (e.g., Garmin errors for 44 days) need an escalation path beyond email — the inbox volume normalized the noise. V2 P6 follow-up: consider digest summary of alarms in continuous-ALARM state >7 days.

---

**Verified:** 2026-05-19 (V2 audit operational sweep)
