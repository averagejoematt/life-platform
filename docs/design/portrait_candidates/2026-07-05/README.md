# Pilot-trio portrait candidates — 2026-07-05 (#587, awaiting the taste gate)

Six hand-authored candidate recipes (A + `__b` variant per persona) for Elena Voss /
Dr. Lisa Park / Dr. Sarah Chen — schema-valid, ALL UNSIGNED, staged here (not
`config/portraits/`) until Matthew's contact-sheet decision. Contact sheet:
the "pilot-trio-v1" artifact (see issue #587). On approval: move the chosen variant to
`config/portraits/<persona_id>.json` (dropping any `__b` suffix inside and on the file),
record `_meta.sign_off`, regenerate the bundle, ship, visual-QA. On kill: delete this dir.
