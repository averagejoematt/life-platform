# FULLREVIEW — 2026-07-16

> **The first /fullreview scorecard.** 17-lens expert panel (upgraded post-red-team form), each lens graded by an
> independent agent and every finding adversarially verified by a lean-REFUTED finding-verifier before it appears here.
> Workflow run `wf_acd72834-8d9` — 34 agents, 0 errors, 89 raw findings → **72 CONFIRMED / 17 REFUTED** (81% survival).
>
> **Ground truth at review time:** Day 4 of cycle 6 (genesis 2026-07-13) · live site build `1b57516` · main `b6408f39`
> (one docs commit ahead, expected) · budget tier 1 ($82 projected vs the $85 ADR-063/133 ceiling) · repo PRIVATE since
> 2026-07-13 (history was public; secret hygiene graded as-if-public) · PR #1193 deployed out-of-band, merge held.
>
> **Grade calibration:** an A means A *for this platform's stated posture* — solo operator, ~$85/month ceiling,
> ADR-103 complexity ledger. Rubric anchors are persisted in `fullreview_grades_2026-07-16.json` for re-run durability.
>
> **Contract:** this document ends at the scorecard. Filing issues (issue-filer) and shipping fixes are separate,
> explicitly authorized steps.

## Scorecard

| Lens | Area | Grade | Confirmed | Refuted |
|---|---|---|---|---|
| a11y | Accessibility | **A-** | 4 | 1 |
| dataviz | Charts & instruments | **A-** | 3 | 4 |
| designer | Design system | **A-** | 4 | 1 |
| devex | Build & ship ergonomics (AI-agent development) | **A-** | 5 | 1 |
| principal | Code health | **A-** | 5 | 0 |
| security | Exposure — privacy absolutes on the live surface, repo secret/PII hygiene, auth surfaces (MCP OAuth, site-api write rate-limits), S3 bucket policy, IAM posture | **A-** | 1 | 1 |
| ai-quality | AI content integrity | **B+** | 4 | 1 |
| cost | Cost engineering | **B+** | 6 | 0 |
| cpo | Product & narrative arc | **B+** | 5 | 0 |
| cto | Architecture & ops | **B+** | 4 | 0 |
| data-architect | Data modeling | **B+** | 4 | 0 |
| growth | Community & commercialization | **B+** | 3 | 4 |
| integrations | Ingestion health | **B+** | 2 | 1 |
| qs | Scientific credibility | **B+** | 6 | 1 |
| reader | First-contact comprehension (cold Reddit reader, live site, desktop 1280 + mobile 390) | **B+** | 5 | 0 |
| narrative | Voice & immersion | **B** | 5 | 2 |
| observability | Telemetry & alerting | **B** | 6 | 0 |

## High-severity confirmed findings (10)

- **[cto]** The #727 scientific-liveness heartbeat has been dead since it shipped: coach-prediction-evaluator's IAM role lacks cloudwatch:PutMetricData, every daily emit fails AccessDenied (demoted to a WARNING by the fail-soft catch), and the grading-stalled alarm — built specifically to catch a dead evaluator — has sat in ALARM on missing-data-breaching for 10 days and can never clear. This is the exact known failure class already fixed on coach-state-updater ('Pre-fix every emit failed with AccessDenied (non-fatal — caught as WARNING)', role_policies.py:827) and the exact #342 lesson (verify the capability, not IAM parity).
- **[ai-quality]** /api/state_of_matthew serves the tombstoned pre-genesis cycle-5 brief as current: handle_state_of_matthew takes the latest DATE# record with no tombstone/phase guard, so the wiped 2026-07-12 'Week 1, Day 1' narrative (cycle-5 Day 1, stamped tombstone=true / experiment_restart_2026-07-13) renders live on /coaching/ 'This week' on Day 4 of cycle 6.
- **[cpo]** Predict-the-week — the cockpit's flagship returnability hook — is live on Day 4 asking readers to bet on 'this week' while its subject is frozen at cycle-5's final week: current_challenge.json has week_id 2026-W27 (today is ISO 2026-W29, cycle 6), was last modified 2026-07-03, is titled 'The week after the wall — does the comeback hold?' (the dead cycle's narrative), and nothing (no lambda, no restart_pipeline step) refreshes or clears it — votes cast today are stored under a week that ended before genesis and can never be revealed.
- **[qs]** Experiment resets silently vanish all open pre-registered bets ungraded: the restart wipe writes no expired/void rows to the CROSS_PHASE calibration ledger, and once tombstoned the phase filter hides old-phase hypotheses from the weekly engine forever — after 5 resets the ledger holds ZERO hypothesis rows, so the taxonomy's own justification for the wipe ('graded outcomes live in the CROSS_PHASE calibration ledger') is unimplemented on the wipe path.
- **[narrative]** Elena's persistent-memory reads ignore restart tombstones, so cycle-5 story threads/callbacks feed cycle-6 installments — the pending 2026-07-14 draft already pays off a wiped thread with a phantom citation ('The Sunday walks... documented in the prologue' — neither published prologue post mentions Sunday walks), the exact failure phase_taxonomy #946 names.
- **[narrative]** /api/state_of_matthew serves the tombstoned cycle-5 'Week 1, Day 1' brief (dated 2026-07-12) with available=true, and the /coaching/ Week lens renders it — so on Day 4 of cycle 6 the coaching door narrates LAST cycle's day 1 (incl. a disagreement resolution claiming 'in deficit for weeks') beside a fresh cycle-6 weekly priority.
- **[observability]** coach-prediction-evaluator's IAM role lacks cloudwatch:PutMetricData, so the #727 scientific-liveness gauge (DaysSinceLastDecided/DecidedCount/GradableCount) has never successfully emitted — every daily run logs a swallowed AccessDenied WARNING, and the grading-stalled alarm has sat red since 2026-07-06 and can never clear (treat_missing=BREACHING with a permanently absent metric). The broken state is codified in the repo: role_policies.py compute_coach_prediction_evaluator() has no CloudWatchMetrics statement while the lambda calls put_metric_data — the exact class role_policies.py:824 documents fixing for coach_state_updater in May.
- **[observability]** The self-healing loop is mostly not closing: 3 of the last 4 remediation-agent runs (Jul 8, 13, 15) ended with EVERY signal in 'untriaged' (needs_human=0, prs=0, auto_fixed=0) and a truncated _raw reasoning tail — the agent's structured triage failed to complete — yet every workflow run concludes 'success'. grading-stalled has been presented to 4 consecutive runs without ever being classified, fixed, or handed off, even though role_policies.py is on the automerge ALLOWLIST (this is precisely the fix class the agent exists for).
- **[data-architect]** Every experiment reset RE-tombstones all previously archived records and overwrites their cycle stamp with the latest closing-run number, destroying the 'archive navigable by reset generation' guarantee that is ADR-077's stated purpose — a February-2026 (cycle-1-era) insight now claims cycle=5, and COACH OUTPUT rows from 2026-07-10/11 (produced in cycle 4) also claim cycle=5.
- **[integrations]** Public /api/source_freshness loses last_update, age_hours, stale_hours, and the ADR-104 'dark N days' stamp for any source whose newest record predates the current cycle: _latest_date_str applies with_phase_filter, and the cycle-6 reset tagged all pre-genesis records phase=pilot — so MacroFactor (the PRIMARY food channel, actually dark since 2026-06-24) and Hevy/measurements/food_delivery all render last_update:null, days_dark:null on the public board, exactly when the lapse is longest. The operator checker disagrees (freshness_checker_lambda.py:399 queries WITHOUT the phase filter and would report 'Last update: 2026-06-24'), and the same file already treats RAW_TIMESERIES as cross-phase with a deliberate include_pilot=True for device agreement (site_api_data.py:390-396) — freshness is pipe/behavior liveness and should read cross-phase too. Recurs after EVERY reset until each source's first current-cycle write.

## Per-lens detail

### a11y — Accessibility — **A-**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** WCAG AA holds on every live page in BOTH themes (verified by an automated token-contrast check, not hand-written comments); every interactive widget is keyboard-operable with visible focus; data charts are named to AT; reduced-motion is honored; tap targets meet the 44px floor site-wide with a gating audit.
- **C:** Landmarks/headings/skip links are mostly right and most widgets are keyboard-reachable, but multiple AA contrast failures exist in at least one theme, some custom widgets (charts, tabs, popovers) are mouse-only or unnamed to screen readers, and there is no regression guard so fixes decay.
- **F:** Div-soup interactive UI (no focus styles, no keyboard path), data charts silent to screen readers, contrast failures on body text, no skip links, animation ignores prefers-reduced-motion.

**Confirmed findings:**

1. **[med/S/class-A]** Light mode never overrides --alert: #CB634C on light --page #F4EFE4 is 3.37:1 (3.65:1 on --surface), failing WCAG AA (4.5:1) for the normal-size .vr-alert .vr-v ring-center value (--fs-h4 ~1.05rem, tokens.css:754-756). The #579 bump fixed dark mode only; both light-mode blocks (tokens.css:271-301) override --ember and --ink-* but not --alert.
   - *Evidence:* site/assets/css/tokens.css:103 (--alert defined once, dark root; grep confirms no light override), tokens.css:756 (.vr-alert .vr-v { color: var(--alert) }), tokens.css:754 (font-size --fs-h4); computed 3.37:1 via WCAG relative-luminance script (output in transcript). .vs-alert .vs-w at fs-h2 1.8rem is large text and squeaks past 3:1.
   - *Path-to-A step:* Add a light-mode --alert (e.g. ~#9E4732, >=4.6:1 on #F4EFE4) to both the @media (prefers-color-scheme: light) block and the :root[data-theme="light"] block in tokens.css — same one-line pattern as the existing light --ember override.
   - *Regression guard:* A stdlib pytest (no new deps, ADR-103-clean) that parses tokens.css hexes and asserts the documented text-color/background pairs meet 4.5:1 in BOTH theme blocks — the same shape as scripts/check_doc_facts.py but for contrast.
1. **[med/S/class-B]** The AA contrast math exists only as hand-written comments in tokens.css and has already drifted once (the light-mode --alert miss above): there is no automated contrast or a11y regression check anywhere (no axe/pa11y, and tests/visual_qa.py checks rendering, not contrast), so the next token tweak can silently break AA again.
   - *Evidence:* site/assets/css/tokens.css:100-106 (hand-computed '5.1:1 on --page' comments); grep for axe/pa11y/lighthouse across tests/ and .github/workflows/ returns nothing; tests/visual_qa.py:492 is the only a11y-adjacent check and it covers tap targets only.
   - *Path-to-A step:* Ship the token-contrast pytest (pure stdlib: parse hex tokens, compute WCAG ratios for the ~10 text pairs per theme, assert AA) into the offline suite — zero standing cost, catches exactly the class of drift that produced finding 1.
   - *Regression guard:* The test IS the guard; wire it into the existing 'Lint + Syntax Check' / pytest CI job so a failing pair reds the build before deploy.
1. **[low/M/class-A]** The home waveform day bars (.wave a.bar — one anchor per experiment day into that day's cockpit) are excluded from the #1010 44px tap-target floor and from visual_qa's advisory TAP_TARGET_SEL; bars are flex:1 with min-width:2px and 2px gap, so as cycle 6 fills (~90 days) each touch target shrinks toward ~2-3px on a 390px phone. Direct sibling of the #1017 constellation fix, which got an invisible 44px hit expander while the bars did not.
   - *Evidence:* site/assets/css/story.css:198 (.wave .bar { flex: 1; min-width: 2px; }) and :202 (.wave a.bar anchors); tokens.css:1141-1164 (#1010 floor selector list — no .wave); tests/visual_qa.py:435 (TAP_TARGET_SEL omits .wave a.bar, and :423 notes the audit is 'Reported, never gated'); story.js:167-171 shows the #1017 hit-circle pattern the bars lack. Keyboard is fine (anchors + :focus-visible at story.css:203); touch is the gap.
   - *Path-to-A step:* On touch widths, route bar taps through the already-shared data-cpts tooltip/readout (story.css:205 says motion.js already wires it) with the tooltip carrying the day link, or give bars a vertical ::after hit expansion per the documented #1010 grammar — no visual change either way.
   - *Regression guard:* Add .wave a.bar to TAP_TARGET_SEL in tests/visual_qa.py and promote the tap-target audit from advisory to gating for the known-good selector set (finishing #1010/#1013 as designed).
1. **[low/S/class-A]** The theme toggle exposes no current state to assistive tech: a static aria-label="Toggle light and dark" with no aria-pressed and no label swap in theme.js, so a screen-reader user cannot tell which theme is active or confirm the toggle worked.
   - *Evidence:* Live https://averagejoematt.com/ HTML (fetched 2026-07-16): <button class="theme-toggle" type="button" aria-label="Toggle light and dark"> with only an aria-hidden span inside; grep of site/assets/js/theme.js shows no aria attribute is ever set (confirmed in transcript).
   - *Path-to-A step:* In theme.js's toggle wiring, set aria-pressed (dark = pressed) or swap the accessible name to 'Switch to light theme' / 'Switch to dark theme' on each apply(), covering both the boot state and post-toggle state.
   - *Regression guard:* One assertion in the Playwright visual_qa sweep: after clicking .theme-toggle, the button's aria state reflects documentElement.dataset.theme.

**Refuted (not defects — kept for the record):**
- ~~The home page's dispatches tabs still use the deprecated aria-pressed toggle loop (story.js sets aria-pressed on .dx-tab) — the exact pattern the project's own tabs.js (#579) documents as a defect ('announces a set of independent toggle buttons' for what is visually a single-select tab strip). dispatches.js and coaching.js migrated to wireTabList (real role=tab, aria-selected, roving tabindex, arrow keys); home did not.~~ — *The finder misread the home page's runtime. story.js:593's aria-pressed loop lives in dxSelectSrc, which is DEAD CODE on home: grep shows it is defined but never called anywhere; home's init unconditionally calls dxTeaser() (story.js:713), which hides [data-dx-tabs] and .dx-layout (lines 608-611) per the P1.1/P1.3 comment 'Home no longer hosts the full master-detail dispatches reader.' The live homepage's data-dx-tabs nav is EMPTY in served HTML (verified by fetch today) — only dispatches.js builds .dx-tab buttons, on /story/, where wireTabList IS wired (dispatches.js:599). No user is ever exposed to a deprecated aria-pressed tab strip on home; at most this is dead-code cleanup, not an a11y defect.*

**Path to A:**
1. Override --alert in both light-mode token blocks to an AA-compliant oxblood (>=4.5:1 on #F4EFE4) — the one confirmed AA text failure on the site (tokens.css:271-301).
1. Add a stdlib token-contrast pytest asserting the documented ink/ember/alert pairs meet AA in BOTH themes, wired into the existing CI pytest job — converts the hand-written AA comments into a gate (zero standing cost, ADR-103-clean).
1. Close the #1010 tap-floor gap on .wave a.bar (route touch through the shared data-cpts readout or add the #1017-style invisible hit expansion) and add the selector to visual_qa's TAP_TARGET_SEL, promoting that audit from advisory to gating.
1. Migrate the home .dx-tab strip from the aria-pressed loop (story.js:593) to the existing wireTabList module, matching dispatches.js/coaching.js.
1. Expose theme-toggle state (aria-pressed or name swap) in theme.js and assert it in the Playwright sweep.

**Lens notes (coverage):** Inspected: live HTML of / plus all 7 door/footer pages (cockpit, data, coaching, protocols, story, method, subscribe) and /data/sleep/ — all have skip link, single h1, <main>, lang, labeled navs, sane heading order; all 8 site CSS files (grep + read of tokens.css palette/light-mode/tap-floor sections, story/cockpit/evidence/subscribe/mind); JS: cockpit.js (pillar rows = real buttons w/ aria-expanded; scope buttons aria-pressed), evidence.js, coaching.js, story.js, dispatches.js, tabs.js (roving tabindex), motion.js (charts get tabindex=0 + arrow-key point exploration; reduced-motion early-exit), theme.js, portraits.js (decorative aria-hidden w/ adjacent name text), coach/provenance popovers (focus trap + return + Escape + aria-expanded/haspopup — textbook), charts.js (every chart role=img with a data-summary aria-label; sparklines correctly decorative), read_aloud.js; computed 19 WCAG ratios across both themes (script output in transcript — dark palette fully AA incl. ink-faint 5.03/4.77, light ink-muted 4.93 passes); native <audio controls> for podcast/read-aloud. This is far above typical content-site a11y — the team runs numbered a11y issues (#579 contrast, #1010 tap floor, #1013 audit, #1017 constellation) and it shows. NOT covered: an actual VoiceOver/NVDA session or full manual keyboard walkthrough in a real browser (static+code analysis only); the remaining ~20 /data/ topic readouts and /story//method/ subpages beyond spot checks; JS-injected .prose heading order inside AI-generated chronicle posts; aria-live coverage when the cockpit scope switcher re-renders (unverified suspicion — content swaps may be silent to AT, but each control is self-announcing); whether podcast episodes carry text transcripts (mitigated: read-aloud episodes are voicings of published text, so the text IS the transcript; the panel podcast's transcript presence unverified). Data-maturity caveats: the .vr-alert light-mode failure needs an out-of-range vital to render (CSS path verified, live occurrence not observed on Day 4); the wave-bar tap-target issue is currently masked because ~4 day-bars are wide — it degrades as the cycle fills. No kill-on-sight items seen: down states use muted ink not red, motion is earned and reduced-motion-gated, no decorative gloss.

### dataviz — Charts & instruments — **A-**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every visual encoding is proportional and honest at any n (lengths/areas never anchored to arbitrary floors), every statistical claim carries visible-and-reachable n/CI on touch and keyboard, low-n charts refuse or degrade with explicit copy, and the live site renders zero contradictory encodings on any surface.
- **C:** Charts render and mostly caption honestly, but multiple encodings exaggerate (truncated-baseline bar lengths, area fills over min-max axes), some correlations ship without visible n, or the hover/tap readouts are dead on a class of charts.
- **F:** Truncated axes or area lies presented as trends, correlations framed causally or shipped with no n at all, fabricated confidence bands, or n=4 data drawn as a confident trend line with no caveat.

**Confirmed findings:**

1. **[med/S/class-A]** Home waveform paints declining days as the 'up' (ember) tier when the window spread is below threshold, and ignores the API's own per-day tier — live today: 3 scored days all served color:'amber' and monotonically declining (236.3→232.7→229.6) render as three ember 'up' bars.
   - *Evidence:* site/assets/js/story.js:483 `bar.className = ... meaningfulSpread ? tierOfRel(pos) : "up"` (meaningfulSpread requires scores.length>=4; live /api/journey_waveform returns 3 scored days, each {"color": "amber"}, ignored by renderWave); story.css:206 `.wave .bar.up { background: var(--ember); }`
   - *Path-to-A step:* Default the no-meaningful-spread tier to 'mid' (muted ink), or honor the payload's own color field — never the positive tier as the fallback.
   - *Regression guard:* A small node-run unit test on renderWave (or an extracted tier function): assert no scored bar receives class 'up' when the window's scores are monotonically declining under the spread threshold.
1. **[med/S/class-A]** Waveform bar HEIGHTS are min-max normalized (14%–100%) — a length-encoded mark whose baseline is the window minimum, so today's real 2.8% score decline renders as an ~86% visual collapse, and it dramatizes even when the code itself has judged the spread 'not meaningful' for color.
   - *Evidence:* site/assets/js/story.js:469-485 `const pos = d.score ? (d.score - lo) / span : null; ... const h = d.score ? 14 + (pos || 0) * 86 : 6` — heights use min-max regardless of the meaningfulSpread guard that gates only color; live payload span is 6.7 points on a ~233 base.
   - *Path-to-A step:* When !meaningfulSpread, render near-equal heights (the guard already exists — extend it from color to height); longer-term, scale height from a stable floor tied to real relative change so bar length stops encoding rank-within-window.
   - *Regression guard:* Unit test: for a window whose max/min ratio is <1.05 and spread below threshold, assert max bar height / min scored bar height is bounded (e.g. <1.3), not 100/14.
1. **[med/M/class-A]** Constellation edge evidence (r, n, significance) lives only in a native SVG <title> — hover-only, unreachable on touch and keyboard, and inconsistent with the site's one shared data-cpts readout, so the ADR-105 n behind the home page's strongest visual claim is invisible to mobile readers.
   - *Evidence:* site/assets/js/story.js:150-153 `const t = document.createElementNS(SVGNS, "title"); t.textContent = `...r=${e.r} over ${e.n} days...`` — no data-cpts on the edges group; motion.js:128-165 is the shared readout every other chart uses (hover + tap + keyboard).
   - *Path-to-A step:* Emit data-cpts points at edge midpoints on the constellation svg (data-cpts-hit="xy"), labels carrying the existing r/n/significance text, and drop the native <title> — same contract as pillarRingCpts.
   - *Regression guard:* tests/visual_qa.py assertion (or a JS unit test) that the constellation svg carries a data-cpts attribute whose point count >= the served edge count.

**Refuted (not defects — kept for the record):**
- ~~sufficiencyBars clamps pct to 100 and then PRINTS the clamped number — a micronutrient consumed at 180% of target displays as '100%', a value-label lie (the actual/target amounts beside it are honest, but the headline percent is wrong).~~ — *The premise cannot occur: pct is capped at 100 UPSTREAM at ingestion — lambdas/ingestion/macrofactor_lambda.py:271 `pct = min(round(actual / target * 100, 1), 100.0)` with the documented contract 'Pct is capped at 100 — exceeding target still scores 100%'. Sufficiency is deliberately defined as min(intake/target, 100%), so the chart never receives a value >100 and a 180% intake reaches charts.js already as pct=100; the clamp at charts.js:538 is defensive parity with the metric's definition, not a value-label lie, and the honest actual/target amounts render beside it. Intentional data semantic, misread as a chart defect.*
- ~~correlationChip silently drops n when the caller omits it — the primitive renders a bare 'r=0.55' with no sample size, violating ADR-105's n-on-every-claim at the shared-primitive level (current callers happen to gate, but the contract isn't enforced where the chip is drawn).~~ — *No reproducible defect: both live callers (evidence_habits.js:46-47, evidence_sleep.js:60-67) pass n AND independently print 'n=… · wk overlap' in the card meta line adjacent to every chip, and the chip is gated behind n>=14 / coefficient-present honesty gates. The falsy-n branch at charts.js:804 is dead defensive code with no live path — the finding itself concedes 'current callers happen to gate'. A hypothetical contract-hardening suggestion, not an ADR-105 violation on any rendered surface.*
- ~~lineChart draws a filled area anchored to the data-min baseline on a min-max (truncated) axis — an area encoding whose floor is arbitrary, mildly exaggerating small moves on charts like the home weight line (the wash is faint and the line is primary, but it is still a non-zero-baseline fill).~~ — *Intentional, documented design, not a defect: the data-min axis is a deliberate reasoned choice (HARD RULE 4 comment at story.js:720-722 — a goal-anchored axis flattened the real slope into the 'empty chart' bug), the home weight lineChart runs spine:true which prints the real min/max scale next to the plot (SIGNATURE 1), and chart-fill is documented in the charts.js header as the ember wash under the primary line — the standard sparkline treatment. The min==max degenerate case is handled (charts.js:46). The finder's own text concedes the wash is faint and the line primary; this is a generic style critique of a documented convention.*
- ~~Constellation node radius encodes pillar score linearly in RADIUS (r = 14 + score/4) — circle area grows quadratically, over-stating gaps between pillar scores (~8x area for 100 vs 0); mitigated by the numeral printed inside each node, but the size channel itself misleads.~~ — *The quantitative claim is wrong in the operating range: with the +14 radius floor (story.js:161), circle AREA understates or approximates score ratios for realistic scores — score 100 vs 50 gives area ratio 2.17x vs value ratio 2x; 75 vs 25 gives 2.62x vs 3x (understates). The '~8x area for 100 vs 0' only holds against a zero-score node whose r=14 floor is a deliberate visibility floor documented in the figcaption ('small = low, a young experiment starts low, not broken', index.html:99-101). The encoding is intentional (#407 'node size = score'), a low/high size legend is shown, and the exact score numeral printed inside each node is the quantitative channel of record.*

**Path to A:**
1. Fix the home waveform's two encoding lies (findings 1+2): neutral 'mid' tier when spread is below threshold (or honor the API's served per-day color), and extend the meaningfulSpread guard from color to bar height — this is the front-page hero and the only live contradictory encoding found.
1. Move constellation edge r/n/significance onto the shared data-cpts readout so the evidence behind the home page's strongest visual is reachable on touch and keyboard (one interaction system, as #583 intended).
1. Harden the two leaky primitives at the source: correlationChip renders an explicit n-absence marker instead of a bare coefficient, and sufficiencyBars prints the real pct while clamping only the bar width.
1. Reserve lineChart's area fill for zero-based domains (one conditional), retiring the last non-zero-baseline area encoding.
1. Add a tiny node-run charts honesty test file (no deps, offline — ADR-103: zero standing infra) locking the contracts that make this area near-A: <4-point refusal, n display on every correlation mark, clamp-label truth, tier defaults; wire it into the existing offline suite.

**Lens notes (coverage):** INSPECTED IN FULL: charts.js (all 905 lines — every primitive: lineChart, arcTrend, weightTrendChart, projectionCone, confLevel, nDots, ciWhisker, dualLineChart, ring, autonomicHero, autonomicQuadrant, sparkline, stackedBar, sufficiencyBars, intakeSpine, targetSpine, heatStrip, dumbbell, landmarkBars, stackedDayColumns/Columns, mealWindowRibbon, correlationChip, barChart, pillarRing/Cpts, radarChart); story.js in full (waveform, constellation v2, numbers beat, okay-panel, mirror); evidence_discovery.js in full; evidence_vitals.js in full; evidence_intelligence.js correlations+calibration sections; evidence_habits.js keystone; evidence_sleep.js correlation board; motion.js readout handler (verified: one shared hover/tap/keyboard implementation, nearest-by-x / by-y / 2-D xy strategies, MutationObserver wiring — the data-cpts affordances genuinely work in code); tier/token CSS (story.css, tokens.css — all chart colors token-driven with real [data-theme=light] + prefers-color-scheme overrides, so theme parity is structural; I did not measure contrast ratios pixel-wise). LIVE PAYLOADS CHECKED: /api/pulse, /api/correlations (empty — the empty-state copy at evidence_intelligence.js:205 is exemplary ADR-105 honesty), /api/pillar_coupling, /api/journey_waveform, /api/journey, /api/observatory_week. NOT COVERED: cockpit.js's 1538 lines beyond its charts.js imports; evidence_body/nutrition/character/reading/meta in full; an actual browser render pass (no screenshots — findings are code+payload verified, not visually confirmed); OG image cards (out of scope per #1193). DATA-MATURITY: sparse charts degrade beautifully almost everywhere — the <4-point refusal with honest count copy is uniform across every trend primitive, projectionCone refuses without a real CI (live: rate_provisional=true, CI null → refusal fires), the correlations/calibration/hypotheses empty states name the small-n reality, the confidence grammar (tight band / dashed band / NO band) is a genuinely Tufte-grade uncertainty language, and habitsKeystone withholds the coefficient below 2 weeks. The waveform findings are the one place low-n degradation is broken. UNVERIFIED SUSPICIONS (not filed): (1) /api/pillar_coupling's 60-day window (2026-05-17→07-15) spans the Jul-13 reset — 57 of 60 days are prior-cycle behavior under a different protocol; the home caption honestly states 'over the last 60 days' but doesn't flag the cross-reset span (defensible under ADR-077 raw_timeseries, worth a one-word 'spans the reset' caption); (2) /api/journey internal rounding mismatch — current_weight_lbs=316 (int) with start 314 and lost_lbs=-1.6 reads as 2.0 by subtraction on the hero (backend rounding, not a renderer defect); (3) constellation 'metabolic' node routes to /data/glucose/ and 'mind'→/data/reading/ vs 'relationships'→/data/mind/ — link mapping looked odd but I didn't verify page intent. KILL-ON-SIGHT SCAN: clean — no decorative glow (edge .effect glow is driven by a live active-effect signal), no causal framing (every correlation surface carries 'correlation, not cause (N=1)'), red reserved for genuine run-down state never direction, no age/genome leakage in the chart layer (fitness age explicitly Option-A). Grade rationale: the primitive library and its uncertainty language are the best I have reviewed at any scale — the A- (not A) is entirely the live homepage waveform rendering contradictory encodings today, plus the small primitive-level enforcement gaps.

### designer — Design system — **A-**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every live surface draws color/type/spacing from tokens.css; the 11px type floor holds when MEASURED (including SVG text under render scale) at 390 and 1280; single-ember discipline and the breakpoint set are enforced by a gate that sweeps all sheets; Day-N-sparse states are visibly designed and honest.
- **C:** The kit and tokens exist and most pages use them, but flagship charts ship sub-floor text, several off-palette raw-hex accents render live, and the token gate covers only a minority of sheets so drift accumulates unpoliced.
- **F:** No token discipline: decorative glow, red-for-down direction encoding, emoji marks, per-page ad-hoc type and colors; empty states are blank, broken, or dressed up to hide sparseness.

**Confirmed findings:**

1. **[med/S/class-A]** Measured #1017 siblings: SVG text renders below the 11px mobile floor (and below it on desktop too) on two flagship data pages — autonomic 2x2 labels at 8.5px effective on /data/vitals/, radar pillar labels at 8.8-9.4px and the tier emblem 'LEVEL' at 7px on /data/character/ — because viewBox-unit font sizes scale with rendered width, the exact bug class #1017 just fixed for the home constellation.
   - *Evidence:* Playwright getScreenCTM measurement on live pages 2026-07-16: {cls:'aq-lab',fs:8,scale:1.06,eff:8.5} at 390 AND 1280 on /data/vitals/; {cls:'radar-lbl',fs:10,scale:0.88,eff:8.8} + {txt:'LEVEL',eff:7.0} on /data/character/. Sources: tokens.css:731-732 (.aq-lab/.aq-ax font-size:8px), evidence.css:670 (.radar-lbl 10px), sigils.js:141 (font-size=8), evidence.css:711 (.radar-chart max-width:320px vs viewBox 320 = scale<=1 always).
   - *Path-to-A step:* Generalize the #1017 fix (story.js sizeConstellation: user-units = max(base, floor/scale) set as CSS vars, rAF-debounced) into a shared charts.js helper and apply to .aq-lab/.aq-ax, .radar-lbl, the tierEmblem text, and .bm-cap (tokens.css:481, 8px in a 270-unit viewBox).
   - *Regression guard:* Add an effective-px SVG-text audit to tests/visual_qa.py (the ~15-line getScreenCTM sweep used here) asserting >=11px on every svg text at 390 and 1280 — extends the existing gating CI job, no new infra (ADR-103: visual-qa is load-bearing).
1. **[med/S/class-A]** Live off-palette raw-hex accents bypass tokens and the single-ember rule: the head-coach 'lead' markers use Tailwind sky blue #0ea5e9 (5 selectors, rendered by coaching.js on /coaching/ team + digest views) instead of the sanctioned --coach channel, and the vice streak cards use green #16a34a for 'holding' (rendered by evidence_sleep.js renderVices, wired in the evidence router).
   - *Evidence:* story.css:639-654 (.team-lead/.dx-item--lead/.dx-item-lead/.coach-head--lead/.coach-head-tier all border/color #0ea5e9) consumed at coaching.js:473,652,1088; evidence.css:240,245,250 (.vice-hold #16a34a) consumed at evidence_sleep.js:274, route 'vices' in evidence.js:67. DESIGN_SYSTEM_V5.md §4: 'Never hardcode a colour... One ember accent'; §8.2 names --coach as the one sanctioned exception.
   - *Path-to-A step:* Ride the lead-coach marker on var(--coach) (already set per coach card) or ember; for vice-hold, pick the §4-compliant 'held' treatment (ember = alive/up; broke = muted ink — the .vice-broke rule already does this correctly) and tokenize.
   - *Regression guard:* Extend scripts/check_css_tokens.py with a raw-hex-color check (allowlist tokens.css) — same fs-ok-style sanction escape hatch; enforced by the existing tests/test_css_tokens.py.
1. **[med/M/class-B]** The token gate's coverage is a fraction of the surface it claims to protect: check_css_tokens.py sweeps only 3 of 8 sheets (story/evidence/cockpit — mind.css with its hardcoded #1d1810 gradient and rgba shadows is unswept), polices font-size but not color, and the DESIGN_SYSTEM_V5 §10.1 breakpoint invariant ('grep returns only those nine numbers') is doc-only with no test — and is already violated live by a rogue (max-width: 520px).
   - *Evidence:* scripts/check_css_tokens.py:31 (SWEPT = story/evidence/cockpit only) and :50 (FONT_SIZE regex — no color check); mind.css:67 (linear-gradient ... #1d1810, no theme scoping); story.css:582 '@media (max-width: 520px) { .cb-style ... }' (live via coaching.js:212); `grep -rhoE '\((max|min)-width...' site/assets/css` returns 520 as a 10th value; `grep -rn '520|breakpoint' tests/pr_render_gate.py tests/test_css_tokens.py tests/visual_qa.py` → no hits.
   - *Path-to-A step:* One PR: extend check_css_tokens.py to all 8 sheets + add the color check (finding 2) + add the nine-number breakpoint assertion verbatim from §10.1 (the grep is already written in the doc — make it the test), and fix story.css:582 to 480 or 600 so the gate lands green. Zero standing cost: it's an offline test in the existing suite (ADR-103: CI gates are load-bearing).
   - *Regression guard:* tests/test_css_tokens.py already runs the script in CI — the extended checks are guarded the day they merge; prove each new check catches a planted violation first (the vacuous-scan trap from the 07-13 session).
1. **[med/S/class-B]** The fs-ok sanction comment 'SVG viewBox units' is the structural loophole that produced finding 1: the gate's own exemption category is exactly the render-scale-dependent text class that #1017 proved unsafe, so every current and future SVG label is waved through the type-floor check by design.
   - *Evidence:* check_css_tokens.py:73 (sanctioned = 'fs-ok:' in line) + evidence.css:596/670 and tokens.css annotations '/* fs-ok: SVG viewBox units */' — the sanctioned lines are the same selectors measured below-floor live (finding 1); commit 80c17630 documents the failure mode ('sizes its text in viewBox units, so on-screen size scales with rendered width').
   - *Path-to-A step:* Re-scope the sanction: 'SVG viewBox units' alone is no longer an accepted fs-ok reason — an SVG text either uses the shared floor-scaling helper (finding 1) or documents why its minimum rendered scale keeps it >=11px; the visual_qa effective-px audit becomes the arbiter instead of the comment.
   - *Regression guard:* The finding-1 visual_qa SVG-text assertion — it measures the rendered result, so no comment-level sanction can hide a sub-floor label again.

**Refuted (not defects — kept for the record):**
- ~~Dead CSS carries a dormant kill-on-sight violation: .pl-lost .pl-tag colors a lost prediction RED (#dc2626) plus green/amber siblings, and four more off-palette status selector families (.cr-confirmed #22c55e, .sc-confirmed #5a8f6b, .hb7-good/mid greens) have no renderer anywhere in site/, lambdas/, or scripts/ — the moment someone wires a prediction-ledger renderer to these classes, red-for-outcome ships silently.~~ — *The central 'dormant / no renderer anywhere' claim is a grep-methodology error: the classes are template-constructed, invisible to a literal full-class-name grep. dispatches.js:210 builds `pl-${esc(b.outcome)}` inside the panel-ledger renderer — the exact 'prediction-ledger renderer' the finding says doesn't exist, so .pl-lost red (#dc2626) is LIVE-wired today, not dormant; coaching.js:142 builds `cr-${esc(r.status)}` (→ cr-confirmed) and coaching.js:1026 builds `sc-${esc(st)}` (→ sc-confirmed). The finding's no-consumer list even includes dx-item--lead, which its own sibling finding 1 correctly shows consumed at coaching.js:1088 — an internal contradiction. Only the hb7-good/hb7-mid family is genuinely unconsumed (zero hits for 'hb7' outside CSS across site/, lambdas/, scripts/). The real residual issue — red-for-lost is live-rendered the moment a lost bet appears in ledger data, violating §4 'never red' — is a different, worse finding than the one filed and folds into finding 1's live off-palette class; as filed (dead CSS, dormant risk, ships-when-someone-wires), this is wrong.*

**Path to A:**
1. Ship the shared SVG-text floor-scaling helper (the #1017 sizeConstellation pattern generalized in charts.js) and apply it to the autonomic 2x2, the pillar radar, the tier emblem, and the body-map captions; add the effective-px SVG-text audit to tests/visual_qa.py so the floor is measured, not asserted by comment.
1. Retokenize the two LIVE off-palette accents: head-coach lead markers -> var(--coach)/ember (story.css:639-654), vice-hold green -> the §4 ember/muted-ink grammar (evidence.css:240-250).
1. Extend scripts/check_css_tokens.py to sweep all 8 sheets and police raw hex colors + the §10.1 nine-number breakpoint set (the doc already contains the exact grep — promote it to the test), fixing story.css:582's rogue 520px in the same PR.
1. Delete the five dead off-palette status selector families (.pl-*, .cr-confirmed, .sc-confirmed, .hb7-*) before any renderer can adopt the dormant red-for-lost encoding.
1. Re-scope the fs-ok sanction policy so 'SVG viewBox units' is no longer a self-service exemption from the type floor.

**Lens notes (coverage):** Inspected: DESIGN_SYSTEM_V5.md in full; all 8 site/assets/css sheets (grep sweeps for raw hex, raw px spacing, breakpoints, glow); scripts/check_css_tokens.py + its test; the #1017 fix commit 80c17630; live renders (Playwright, read-only) of /cockpit/, /coaching/, /protocols/, /data/vitals/, /data/character/, /data/mind/ at 390x844 and 1280x900 with measured SVG-text effective sizes; live APIs (/api/vice_streaks, experiments, challenges, supplements, predictions); door-chrome consistency via curl (page-hero + loop-ribbon + lr-here present on all 5 doors + the cockpit variant; home carries the loop diagram — consistent). Day-4 empty states are genuinely DESIGNED, not accidental: coaching leads with 'DAY 4 · WEEK 1' + 'THE RECORD 62 falsifiable calls · none decided yet — graded daily as windows close'; the cockpit consistency ring reads 11 with the dashed not-yet-instrumented track (#747); discoveries' empty copy says 'this cycle is only days old'; protocols carries the #978 pre-start framing — no defect filed there. The huge blank mid-page stretches in my full-page coaching/protocols screenshots were verified to be the known scroll-reveal screenshot artifact (content renders fully after scrolling; matches the memory note), NOT a live defect. Glow discipline checks out: every box-shadow/pulse site-wide is annotated earned (cockpit sync strip #406/#589, tokens.css:1488/1501) and I found zero decorative glow. Spacing-token adoption is excellent (560 var(--sp-) uses vs one 6px literal). Vice names on the public API are benign and server-filtered (_is_blocked_vice, site_api_common.py:384) — no sensitive-content violation. NOT covered: light/dark AA contrast measurement; /story/ subpages, /gear/, /subscribe, /method/ renders; the 44px tap-target audit rerun; performance budgets; OG cards (known PR #1193). Unverified suspicions (not filed): (1) mind.css .sp-face book-spine gradient to hardcoded #1d1810 may read heavy in light mode — plausibly intentional 'dark spine' art, unswept by the gate either way (folded into finding 4); (2) the cockpit intro modal's 'shown once' footnote appeared partially occluded behind the mobile app-bar at load in my 390px shot — likely scrollable, didn't confirm; (3) /protocols/ supplements hero shows as-of 2026-06-26 (~20 days) — honest provenance display working as designed, but the staleness itself belongs to the accuracy lens. Data-maturity caveat: current-cycle surfaces carry ~4 days of data by design; nothing sparse was graded as a defect.

### devex — Build & ship ergonomics (AI-agent development) — **A-**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every load-bearing claim an agent reads in its first 5 minutes (CLAUDE.md, CONVENTIONS, the /deploy skill) is either machine-synced or provably current; all deploy paths verifiably stage through one bundle; the fast deploy-critical lane runs green offline in seconds; incident classes are encoded in gates/agents rather than relying on agent memory; drift gates cover process/ownership/date facts, not just counts.
- **C:** The one-bundle rule and CI gates exist and mostly work, but agent-facing docs carry multiple contradictions an agent must resolve by reading source (stale ship paths, wrong stack owners, dead command signatures), the doc-sync gates cover only what they were hand-taught, and a fresh agent's first deploy requires tribal knowledge from memory topics to avoid a known incident class.
- **F:** Deploy paths diverge (some functions ship partial bundles), CLAUDE.md commands fail or silently do the wrong thing, CI pins drift from local tooling so local-green ≠ CI-green, and the handover/memory system has rotted into an unindexed pile — a fresh agent's first change is more likely to regress production than to land.

**Confirmed findings:**

1. **[med/S/class-A]** CLAUDE.md's restart-pipeline section states the experiment anchor as 'currently 2026-07-12, cycle 5' while lambdas/constants.py says EXPERIMENT_START_DATE=2026-07-13 (cycle 6) — CLAUDE.md self-contradicts (its session-status block says cycle 6, genesis 2026-07-13), 3 days after a reset whose pipeline claims to sync docs; neither sync_doc_metadata RULES, check_doc_facts, nor restart_docs_update.py targets this line (grep for EXPERIMENT_START_DATE/genesis in both gate scripts returned nothing).
   - *Evidence:* CLAUDE.md:145 ('currently **2026-07-12**, cycle 5') vs lambdas/constants.py:13 (EXPERIMENT_START_DATE = "2026-07-13"); `grep -n 'EXPERIMENT_START_DATE\|genesis' deploy/sync_doc_metadata.py scripts/check_doc_facts.py` → empty; deploy/restart_docs_update.py targets docs/ pages only, not CLAUDE.md
   - *Path-to-A step:* Add a genesis_date/cycle fact to check_doc_facts.py (ground truth imported from lambdas/constants.py + SSM cycle mirror in config) and make CLAUDE.md:145 a synced literal via a sync_doc_metadata RULE, so every restart_pipeline run (which already runs the sync) rewrites it — zero new standing machinery, reuses the existing gate (ADR-103: extends a load-bearing gate).
   - *Regression guard:* check_doc_facts.py fails CI on a stale genesis date in any live doc — prove it by planting '2026-07-12' in a scratch copy per the #1189 vacuous-scan lesson (test lives next to the gate's existing planted-violation tests).
1. **[med/S/class-A]** The /deploy skill's site mode is a full pipeline stale: it says the primary site path is 'merge to main — CI deploys the site' via ci-cd.yml with a 'manual production approval' gate (citing #393), but since #750 site pushes deploy via .github/workflows/site-deploy.yml which deliberately has NO approval gate — a fresh agent invoking /deploy site would wait on (or ask Matthew for) an approval that never comes, and would watch the wrong workflow for the QA/rollback verdict.
   - *Evidence:* .claude/commands/deploy.md:10-14 ('Primary path (since #393) … manual `production` approval … deploy_site.sh') vs .github/workflows/site-deploy.yml:9-10 ('ci-cd.yml's deploy job sits behind environment: production (manual approval). A site deploy must NOT wait on that gate') and docs/CONVENTIONS.md:65-74 ('deliberately NO production-approval gate on that workflow')
   - *Path-to-A step:* Rewrite deploy.md Mode 1 as a one-line pointer to CONVENTIONS §2 (the page's own meta-rule: a rule stated differently elsewhere is stale — fix it to a pointer), and add a tombstone rule for the retired claim (e.g. 'since #393.*CI deploys the site') — #750 retired the approval-gated site path but never added its tombstone, violating CONVENTIONS §8's 'retiring something = adding its tombstone rule in the same PR'.
   - *Regression guard:* scripts/check_doc_tombstones.py already scans .claude/commands/*.md (confirmed at its lines 14-16) — the new tombstones.txt rule makes any resurrection of the #393-era claim red CI.
1. **[med/S/class-A]** The /deploy skill's site-api special case names the wrong stack owner — 'CDK — LifePlatformOperational — owns the function's infra' — but #793 moved site-api/site-api-ai to serve_stack.py (LifePlatformServe); this is the exact doc CLAUDE.md delegates to for ownership ('ownership rules per #794 — see .claude/commands/deploy.md'), so the canonical pointer chain terminates on a stale fact and an agent doing infra work would cdk-deploy the wrong stack.
   - *Evidence:* .claude/commands/deploy.md:58-59 ('#794 ownership: CDK — LifePlatformOperational — owns the function's infra') vs cdk/stacks/serve_stack.py:46 ('site-api + site-api-ai (functions, Function URLs, roles, alarms, outputs) now…') and cdk/stacks/operational_stack.py:572 ('site-api / site-api-ai moved to serve_stack.py — #793, cdk refactor 2026-07-08'); CLAUDE.md Serve/Compute section points at deploy.md for the ownership rules
   - *Path-to-A step:* Correct deploy.md to LifePlatformServe and add a tombstone rule 'LifePlatformOperational.*site-api' (the #793 refactor retired that ownership but added no tombstone — same missed reflex as the #750 case).
   - *Regression guard:* The tombstones gate (already scanning .claude/commands/) reds CI if any live page re-asserts Operational ownership of site-api.
1. **[low/S/class-A]** CLAUDE.md's Commands section documents 'bash deploy/deploy_lambda.sh <function-name>' and 'bash deploy/deploy_and_verify.sh <function-name>' but both scripts hard-require a second <source-file> argument and exit 1 with a usage error — a fresh agent copy-pasting the canonical quickstart command gets an immediate failure (loud, self-correcting, but it is a wrong command in the single most-read file).
   - *Evidence:* CLAUDE.md Commands block ('bash deploy/deploy_lambda.sh <function-name>') vs deploy/deploy_lambda.sh:35-39 ('if [ $# -lt 2 ]; then echo "Usage: $0 <function-name> <source-file>"; exit 1'); deploy/deploy_and_verify.sh:9 shows the same two-arg usage
   - *Path-to-A step:* Fix the two CLAUDE.md command lines to include <source-file> (matching deploy.md's mapping-table usage), in the same pass as the genesis-line fix.
   - *Regression guard:* Add a doc-facts rule (or a tiny test in tests/) that greps live docs for 'deploy_lambda.sh <function-name>`' without a following <source-file> token — same precision-first pattern as the existing grep-count rule for MCP tools.
1. **[low/S/class-B]** One memory topic file is orphaned — project_nutrition_24h_lag.md exists in the memory directory but is referenced from neither MEMORY.md nor the shipped archive index, so no future agent can discover whatever incident/lesson it holds; the rest of the index is clean (0 broken links, 0 broken wikilinks, 1 orphan of ~140 files).
   - *Evidence:* Ran `for f in *.md; do grep -q "$f" MEMORY.md project_shipped_archive.md || echo ORPHAN: $f; done` in /Users/matthewwalker/.claude/projects/-Users-matthewwalker-Documents-Claude-life-platform/memory/ → 'ORPHAN … project_nutrition_24h_lag.md'; the reverse link check found zero broken links
   - *Path-to-A step:* Re-index the orphan (one line in MEMORY.md or the shipped archive) and add the two-line orphan/broken-link check to the /wrap skill's memory-hygiene step so index drift is caught at every session close instead of by an annual audit.
   - *Regression guard:* The wrap-skill step: wrap fails its checklist if the orphan check emits any line (process gate — the memory dir lives outside the repo so repo CI cannot own it).

**Refuted (not defects — kept for the record):**
- ~~The shared main checkout is parked on a leftover feature branch (feat/essay-og-card) with a dirty .claude/settings.local.json — content is identical to origin/main today (0 commits either direction), so it is harmless now, but it is exactly the precondition for the CONVENTIONS §2 deploy-from-wrong-tree class the moment main advances, and the worktree-implementer agent contract assumes the main tree stays on main.~~ — *The cited precondition no longer exists: in the current repo state `git branch --show-current` returns 'main' and `git worktree list` shows the shared tree on main at b6408f39, 0 commits ahead/behind origin/main. The feat/essay-og-card parking captured in the finder's status snapshot has already been resolved (the branch's PR #1193 remains open, but the main tree is back on main). Only the routine dirty .claude/settings.local.json remains — local settings noise, not the CONVENTIONS §2 deploy-from-wrong-tree class. Nothing to fix.*

**Path to A:**
1. Make the experiment anchor machine-synced everywhere it appears: add CLAUDE.md:145 to sync_doc_metadata RULES (source: lambdas/constants.py) and a genesis_date fact to check_doc_facts.py, with a planted-violation test — kills the class where the restart pipeline updates code+docs/ but the top agent brief silently keeps the old cycle.
1. One pass over .claude/commands/deploy.md: rewrite Mode 1 (site) as a one-line pointer to CONVENTIONS §2, correct LifePlatformOperational→LifePlatformServe, fix the CLAUDE.md command signatures (<source-file> args) — the meta-rule already mandates pointers over restatements; this doc just predates it.
1. Close the tombstone-reflex gap for non-count retirements: #750 (approval-gated site path) and #793 (Operational owning site-api) each retired a concept without adding its tombstone rule — add both rules now, and add a 'ship-path or ownership changed? → tombstone rule in this PR' line to the PR template's existing Docs-impact checklist (zero standing cost, extends the CONVENTIONS §8 contract already in force).
1. Add memory-index hygiene (orphan + broken-link one-liner) and 'main tree back on main' to the /wrap skill checklist — both are two-line process gates on machinery that already exists.
1. No new standing infrastructure is needed: all four deploy paths verifiably stage through build_bundle.py, CI pins match requirements-dev.txt, the gate ordering doc matches ci-cd.yml, and the deploy-critical lane runs green offline in 3.79s — the remaining gap is purely that drift gates were taught counts but not process/ownership/date facts.

**Lens notes (coverage):** INSPECTED: (1) CLAUDE.md spot-checks — one-bundle claim (verified: build_bundle.py referenced by deploy_lambda.sh:100, deploy_fleet.sh:35-36, deploy_site_api.sh:21, cdk lambda_helpers.py:66-75 + mcp_stack.py:71-74); MCP tool count (AST discoverer returned 64 ✓); lambda count (94 ✓); 9 CDK stacks (9 stack files + 4 helpers ✓); genesis date (STALE — finding 1); command signatures (STALE — finding 4). (2) CONVENTIONS §4 vs ci-cd.yml: gate order flake8→black→ruff→mypy→py_compile→lambda_map→content-policy→doc-drift→wiki-gates matches; if:always() present on every post-flake8 gate; pins MATCH (black 25.9.0, ruff 0.14.14, mypy 2.1.0 in both CI and requirements-dev.txt; aws-cdk@2.1129.0 CLI pin, aws-cdk-lib 2.261.0 in both cdk/requirements.txt and requirements-dev.txt) — the documented drift risk is currently zero. (3) Doc-sync machinery: sync_doc_metadata --check PASSED; check_doc_facts PASSED (its auto-discovered ground truths printed sane: 64 tools, 94 lambdas, 3646 tests, ADR max 135) — what drifts anyway is the non-count class: dates, workflow names, stack ownership (findings 1-3); both gates DO scan .claude/commands/*.md so the gap is rule coverage, not scan scope. (4) visual-qa two-copy sync (§4b 'change one, change both'): both copies run identical sweep + accuracy-gate commands — in sync. (5) .claude/agents: worktree-implementer and finding-verifier are exemplary — every memory incident class (case-twin, stash races, doc-sync literals, push-before-merge, black-on-JSON) is encoded as a numbered non-negotiable, so the system does NOT rely on agents reading memory topics. (6) Handovers: 107 top-level + 345 archived, 3.2MB total, HANDOVER_LATEST.md present — growth is by design and trivial at this scale; no pruning trigger exists but none is needed yet. (7) Time-to-first-safe-change: deploy-critical lane green offline with FAKE creds in 3.79s (1296 passed) — a fresh agent can validate a change in under 5 seconds, which is genuinely excellent; the things it would get WRONG are exactly findings 1-4 (wrong cycle in its mental model, waiting on a phantom site-deploy approval, deploying the wrong stack for site-api infra, a failing quickstart command). Checked 13b resolved inventory + the do-not-refile list — no overlaps (the stale /method/ builder stats are already queued per shared context; not refiled). NOT COVERED: smoke_test_site.sh internals, the remediation-agent workflow, live AWS parity (zero AWS calls made — this review was entirely local+git per read-only economy), the reconcile job's generator whitelist correctness, docs-ci.yml, the full 5106-test suite (ran the critical lane only). UNVERIFIED SUSPICIONS: CONVENTIONS.md 'Verified: 2026-07-10' header predates its own §8 edits from #1189 (2026-07-13) — cosmetic freshness-stamp lag; deploy.md's 50-line function→file mapping table is hand-maintained and I did not verify all ~55 rows against ci/lambda_map.json (the two could drift; a sync check may already exist in test_lambda_map_imports.py — unconfirmed). DATA-MATURITY: not applicable to this lens; nothing here depends on cycle-6 data thinness.

### principal — Code health — **A-**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Full offline suite green and fast with creds blanked; every stated quality gate (coverage floor, mypy tier set, deploy-critical lane, format gates) matches measured reality within a small tolerance; no unreferenced code shipping in the every-bundle shared modules; exactly one canonical implementation per platform invariant (float→Decimal, Pacific-date, ISO parse); golden tests pinned against the known wall-clock defect class.
- **C:** Suite green but the gates are partly decorative — coverage floor drifted far below actual so backsliding is possible, gate comments/config contradict enforcement status, hundreds of dead lines ship in every Lambda bundle, and invariant helpers are forked with divergent edge-case semantics.
- **F:** Red main tolerated or gates disabled to get green; tests mirror implementation so defects cannot fail them; no coverage or type gate at all; wall-clock time bombs in fixtures with no encoded lesson.

**Confirmed findings:**

1. **[med/S/class-B]** The coverage floor (25%) has drifted ~20 points below measured coverage (45.58%), so nearly half the suite's line coverage could vanish before CI notices — and the gate's own comment ('offline line-coverage baseline is ~28%, #419 ratchet') is itself stale by 17+ points, the exact drift class PR #1189 just built guardrails against.
   - *Evidence:* .github/workflows/ci-cd.yml:593-609 (--cov-fail-under=25, comment 'baseline is ~28%') vs gh run view 29270686464 log: 'Required test coverage of 25% reached. Total coverage: 45.58%' (green main run 2026-07-13).
   - *Path-to-A step:* Ratchet --cov-fail-under to 40, correct the ~28% comment to the measured 45.6%, and add a one-line step to the existing coverage gate that warns when (measured − floor) > 10pts so the ratchet is self-reminding — no new standing machinery (extends the existing ADR-080 gate).
   - *Regression guard:* A CI step in the existing coverage job that parses coverage.xml's line-rate and emits a ::warning:: when the floor lags measured coverage by >10 points.
1. **[med/M/class-A]** floats→Decimal conversion is re-implemented ~10 times outside the canonical lambdas/numeric.py with divergent NaN/Inf semantics: character_engine maps NaN/Inf→None, but the canonical floats_to_decimal and copies like scenario_explorer_lambda pass NaN through as Decimal('NaN'), which DynamoDB rejects at write — the R13-F10 'full dedup deferred' never completed and the copies have since diverged.
   - *Evidence:* grep 'def _to_decimal' → 10 hits (lambdas/character_engine.py:2003 NaN→None; lambdas/compute/scenario_explorer_lambda.py:82 no NaN guard; lambdas/ingestion/measurements_ingestion_lambda.py:62; hevy_common.py:216; coach/*, mcp/tools_lifestyle.py:216, …) vs lambdas/numeric.py:25 floats_to_decimal (no NaN guard) whose docstring claims the 8 local copies became 're-export shims'.
   - *Path-to-A step:* Add NaN/Inf→None handling to numeric.floats_to_decimal (adopting character_engine's battle-tested behavior), point the local copies at it, and delete the forks — zero standing cost since #781 ships numeric.py in every bundle.
   - *Regression guard:* A structural test in the test_ddb_patterns.py style asserting no `def _to_decimal`/`def _decimalize` definition exists outside lambdas/numeric.py.
1. **[low/S/class-A]** 269 lines of verified-dead code (6 unreferenced functions: write_action, check_action_completion, build_action_history_for_prompt, compute_all_credibility, summarize_coach_month, read_thread_summaries) ship in every Lambda bundle via intelligence_common.py, plus 2 dead public fetchers in character_engine.py referenced only by an archived backfill script.
   - *Evidence:* AST scan + repo-wide grep: zero references to the 6 functions anywhere in lambdas/ mcp/ tests/ deploy/ scripts/ site/; lambdas/intelligence_common.py:797-1052,1750-1881; character_engine.py:1958-2001 fetch_character_sheet[_range] only in backfill/archive/retrocompute_character_sheet.py:44.
   - *Path-to-A step:* Delete the 6 intelligence_common functions and the 2 character_engine fetchers (git history preserves them; the only consumer is already archived) — shrinks the every-bundle shared module by ~14%.
   - *Regression guard:* Full suite re-run (5108 tests) proves nothing consumed them; optionally a dead-symbol pass folded into the existing docs-ci cadence rather than a new gate (ADR-103: no new standing machinery).
1. **[low/M/class-A]** site_api_data.py is a 4,184-line grab-bag of ~45 handlers spanning 8+ domains (glucose/sleep/phenoage/labs belong with site_api_vitals; correlations/forecast/scenarios with site_api_intelligence), with two 290-350-line functions and a module docstring whose endpoint list omits ~15 of the handlers it now contains.
   - *Evidence:* wc -l lambdas/web/site_api_data.py = 4184 (largest file in the repo); AST: handle_habits 349 lines (l.1694), handle_observatory_week 293 lines (l.3527); docstring l.1-18 lists ~20 endpoints but the module defines 45 handle_* functions (phenoage, circadian, forecast, state_of_matthew, inference_receipt, survival, wrong… all absent from the list).
   - *Path-to-A step:* Finish the P1.1 Phase B decomposition it started: move the vitals-adjacent and intelligence-adjacent handler clusters to their existing sibling modules (routing table in site_api_lambda.py already imports per-module, so moves are mechanical) and regenerate the docstring endpoint list from the routed handlers.
   - *Regression guard:* The existing routing import block in site_api_lambda.py plus test_wiring_coverage.py-style assertion that the module docstring's endpoint list matches its routed handle_* set.
1. **[low/S/class-A]** mypy's enforcement status contradicts itself across config: mypy.ini and requirements-dev.txt both say 'advisory / non-blocking' while ci-cd.yml runs it as an ENFORCED blocking gate on the 19-module clean set — a small doc-truth crack in the platform whose stated moat is honesty and which just shipped a doc-drift guardrails pass.
   - *Evidence:* mypy.ini:2 'Advisory mypy — non-blocking' and requirements-dev.txt:23 '# Type checking (advisory …)' vs .github/workflows/ci-cd.yml:297 'Mypy gate (ENFORCED — clean-module set, ADR-080)' (verified blocking: no `|| true`, if: always() reports independently). The module lists themselves do NOT drift — CI's 19 modules exactly match tests/test_mypy_clean_modules.py (scripted diff: 0/0).
   - *Path-to-A step:* One-line comment fixes: 'Enforced on the clean-module set (tests/test_mypy_clean_modules.py); advisory elsewhere' in both files.
   - *Regression guard:* A scripts/check_doc_facts.py rule for gate-status words ('advisory'/'non-blocking') on tools that ci-cd.yml marks ENFORCED — fits the existing generalized stale-fact gate.

**Path to A:**
1. Ratchet the coverage floor 25→40 in ci-cd.yml, fix the stale '~28% baseline' comment (measured 45.58% on green main run 29270686464), and add a slack-warning line to the existing gate (S, extends ADR-080 — no new machinery).
1. Consolidate the ~10 divergent _to_decimal forks onto lambdas/numeric.floats_to_decimal, adding character_engine's NaN/Inf→None guard to the canonical, with a structural test banning new local definitions (M, closes the deferred R13-F10 for real).
1. Delete the 269 verified-dead lines in intelligence_common.py and the 2 archive-only character_engine fetchers (S, shrinks the every-bundle module ~14%).
1. Move site_api_data.py's vitals-cluster and intelligence-cluster handlers to their existing sibling modules and regenerate its stale docstring endpoint list (M, finishes the decomposition the module itself documents).
1. Fix the advisory-vs-ENFORCED mypy comment contradiction in mypy.ini:2 and requirements-dev.txt:23, and add a gate-status rule to check_doc_facts.py (S).

**Lens notes (coverage):** Inspected: full offline suite run locally with creds blanked (5108 passed / 76 skipped / 10 xfailed / 0 failed, 51.9s — genuinely green and fast); skip reasons audited (all benign: creds-gated integration tests + structural no-op skips); the deploy-critical lane exercised (1296 tests in 3.7s — substantial, not decorative); coverage measured from the latest green main CI run's log since pytest-cov isn't installed locally and I wouldn't mutate the environment to install it; mypy tier-1+2 config verified three ways (mypy.ini, CI gate, tests/test_mypy_clean_modules.py — the 19-module lists match exactly, zero drift); CI job graph mapped (lint→test-critical→plan→deploy with the full `test` job deliberately non-gating per ADR-117 — documented tradeoff, not filed; #749 if:always() independence verified in-file on every lint gate; tool pins black/ruff/mypy match requirements-dev.txt exactly); dead-symbol AST scan over the 5 largest modules; wall-clock scan across all 348 test files — the known golden-test time-bomb class is properly remediated (test_daily_brief_golden.py pins journey_start_date to 2024 with an explanatory comment encoding the exact past defect; the 95 unmocked now() uses are the SAFE now-relative pattern, dynamic dates feeding now-relative logic); test quality sampled (test_character_engine.py, test_now_remainder_batch.py — behavior-asserting with finding-ID rationale comments, not implementation mirrors); section 13b resolved-findings inventory checked (my decimal finding extends R13-F10's explicitly-deferred dedup, not a re-report). NOT covered: mcp/ module health beyond registry/wiring checks, cdk/ TypeScript-side code health, flake detection (single suite run), mutation-level assertion strength, the creds-gated integration suite (by design). Data-maturity caveats: none — this lens is cycle-independent. Unverified suspicions: (1) local dev runs Python 3.14 vs CI/runtime 3.12 — suite passes on both but I didn't hunt for 3.14-masked discrepancies; (2) numeric.py's docstring claims the 8 pre-consolidation copies became re-export shims, but the copies I read are full implementations — either the shims regressed or new copies accreted post-Phase-4.2; (3) whether a NaN can actually reach scenario_explorer's DDB write path is plausible (quantile math over degenerate series) but unproven.

### security — Exposure — privacy absolutes on the live surface, repo secret/PII hygiene, auth surfaces (MCP OAuth, site-api write rate-limits), S3 bucket policy, IAM posture — **A-**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Live public JSON exposes zero chronological age / birth year / genome identifiers / vice-substance names (verified by curl+grep, not docs); sensitive S3 prefixes (raw/config/uploads/dashboard) return 403/404 publicly; no live secret or PII in tracked repo files or recent history; the MCP OAuth consent+PKCE gate and DDB-backed per-IP rate limits are provably enforced with no spoofable trust boundary; every Lambda has its own least-privilege role with Resource:* only on actions that cannot be resource-scoped.
- **C:** Privacy absolutes hold on the sampled live endpoints but one enforcement boundary is bypassable (e.g. spoofable client-IP so rate limits don't actually bind) or a known-inversion residual is unmitigated, OR an over-broad IAM wildcard / publicly readable sensitive prefix exists without a compensating control.
- **F:** Any live public endpoint returns chronological age, a genome identifier, or a named vice/substance; a live credential is committed to the repo; /token or /authorize mints a bearer without consent; or raw health data is publicly fetchable.

**Confirmed findings:**

1. **[med/S/class-A]** Per-IP rate limiting is bypassable: client-IP is taken from the FIRST X-Forwarded-For entry, but CloudFront appends the trusted viewer IP at the END, so the first entry is fully attacker-controlled — rotating a spoofed value per request yields a fresh rate-limit bucket every time. Systemic across every IP-gated write (subscribe, votes, follows, nudges, checkins, board_ask).
   - *Evidence:* lambdas/web/site_api_social.py:82 (`xff = (headers.get("x-forwarded-for") or "").split(",")[0].strip()`) consumed by _extract_client_ip at 13 call sites (297,356,614,692,901,980,1212,1375,1429,1525,1624); identical pattern lambdas/web/email_subscriber_lambda.py:508. AWS CloudFront appends the observed viewer IP to the END of X-Forwarded-For, so [0] is client-supplied. WAF was removed 2026-06 (CLAUDE.md), so no upstream sanitization.
   - *Path-to-A step:* Take the LAST X-Forwarded-For entry (the CloudFront-appended, edge-observed IP) in a single shared helper used by both site_api_social and email_subscriber; keep sourceIp as the only fallback.
   - *Regression guard:* Unit test on the IP-extraction helper asserting that for header 'evil-spoof, 203.0.113.9' it returns 203.0.113.9 (the last hop), not 'evil-spoof'.

**Refuted (not defects — kept for the record):**
- ~~PhenoAge inversion residual (known, flagged PHY-01): /api/phenoage publishes the phenotypic age plus all 9 Levine driver values at full precision, and the same 9 markers are on /api/labs; the Levine formula is public and linear in chronological age (coef 0.0804), so a reader can algebraically approximate Matt's true age — the exact value the DEXA path deliberately omits.~~ — *The technical claim reproduces (curl /api/phenoage returns phenoage:28 + all 9 driver values; site_api_data.py:3202 uses +0.0804*age_years and the Levine formula is algebraically invertible). However this is an already-documented, owner-accepted residual — the code docstring at lines 3157-3159 explicitly states the exact concern: 'the 9 markers are public on the labs page, so a determined reader applying this formula could approximate age from a precise phenotypic number — flagged for review.' The finder itself labels it 'known, flagged PHY-01' and cites project_phenoage_privacy.md. Per the rubric a finding duplicating an already-tracked/documented item is REFUTED as new work. Additionally the returned phenoage is rounded to an integer (approximate recovery only), and Option A is a deliberate documented privacy decision, not a defect.*

**Path to A:**
1. Fix client-IP extraction to use the CloudFront-appended last X-Forwarded-For hop in a shared helper across site_api_social + email_subscriber, with a unit test — this is the one enforcement boundary currently spoofable, and closing it makes the DDB-backed rate limits actually bind.
1. Apply the PHY-01 mitigation (band published PhenoAge to nearest 5 and/or drivers direction-only) to close the age-inversion residual and make the chronological-age absolute truly non-deducible.
1. Add a standing privacy-regression test that curls the live public JSON endpoints and greps for chronological age / birth year / genome IDs / substance names, so any future field addition that leaks an absolute reds the build.

**Lens notes (coverage):** INSPECTED (all reproduced, not from docs): (1) Live public JSON — curled /generated/public_stats.json (root path 200; /generated/public_stats.json 404), /api/character, /api/labs, /api/vitals, /api/phenoage, /api/coaches, /api/observatory_week, /api/vacation_fund and grepped each for chronological/birth/genome/rs####/marijuana/cannabis/porn/alcohol — ZERO hits except benign 'data_coverage' substrings; DEXA/observatory code (site_api_observatory.py:1868) explicitly omits chronological_age + biological_age_delta; phenoage docstring/code confirm chronological age is compute-only. (2) Repo secret hygiene — git grep for AKIA/BEGIN PRIVATE/xoxb/sk-ant/ghp_/AIza across tracked files: only one hit, a documented public Firebase web key (DECISIONS.md, by-design embeddable); secret-setup scripts under archive/ read values from env vars, no hardcoded values; no stray PII emails in lambdas/mcp/web source; scanned last 30 commit subjects. (3) Auth — read the full MCP #893 fix: /authorize now requires an HMAC-derived passcode or a signed HttpOnly/Secure/SameSite cookie (no auto-approve), redirect_uri allowlisted (exact host or subdomain, https-only except loopback), /token exchanges only server-minted single-use PKCE(S256)-bound codes, all comparisons via hmac.compare_digest, fail-closed with no API key, auth-failure metric emitted — the R22 fix HELD. rate_limiter.py is genuinely DDB-backed (atomic ADD update_item + TTL) and fail-CLOSED on the expensive AI paths. (4) S3 — bucket policy public-reads only site/blog/generated; raw/config/uploads/dashboard NOT public (verified profile.json + clinical.json + raw whoop json all 403 via direct S3 / 404 via CloudFront); DeleteObject Deny on raw/config/uploads/generated; BucketOwnerEnforced + BlockPublicAcls on. (5) IAM — 104 distinct per-function roles (one-role-per-lambda confirmed); sampled the MCP role and a Compute role: every wildcard Resource:* is on xray:PutTraceSegments/PutTelemetryRecords + cloudwatch:PutMetricData, which do not support resource-level scoping — proper least-privilege. NOT COVERED / caveats: did not send spoofed X-Forwarded-For requests to LIVE-prove the rate-limit bypass (would be an abuse mutation, out of the read-only contract) — the finding rests on the code taking [0] + AWS-documented CloudFront append-at-end behavior, so it is code-reproduced and design-analytic, not black-box exploited. Did not enumerate all ~64 MCP tool payloads for PII leakage (auth surface only). Did not scan the full git history commit-by-commit for historically-committed-then-removed secrets (spot-checked recent 30 + broad tracked-file grep only) — the repo history WAS public, so a deep historical secret-scan (e.g. trufflehog over all refs) is the one residual verification I could not complete in-window; no evidence of a leak, but absence-of-proof. Note (not filed): generated/ is directly readable via the S3 REST endpoint bypassing CloudFront (public_stats.json 200) — but that prefix is already-public content, so no exposure delta. DATA-MATURITY: cycle-6 day-4 thinness (fresh character sheet, sparse current-cycle intelligence) is intentional per the reset and did not affect any exposure judgment — cross-phase labs/DEXA/profile are fully populated and were the actual privacy-inversion surface I tested.

### ai-quality — AI content integrity — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every reader-facing AI narrative passes a deterministic grounding gate with the audience-correct fail mode (fail-closed public, keep-best internal), every prompt carries the phase/day truth, a reset leaves zero wiped-cycle narrative reachable on any live endpoint, and live spot-checks find no number, date, or verdict a Day-4 reader couldn't trace to real data.
- **C:** The grounding harness exists and covers the flagship surfaces, but coverage is convention rather than structure — at least one live surface serves stale, ungated, or mischaracterized AI content and no test or gate would catch it.
- **F:** Models receive raw data and ship unchecked numbers or claims to readers, or a reset/budget state silently narrates wiped or fabricated history as current with no tombstone semantics at all.

**Confirmed findings:**

1. **[high/S/class-A]** /api/state_of_matthew serves the tombstoned pre-genesis cycle-5 brief as current: handle_state_of_matthew takes the latest DATE# record with no tombstone/phase guard, so the wiped 2026-07-12 'Week 1, Day 1' narrative (cycle-5 Day 1, stamped tombstone=true / experiment_restart_2026-07-13) renders live on /coaching/ 'This week' on Day 4 of cycle 6.
   - *Evidence:* lambdas/web/site_api_data.py:2977-2988 (no guard); live curl https://averagejoematt.com/api/state_of_matthew → {tombstone: true, tombstoned_reason: "experiment_restart_2026-07-13", available: true, date: "2026-07-12", narrative: "# State of Matthew — Week 1, Day 1…"}; rendered by site/assets/js/coaching.js:386-391 (checks only som.available && som.narrative); state_of_matthew is EXPERIMENT_SCOPED (lambdas/phase_taxonomy.py:120); the #946 fix guarded get_item readers (site_api_coach.py singleton_visible) — this query reader was missed.
   - *Path-to-A step:* In handle_state_of_matthew, return {available: false} when the latest record carries tombstone=true or a non-current phase (reuse the singleton_visible predicate on the queried item) — the honest empty state coaching.js already handles ('honest-absent until the first Sunday run of the cycle').
   - *Regression guard:* Unit test: latest SOURCE#state_of_matthew record with tombstone=true → handler returns available=False; plus a post-reset smoke assertion (restart_pipeline verify step) that no public /api/* payload contains tombstone:true.
1. **[med/M/class-A]** A number-grounded but semantically false verdict ships to readers: the live mind-expert analysis calls 44% Whoop recovery 'Strong biometric recovery' — 44% is Whoop's yellow band and the payload's own recovery_status vocabulary exists deterministically, but the ADR-104 gate checks digits only, never the adjective/verdict attached to them.
   - *Evidence:* Live curl https://averagejoematt.com/api/ai_analysis → "Strong biometric recovery—44% on Whoop, 34 ms HRV, resting heart rate holding at 62" (44% + HRV 34ms vs his hrv_30d_avg 47.6 per /api/vitals); grounding gate is number-only: lambdas/grounded_generation.py:166-187 (contradiction check covers value mismatches, not band mischaracterization).
   - *Path-to-A step:* Add a deterministic band-adjective contradiction class to grounding_guard/grounding_findings: map recovery/HRV/sleep values to their existing computed band (e.g. recovery <50% = 'yellow/low') and flag superlatives ('strong', 'excellent', 'great recovery') attached to a sub-band value — pure regex + threshold, no new AI, thresholds from personal_baselines per ADR-105.
   - *Regression guard:* Golden-surface eval fixture: a draft stating 'strong recovery — 44%' against facts {recovery_pct: 44} must yield a grounding finding; wire into the existing golden_surface_eval harness (#812).
1. **[med/S/class-A]** handle_forecast and handle_scenarios share the same unguarded latest-DATE# reader pattern as state_of_matthew: for up to a day after a reset (or indefinitely if the compute pauses at budget tier ≥1), a wiped-cycle record serves as current with available:true.
   - *Evidence:* lambdas/web/site_api_data.py:2916-2921 (forecast) and 2949-2954 (scenarios) — latest DATE# record, _INTERNAL strip removes 'phase' rather than filtering on it; both partitions are EXPERIMENT_SCOPED (lambdas/phase_taxonomy.py:117,129). Live /api/forecast is currently clean (recomputed daily) — the exposure is the post-reset window, proven by the sibling state_of_matthew leak.
   - *Path-to-A step:* Apply the same tombstone/phase guard to all three query readers in one pass (shared helper next to singleton_visible), so the fix is the pattern, not the instance.
   - *Regression guard:* Parametrized unit test over every public latest-DATE# reader on an EXPERIMENT_SCOPED partition (enumerate from phase_taxonomy) asserting tombstoned/non-current-phase records return available=False.
1. **[low/M/class-A]** Fabricated calendar dates are structurally invisible to the number gate: the benign set whitelists 0-12 and years 2020-2030, so an invented ISO date like '2026-07-08' tokenizes entirely to benign numbers and passes; proper names are checked nowhere outside the coach quality gate's voice heuristics. The chronicle recap already solves this locally with a date allow-list — it never generalized.
   - *Evidence:* lambdas/grounded_generation.py:52-54 (_BENIGN_NUMBERS includes range(0,13) + years 2020-2031) with numbers_in_text splitting '2026-07-08' into 2026/7/8; the local solution exists at lambdas/emails/wednesday_chronicle_lambda.py:1127 ('The set of dates a beat may legitimately cite').
   - *Path-to-A step:* Generalize the chronicle's date cross-check into grounding_findings: extract ISO/long-form date tokens from output and require each to appear in the input allow-text (dates are strings, not floats — a separate token class), keeping the number gate untouched.
   - *Regression guard:* Unit test: output citing '2026-07-08' with an allow-list built from input lacking that date yields a fabricated_date finding; existing surfaces' fixtures re-run green.

**Refuted (not defects — kept for the record):**
- ~~Eight internal narrative email surfaces call the model over numeric context with no deterministic ADR-104 gate — only prompt-level 'stay grounded' instructions — so a fabricated number in the Monday compass, weekly/monthly digest, nutrition review, weekly plate, partner email, evening nudge, or anomaly email reaches the operator (and partner) unchecked, against ADR-104's 'every AI narrative surface' scope.~~ — *The core claim ('no deterministic ADR-104 gate — only prompt-level instructions') is false. Every listed AI-calling email surface runs its output through validate_ai_output, which per ADR-104 auto-loads canonical facts as health_context (the 'dormant validator woke up' arm — deterministic checks incl. the ±25% metric-deviation check, live at every call site): monday_compass_lambda.py:877 (and it BLOCKS on _val.blocked, replacing output with a fallback), nutrition_review:836, monthly_digest:1044, weekly_digest:2054, weekly_plate:595, partner_email:801, anomaly_detector:977. Additionally: (a) evening_nudge_lambda.py contains NO model call at all (0 call_anthropic/bedrock references) — the finder's 8th surface doesn't exist; (b) the cited evidence monday_compass_lambda.py:1015 doesn't reproduce — the file is 938 lines and the quoted 'grounded in the data already in this context' phrase is absent; (c) not extending the full grounded_generation allow-list gate to internal/lower-stakes surfaces is a recorded deliberate posture (ADR-108 scope row in docs/DECISIONS.md:2989 — 'deliberately NOT extended further', re-open only with a measured failure rate; ADR-104 names its retrofit surfaces explicitly).*

**Path to A:**
1. Guard the three unguarded latest-DATE# public readers (state_of_matthew, forecast, scenarios) with the #946 tombstone/phase predicate and add the post-reset 'no tombstone:true on any public payload' smoke assertion — this closes the one live reader-facing honesty breach (S effort).
1. Add the deterministic band-adjective contradiction class to grounding_guard so 'strong recovery — 44%' fires a finding: value→band mapping from the platform's own computed vocabulary, thresholds from personal baselines (ADR-105), no new AI calls.
1. Generalize the chronicle's date allow-list cross-check into grounding_findings as a fabricated_date token class, killing the invented-date hallucination surface the number gate structurally cannot see.
1. Extend the allow-list gate to the 8 ungated internal email narrators by reusing _ground_legacy_output — pure reuse of the bundled harness, keeping ADR-104's 'every AI narrative surface' claim true rather than aspirational.
1. Make gate coverage structural: a wiring test that every model-calling module imports a grounding gate or sits on an audited exemption ledger, so the next new narrative surface cannot ship ungated by omission (the same reflex test_wiring_coverage.py gives MCP tools).

**Lens notes (coverage):** Inspected: lambdas/grounded_generation.py (full), ai_calls.py (_enforce_quality_gate, _ground_legacy_output, _run_coach_v2_pipeline, CoachHold/AI_UNAVAILABLE semantics), ai_context.py (build/format_experiment_phase_context, EARLY_PHASE_GUARDRAIL_DAYS=14), ai_summaries.py (pure deterministic builders — ADR-105 'compute before narrate' is genuinely honored), site_api_ai_lambda.py gate wiring (ask/why/board_ask all fail-closed with regen), wednesday_chronicle gate + date allow-list, coach_panel_podcast er03 fail-closed gates, state_of_matthew_lambda gate + causal-language check, hypothesis engine (deterministic verdict + CI/n, Haiku narrates only — the ADR-105 exemplar), field_notes SS-10 block-and-regen, character_engine behavioral-absence (=0) semantics, phase_taxonomy classifications, handle_state_of_matthew/forecast/scenarios readers. Live checks: /api/coaches (honest 'track record accruing'), /api/state_of_matthew (the tombstone leak — reproduced), /api/ai_analysis ('strong 44%' — reproduced), /api/vitals, /api/hypotheses (cycle-6 prereg h1/h2 with test_spec/CI/n fields — the pre-registration IS live and grounded), /api/forecast (clean, coverage carries n), /api/correlations (honest count:0), /story/chronicle/ (2 posts correctly labeled Prologue I/II, 'as of 2026-07-13'), public_stats elena_hero_line. Phase-awareness verified live: the mind analysis says 'Four days in' — Day-4 plumbing works end-to-end. NOT covered: MCP tool output integrity, podcast audio content (gate code only), email HTML output (no live email inspected), a live board_ask generation (write endpoint — skipped under read-only + cost discipline), reading/journal-enrichment surfaces, the coach quality-gate lambda's own rubric internals. Data-maturity caveats (not defects): calibration section absent (n=0 post-reset, correctly omitted per sections_available), correlations empty, coach track records accruing — all designed honest-empties; tier-1 pauses (ensemble/chronicle-editor/coherence-semantic) observed and not filed per instruction. Unverified suspicions: elena_hero_line 'Steps nearly doubled to 1,934' not cross-checked against raw step history (doubling from ~1,000 plausible but unproven); daily_brief_lambda.py:2146 computes days_in from server-UTC today rather than PT (unlike ai_context's PT anchor) — no live impact at the 17:00 UTC run time, but a divergence to reconcile opportunistically; the SOM 'Week 1, Day 1' narrative's forecast-resolution numbers (n=12/14) trace to the pre-reset calibration ledger, which is cross-phase by design, so not filed.

### cost — Cost engineering — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** The ADR-063/133 ceiling is enforced at the bedrock_client chokepoint with the ADR-125 audience-ordered ladder, the projection math has no double-count or de-escalation traps, every public and operator-facing cost number (ceiling, monthly total, tier labels) matches the live governor state per ADR-104, and per-feature AI cost is visible on a live public receipt.
- **C:** A hard stop exists somewhere but the tier labels, public cost numbers, and the actual degradation ladder disagree with each other; or the projection has a known over/under-count failure mode; or the operator cannot attribute AI spend per feature.
- **F:** No near-real-time enforcement (AWS Budgets alone with its 24-48h Bedrock lag), reader traffic can spend unbounded through the public AI endpoints, or the public cost claims are fabricated rather than derived from metering.

**Confirmed findings:**

1. **[med/S/class-A]** The public 'inference receipt' — the platform's flagship cost-honesty surface — hardcodes budget_ceiling_usd: 75 and a note saying 'The $75 ceiling covers the WHOLE platform', while the real ceiling is $85 base (ADR-133 amendment 2026-07-08) and $100 right now (surge mode is ACTIVE: SSM /life-platform/budget-breakdown shows ceiling 100.0, recent_uniques 972 >= 900). Even the page's own registry blurb says 'the meter behind the $85 ceiling' while the API returns 75.
   - *Evidence:* curl https://averagejoematt.com/api/inference_receipt -> "budget_ceiling_usd": 75; source lambdas/web/site_api_data.py:4002 ('"budget_ceiling_usd": 75,') and the note string ~4010 ('The $75 ceiling covers the WHOLE platform'); vs aws ssm get-parameter /life-platform/budget-breakdown -> {"ceiling": 100.0, "surge_active": true, "recent_uniques": 972}
   - *Path-to-A step:* The handler already reads SSM for the tier three lines below — read /life-platform/budget-breakdown in the same block and emit its 'ceiling' + 'surge_active' instead of the literal 75; rewrite the note to name the base/$-in-effect pair. No new machinery (reuses the #822 breakdown param; ADR-103 neutral).
   - *Regression guard:* A truth test (tests/, offline with mocked SSM) asserting the receipt payload's budget_ceiling_usd equals the breakdown param's ceiling, plus extending scripts/check_doc_facts.py's budget ground-truth rule to scan lambdas/web/*.py string literals for stale dollar ceilings.
1. **[med/S/class-A]** The tier-change alert emails Matthew receives describe the pre-ADR-125 degradation ladder: cost_governor's _TIER_LABELS say tier 1 = 'heavy coach AI paused (narrative/ensemble/chronicle)' and tier 2 = '+ public website AI paused (/api/ask)', but budget_guard actually pauses coach narrative/chronicle at tier 2 and website AI at tier 3 — so a tier-2 alert claims the public ask endpoint is down when it is not, and omits what actually paused. The module docstring (lines 19-21) carries the same stale ladder.
   - *Evidence:* lambdas/operational/cost_governor_lambda.py:438-443 (_TIER_LABELS) and :19-21 (docstring) vs lambdas/budget_guard.py:64-117 (_FEATURE_CUTOFF: coach_narrative=2, chronicle=2, website_ai=3, ADR-125); _alert() at :446-465 sends _TIER_LABELS[new] via SNS.
   - *Path-to-A step:* Rewrite _TIER_LABELS (and the docstring tier table) to match budget_guard's bands: 1 = internal/dev AI, 2 = + reader narratives (coach/chronicle/state-of-Matthew), 3 = + ask endpoints and brief AI.
   - *Regression guard:* A unit test importing both modules that derives the expected label content from budget_guard._FEATURE_CUTOFF band membership and asserts each _TIER_LABELS entry names the right band (extend tests/test_budget_guard_ladder.py, which already pins the ordering).
1. **[med/S/class-A]** The public cost page's headline number, platform_stats monthly_cost = '~$60', understates the real bill by ~25%: June actual was $79.80 and July projects $82.22 (the governor's own breakdown). The inline comment even justifies it against the retired '$75 cap'. On the page whose blurb is 'the radical-accessibility receipt', the headline dollars are stale.
   - *Evidence:* lambdas/web/site_api_common.py:112 ('"monthly_cost": "~$60"' with comment 'still « $75 cap'); vs aws ce get-cost-and-usage June total $79.80, July MTD $41.90/16d, and SSM budget-breakdown projected 82.22.
   - *Path-to-A step:* Either derive the figure at request time from the persisted budget-breakdown (projected/mtd already in SSM — same source the daily brief uses) or update the literal to '~$80' and pin it to ground truth so it can't silently drift again.
   - *Regression guard:* Add monthly_cost to the sync_doc_metadata/check_doc_facts ground-truth set with the governor's ProjectedMonthlySpend (LifePlatform/Budget metric) as the discoverer, failing when the literal deviates >25% from the trailing projection.
1. **[low/S/class-A]** The tier-3 hard-stop exception message at the chokepoint says 'monthly $75 budget ceiling reached' — stale vs the $85/$100 ADR-133 ceiling; this string surfaces in logs and any caller that renders BudgetExceeded.
   - *Evidence:* lambdas/bedrock_client.py:221: raise BudgetExceeded("AI paused — monthly $75 budget ceiling reached (tier 3). ...")
   - *Path-to-A step:* Drop the dollar figure from the message ('monthly budget ceiling reached (tier 3)') or interpolate the breakdown's ceiling — never a literal.
   - *Regression guard:* Same check_doc_facts.py source-scan rule as finding 1 (stale dollar ceiling in any lambdas/ string literal).
1. **[low/S/class-A]** The governor's cadence documentation is wrong in three places: the module docstring says 'Runs hourly' / 'Schedule: hourly' and budget_guard's cache comment says 'matches the governor's hourly cadence', and the public /method/build editorial says 'projects month-end spend every hour' — but the live EventBridge rule is cron(0 0/8 * * ? *), every 8 hours (CLAUDE.md agrees). Up to ~8h of tier-detection lag is fine (harsh tiers require actual dollars anyway), but the claims should match reality.
   - *Evidence:* aws events list-rules -> LifePlatformOperational-CostGovernorSchedule... ScheduleExpression 'cron(0 0/8 * * ? *)' vs lambdas/operational/cost_governor_lambda.py:24,:47 ('Runs hourly'), lambdas/budget_guard.py:55, and site/method/cost/index.html:49 build editorial ('projects month-end spend every hour').
   - *Path-to-A step:* One sweep replacing 'hourly' with 'every 8h' in the two docstrings and the build editorial (regenerate the registry).
   - *Regression guard:* check_doc_facts.py rule with the EventBridge schedule as ground truth (discoverer: grep the CDK operational stack's cron expression) matching 'hourly|every hour' claims about the governor.
1. **[low/S/class-A]** The chronicle lambda bypasses allow('chronicle') with a raw current_tier() >= 2 read, kept only because of the retired shared-layer deploy constraint — its own comment says 'Revert to allow("chronicle") at the next layer bump', and the layer was retired by #781 (bundles ship everywhere now). The stale header comment above it also says 'at Tier >= 1 skip', contradicting the >=2 code. Behavior is correct today only because the two numbers happen to agree; a future cutoff change in budget_guard would silently not apply here.
   - *Evidence:* lambdas/emails/wednesday_chronicle_lambda.py:2363-2375 — header comment 'at Tier ≥ 1 skip this week's chronicle' vs 'if current_tier() >= 2'; comment 'Revert to allow("chronicle") at the next layer bump' (layer retired 2026-07-06, #781).
   - *Path-to-A step:* Execute the code's own TODO: replace the raw tier read with budget_guard.allow('chronicle') and fix the stale tier-1 header comment, so the cutoff has exactly one home in _FEATURE_CUTOFF.
   - *Regression guard:* tests/test_budget_guard_ladder.py already pins chronicle=2; add an assertion (or grep-gate) that no lambda outside budget_guard/bedrock_client hardcodes a numeric tier comparison for a feature listed in _FEATURE_CUTOFF.

**Path to A:**
1. Make the public inference receipt read the effective ceiling + surge state from the /life-platform/budget-breakdown SSM param instead of the hardcoded 75, and fix its '$75 ceiling' note text (lambdas/web/site_api_data.py:4002; S, reuses existing machinery).
1. Replace the hand-maintained monthly_cost '~$60' in PLATFORM_STATS with a breakdown-derived figure (or '~$80' pinned by a truth test) so the public cost page matches the ~$80 actual run-rate (site_api_common.py:112; S).
1. Sync cost_governor's _TIER_LABELS and docstring tier table to budget_guard's ADR-125 ladder and pin them with a test against _FEATURE_CUTOFF, so tier-change alert emails describe what actually paused (S).
1. Sweep the remaining stale literals in one pass: bedrock_client.py:221 '$75' message, the three 'hourly' cadence claims (governor docstring, budget_guard comment, /method/build editorial), and the chronicle lambda's allow('chronicle') revert per its own TODO (S).
1. Extend scripts/check_doc_facts.py's budget ground-truth rule to also scan lambdas/ source string literals (not just docs) for stale dollar ceilings and governor-cadence claims — the structural fix that prevents this class recurring (B; no new standing infra, extends the existing gate).

**Lens notes (coverage):** Inspected: CE actuals Apr-Jul grouped by SERVICE (Apr $35.01 / May $48.19 / Jun $79.80 / Jul MTD $41.90 through day 16, ~$81 pace) — Bedrock is the growth line ($0 -> $14 -> $44 -> ~$42/mo run-rate, now ~50% of the bill; Haiku $12.94 + Sonnet $8.84 MTD), CloudWatch second (~$17/mo = ~$9.6 custom metrics + ~$7 for ~69 alarms — proportionate to the self-healing/QA posture per ADR-103, not waste); WAF removal (June) already banked ~$9/mo. Read cost_governor_lambda.py + budget_guard.py fully: projection math is genuinely sound — the Bedrock double-count bug (2026-06-17 false tier-3) is fixed by code-side 'bedrock in svc name' exclusion, trailing-7d run-rate fixes the day-1-lump and post-pause-can't-de-escalate modes, and _decide_tier caps projection at actual-mtd-tier+1 so tiers 2/3 require real dollars. Price table verified current against Anthropic pricing (Sonnet 4.6 $3/$15, Haiku 4.5 $1/$5, cache 0.1x/1.25x) with unknown-model->most-expensive + 15% buffer — conservative by design. Enforcement verified live: OBSERVE_MODE=false, SSM tier=1, tier-3 backstop in bedrock_client.invoke():220, feature gates at each callsite. Surge mode (ADR-133) has a real reader source and is ACTIVELY ENGAGED right now: UniqueVisitors7d=972 >= 900, ceiling floated to $100, surge is traffic-pure (can't be tripped by dev spend, fails closed to base ceiling). Tier-degradation copy curled/read: tier-3 ask copy is honest ('paused for the rest of the month... back on the 1st'), tier-2 chronicle sets an honest reader-facing pending note. Per-feature visibility EXISTS and is live: /api/inference_receipt (200) returns per-model and per-LambdaFunction month token attribution, and /method/cost/ + the 'inference' registry page render it — the A-bar for operator visibility is met in structure; the defects are stale dollars on those very surfaces. NOT covered: per-lambda memory-size sweep across ~94 functions (only spot-checked governor at 256MB), per-secret usage audit (21 secrets x $0.40 = ~$8.4/mo — plausible, unaudited), log-retention settings, alarm-by-alarm utility review. Data-maturity: cycle-6 day 4 doesn't affect this lens. Unverified suspicions: (a) the receipt's ai_month_to_date_usd $18.69 vs CE Bedrock actual $21.78 MTD — consistent with the unbuffered token-math estimate (x1.15 = $21.5), disclosed as an estimate on-page, and the governor's CostMetricDriftRatio monitors self-reported under-count, so not filed; (b) PLATFORM_STATS says cdk_stacks: 8 while CLAUDE.md says 9 — sync-owned field, out of this lens, flagging for the accuracy lens; (c) dead _JUNE_2026_THRESHOLDS code auto-reverted and is harmless cruft. The known-queued /method/build builder-stats item (116/59/$13) was NOT re-filed — the live build editorial I fetched already says '$85-a-month ceiling' and platform_stats shows 64/94; my monthly_cost and receipt-ceiling findings are different literals on different surfaces. Headroom verdict: $82.22 projected vs $100 effective (surge) / $85 base — fits with headroom; growth is Bedrock (reader+internal AI), which is exactly what the tier ladder and surge float were built to manage.

### cpo — Product & narrative arc — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every door's first screen states its loop role; every live engagement hook (predict-the-week, ask-the-board, chronicle/podcast cadence) is demonstrably alive-or-honestly-absent on ANY day of a cycle including Day 4 of a fresh reset; a shared bare URL lands on the page; each of the 4 audiences finds its surface within one click of Home; the early-cycle state reads as a deliberate season premiere (day counter, pre-registration, prequels, cycle-vs-cycle context).
- **C:** Doors render and the loop is taught, but one or more engagement hooks silently serve stale or dead-cycle content, shared links 404, or the fresh-cycle state depends on the reader's forgiveness to not read as broken — returnability exists on paper but not on the calendar.
- **F:** The early-cycle site reads as broken or dishonest: blank doors, no day counter or pre-registration, hooks dead or fabricating (gains hidden, dead weeks presented as live, chronicle inventing content beyond the logged data).

**Confirmed findings:**

1. **[high/S/class-A]** Predict-the-week — the cockpit's flagship returnability hook — is live on Day 4 asking readers to bet on 'this week' while its subject is frozen at cycle-5's final week: current_challenge.json has week_id 2026-W27 (today is ISO 2026-W29, cycle 6), was last modified 2026-07-03, is titled 'The week after the wall — does the comeback hold?' (the dead cycle's narrative), and nothing (no lambda, no restart_pipeline step) refreshes or clears it — votes cast today are stored under a week that ended before genesis and can never be revealed.
   - *Evidence:* curl https://averagejoematt.com/api/predict_week → {"active": true, "week_id": "2026-W27", ...tallies all 0}; aws s3api list-objects-v2 → site/config/current_challenge.json LastModified 2026-07-03T02:10:52Z; python3 isocalendar(2026-07-16) → week 29; site/assets/js/cockpit.js:608 renders 'Will <label> go up, hold, or down this week?'; grep of lambdas/+scripts/+deploy/ shows only readers of current_challenge.json (site_api_social.py:1478 _predict_subject), no writer besides deploy/current_challenge.sample.json
   - *Path-to-A step:* Make _predict_subject() fail closed on week mismatch (if week_id != the current ISO week, return None — the widget already self-hides on None, matching its documented fail-closed intent), then put current_challenge.json on a lifecycle: cleared by restart_pipeline.py at reset and re-seeded weekly. ADR-103: extends the existing load-bearing reader-engagement loop; zero new standing infra.
   - *Regression guard:* A qa_smoke_lambda / smoke_test_site.sh assertion: when /api/predict_week returns active:true, its week_id must equal the current ISO week — fails the nightly run the Monday a subject goes stale.
1. **[med/S/class-A]** Every door URL without a trailing slash 404s for a newcomer: the S3 website origin 302s /data → /site/data/ (exposing the internal site/ prefix) which returns 404 — so a hand-typed or slash-stripped shared link (Reddit comments and many autolinkers drop trailing slashes) lands on a dead page instead of the door.
   - *Evidence:* curl -w '%{http_code} -> %{redirect_url}' https://averagejoematt.com/data → '302 -> https://averagejoematt.com/site/data/'; curl https://averagejoematt.com/site/data/ → 404 (same for /cockpit, /coaching, /protocols, /story, /method); the behavior is known-but-unfixed per scripts/v4_migration_inventory.py:169 comment ('bare /cockpit 302s to /site/cockpit/ via the S3 origin'); no open issue matches (gh issue list --search 'trailing slash' → empty; R22 #804/#805/#807/#788 are different UX items, all CLOSED)
   - *Path-to-A step:* Add extensionless-path slash normalization (301 /x → /x/) to the existing v4-redirects CloudFront viewer function that scripts/v4_migration_inventory.py already regenerates — the edge function is already deployed machinery, so no new standing complexity (ADR-103: load-bearing serving path, additive rule only).
   - *Regression guard:* deploy/smoke_test_site.sh asserts curl -L on each bare door URL (/data, /cockpit, /coaching, /protocols, /story, /method) terminates in HTTP 200 on the door page, never a /site/* hop.
1. **[low/S/class-B]** The Prologue Part II read-aloud audio exists but never attaches to its article, and the audio narrates the superseded genesis date: /podcast/episodes.json has the episode ('The Plan, On the Record') dated 2026-07-11 with excerpt 'Tomorrow morning — July 12, 2026', while the article in /journal/posts.json is dated 2026-07-12 and says 'Tomorrow morning — July 13, 2026' — the exact-date join (by design, reset-safe) misses, so the player silently never renders on the top story artifact of the new cycle.
   - *Evidence:* curl https://averagejoematt.com/podcast/episodes.json → episode date '2026-07-11', excerpt 'Tomorrow morning — July 12, 2026'; curl https://averagejoematt.com/journal/posts.json → Part II date '2026-07-12', excerpt 'Tomorrow morning — July 13, 2026'; site/assets/js/read_aloud.js:16-19 joins strictly on String(e.date) === String(ent.date)
   - *Path-to-A step:* Regenerate the Part II read-aloud episode from the re-anchored article text with a matching date (one pipeline re-run), and add a re-anchor step to the restart/prereg runbook: any curated prologue kept across a genesis change regenerates its read-aloud feed entry.
   - *Regression guard:* A pipeline/QA parity check: every entry in /journal/posts.json that has a same-title episode in /podcast/episodes.json must also date-match — flags an orphaned read-aloud at generation time instead of failing silently at join time.
1. **[low/M/class-A]** The single best 'a new cycle started 3 days ago' product surface — the live cycle-vs-cycle comparison (/api/cycle_compare already returns current_cycle 6 vs cycles 1–5 over the same 4-day window) — is footer-buried under The Method during the exact window it is most compelling; Home and the cockpit never mention cycle 6 or link the reset log, so the season-premiere moment is legible (day counter, prequels, prereg) but not exciting.
   - *Evidence:* curl https://averagejoematt.com/api/cycle_compare → {"window_days": 4, "current_cycle": 6, "cycles": [{cycle 1, genesis 2026-04-01, weight_delta -5.6...}]}; /tmp/cpo_home.html footer 'The Technology' column links /method/, /story/build/, /method/platform/, /method/pipeline/, /method/cost/, /gear/ — no /method/cycles/ link and no cycle mention anywhere on Home or in site/now→/cockpit/ shell
   - *Path-to-A step:* Add a self-hiding early-cycle beat to Home (and/or a cockpit strip): 'Start six, day 4 — how it compares to the five starts before it', rendered client-side from the already-live /api/cycle_compare, hidden past ~day 21 and outside fresh cycles. ADR-103: reuses an existing endpoint and the established self-hiding-beat pattern (beat-since), no new lambda.
   - *Regression guard:* tests/visual_qa.py AI-QA prompt for Home during days 1–21 of a cycle: the page must carry an explicit current-cycle context element; deterministic check that the beat renders when /api/cycle_compare.window_days <= 21.
1. **[low/S/class-B]** The canonical intent registry documents the podcast feed at a path that 404s live: docs/SITE_MAP_AND_INTENT.md names /generated/panelcast/episodes.json as the Story door's source, but the site serves /panelcast/episodes.json (dispatches.js) — the doc a redesign is told to start from points at a dead URL.
   - *Evidence:* docs/SITE_MAP_AND_INTENT.md:74 '**Sources:** `/journal/posts.json`, `/generated/panelcast/episodes.json`'; curl https://averagejoematt.com/generated/panelcast/episodes.json → 404 page; site/assets/js/dispatches.js:31 url: "/panelcast/episodes.json" → curl 200 with ep0
   - *Path-to-A step:* Correct the path in SITE_MAP_AND_INTENT.md and fold viewer-path URLs in docs under the existing scripts/check_doc_facts.py-style discipline (a tiny rule: any absolute site path quoted in the v5-brief docs must return non-404 in the docs-ci sweep).
   - *Regression guard:* docs-ci link-liveness check over URLs/paths quoted in the four v5-brief docs (HEAD each against the live origin, allow 301).

**Path to A:**
1. Fail predict-the-week closed on week mismatch (server guard in site_api_social._predict_subject: week_id must equal the current ISO week) and give site/config/current_challenge.json a real lifecycle — cleared by restart_pipeline.py, re-seeded weekly — with a nightly qa_smoke assertion; the flagship cockpit hook must be alive-or-honestly-hidden, never mis-live (ADR-103: hardening an existing load-bearing loop, no new infra).
1. Slash-normalize extensionless viewer paths in the already-deployed v4-redirects CloudFront function so /data, /cockpit etc. 301 to their slashed doors, plus a bare-URL assertion in deploy/smoke_test_site.sh — shared links are the Reddit-newcomer front door and today they 404.
1. Ship the early-cycle 'start six vs the five before it' beat on Home/cockpit from the already-live /api/cycle_compare (self-hides after ~day 21) — turn the reset from a legible fact into the season-premiere hook it deserves to be.
1. Add a deterministic cadence line to the Chronicle and Podcast lists ('the next installment lands Wednesday' — derived from the cron schedule, honest-pending when withheld) so a returning follower knows exactly when to come back; today returnability leans entirely on subscribe/RSS.
1. Regenerate the Prologue Part II read-aloud episode against the re-anchored article date and add the date-join parity check, so curated prequels kept across a reset never strand their audio.

**Lens notes (coverage):** Inspected live (build 1b57516): Home + all 5 doors + /method/ + /method/cycles/ + /subscribe/ via curl; ~15 API endpoints (public_stats, journey, snapshot, weekly_priority, field_notes, character, coaches, coach_team, predictions, experiments, challenges, predict_week, cycle_compare, labs, source_freshness); the feeds (journal/posts.json, journal/blog.json, panelcast + podcast episodes.json); renderer source for every dynamic claim (story.js, cockpit.js, coaching.js, dispatches.js, read_aloud.js, evidence_discovery.js, site_api_social.py); site/config/current_challenge.json read from S3; gh cross-checks against R22 UX issues (#788/#804/#805/#807 all CLOSED and distinct) and section 13b of generate_review_bundle.py. What lands well and earns the B+ side: every door's first screen states its loop position (loop-ribbon + ph-promise), the hero day counter + '16 climbs before' + falsifiable 185/90d checkpoint answer the newcomer in one screen, the Day-4 gain renders honestly ('lbs up', 'up 1.6 lb... last weigh-in Jul 13' — story.js:255-305), the coaching door ships real static Day-1 board reads to no-JS/bots, prereg is discoverable as journal Prologue Part II ('16 board predictions filed') with /api/predictions showing 62 pending/0 decided honestly, ask-the-board is live in coaching.js, the 'Is he okay' family card and honest empty states (field_notes 0 → honest fallback story.js:532; chronicle pending/missing-week notices dispatches.js:551-570) are exemplary. NOT covered: no browser/Playwright render (curl + source-reading only — interactive states like the constellation tap, mirror widget, and theme are inferred from code); no POST to /api/ask, board_question, votes, or subscribe (read-only contract + Bedrock ban), so conversion flows are verified to exist but not exercised; email surfaces, OG cards (known PR #1193), and RSS content unreviewed; no WebSearch benchmarking done. Data-maturity caveats (intentional, not filed): coach track records/scorecard empty, 62/62 predictions pending, field notes 0, chronicle = 2 prequels only, correlations thin — all render honest empty states. Unverified suspicions: public_stats.hero.why_paragraph claims '19 data sources' vs 16 in SOURCE_REGISTRY (the paragraph appears unrendered by any site JS — may count HAE sub-streams; worth the accuracy lens's eye); the predict-week reveal loop may NEVER have closed for any week (only one subject file exists, result null — history unverifiable from here); labs biomarker name 'Aalp Apo A1' looks malformed; the first cycle-6 chronicle is due today (Wednesday) and could not be graded — if it fails to land, the #803 pending-notice machinery should catch it.

### cto — Architecture & ops — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every resilience/ops claim in ARCHITECTURE.md reproduces against live CDK + AWS state; liveness sensors are capability-verified (not just IAM-parity-verified) after deploy; no alarm sits in ALARM unexplained beyond ~72h; the ADR-103 ledger rows match live subsystem state — all inside the solo-operator $85/mo envelope with no added standing machinery.
- **C:** The core pipeline (ingest→DLQ→digest, S3/DDB protection, per-lambda roles) works, but one or more monitoring paths are silently dead, the canonical architecture doc materially misdescribes live behavior (crons, stack ownership, quotas), and stale alarms/ledger rows accumulate with no reconcile loop.
- **F:** Async paths without DLQ/alarm coverage, shared over-privileged IAM roles, S3/DDB unprotected against operator error, or documentation wrong enough that an incident responder acting on it would make things worse.

**Confirmed findings:**

1. **[high/S/class-A]** The #727 scientific-liveness heartbeat has been dead since it shipped: coach-prediction-evaluator's IAM role lacks cloudwatch:PutMetricData, every daily emit fails AccessDenied (demoted to a WARNING by the fail-soft catch), and the grading-stalled alarm — built specifically to catch a dead evaluator — has sat in ALARM on missing-data-breaching for 10 days and can never clear. This is the exact known failure class already fixed on coach-state-updater ('Pre-fix every emit failed with AccessDenied (non-fatal — caught as WARNING)', role_policies.py:827) and the exact #342 lesson (verify the capability, not IAM parity).
   - *Evidence:* aws logs filter-log-events /aws/lambda/coach-prediction-evaluator: daily '[liveness] metric emit failed (non-fatal): AccessDenied ... cloudwatch:PutMetricData' 2026-07-06→07-16; aws cloudwatch describe-alarms: grading-stalled ALARM since 2026-07-06T20:21Z, reason 'no datapoints ... treated as [Breaching]'; get-metric-statistics LifePlatform/Predictions::DaysSinceLastDecided = zero datapoints since 06-25; cdk/stacks/role_policies.py:788-816 (compute_coach_prediction_evaluator — no PutMetricData) + _compute_base at :484 (none).
   - *Path-to-A step:* Add a cloudwatch:PutMetricData statement (resource '*', namespace-conditioned if desired) to compute_coach_prediction_evaluator() and deploy LifePlatformCompute; also seed the EVALUATOR#coach_prediction STATE#last_decided marker from genesis in restart_pipeline so a cycle reset doesn't emit the 999 sentinel and guarantee a false 14-day ALARM window every cycle.
   - *Regression guard:* A lockstep unit test in the proven test_site_api_write_scope.py pattern: grep every lambdas/ source calling put_metric_data and FAIL if its role_policies function does not grant cloudwatch:PutMetricData — this class has now bitten three times (ai-expert-analyzer, coach-state-updater, coach-prediction-evaluator).
1. **[med/S/class-B]** No alarm-aging loop: a digest-routed alarm can sit in ALARM indefinitely with no escalation — grading-stalled fired daily into the digest for 10 days (spanning the remediation agent's daily triage, in shadow mode) and produced no fix, no issue, and no needs-human item that got acted on; the solo operator's attention is the only consumer of the digest and it silently absorbed a dead load-bearing sensor.
   - *Evidence:* aws cloudwatch describe-alarms --state-value ALARM: grading-stalled StateUpdatedTimestamp 2026-07-06T20:21Z (10 days before review date 2026-07-16); alarm action = life-platform-alerts-digest only (describe-alarms AlarmActions); ssm get-parameter /life-platform/remediation-mode = 'shadow'; .github/workflows/remediation-agent.yml triages alarms daily per CLAUDE.md.
   - *Path-to-A step:* Add an alarm-age check to the existing remediation agent's daily triage (or the alert-digest lambda): any alarm in ALARM > 72h becomes a named needs-human line with its age, until acknowledged — zero new standing infrastructure, reuses the ADR-064 machinery (load-bearing per ADR-103's 'silent-failure coverage class' row).
   - *Regression guard:* A remediation-agent triage assertion (or nightly qa_smoke check) that fails/reports when describe-alarms returns any ALARM older than 72h without a matching open issue or ack marker.
1. **[med/M/class-B]** ARCHITECTURE.md (header: 'canonical · Verified 2026-07-10') carries multiple false operational claims in the prose class check_doc_facts.py doesn't police: the compute cron table (17:20–17:35 UTC) says computes run AFTER the 17:00 brief when CDK deliberately runs them 16:30–16:50 BEFORE it (Phase 3.1); site-api is attributed to 'Stack: LifePlatformOperational' (moved to LifePlatformServe by #793); 'Concurrency quota 10 (default; raise filed)' vs live 100; 'Rate limiting: In-memory sliding window' vs the DDB-backed rate_limiter.py (in-memory is fail-open fallback only); cost profile '~$13/month … Anthropic API $8-12' vs the $85 Bedrock/budget-governor reality stated correctly 300 lines earlier; 'three doors' vs the live five-door v5 IA. This is the recurrent class already 'resolved' at R18-F01 and R19-F02 — an incident responder using the cron table would misdiagnose a stale-brief incident.
   - *Evidence:* docs/ARCHITECTURE.md:135-138 (cron 17:20-17:35) vs cdk/stacks/compute_stack.py:192 ('17:35→16:30 … so character_sheet completes BEFORE daily-brief at 17:00'); ARCHITECTURE.md:243 vs cdk/stacks/serve_stack.py:74-102; ARCHITECTURE.md:90 vs aws lambda get-account-settings ConcurrentExecutions=100; ARCHITECTURE.md:276 vs lambdas/rate_limiter.py:1-25 and site_api_ai_lambda.py:142-143 ('legacy, only used if DDB rate_limiter fails'); ARCHITECTURE.md:383-394 vs core_stack.py:125 (amount=85); scripts/check_doc_facts.py covers tool/stack counts + budget dollars only (forward-only).
   - *Path-to-A step:* One accuracy pass fixing the six verified lines, then make the compute/email cron table a synced artifact: add a sync_doc_metadata discoverer that parses the schedule= strings out of cdk/stacks/*.py and regenerates the table, so the highest-stakes operational claim in the doc can no longer be hand-written (extends existing PR-#1189 machinery, no new standing complexity).
   - *Regression guard:* A check_doc_facts.py / sync_doc_metadata rule that diffs every cron() expression quoted in docs/ against the schedule strings in cdk/stacks/*.py and fails on mismatch — would have caught the 2-month-stale table on the day compute_stack.py changed.
1. **[low/S/class-B]** ADR-103 ledger rows have drifted from live state despite the ledger's own one-line-dated-edit maintenance rule: the MCP row still lists '~105 unused tools + the 64-entry orphan allowlist' as the standing retire-candidate although the prune executed 2026-07-08 (#395, 143→60 tools, ledger docs/MCP_TOOL_AUDIT.md), and the 'Weekly Panel podcast pipeline — currently dark — revival is backlog #374' row predates the no-touch pipeline shipping live (ADR-135, ep0 LIVE 2026-07-13). The ledger is the anti-re-litigation instrument; stale rows re-litigate.
   - *Evidence:* docs/DECISIONS.md ADR-103 table (rows 'MCP server — ~105 unused tools are the retire-candidate INSIDE it (#398)' and 'Weekly Panel podcast pipeline — Currently dark — revival is backlog #374') vs CLAUDE.md 'pruned 143 → 60 on 2026-07-08 (#395, ER-04)' and 'no-touch contract ADR-135 … new ep0 LIVE' (2026-07-13); ADR-103 'Maintenance. Posture changes are one-line edits to this table with a dated note.'
   - *Path-to-A step:* Two dated one-line edits: mark the MCP overweight/retire-candidate row executed via #395 (with the AUDITED_AT ratchet as the ongoing control) and update the Panel row to load-bearing/LIVE per ADR-135 — plus fold 'reconcile ADR-103 rows' into the existing quarterly-review checklist the ADR already names.
   - *Regression guard:* Extend the existing tombstone/doc-facts scan with the ledger's known-stale phrasings (e.g. '~105 unused', 'Currently dark') so a terminal state change that leaves the ledger row untouched reds docs-ci, the same mechanism that caught '~125 MCP tools'.

**Path to A:**
1. Grant cloudwatch:PutMetricData to compute_coach_prediction_evaluator() and deploy LifePlatformCompute, then confirm a DaysSinceLastDecided datapoint lands within 24h (capability check, not parity check); seed the last-decided marker at reset so each new cycle doesn't open with a guaranteed false 14-day ALARM.
1. Ship the put_metric_data↔IAM lockstep test (test_site_api_write_scope.py pattern) — third occurrence of this exact fail-soft AccessDenied class; ADR-103 frame: deploy-guardrail, load-bearing, earned by a real incident.
1. Add alarm-aging (>72h in ALARM = named needs-human digest line) to the existing remediation agent triage — closes the 'digest's only consumer is one human' single point of failure with zero new standing infrastructure.
1. Run the ARCHITECTURE.md accuracy pass on the six verified stale claims, then convert the compute/email cron table into a sync_doc_metadata-generated artifact and add a docs-vs-CDK cron-diff rule to check_doc_facts.py so this recurrent class (R18-F01, R19-F02, now again) becomes structurally impossible.
1. Reconcile the two stale ADR-103 ledger rows (MCP prune executed #395; Panel live per ADR-135) with dated one-line edits and add their phrasings to the tombstone scan.

**Lens notes (coverage):** INSPECTED: docs/ARCHITECTURE.md end-to-end vs cdk/app.py + all 9 stacks (boundaries, lambda_helpers DLQ/retry/alarm conventions, serve/operational split #793 — verified real, Function-URL pinning documented, rationale sound); role_policies.py spot-checks (site_api: condition-keyed LeadingKeys writes + lockstep test — genuinely excellent least-privilege for a public lambda; coach evaluator; state updater; _compute_base); resilience wiring read not assumed (EventBridge async → default-2 retries, Garmin retry_attempts=0 with documented OAuth rationale → shared DLQ → depth alarms in monitoring+operational stacks → dlq_consumer that confirms retry before delete; freshness checker with per-source registry thresholds + behavioral-vs-infra distinction; gap-aware backfill covers source recovery; daily brief reads latest-available records so a missed 16:30-16:50 compute degrades to yesterday's data, not a failure; DDB ThrottledRequests alarm exists). LIVE-VERIFIED: DLQ depth 0; exactly 67 alarms (matches the auto-synced doc line — the synced facts are true while hand prose drifted, which is the meta-point); grading-stalled ALARM 10 days with root cause traced through logs to the IAM gap; DDB deletion protection true + PITR ENABLED; S3 versioning Enabled + delete-deny policy live and broader than docs (14 protected prefixes); concurrency 100; budget tier 1; remediation-mode shadow (consistent with the ADR-103 portfolio posture); chronicle-podcast retire-candidate exists but is unscheduled per its documented trigger — posture honest, not grown. DR POSTURE: single-region/single-account is the accepted solo-operator frame; PITR + versioning + delete-deny + restart/rollback tooling + the stolen-laptop epic (#1024) make the recoverability story credible; I did NOT re-verify the 2026-03-30 PITR drill or exercise restart_rollback. NOT COVERED: web_stack/mcp_stack/ingestion_stack deep read; per-lambda DLQ attachment across all 94 functions (sampled via the stacks' shared kwargs); IAM simulation of every role; budget governor runtime behavior; cost dollars (cost lens's grade — overlap noted: ARCHITECTURE.md's ~$13-23 cost table and '21 secrets $8.40' are doc-drift here, dollar accuracy is theirs; the stale /method/ builder stats are already queued, not re-filed). DATA-MATURITY: day-4 cycle-6 means grading legitimately has nothing to decide — the finding is the dead sensor + IAM gap (pre-dates the reset), not the empty track record; note the alarm will legitimately read high early each cycle unless the marker is genesis-seeded. UNVERIFIED SUSPICIONS (not filed): role_policies.py:793 claims inter_coach_dialogue_lambda's budget_guard call 'silently fail-opens' un-granted — likely stale since lambda_helpers.py:255 grants budget-tier SSM read to every CDK-owned role, but I didn't trace that lambda's role; ARCHITECTURE.md:366 'ai-keys — 24 Lambdas — Anthropic API key' smells stale post-ADR-062 (Bedrock IAM) but I didn't enumerate consumers; app.py docstring '13 ingestion Lambdas'/'All 8 stacks wired' comments are trivially stale. DO-NOT-REFILE checked: findings distinct from #795/#816/#817 (different drift items, mine post-date), #342 (referenced as the lesson, filed as a concrete new instance with fix), R22 #800 (different canary).

### data-architect — Data modeling — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Live DynamoDB items, docs/SCHEMA.md, and the phase-taxonomy registry are mutually consistent under spot-sampling; exactly the 2 sanctioned GSIs (ADR-097) exist; the reset machinery is provably total AND idempotent (a double reset preserves each record's original cycle provenance); raw-S3 layouts match their source_registry raw_layout facets including filename scheme.
- **C:** The schema doc is mostly accurate and GSI count holds, but the reset/provenance machinery drifts from its stated contract in live data (wrong cycle stamps, claimed write-time stamping absent), or new pk families can silently escape the taxonomy's totality guarantee.
- **F:** Undocumented GSIs or partitions in live; float writes crashing or silently failing DDB persistence; tombstoned records from a prior cycle leaking into current-cycle reads; or archive records destroyed/overwritten with no way to reconstruct provenance.

**Confirmed findings:**

1. **[high/S/class-A]** Every experiment reset RE-tombstones all previously archived records and overwrites their cycle stamp with the latest closing-run number, destroying the 'archive navigable by reset generation' guarantee that is ADR-077's stated purpose — a February-2026 (cycle-1-era) insight now claims cycle=5, and COACH OUTPUT rows from 2026-07-10/11 (produced in cycle 4) also claim cycle=5.
   - *Evidence:* deploy/restart_intelligence_wipe.py:264-266 (is_already_tombstoned skips only when tombstoned_reason == CURRENT reason) + :276-290 (build_update unconditionally SETs #cyc = :cycle). Live proof: aws dynamodb query pk=USER#matthew#SOURCE#insights → INSIGHT#2026-02-23T02:13:57 | cycle=5 | reason=experiment_restart_2026-07-13; pk=COACH#explorer_coach sk=OUTPUT#2026-07-10#daily_brief… | cycle=5 (cycle 5 did not begin until 2026-07-12).
   - *Path-to-A step:* Change build_update to `#cyc = if_not_exists(#cyc, :cycle)` (and preserve original tombstoned_at similarly), or skip any item already carrying tombstone=true in 'all'-mode partitions; then run a one-off backfill that re-derives true cycles by date-inference against the known genesis dates (cycle markers live in USER#matthew#MEMORY).
   - *Regression guard:* A unit test in tests/test_restart_wipe_coverage.py (or a new test_wipe_idempotency.py) that runs build_update/should_tombstone twice with two different TOMBSTONE_REASONs against a fixture item and asserts the first cycle stamp survives the second pass.
1. **[med/M/class-A]** phase_taxonomy.py's docstring promises 'Going forward, experiment_scoped writers stamp cycle=<current> + phase=experiment at write time so records are self-describing even on partitions the tagger cannot reach' — but live day-4 COACH# rows carry neither, and no writer anywhere stamps `cycle` at write time; provenance for the tagger-unreachable partitions rests entirely on the (buggy, see finding 1) reset-time wipe.
   - *Evidence:* lambdas/phase_taxonomy.py:37-39 (the claim) vs live: aws dynamodb query pk=COACH#explorer_coach → TRACE#2026-07-16#daily_brief… and VOICE#state have tombstone:None cycle:None phase:None. grep -rln current_cycle lambdas/ hits only notion_lambda.py and site_api_data.py — no intelligence writer reads the SSM cycle at write time (output_writers.py:567 stamps phase only, on some paths).
   - *Path-to-A step:* Add one shared stamp helper (phase=experiment + cycle from a cached SSM /life-platform/experiment-cycle read) and call it from the COACH#/ENSEMBLE#/NARRATIVE# writers — or, if write-time stamping is deliberately abandoned, amend the phase_taxonomy docstring and lean on the fixed wipe; either resolution ends the doc-vs-behavior drift the 2026-07-13 external assessment pattern-matched ('governance expressed but not achieved').
   - *Regression guard:* Extend scripts/check_doc_facts.py (already in docs-ci) with a rule pinning the phase_taxonomy docstring claim to a grep-verifiable stamp-helper call site, or a unit test asserting the coach output writer's put_item payload contains phase and cycle keys.
1. **[med/M/class-B]** The taxonomy's totality guarantee is fail-loud only for USER#…#SOURCE# families (the tagger live-scans that prefix and classify() raises on unknowns); non-SOURCE top-level pk families are covered only by a frozen 2026-06-07 census fixture plus manually-added _PK_RULES, and the wipe iterates a hand-named PARTITIONS list — so a NEW experiment-scoped top-level family (the next COACH#-like tier) would silently survive every reset, which is exactly the divergence class ADR-077 was written to kill.
   - *Evidence:* deploy/restart_phase_tag.py:147 (scan FilterExpression begins_with(pk, USER#…) only); lambdas/phase_taxonomy.py:245-247 ('None are traversed by the restart tooling (the tagger scans USER#…#SOURCE# only)'); tests/test_phase_taxonomy.py:3-4 (LIVE_FAMILIES is 'the 2026-06-07 census' — a static fixture, extended manually by #930/#951).
   - *Path-to-A step:* Add a pk-family census preflight to deploy/restart_pipeline.py dry-run: one paginated scan projecting pk+sk (table is 32,329 items / 46 MB — pennies, and it runs only at reset), reduce to distinct families, classify() each, and FAIL the dry-run on any KeyError. ADR-103 justification: extends the existing load-bearing reset tooling with zero standing cost — no new infra, runs only when a reset runs.
   - *Regression guard:* The preflight itself is the guard (a planted unknown pk in a test fixture must fail it — per the vacuous-scan-trap lesson); plus a test asserting the census step is wired into restart_pipeline's dry-run sequence.
1. **[low/S/class-A]** Raw-zone layout documentation under-specifies a real filename-generation fracture: garmin flipped from DD.json to YYYY-MM-DD.json mid-2026 within the same date-tree, whoop uses YYYY-MM-DD.json, todoist uses DD.json — yet CLAUDE.md documents the pattern as raw/matthew/{source}/{YYYY}/{MM}/{DD}.json and the registry's raw_layout scheme facet ('date-tree') doesn't distinguish filename variants, so anyone deriving keys from the documented pattern misses objects.
   - *Evidence:* aws s3api list-objects-v2: raw/matthew/whoop/2026/07/2026-07-01.json (full-date filename) vs raw/todoist/2022/03/17.json (DD.json) vs garmin's own split — raw/matthew/garmin/2025/01/22.json then raw/matthew/garmin/2026/06/2026-06-13.json. CLAUDE.md Store section claims '{YYYY}/{MM}/{DD}.json'; lambdas/source_registry.py:152/417 raw_layout facets say only scheme:'date-tree'. (MEMORY.md's 'raw/{source}/{datatype}/{YYYY}/{MM}/{DD}.json' is also wrong.) No programmatic consumer of raw_layouts() found outside tests, so risk is latent/doc-level today.
   - *Path-to-A step:* Add a filename facet to each raw_layout (e.g. filename:'YYYY-MM-DD.json'|'DD.json'|'mixed — flipped 2026-06') and correct the CLAUDE.md/SCHEMA.md/MEMORY.md pattern lines to say filenames vary per source — read raw_layout, don't construct keys.
   - *Regression guard:* A scripts/check_doc_facts.py rule (the gate already exists and is wired into docs-ci) pinning the documented raw-path pattern to the registry facets, so the pattern string can't drift from the facet again.

**Path to A:**
1. Fix the wipe's re-stamp clobbering (deploy/restart_intelligence_wipe.py): if_not_exists on cycle/tombstoned_at or skip already-tombstoned items in 'all' mode, with a double-reset idempotency unit test — one small diff, closes the highest-severity provenance bug.
1. One-off backfill (dry-run first) re-deriving true cycle stamps on archived records by date-inference against the known cycle genesis dates, restoring the 'archive navigable by reset generation' property that live data currently violates.
1. Resolve the write-time-stamping doc-vs-behavior drift: either ship the shared phase+cycle stamp helper into the COACH#/ENSEMBLE#/NARRATIVE# writers, or amend phase_taxonomy.py's docstring to state that provenance is reset-time-only — and pin whichever answer wins with a check_doc_facts.py rule.
1. Add a pk-family census preflight to restart_pipeline dry-run (one cheap 32k-item scan, reset-time only — ADR-103: extends existing load-bearing tooling, zero standing cost) so a new non-SOURCE pk family fails loud before a reset instead of silently surviving it; prove it catches a planted unknown family.
1. Correct the raw-path pattern lines in CLAUDE.md/SCHEMA.md/MEMORY.md and add a filename facet to raw_layout, gated by check_doc_facts.py.

**Lens notes (coverage):** INSPECTED: describe-table (exactly GSI1+GSI2, both ALL-projection, matching ADR-097/SCHEMA.md §GSIs — no drift; 32,329 items / 46 MB, on-demand billing). Sampled 6 partition families with bounded queries: whoop raw daily+workout (every live field matches SCHEMA.md's whoop table exactly — unusually good doc fidelity), computed_metrics derived (rich, ACWR fields carry their own coupling-caveat text — ADR-105 in the data itself), COACH#explorer_coach (current + tombstoned rows), ledger (CYCLE_TOTALS#002–005 + LIFETIME#aggregate — clean, exactly the ADR-072/077 design), insights (wiped partition, cycle-stamp check), reading STATE via a live GSI2 query (READING_STATUS#reading returns rows; key shape matches reading_keys/SCHEMA). Read phase_taxonomy.py in full plus the wipe/tagger code and tests (test_phase_taxonomy.py census fixture, test_restart_wipe_coverage wiring, test_source_enumeration_drift — the taxonomy↔wipe coverage assertion is REAL and CI-run; the live↔taxonomy leg is the gap in finding 3). Verified S3 raw layouts for whoop/todoist/hevy/weather/garmin against registry facets — the three-generation X-9 fracture is accurately registered; garmin's raw+DDB staleness (last 2026-06-15) is honestly declared paused per ADR-074, NOT a finding. Decimal hygiene: structural check only — shared _to_decimal helpers, 111 Decimal(str()) call sites, boto3 fail-loud on float means violations can't be silent; no concrete float-write reproduced, so none filed. Query-then-filter: FilterExpression use is modest (22 sites) and the phase filter is centralized in phase_filter.py — tombstone-in-place inherently costs a filter on every read, a deliberate ADR-077 Interpretation-B trade-off I judge sound at this scale; no hot-partition risk at 46 MB. Vice names ('No marijuana', 'No porn') exist in computed_metrics.habits_mvp but the public path has a layered scrub (privacy_guard + _scrub_blocked_terms/_is_blocked_vice) — no leak reproduced. NOT COVERED: exhaustive field-diff of all ~30 SCHEMA.md source sections (did whoop deeply, ledger/reading/computed_metrics structurally); no live distinct-pk census (read-only contract bans unbounded scans — finding 3's totality gap is code-read-based); the 22 FilterExpression sites not individually audited; GSI item counts not sampled beyond one GSI2 query. DATA-MATURITY: day 4 of cycle 6 — empty current-cycle intelligence partitions are by design; my initial COACH#marsh EMPTY result was a wrong coach-id guess (real ids like explorer_coach), not a defect. KNOWN/NOT REFILED: SCHEMA.md itself flags CHECKIN# reclassification as pending (#917). UNVERIFIED SUSPICION: whether any pre-cycle-5 tombstoned_reason values survive anywhere (all samples showed the 2026-07-13 reason — consistent with full clobbering, but I sampled ~6 rows, not the whole archive).

### growth — Community & commercialization — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every shareable page emits a current, data-driven OG card; the subscribe flow is double-opt-in, canary-tested, AND its conversion (attempts → confirms → list size) is visible in the weekly traffic digest; subscribers get a fixed cadence that matches the promise copy on /subscribe/; engagement hooks (predict, board-ask, votes) work on Day 1 of a fresh cycle and are discoverable from the pages newcomers land on; commercial option value (list, RSS, gear, podcast) is real and measured with zero dishonest monetization.
- **C:** Capture and email machinery works end-to-end but the funnel is unmeasured (signups can silently die in pending state), share surfaces ship a generic or stale card, and engagement hooks exist in code but are not reachable or not functional on Day 1 of a cycle.
- **F:** The funnel is broken or dishonest: subscribe silently fails or is single-opt-in spam, share cards present wrong/stale numbers as current, engagement endpoints 500, or monetization (undisclosed affiliates, paywalled 'proof') contradicts the proof-not-promises posture.

**Confirmed findings:**

1. **[med/S/class-A]** 10 of the 12 data-driven OG cards the og-image-generator renders daily (og-sleep, og-glucose, og-training, og-nutrition, og-mind, og-labs, og-chronicle, og-weekly, og-experiments, og-builders) are referenced by ZERO non-legacy pages — every /data/ subpage and the chronicle share the generic og-home.png, so the platform's best discovery asset (fresh per-topic numbers in the link preview) is generated and then never served.
   - *Evidence:* lambdas/web/og_image_lambda.py:254-267 (PAGES list of 12 builders, daily cron 11:30 AT PT); grep -rl 'images/og-sleep.png' site/ (excluding legacy) = 0 pages, same for 9 others; only og-home (72 pages) and og-character (3 pages) are wired; live cards ARE current (Last-Modified: Thu, 16 Jul 2026 17:07 GMT, 'Day 4 / -2 lbs')
   - *Path-to-A step:* Point each topic page's og:image at its matching generated card (site/data/sleep/ → og-sleep.png, /story/chronicle/ → og-chronicle.png, etc.) — pure HTML edits reusing already-generated assets; alternatively prune unwired builders via an audited ratchet like docs/MCP_TOOL_AUDIT.md. Wiring is the growth-positive choice at zero standing cost.
   - *Regression guard:* Unit test asserting every name in og_image_lambda.PAGES is referenced by at least one non-legacy site/ page (or is on an explicit orphan allowlist) — fails when a card is generated but unserved.
1. **[low/S/class-A]** The home OG card — the image on 72 pages' share previews — hardcodes 'One man. 25 data sources. Total transparency.' while source_registry.py has 16 top-level sources; an uncomputed, likely-inflated count on the platform's most-distributed surface is exactly the ADR-104 honest-numbers / stale-count drift class the doc-facts gate was built to kill, but the gate doesn't police lambda-emitted strings.
   - *Evidence:* lambdas/web/og_image_lambda.py:79 draw.text(..., "One man. 25 data sources. Total transparency."); top-level registry keys in lambdas/source_registry.py = 16 (whoop…dropbox); scripts/check_doc_facts.py polices docs, not lambda source
   - *Path-to-A step:* Compute the count (len of the registry, or a field in generated/public_stats.json which the lambda already loads at line 273) instead of hardcoding — or, if 25 counts HAE sub-streams, derive that number from the registry so it can never drift.
   - *Regression guard:* Extend scripts/check_doc_facts.py (it already knows ground-truth counts from sync_doc_metadata's discoverers) to scan reader-facing string literals in lambdas/web/og_*.py for the policed counts.
1. **[low/S/class-A]** RSS is summary-only with a hard 357-char excerpt cut, so the QS/hacker audience that lives in feed readers — the platform's stated depth audience — never gets full posts, and each truncated item ends mid-sentence in most readers.
   - *Evidence:* scripts/v4_build_rss.py:101 'excerpt = excerpt[:357].rstrip() + "…"'; curl https://averagejoematt.com/rss.xml — 3 valid items, descriptions cut mid-word ('before any dat', 'measures his stre'); feed is otherwise valid RSS 2.0 with atom:link self and autodiscovery <link rel=alternate> in every page head
   - *Path-to-A step:* Emit <content:encoded> with the full post HTML (the builder already reads the post files) alongside the existing description; keeps the excerpt for legacy readers, gives full content to the audience most likely to resubscribe by email later.
   - *Regression guard:* A test on v4_build_rss.py output asserting each item has content:encoded whose length exceeds the description, plus XML validity.

**Refuted (not defects — kept for the record):**
- ~~The cockpit (/cockpit/) — the single most shareable page, the 'look at his live dashboard' link — has NO og:image, no og:title, and no canonical tag; every other door page has all three, so a cockpit share renders with no card at all.~~ — *The tag absence reproduces (site/cockpit/index.html: 0 og:image / 0 og:title / 0 canonical vs 1/1/1 on the other doors), but it is intentional, not a defect. The live and repo page carries <meta name="robots" content="noindex"> plus the explicit in-file comment 'Cockpit is the daily tool, not a public landing page.' The v4 ADR records this posture verbatim: docs/DECISIONS.md:2280 — 'Cockpit (/now/, today's live data — noindex, the daily tool)'; docs/SITE_MAP_AND_INTENT.md gives its audience as 'Matthew (daily return) + curious visitors.' The finder's premise that the cockpit is 'the single most shareable page' contradicts the documented design: it is deliberately de-indexed and excluded from the public-landing/share surface (canonical on a noindexed page would be incoherent). Adding OG tags there is at most a growth suggestion that argues against a recorded decision, not a bug.*
- ~~The subscribe funnel's biggest leak is invisible: 2 of the 3 all-time signups sat in pending_confirmation with 48h tokens that expired 2026-07-03 and died silently — there is no confirmation-reminder, no operator notification of a new signup, and the weekly traffic digest reports uniques/referrers but nothing about subscribe attempts or confirms, so the one flywheel joint that isn't measured is capture conversion.~~ — *The DDB data reproduces (Count 3: 1 confirmed 2026-03-28, 2 pending_confirmation 2026-07-03) but the finder misread what the 2 pending rows ARE: I read the email fields — both are canary+<ts>@mattsusername.com, synthetic rows written by canary_lambda.check_subscribe_flow (POSTs a throwaway subscriber every 4h to verify the flow, then deletes; these two are cleanup leftovers from the era when its update_item cleanup was failing). They are NOT reader signups, so no real signup ever 'died silently' — the funnel leak the finding is built on does not exist, and /api/sub_count=1 is the correct real-human count. This synthetic-signup class is already documented in ADR-101 (docs/DECISIONS.md:2930): '~1 confirmed subscriber (the other 425 records were the canary's synthetic signups).' The residual observations (no reminder re-send, no subscribe line in traffic_digest_lambda.py) are true but are feature ideas hanging off a false premise, not the claimed invisible leak.*
- ~~No distribution loop closes the flywheel: after ~4 months public the list is 1 confirmed subscriber and 3 signups ever, discovery is entirely passive (the traffic digest tracks Reddit referrers but nothing establishes a publish→share beat), even though subscribe records already capture source/referrer for attribution — the machinery to measure a push exists, the push does not.~~ — *Every load-bearing element is either factually wrong, a recorded deliberate posture, or already tracked. (a) '3 signups ever' counts the 2 canary synthetic rows (canary+@mattsusername.com) — the real total is 1, and ADR-101 already measured exactly this ('~1 confirmed subscriber... the other 425 records were the canary's synthetic signups'). (b) 'nothing establishes a publish→share beat' — the machinery exists: #405 closed COMPLETED shipped lambdas/chronicle_share_kit.py wired into wednesday_chronicle_lambda.py/chronicle_approve_lambda.py (per-chronicle ready-to-post kit in the approval email), and its design text explicitly REJECTS a manual posting rhythm ('would fail the site's no-performing rule... no auto-posting — the human step stays optional by design; if posting decays, the honest fallback is feed-and-search only'). (c) ADR-101 'Distribution before monetization' codifies the posture with concrete revisit triggers (100 confirmed human subscribers / wedge-A gate). (d) The one push that IS wanted — publish + measure travel — is open issue #741, on the already-tracked list, explicitly gated on Matthew's own posting permission. The 'push does not exist' state is a documented decision plus tracked work, not a gap finding.*
- ~~There is no reader-to-reader surface at all — every hook (board_ask, submit_finding, votes, follows, checkins) is reader-to-Matthew, and the only proof other readers exist is the anonymous predict-the-week tally; the loop 'you predicted, here's how readers collectively did vs the data' is computed (tallies + graded result) but never narrated back to subscribers.~~ — *The core factual claim — the collective-prediction result is 'computed... but never narrated back' — is contradicted by the front-end: site/assets/js/cockpit.js:585-594 _renderPredictItem renders the full reveal once a week grades: 'readers said <up/down/flat tallies>' + 'you called it' (localStorage prior vs result) + 'actual: <direction>' from handle_predict_week_tally's result field. The loop IS closed on-site for every returning reader; the 0/0/0 tally the finder quoted is Day-4-of-cycle-6 sparseness (week 2026-W27 just started), which the verification brief classifies as REFUTED when misread as a defect. The remaining assertion — no comment/reply/profile surface — is true but is the platform's deliberate single-subject-documentary shape (reader→platform hooks plus published board answers via scripts/publish_board_answer.py, which readers do see); proposing a reader-to-reader forum is a product pitch against the design, not a defect, and the finding's only concrete evidence claim failed reproduction.*

**Path to A:**
1. Wire the share surface in one PR: add og:image/og:title/canonical to /cockpit/ and point each /data/ topic page + /story/chronicle/ at its already-generated daily card (og-sleep, og-glucose, og-chronicle, ...) — S effort, zero new machinery, immediately upgrades every external share.
1. Make capture measured: add a subscriber-funnel section (attempts / confirms / pending-expired / sub_count trend) to the existing weekly traffic digest, reading the subscribers partition the platform already owns — this is the last unmeasured joint of the flywheel.
1. Stop silent signup death: one 24h confirmation-reminder branch in the existing subscriber-onboarding lambda for pending records with live tokens (2 of 3 all-time signups died this way).
1. Establish the distribution beat as process (fix_class B): every published chronicle/dispatch gets one tagged external share (?src= is already persisted by handle_subscribe) with the result read back in the next digest — the flywheel needs inbound flow, not more machinery.
1. Honest-numbers cleanup on growth surfaces: compute the og-home '25 data sources' claim from source_registry/public_stats, and emit full-content RSS (<content:encoded>) for the feed-reader audience.

**Lens notes (coverage):** INSPECTED: live HTML head of / (title, meta, canonical, OG, RSS autodiscovery — all present and on-message), /data/, /story/, /coaching/, /cockpit/ (OG/canonical MISSING), /now/→/cockpit/ 301; robots.txt (clean, sitemap pointer); sitemap.xml (77 URLs, lastmod 2026-07-13); rss.xml (valid RSS 2.0, 3 items — thin count is expected Day 4 post-reset with the two restored prequels + essay); downloaded and visually reviewed og-home.png and og-character.png (Last-Modified today 17:07 UTC, 'Day 4', '-2 lbs', no tofu — PR #1193's out-of-band deploy is working; NOT re-filed per shared context); subscribe end-to-end in code (email_subscriber_lambda.py: double opt-in, 48h token, DDB rate limit 60/5min replacing the old WAF rule, canary path; subscriber_onboarding_lambda.py Day-2 bridge email; welcome email on confirm at line 387); subscriber DDB partition (3 records: 1 confirmed since March, 2 expired pendings); retention cadence from email_stack.py (Sun 9:30 PT weekly-signal + Wed 8:10 PT chronicle to confirmed subscribers — matches the /subscribe/ promise copy exactly, which is the honesty bar met); engagement APIs live (predict_week active:true on Day 4 with week 2026-W27 — Day-1 machinery works; board_ask POST route in site_api_ai_lambda.py:1103 with front-end callers in coaching.js; follow/checkin/finding handlers in site_api_social.py); /api/sub_count ({count:1}); gear page (1 external link, zero affiliates — commercialization is clean today); traffic measurement (traffic_digest_lambda: weekly uniques/returning/top-pages/referrers, privacy-clean hashed IPs, CloudWatch surge-mode metrics per ADR-133). Checked section 13b resolved inventory — none of my findings appear there, and the stale /method/ build stats are already queued so not re-filed. NOT COVERED (honest gaps): I did not POST to subscribe/vote/board_ask (read-only contract — so the confirmation email's actual rendering and deliverability are unverified); no SPF/DKIM/DMARC check; the per-moment share cards (og_moments.py, card_engine.py, generated/moments/) were not audited; EMF engagement telemetry depth unverified; no Search Console/indexing verification possible. DATA-MATURITY CAVEATS: predict tallies 0/0/0, RSS at 3 items, sub-page empty states — all expected on Day 4 of cycle 6 and not graded as defects. UNVERIFIED SUSPICIONS: the 2 expired pendings (both 2026-07-03) may be Matthew's own tests rather than lost readers — but the machinery gap (no reminder, no funnel metric) is real either way; '25 data sources' might be defensible counting HAE sub-streams, but it is hardcoded, which is the actual defect; 2-of-3 unconfirmed could also indicate confirmation emails landing in spam (deliverability worth one manual test). Benchmarking was light — the web search yielded general QS-movement context, not growth benchmarks (sources: [Quantified Self blog](https://quantifiedself.com/blog/), [Ness Labs — Personal Science](https://nesslabs.com/personal-science)); grade is anchored to the platform's own posture per instructions. GRADE RATIONALE: capture/retention machinery is genuinely A-grade (double opt-in, canary, onboarding sequence, honest 2x/week cadence); the deductions are all at the joints — the flagship page shares with no card, 10 of 12 daily-built cards are orphaned, and capture conversion is the one unmeasured flywheel segment. B+ with a short, cheap path to A.

### integrations — Ingestion health — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every source's declared cadence/threshold matches its live EventBridge rule with a written derivation (the #471 Todoist standard); the public board and the operator checker tell the same cross-reset-honest story (real last_update + 'dark N days'); a dead OAuth token pages within ~one cadence via alarms that are green because they work, not because they can't fire; dead sources are disclosed everywhere they surface — all with no machinery beyond the one canonical registry.
- **C:** Cadences and thresholds are mostly honest, but at least one public surface omits or misstates staleness (e.g. a real lapse rendered as 'no data' with no duration), or a dead source appears live on a reader-facing surface, or token death is only noticed by a daily human glance rather than an alarm.
- **F:** Registry facets contradict the live rules (thresholds impossible for the cadence), staleness is silently hidden or fabricated on the public board, dead sources are presented as flowing, and there is no alarm path for a dead token.

**Confirmed findings:**

1. **[high/S/class-A]** Public /api/source_freshness loses last_update, age_hours, stale_hours, and the ADR-104 'dark N days' stamp for any source whose newest record predates the current cycle: _latest_date_str applies with_phase_filter, and the cycle-6 reset tagged all pre-genesis records phase=pilot — so MacroFactor (the PRIMARY food channel, actually dark since 2026-06-24) and Hevy/measurements/food_delivery all render last_update:null, days_dark:null on the public board, exactly when the lapse is longest. The operator checker disagrees (freshness_checker_lambda.py:399 queries WITHOUT the phase filter and would report 'Last update: 2026-06-24'), and the same file already treats RAW_TIMESERIES as cross-phase with a deliberate include_pilot=True for device agreement (site_api_data.py:390-396) — freshness is pipe/behavior liveness and should read cross-phase too. Recurs after EVERY reset until each source's first current-cycle write.
   - *Evidence:* lambdas/web/site_api_data.py:259-269 (with_phase_filter + Limit:1) and :347 (_days_dark(last_update=None) → None); live curl https://averagejoematt.com/api/source_freshness returned {"id":"macrofactor",..."last_update":null,"stale_hours":null,"status":"behavioral-stale"} and measurements/food_delivery with "days_dark":null; aws dynamodb query shows macrofactor newest = DATE#2026-06-24 with phase=pilot, hevy DATE#2026-06-25 phase=pilot; site/assets/js/evidence_meta.js:187 only renders 'dark Nd' when days_dark != null.
   - *Path-to-A step:* Pass include_pilot=True in _latest_date_str (and the same-basis pulse read at site_api_data.py:553-604) so the board reports the true newest DATE# across phases — restoring the honest last-update date and dark-N-days count; no new machinery, and it matches the taxonomy's stated cross-phase intent for raw timeseries.
   - *Regression guard:* Unit test in the site-api suite that seeds a source partition whose ONLY records are phase=pilot (post-reset simulation) and asserts handle_source_freshness returns non-null last_update, stale_hours, and days_dark > 0 for a manual source.
1. **[low/S/class-B]** Two hand-created EventBridge rules exist in live AWS but nowhere in the repo, each double-scheduling a lambda that CDK already schedules: pipeline-health-check-daily cron(0 13) duplicates the CDK 5x/day PipelineHealthCheck schedule (same target function), and subscriber-onboarding-daily cron(0 16) duplicates CDK's SubscriberOnboardingSchedule cron(5 17) (same target; the lambda's own docstring still names the orphan's cron). Onboarding is idempotent (attribute_not_exists(onboarding_sent)) so no duplicate emails — the cost is wasted invokes and live-infra-vs-code drift. This is epic #342's class (live-infra-matches-code) — referencing it, not filing new.
   - *Evidence:* aws events list-targets-by-rule: both pipeline-health-check-daily and LifePlatformOperational-PipelineHealthCheckSchedule-* target arn:...:function:pipeline-health-check; subscriber-onboarding-daily and LifePlatformEmail-SubscriberOnboardingSchedule8D147-* both target function:subscriber-onboarding; grep of cdk/stacks/*.py finds neither orphan rule name; lambdas/web/subscriber_onboarding_lambda.py:6 documents cron(0 16) but CDK synthesizes cron(5 17); idempotency at :155,:196-198.
   - *Path-to-A step:* Under #342: delete the two orphan rules (or cdk-import them), and align the onboarding lambda docstring with the CDK cron.
   - *Regression guard:* Extend tests/test_integration_aws.py (the I-series) with a read-only assertion that every ENABLED EventBridge rule targeting a life-platform lambda appears in the synthesized CDK template — flags any future hand-made rule.

**Refuted (not defects — kept for the record):**
- ~~Public /api/vacation_fund presents the dead macrofactor_workouts partition (no writer for ~4 months, #485, honestly marked dead in phase_taxonomy.py and the email lambdas) as a live contributor: extra_sources_enabled: ["hevy","macrofactor_export"] with a per-source 0.0 row and an overlap warning, but no disclosure that the source stopped ingesting — a reader sees a permanently-zero 'enabled' source with no way to know it can never move.~~ — *The raw payload reproduces (curl /api/vacation_fund → extra_sources_enabled [hevy, macrofactor_export], per_source.macrofactor_export: 0.0; vacation_fund.py:53-57 maps to the dead macrofactor_workouts partition per phase_taxonomy.py:70), but the defect framing misreads the context. (1) No reader surface exists: grep of site/ finds ZERO consumers of vacation_fund — no public page renders this endpoint, so 'a reader sees a permanently-zero enabled source' is false on every rendered surface. The only presentation consumer is Matthew's own daily brief (html_builder.py:488-490), which reads total_usd/total_miles only — never per_source or extra_sources_enabled. (2) The flagged fields are a faithful echo of the operator's own config/vacation_fund.json ('extra_sources': ['hevy', 'macrofactor_export']) — the endpoint reporting the configured state is behavior-as-designed, not a fabrication; ADR-104's grounded-generation gate governs narrative surfaces, not a config echo in raw JSON. (3) On Day 4 of cycle 6 every per_source row including live strava/hevy is 0.0 with an explicit genesis warning, so the dead source is indistinguishable even to a hypothetical raw-API reader. At most this is a one-line operator config tidy-up, not a public-honesty defect.*

**Path to A:**
1. Make the public freshness read cross-phase: include_pilot=True in _latest_date_str and the same-basis pulse read (site_api_data.py:553-604), restoring real last_update/age/days_dark after every reset — one-line class of fix, no new machinery, matches the RAW_TIMESERIES cross-phase precedent already in the same file.
1. Add the post-reset regression test: seed a pilot-only source partition and assert handle_source_freshness returns non-null last_update + days_dark — this is the test that would have caught the current live gap and will catch it again at cycle 7.
1. Under epic #342, delete or cdk-import the two orphan EventBridge rules (pipeline-health-check-daily, subscriber-onboarding-daily) and add a read-only ENABLED-rule-vs-CDK-template parity assertion to the I-series integration tests (zero standing cost — it's a test, not infrastructure; ADR-103: strengthens the existing load-bearing parity gate rather than adding a subsystem).
1. Disclose the dead macrofactor_export honestly in the public vacation_fund payload or drop it from extra_sources_enabled.
1. Optional polish: when an HAE datatype's last_seen exceeds the liveness scan window (CGM currently shows dark:true with last_seen:null), render 'dark >Nd (beyond scan window)' instead of a bare null — still computed, never fabricated (ADR-104).

**Lens notes (coverage):** INSPECTED: full read of lambdas/source_registry.py (all 15 registry entries + facet helpers); live /api/source_freshness (twice, quoted verbatim); every ingestion EventBridge rule via aws events list-rules + targeted list-targets-by-rule — declared cadences all match live rules (whoop/withings/strava/eightsleep/habitify/notion hourly 0-5,12-23 UTC; todoist cron(0 14) matching its #471-derived 72h threshold — honest; weather 2x daily; hevy 12-23 UTC; dropbox rate(30m); garmin has NO rule, consistent with paused/ADR-074). Gap-aware backfill verified in ingestion_framework.py: _find_missing_dates (:374-417) queries WITHOUT the phase filter so existing pilot records aren't refetched, and _store_item stamps phase_for_date (:466-467) so any backfilled pre-genesis day lands as pilot — the genesis boundary is handled correctly on both the detect and write sides; whoop additionally has refresh_trailing_days=2 + a daily reconciler with its own MissingActivityCount alarm. OAuth fragility: auth circuit-breaker in the framework (_mark_auth_failure), IngestAuthHealthy → ingest-auth-unhealthy-24h alarm, per-source ConsecutiveFailures alarms (whoop/withings/strava/eightsleep/hevy), pipeline-health-check 5x/day, freshness checker daily SNS — all alarms OK at review time; MTTN for a dead token is ~hours for hourly sources, ≤24h worst; re-auth runbooks exist in setup/ (setup_whoop_auth.py, setup_garmin_browser_auth.py, fix_withings_oauth.py, setup_eightsleep_auth.py). HAE dedup read in full (merge_day_to_dynamo): per-reading timestamp map _rd_{field}, total recomputed from the merged map, GREATEST-on-write monotonic guard for activity totals, validation gate before every merge — correct for both incremental and full-day re-sends. Dead sources: macrofactor_workouts and google_calendar are honestly marked in phase_taxonomy, the email lambdas, and the export extras; neither appears on the public board (correct) — the one smudge is vacation_fund (finding 3). NOT COVERED: I did not read the other 12 ingestion lambdas' bespoke logic (framework + whoop + HAE + spot checks only); did not exercise any token (read-only contract); did not verify the DATATYPE_LIVENESS computation window in the checker; did not check the non-ingestion hand-named email rules beyond target inspection (each targets its own distinct lambda — not duplicates). UNVERIFIED SUSPICIONS: (a) HAE's dedup read-merge-write is non-atomic — two concurrent webhook posts could race and drop a reading; single-phone source makes this low-probability and I did not attempt to reproduce; (b) summary.stale lumps behavioral-stale and unknown into one 'stale' count (site_api_data.py:365-366) — currently reads 'stale: 5' when all five are behavioral; I did not verify whether any front-end renders the summary number raw. DATA-MATURITY CAVEATS: the manual channels genuinely went quiet ~June 21-25 (pre-reset behavior, visible in DDB) — MacroFactor/Hevy/water/CGM darkness is Matthew's lapse plus the cycle boundary, not a pipe fault, and strava's behavioral-stale at 65.7h is honest; Day-4 thinness elsewhere is intentional and was not graded against.

### qs — Scientific credibility — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every public claim is correlative and carries a real, resolvable citation; every protocol lever states the data it targets and the hypothesis that spawned it; every pre-registered bet is either graded or explicitly voided ON THE RECORD across resets; thresholds derive from personal variance or are labelled population constants; every 'how it holds up' surface is either live or honestly retired.
- **C:** The methodology pages state the right posture (correlative-only, pre-registration, calibration) but spot-checks find fabricated-looking citations, pre-registered bets that silently vanish at reset, or dead credibility surfaces a skeptic can trip on.
- **F:** Causal claims stated as fact, invented or search-URL citations presented as studies, calibration numbers that misrepresent the actual record, or magic-number thresholds presented as personalized.

**Confirmed findings:**

1. **[high/M/class-A]** Experiment resets silently vanish all open pre-registered bets ungraded: the restart wipe writes no expired/void rows to the CROSS_PHASE calibration ledger, and once tombstoned the phase filter hides old-phase hypotheses from the weekly engine forever — after 5 resets the ledger holds ZERO hypothesis rows, so the taxonomy's own justification for the wipe ('graded outcomes live in the CROSS_PHASE calibration ledger') is unimplemented on the wipe path.
   - *Evidence:* DDB query pk=USER#matthew#SOURCE#calibration: 23 rows, all record_type=forecast_resolution, zero hypothesis rows; grep 'calib' deploy/restart_pipeline.py + restart_*.py = no hits; lambdas/compute/hypothesis_engine_lambda.py:22 promises 'Every resolution (confirmed/refuted/expired-undecided) writes a row' but reads via with_phase_filter (line ~76 import, ADR-058) so wiped hypotheses are never re-seen; lambdas/phase_taxonomy.py:100-118.
   - *Path-to-A step:* Add a reset-time grading pass to restart_pipeline.py: before phase-tagging, write one 'voided_at_reset' CALIB# row per open hypothesis and open coach PREDICTION# (cycle-stamped, non-Brier-scorable but on the permanent record), matching the taxonomy's stated design. No new standing infra — one function in the existing sanctioned reset tool.
   - *Regression guard:* A restart dry-run test asserting count(open hypotheses+predictions pre-wipe) == count(new voided CALIB# rows), added next to the existing phase_taxonomy coverage assertion.
1. **[med/S/class-A]** Four supplement 'challenges' citations are PubMed search-query URLs dressed as studies — e.g. a link titled 'cost-effectiveness analysis' resolves to pubmed.ncbi.nlm.nih.gov/?term=magnesium+L-threonate+bioavailability+comparison; a skeptic who clicks the dissent (the exact reader the stance feature courts) finds the citation doesn't exist.
   - *Evidence:* curl /api/supplements: 4 SEARCH-URLs (Magnesium L-Threonate, Apigenin, L-Theanine, Lion's Mane), all stance=challenges; rendered publicly via site/assets/js/evidence_body.js:51 ('challenges it' source list).
   - *Path-to-A step:* In the S3 supplement registry (served by lambdas/web/site_api_data.py:1352 handle_supplements), replace each search URL with a real PMID or relabel the entry as 'open question — no direct study found', which is itself an honest stance.
   - *Regression guard:* A registry schema test rejecting any sources[].url containing '?term=' or not matching a pubmed.gov/doi.org article pattern, run in the offline suite.
1. **[med/S/class-A]** Supplement 'science' bullets state supplement-marketing claims as flat facts — '80% of Americans are magnesium deficient' (commonly-cited literature figure is ~45-50% inadequate intake) and 'Only magnesium form shown to cross the blood-brain barrier effectively' (contested) — on a platform whose moat is honest framing.
   - *Evidence:* curl /api/supplements, Magnesium L-Threonate item, science array: '80% of Americans are magnesium deficient'; rendered under 'the work — science, sources' via evidence_body.js:51.
   - *Path-to-A step:* Sweep the registry's science bullets with a citation-or-cut rule: every quantitative population claim gets a hedge + the actual literature figure with its source, or is deleted.
   - *Regression guard:* Extend scripts/check_doc_facts.py-style precision-first pattern to the supplement registry: flag unhedged 'X% of Americans/people' claims lacking an adjacent supporting source entry.
1. **[med/M/class-A]** The public /method/benchmarks page ('Where the numbers sit vs age-band and centenarian targets') reads a partition that has NO writer anywhere in the codebase and is empty even unfiltered, while its empty state promises the data 'will re-populate from the current genesis as each metric accrues enough post-reset readings' — a promise no code can fulfill; the Wednesday chronicle links readers straight to it.
   - *Evidence:* curl /api/benchmark_trends → {"trends": []}; DDB query pk=USER#matthew#SOURCE#benchmarks unfiltered Count=0; grep for SOURCE#benchmarks writers → only the reader (lambdas/web/site_api_observatory.py:2716) and the link (lambdas/emails/wednesday_chronicle_lambda.py:1862); false empty-state copy at site/assets/js/evidence_intelligence.js:281; phase_taxonomy.py:104 classifies it CROSS_PHASE (contradicting the 'from the current genesis' copy).
   - *Path-to-A step:* Per ADR-103, either ship the small writer (age-band targets are static config + weekly personal readouts, fits the existing weekly compute) or retire the page + chronicle hook until one exists; either way fix the empty-state copy to match the taxonomy class.
   - *Regression guard:* An orphan-surface test: every SOURCE# partition read by a registered site-API endpoint must have a writer in lambdas/ (AST grep), or the endpoint is on an explicit retire/pre-launch allowlist.
1. **[low/M/class-A]** The calibration scoreboard ('the honesty moat, made public') shows n=0/nascent platform-wide while the cross-phase ledger it queries holds 23 graded forecast resolutions with confidence and covered/missed outcomes — the one graded track record the platform owns is invisible on its flagship credibility page after every reset (coverage lives only on /api/forecast).
   - *Evidence:* curl /api/calibration → platform n=0; DDB CALIB# query → 23 forecast_resolution rows (resolved 2026-07-06..16); lambdas/calibration_core.py:56-63 outcome_to_binary skips them (no outcome field); curl /api/forecast → coverage 82.6% n=23 — graded, honest, but not on the calibration page.
   - *Path-to-A step:* Fold the forecast interval-coverage block (already computed) into handle_calibration's payload as a separate 'interval forecasts' section, so the scoreboard is never falsely empty across resets.
   - *Regression guard:* Unit test: handle_calibration payload includes forecast coverage whenever the ledger holds forecast_resolution rows; visual-AI-QA prompt for /method/calibration flags 'page claims every forecast graded but shows none' when the ledger is non-empty.
1. **[low/S/class-A]** The public experiment library pads its count with near-duplicate entries — magnesium-before-bed vs magnesium-sleep, no-alcohol-30-days vs eliminate-alcohol-30d, sauna-2x-week vs sauna-2x-week-6wk, gratitude-log vs gratitude-journal — a close reader sees copy-paste inflation in a catalog whose credibility rests on each entry being a distinct pre-registered design.
   - *Evidence:* curl /api/experiments: 71 entries including the four duplicate pairs listed (ids quoted from the payload).
   - *Path-to-A step:* Dedupe the library catalog (merge each pair, keep the better-cited variant).
   - *Regression guard:* Library lint: no two entries whose normalized name token-sets overlap beyond a threshold without an explicit variant_of link.

**Refuted (not defects — kept for the record):**
- ~~Three cognitive-stack supplements (Lion's Mane, Cordyceps, Reishi) lack hoped_outcome and measured_by — the only levers on the page that don't state what data they target or how they'd be falsified, and they are also the weakest-evidence items (evPct 20-35), exactly where the justification contract matters most.~~ — *The field gap reproduces but is deliberate, documented, and shipped: commit 59c8457b (PR #1148, 'Fixes #1116', merged 2026-07-12) states in its message 'the 3 paused cognitive entries deliberately left unannotated.' Lion's Mane, Cordyceps, and Reishi all carry paused:true with pausedReason 'Not in the starting stack — emerging evidence only, held until adherence can be tracked' in config/supplement_registry.json — they are not being taken, so there is no hoped_outcome/measured_by hypothesis to state, and evidence_body.js renders them honest-empty per ADR-104 by explicit design (plus a 'N/M with stated hypotheses' completeness figure disclosing the gap). The finder missed the paused flag and the recorded decision.*

**Path to A:**
1. Grade-or-void at reset: restart_pipeline writes a cycle-stamped 'voided_at_reset' row to the cross-phase calibration ledger for every open hypothesis and coach prediction before tombstoning, closing the survivorship hole in 'every forecast graded' (highest-leverage — it protects the platform's central claim through the very resets the story is about).
1. Citation integrity sweep of the supplement registry: replace the 4 PubMed search-URLs with real PMIDs or honest 'no direct study' labels, and apply a citation-or-cut rule to unhedged population claims like '80% of Americans are magnesium deficient'; add the schema lint so it can't regress.
1. Resolve /method/benchmarks: build the small weekly writer for SOURCE#benchmarks or retire the page and its chronicle hook (ADR-103 retire-candidate call), and fix the empty-state copy that promises re-population no code performs.
1. Surface the existing graded record on the calibration scoreboard: fold forecast interval-coverage (n=23, 82.6% vs 80% target — already computed and honest) into /api/calibration so the honesty-moat page is never falsely empty in the post-reset window.
1. Complete the justification contract: hoped_outcome/measured_by on the 3 cognitive supplements and dedupe the 4 near-identical experiment-library pairs, with schema lints for both.

**Lens notes (coverage):** INSPECTED: live /api/supplements, /api/experiments, /api/calibration, /api/predictions, /api/forecast, /api/benchmark_trends; /data/ and /method/methodology page sources (the methodology editorial is genuinely strong: correlative-only vocabulary, BH-FDR with worked q-value example, confidence tiers by n); lambdas/personal_baselines.py in full (exemplary ADR-105 rule-4 machinery — percentile bands from Matthew's own distribution, MIN_N=30 floor-guard, labelled population fallbacks, documented Gabbett/clinical carve-outs); stats_core.py function survey (min-n gated Pearson, effective-n with autocorrelation, moving-block bootstrap CIs, BH-FDR, Brier + reliability bins — serious statistics for a solo platform); calibration_core.py in full; hypothesis_engine_lambda.py header + expiry paths (frozen machine-checkable test_spec = true pre-registration, deterministic CI verdicts, LLM narrates only); phase_filter.py in full; ADR-089 in full (the cut-benchmark is honest: descriptive-only, no predictor because n_held=0, confidence+n everywhere, a unit test banning failure-count strings); experiments serving contract (frozen design + prereg_url + #1117 why_now provenance); bounded DDB queries on calibration (23 rows), benchmarks (0), weight_episodes (29), training_reference (5); section 13b resolved-findings inventory checked — nothing here re-files R22 content-honesty items. NOT COVERED: browser rendering (JS-heavy pages read via source + API); exhaustive causal-language audit of AI-generated narrative surfaces (chronicle, coach commentary — grounded-generation gate not exercised); MCP tools_benchmark output strings beyond the ADR; scenario-explorer payload audit; the cycle-6 pre-registration document itself (taken as published per shared context; /api/predictions confirms 62 pending pre-registered coach predictions with per-coach hit-rate scaffolding). DATA-MATURITY CAVEATS (not defects): calibration n=0 for binary predictions is the designed day-4 state; live experiment list empty is intentional; forecasts[] empty today is the EWMA min_n gate behaving correctly. UNVERIFIED SUSPICIONS: (1) prior-cycle coach PREDICTION# records likely also tombstoned ungraded — same class as finding 1, mechanism confirmed for hypotheses only; (2) supplement evPct values (66/95/35...) look hand-set with no derivation or source — could not trace their provenance; if they're vibes-percentages they belong in the citation-integrity sweep; (3) the 'qualitative' eval_type predictions (metric:null) may be undecidable by the deterministic evaluator — grading path not traced. The core verdict: the measurement machinery is A-grade and unusually honest; the gaps are at the edges — a citation layer that would embarrass the platform under exactly the skeptic's click, and a reset path that quietly erases open bets from the permanent record.

### reader — First-contact comprehension (cold Reddit reader, live site, desktop 1280 + mobile 390) — **B+**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** A stranger from Reddit gets what/who/why in one screen on every door, every platform term is glossed inline where it first appears, every visible number is self-consistent and date-scoped, and a 4-day-old experiment reads as deliberately young rather than empty or broken.
- **C:** The pages render and the premise is findable, but unexplained jargon, contradictory or stale numbers, or broken-looking text force the reader to do reconciliation work before they can trust the site.
- **F:** A first-time reader cannot tell what the site is or whether it is alive — blank regions, raw template bindings, dead data, or a story surface that looks abandoned.

**Confirmed findings:**

1. **[med/S/class-A]** Reader-facing AI text is hard-cut mid-word with no ellipsis on two of the six doors: the /story/ chronicle excerpt ends '…writing the whole plan down, in public, before any dat' and the /coaching/ 'EACH COACH'S READ' cards end '…1,500 calories alloc', '…The real pressure point i', '…Critically, th', '…across four days—a te' — to a cold reader this reads as a rendering bug on the door aimed at friends/family.
   - *Evidence:* Rendered: https://averagejoematt.com/story/ and /coaching/ (screenshots crop_story_main.png, crop_coaching_cards.png in scratchpad). Feed: /journal/posts.json excerpts are exactly len=300 ('before any dat'); /api/coaching-dashboard position_summary exactly len=200 ('calories alloc'). Source: lambdas/emails/wednesday_chronicle_lambda.py:1795 `[:300].strip()`; lambdas/web/site_api_lambda.py:831-832 `observatory_summary[:200]` / `content[:200]`; also lambdas/intelligence_common.py:1693 `narrative[:200]`. Rendered verbatim by site/assets/js/story.js:582 and site/assets/js/coaching.js:343 (no ellipsis path).
   - *Path-to-A step:* Add one word-boundary truncation helper (cut at last space before N, append '…') and apply it at the three generator sites; front-end needs no change. No new machinery — a pure-function fix inside existing bundled shared code (ADR-103 load-bearing serve path).
   - *Regression guard:* Unit test on the helper + a reader-truth assertion (extend lambdas/reader_truth_qa.py or qa_smoke) that no reader-facing excerpt/summary field ends in a mid-word fragment (regex: ends [a-z] with the preceding source text continuing).
1. **[med/S/class-A]** The hero's first data claim doesn't add up for a numerate reader: 'up 1.6 lb in 4 days (last weigh-in Jul 13)' claims a 4-day trend off a single Day-1 weigh-in, and the stat row shows '1.6 LBS UP / 316 LBS AT LAST WEIGH-IN / start 314' where 316−314=2≠1.6 (316 is 315.6 pre-rounded by the API while the delta uses the unrounded value); '-1.2% TO GOAL' as a negative 'to goal' label compounds the confusion. On a 'proof, not promises' site the very first proof fails mental arithmetic.
   - *Evidence:* https://averagejoematt.com/ rendered text: 'up 1.6 lb in 4 days (last weigh-in Jul 13)', '1.6 LBS UP · 316 LBS AT LAST WEIGH-IN (JUL 13) · -1.2% TO GOAL', 'LATEST 315.6 LB · 1 READING SO FAR'; /data/ shows '› now 316 lb · up 1.6 lb from 314'. curl https://averagejoematt.com/api/vitals → weight_lbs: 316 (pre-rounded), weight_as_of 2026-07-13. Genesis baseline is 314 (cycle-6 override), so the 1.6 delta elapsed in ~0 experiment days, not 4.
   - *Path-to-A step:* One display policy pass on the weight sentence/tiles: show one decimal consistently (315.6 everywhere, or compute the delta from the displayed rounded values), phrase the delta honestly ('up 1.6 lb since the start — one weigh-in so far, Jul 13' — ADR-105), and relabel '-1.2% to goal' (e.g. 'progress −1.2%'). Deterministic code, no AI arithmetic involved.
   - *Regression guard:* qa_smoke/reader-truth check asserting displayed_now − displayed_start == displayed_delta (on the rounded values actually rendered) and that any 'in N days' phrasing has ≥2 weigh-ins spanning N days.
1. **[med/M/class-A]** The /coaching/ 'EACH COACH'S READ' digest cards carry no as-of date and quote Day-1 vitals — 'recovery dip (60% → 44%)', 'Day 1 baselines: recovery score 44%… resting heart rate 62 bpm' — while the cockpit one click away shows recovery 96% and RHR 57 for last night; a cold reader hits a flat vitals contradiction with no way to reconcile it. This is a recurrence/incomplete fix of closed #787 (R22-CONTENT-02: coaches quote contradictory current vitals — reconcile to one as-of read), so reference that issue rather than filing new.
   - *Evidence:* https://averagejoematt.com/coaching/ rendered cards (crop_coaching_cards.png) vs curl https://averagejoematt.com/api/vitals → recovery_pct 96.0, rhr_bpm 57.0, hrv_ms 62.0 (night_of 2026-07-15). /api/coaching-dashboard coaches[].position_summary has no as-of/date field rendered by site/assets/js/coaching.js:335-343. Prior art: gh issue #787 [CLOSED].
   - *Path-to-A step:* Stamp each digest card with its analysis date ('read on Jul 13') — the analysis records in DDB are dated, so plumb the date through /api/coaching-dashboard and render it as a card kicker; completes the #787 fix on the surface it missed. Uses existing data, no new standing machinery.
   - *Regression guard:* Extend lambdas/reader_truth_qa.py (already a nightly ADR-104 surface) with a rule that any coach narrative quoting recovery/HRV/RHR must carry an as-of date, and flag quoted values that diverge >X% from the vitals of that as-of date.
1. **[low/S/class-A]** On the cockpit, the daily line is labeled 'TODAY … WRITTEN 10:07 AM THIS MORNING' and says 'HRV dropped to 34ms', directly above the last-night panel reading 'hrv 62 ms' — both true at different scopes (the line reads yesterday; the panel reads the night of Jul 15) but the permanent UI never says so; the one-time 'NEW HERE?' card is the only place the yesterday-scope is explained, and it never shows again.
   - *Evidence:* https://averagejoematt.com/now/ rendered text: daily line 'HRV dropped to 34ms' (line 93 of desktop1280_now.txt) adjacent to 'LAST NIGHT → TODAY · THE NIGHT OF JUL 15 … hrv 62 ms'; /api/vitals hrv_ms 62.0 confirms the panel; explainer text 'The daily line is the morning brief's one-sentence read of yesterday' appears only in the dismiss-once card ('SHOWN ONCE. IT WON'T INTERRUPT AGAIN.').
   - *Path-to-A step:* Make the scope permanent in the section kicker: 'THE DAILY LINE · YESTERDAY'S READ · WRITTEN 10:07 AM' — one string change in the cockpit template/JS.
   - *Regression guard:* Same reader-truth vitals cross-check as the coach-card guard: a narrative HRV/recovery number adjacent to a live panel must either match the panel's as-of or carry an explicit different date scope.
1. **[low/S/class-B]** Carried-forward levers show pre-genesis dates with no explanation on a site that just told the reader the experiment is 4 days old: cockpit training lever reads 'IN PLAY TODAY … LAST PRESCRIBED 2026-06-26' and /protocols/ supplements read 'AS OF 2026-06-26' — 17 days before Day 1, which reads as staleness rather than deliberate carry-forward (cross_phase class per phase taxonomy).
   - *Evidence:* https://averagejoematt.com/now/ rendered: 'TRAINING Foundation · legs — 6 exercises · 16 sets / LAST PRESCRIBED 2026-06-26' under 'THE LEVERS · IN PLAY TODAY'; https://averagejoematt.com/protocols/ rendered: '21 COMPOUNDS … 2026-06-26 AS OF' while the page banner says 'DAY 4 · WEEK 1, SINCE JULY 13 2026'.
   - *Path-to-A step:* In the reset lifecycle (deploy/restart_pipeline.py verify step), either re-stamp carried-forward cross_phase artifacts at genesis or render a 'carried in from the prep phase' annotation when a lever's as-of predates EXPERIMENT_START_DATE — a one-line conditional in the two render sites plus a pipeline check.
   - *Regression guard:* Add to the restart pipeline's 40-URL verify sweep: any reader-facing 'as of / last prescribed' date earlier than genesis must co-render a carry-forward marker.

**Path to A:**
1. Apply a word-boundary+ellipsis truncation helper at the three generator sites (wednesday_chronicle_lambda.py:1795, site_api_lambda.py:831-832, intelligence_common.py:1693) — kills the only 'site looks broken' moment a first-time reader hits (S).
1. One coherence pass on the hero weight surface: single rounding policy, delta computed from displayed values, 'since the start (one weigh-in)' phrasing instead of 'in 4 days', and a non-negative-ambiguous 'to goal' label (S).
1. Complete #787 on the coaching digest: plumb each coach read's analysis date through /api/coaching-dashboard and stamp the cards; make the daily line's 'yesterday' scope a permanent kicker instead of a dismiss-once explainer (M).
1. Extend the existing nightly reader-truth lane (lambdas/reader_truth_qa.py / qa_smoke — ADR-103 load-bearing, no new infra) with two deterministic rules: no reader-facing string ends mid-word, and displayed weight arithmetic must be internally consistent (S).
1. Annotate pre-genesis 'as of / last prescribed' dates on carried-forward levers via a restart-pipeline verify rule, so cycle N+1's Day-1 reader never sees unexplained stale dates (S).

**Lens notes (coverage):** Coverage: rendered the LIVE site (build 1b57516 confirmed via /version.json, matching the expected deploy) with Playwright, service workers blocked, real JS executed, full scroll-through before capture, at 1280x900 and 390x844 — all six target pages (/, /now/, /data/, /coaching/, /protocols/, /story/). Checked: console errors (zero on all 12 renders), page errors (zero), failed requests (zero), horizontal overflow (none at 390 — the PWA fixed-element trap did not bite), raw mustache (none), empty data-binds (home's 'wave' was a false positive — the score bars are non-text links and render; 'wall-week' is a benign empty footer label; /now/'s 19 empty binds are all inactive-tab or hidden empty-state elements, verified not visible blank regions in screenshots), inline SVG geometry (one zero-bbox SVG on mobile /now/ I could not locate visually — likely a decorative icon; unverified, not filed). Screenshots + text dumps in the session scratchpad (desktop1280_*.png/.txt, mobile390_*.png). NOT covered: subpages and deep tabs (/story/ full pieces, /data/ topics beyond the default weight view, /coaching/ By Coach/Scorecard/Team/Lab-notes tabs, /method/), the subscribe flow, ask-the-data POST, and OG share cards (known pending #1193). Data-maturity: Day-4 thinness is handled genuinely well — the site TELLS the new-cycle story rather than looking empty ('4 DAYS INTO THE EXPERIMENT · WEEK 1' hero, 'DAY 4 · WEEK 1, SINCE JULY 13 2026' banner on every door, the 4-bar wall, chronicle Prologue I/II, '16 board predictions filed', scorecard '62 falsifiable calls · none decided yet — graded daily', 'off the grid — 2 days without a log', /data/'s ranked honest-empty-state capture backlog). Jargon discipline is excellent for a cold reader: 'pillars', 'character level', 'the Third Wall', 'N=1', 'preliminary · n=9' are all glossed inline at first contact; 'cycle'/'genesis'/'wk0' never leak into reader-facing copy. Kill-on-sight scan: no vices named, chronological age deliberately withheld (PhenoAge copy says so), correlative flagging consistent ('correlation, not cause; announced once'), no decorative glow, no AI arithmetic observed. Unverified suspicions: 'Dr. Kai Nakamura' appears as a supplement 'src' on /protocols/ but is not on the cockpit's eight-coach roster (may be introduced on the unvisited Team tab); the Home family panel 'AS OF 2026-07-15' lags one day (plausibly the compute cadence, not checked). Bookmark verdict: yes — the honesty voice and the live instrument are genuinely distinctive; the B+ (not A-) is because the two most-trafficked first-contact surfaces (the hero's first data claim, and visibly cut-off sentences on /story/ and /coaching/) each hand a skeptical newcomer a concrete reason to doubt a site whose entire moat is 'the numbers are real'.

### narrative — Voice & immersion — **B**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every live narrative surface tells one coherent cycle-6 story — reset seams are invisible to a binge reader, restored prequels are internally reconciled, each coach is distinguishable blind (and the voice-fidelity scoreboard has evidence), and no page contradicts another page's account of the same week.
- **C:** Page-by-page prose is competent but cross-surface continuity breaks survive a reset — stale prior-cycle narratives render on live doors, restored posts contradict each other on plan facts, or coach attributions add no voice — so an attentive reader catches the machinery mis-telling the story.
- **F:** Personas are interchangeable or the narrator voice is absent, wiped-cycle narratives dominate live pages as if current, or narrative copy contradicts the platform's own data (hype over honesty, unearned glow).

**Confirmed findings:**

1. **[high/S/class-A]** Elena's persistent-memory reads ignore restart tombstones, so cycle-5 story threads/callbacks feed cycle-6 installments — the pending 2026-07-14 draft already pays off a wiped thread with a phantom citation ('The Sunday walks... documented in the prologue' — neither published prologue post mentions Sunday walks), the exact failure phase_taxonomy #946 names.
   - *Evidence:* lambdas/emails/wednesday_chronicle_lambda.py:2139-2196 (_elena_notebook_block filters only status=='open'/'pending', no tombstone check; same hole in _due_callback_promises ~2230 and lambdas/emails/between_chronicle_lambda.py:158); aws dynamodb query pk=PERSONA#elena → 7 THREAD# status=open + 2 CALLBACK# status=pending, ALL tombstone=True; draft at pk=USER#matthew#SOURCE#chronicle sk=DATE#2026-07-14 content_markdown: 'The Sunday walks that appeared in that earlier data... the pre-experiment period documented in the prologue'; phase_taxonomy.py:203-207 states the wipe rationale this defeats.
   - *Path-to-A step:* Filter tombstoned/non-current-phase records in all three PERSONA#elena read paths (reuse the singleton_visible/#946 pattern already in site_api_coach.py), then regenerate or hand-edit the held 07-14 draft before Matthew approves it — no new machinery, extends the existing ADR-077 tombstone discipline.
   - *Regression guard:* Unit test seeding tombstoned PERSONA#elena THREAD#/CALLBACK#/STANCE# records and asserting _elena_notebook_block() returns '' and _due_callback_promises() returns []; add persona partitions to restart_pipeline's post-wipe verification sweep.
1. **[high/S/class-A]** /api/state_of_matthew serves the tombstoned cycle-5 'Week 1, Day 1' brief (dated 2026-07-12) with available=true, and the /coaching/ Week lens renders it — so on Day 4 of cycle 6 the coaching door narrates LAST cycle's day 1 (incl. a disagreement resolution claiming 'in deficit for weeks') beside a fresh cycle-6 weekly priority.
   - *Evidence:* curl /api/state_of_matthew → {tombstone: true, tombstoned_reason: 'experiment_restart_2026-07-13', available: true, narrative: '# State of Matthew — Week 1, Day 1', date: '2026-07-12'}; lambdas/web/site_api_data.py:2977-2988 (handle_state_of_matthew sets available=True with no tombstone/phase guard, unlike site_api_coach's singleton_visible); site/assets/js/coaching.js:386 gates render only on som.available && som.narrative. Siblings handle_forecast (:2904) and handle_scenarios (:2938) share the unguarded latest-record pattern.
   - *Path-to-A step:* Add the tombstone/phase guard (return available:false when tombstoned) to handle_state_of_matthew, handle_forecast, and handle_scenarios — the guard function already exists in the coach handlers; the front-end's existing honest-empty copy ('written Sundays; honest-absent until the first Sunday run of the cycle') then takes over.
   - *Regression guard:* Extend the #946/#1085 test family: for each singleton endpoint, seed a tombstone=true latest record and assert available:false; add an AI visual-QA prompt on /coaching/ Week lens checking the brief's date belongs to the current cycle.
1. **[med/S/class-A]** The two restored prologue installments contradict each other on the plan's core facts with no reconciliation: Part I says 'He started at 302' on an '1,800-calorie diet with 190 grams of protein daily'; Part II (5 days later, same Prologue) says '314.0 pounds on the morning of Day 1... 1,500 calories... protein floor of 170 grams' — a binge reader hits the contradiction within ten minutes of starting the story.
   - *Evidence:* curl https://averagejoematt.com/journal/posts/week-01/ ('His goal weight is 185 pounds. He started at 302... Phase 1 targets three pounds per week on an 1,800-calorie diet with 190 grams of protein daily') vs /journal/posts/week-02/ ('314.0 pounds on the morning of Day 1... 1,500 calories a day... protein floor of 170 grams'); /api/cycle_compare confirms 302.1 was the CYCLE-1 (April) start — accurate when Part I was drafted, stale as restored.
   - *Path-to-A step:* Add a short dated editor's note to Part I (Margaret's editor's-note device already exists) — 'written before the final plan was frozen; the numbers that count are in Part II' — rather than silently rewriting a dated artifact; honesty-preserving and one paragraph of work.
   - *Regression guard:* restart_pipeline's --keep-chronicle step cross-checks each kept post's plan figures (start weight, kcal, protein) against constants.py/the pre-registration and warns on mismatch before restoring.
1. **[med/S/class-A]** The pending cycle-6 chronicle draft contains a deterministic calendar error — 'The experiment begins on a Sunday in July... On the morning of July 13th' (2026-07-13 was a Monday), repeated in the closer ('said ready on Sunday said diminished on Monday' mapping the 13th→Sunday, 14th→Monday) — a checkable fact the ADR-104 grounding gate let through.
   - *Evidence:* DDB get-item pk=USER#matthew#SOURCE#chronicle sk=DATE#2026-07-14 content_markdown ('The experiment begins on a Sunday in July... On the morning of July 13th, Matthew's recovery score reads 90 percent'); python datetime.date(2026,7,13) → Monday. Draft is approval-gated (status=draft) so not yet reader-visible.
   - *Path-to-A step:* Add a deterministic date→weekday map for the covered window to the chronicle's grounding-facts bundle (ADR-104 gate already exists; this extends its evidence, not its machinery), and correct the held draft before approval.
   - *Regression guard:* A grounded-generation check that regexes weekday+date pairs in generated narrative and verifies them against the calendar — deterministic, zero AI cost.
1. **[low/S/class-B]** phase_taxonomy documents two contradictory intents for Elena: line 207 classifies PERSONA#elena EXPERIMENT_SCOPED (wiped — which is what actually happened), while the PERSONA#* CROSS_PHASE rule's comment claims 'the cycle-5 reset carried Elena straight into EP0' and that wiping personas 'would be a new decision' — the specific rule shadows the general one, the comment is now false, and even this review's briefing inherited the wrong claim ('PERSONA#elena carried across deliberately').
   - *Evidence:* lambdas/phase_taxonomy.py:203-207 (elena EXPERIMENT_SCOPED, #946 rationale) vs :256-261 (PERSONA#* CROSS_PHASE, 'carried Elena straight into EP0'); DDB query pk=PERSONA#elena → all 15 records tombstone=True, phase=pilot (she was NOT carried).
   - *Path-to-A step:* Resolve the deferred #930 PERSONA#* ruling and rewrite the CROSS_PHASE comment to name margaret-only scope, so the registry — the single source the wipe derives from — carries one truthful intent per persona.
   - *Regression guard:* A registry self-test asserting classify('PERSONA#elena', ...) matches the documented per-persona intent table, plus the existing doc-facts gate style check that comments naming a classification agree with the rule that actually fires.

**Refuted (not defects — kept for the record):**
- ~~The 16 pre-registration predictions are voice-interchangeable — all 8 coaches write the identical clinical register ('will be maintained on at least 5 out of 7 days... as the structural constraint...') immediately after Part I promises distinct personalities ('Dr. Webb is blunt. Dr. Park is gentle but firm'); the attributions add no character, and the voice-fidelity scoreboard has zero evidence either way (n=0).~~ — *The evidence reproduces (I extracted all 16 prediction texts — they do share one clinical register; Part I does promise 'Dr. Webb is blunt. Dr. Park is gentle but firm'; /api/voice_fidelity → n=0 insufficient_data; daily coach endpoints are distinct), but the uniformity is intentional design, not a defect. The generator prompt (deploy/seed_genesis_preregistration.py _GEN_SYSTEM) contains ZERO voice/persona instruction — it deliberately enforces the ADR-105 rigor bar: every claim must carry a concrete number/timeframe verbatim from the plan, be machine-checkable, name confidence honestly, and never invent baselines; a deterministic cross-coach Jaccard dedup guards content, not style. These are graded formal records feeding the prediction-grading engine — the artifact self-describes as 'a commitment device... Nothing here can be quietly revised later. That is the point.' It also went through the documented human dry-run review before freezing (the prereg dry-run-review convention) and was published as-is. Voice belongs to the narrative surfaces, which the finder concedes are genuinely distinct. n=0 on the cross-phase voice-fidelity scoreboard means the monthly harness hasn't sampled yet, not that voice failed. A style-uniformity observation about a deliberately clinical, reviewed rigor artifact is a design posture, not a bug.*
- ~~Microcopy drift at the seams: the daily hero line grades 1,934 steps as 'real movement progress' against a plan of ~3 miles/day (unearned glow softened only by its own caveat), and the podcast surfaces say 'Matt' ('Meet Elena, meet Matt', 'from Matt himself') where every prose surface says 'Matthew' — small cracks in an otherwise remarkably disciplined copy voice.~~ — *Both quotes reproduce live (public_stats.json elena_hero_line 'Steps nearly doubled to 1,934 — real movement progress — but HRV dropped to 34ms and zero habits logged...'; episodes.json 'Meet Elena, meet Matt'; wk0.transcript.json Marsh 'came from Matt himself'), but neither is a defect. The hero line states the honest number and counterweights in the same sentence (HRV drop + zero habits + 'the body is working harder than the system knows') — 'nearly doubled' is a true relative statement on Day 2-3 of a cycle, consistent with the ADR-104 honest-numbers posture; grading it 'unearned glow' is taste, not a rule violation. 'Matt' IS the brand — the site is averagejoematt.com ('Average Joe Matt', a self-applied nickname Part I itself discusses) — used in informal spoken-register podcast copy shipped through the reviewed ADR-135 no-touch pipeline; no documented convention mandates 'Matthew' on every surface. Subjective microcopy preference, not a crack.*

**Path to A:**
1. Close the tombstone seam in one pass: guard the three unguarded singleton endpoints (state_of_matthew/forecast/scenarios, site_api_data.py:2904/2938/2977) and the three PERSONA#elena read paths (chronicle notebook, Margaret callbacks, between-chronicle stance) with the existing singleton_visible pattern — all S-effort, zero new machinery (ADR-103: extends load-bearing ADR-077 discipline).
1. Before approving the held 'Day One, Again' draft, regenerate it post-fix or hand-edit: remove the phantom 'documented in the prologue' Sunday-walks callback and fix the Sunday/Monday weekday error — the approval gate exists precisely for this.
1. Reconcile Prologue Part I with Part II via a dated Margaret editor's note (302→314, 1800→1500, 190→170) instead of silent revision — preserves the honesty posture while removing the binge-reader contradiction.
1. Add deterministic calendar facts (genesis weekday, date→weekday map) and plan constants to the chronicle's ADR-104 grounding bundle so date and plan-figure errors fail the gate instead of reaching drafts.
1. Give the coaches their voices on the flagship prereg artifact: one in-voice rationale sentence per prediction (claim text stays verbatim for gradability), and feed prereg passages into the voice-fidelity harness's blind sample when its first cycle-6 run lands.

**Lens notes (coverage):** INSPECTED (live curls + code + read-only DDB): /story/ hub, both restored prequel posts full-text (week-01, week-02 — week-02 IS the cycle-6 pre-registration post, present and excellent: 16 predictions, confidence + grading windows, 'come back and grade us' with a working /method/predictions/ link showing 62 pending), /journal/posts.json, /panelcast/episodes.json + EP0 full transcript (Elena's podcast voice is consistent with her chronicle persona, appropriately punchier for audio), /api/coaches (9, disclosure line present), /api/coach_team, /api/coaching-dashboard, /api/coach/{sleep,nutrition,mind,physical} recent outputs, /api/state_of_matthew (+ its handler and FE render path), /api/voice_fidelity, /api/panel_ledger, /api/predictions, /api/cycle_compare, home + 404 copy, PERSONA#elena DDB partition, the unpublished chronicle draft DATE#2026-07-14, phase_taxonomy registry, and the chronicle lambda's memory path. EDITOR'S VERDICT ON BINGE-ABILITY: genuinely high — the Elena Voss frame ('what happens when a person losing the fight against himself lets algorithms into the ring'), the commitment-device prereg post, the honest empty states ('No bets on the board yet', 'insufficient_data'), and distinct daily coach registers (Reeves' 'more like your Tuesday' is a keeper line) would carry a reader; the defects are reset-seam continuity leaks, not craft failures. NOT COVERED: email narrative surfaces (daily brief/debrief prose), /story/timeline, /story/agents, the first-person journal, the read-aloud chronicle podcast feed, all 62 prediction texts, coach relationship/memoir content, portrait/visual identity. DATA-MATURITY CAVEATS (not filed as defects): voice-fidelity scoreboard n=0 — the machinery is well-designed (blind 3-judge panel, deterministic scoring, honest disclosure) but has NO evidence yet on distinctness (monthly cron, tier>=1 pauses it; we are tier 1); all 8 position_summaries empty on Day 4 with an honest empty state (its 'refresh with the next daily sync' phrasing will read stale if the gap persists another few days); board answers empty post-reset, honestly. Elena continuity verdict: her PERSONA state was WIPED (all tombstoned), contradicting the briefing's 'carried across deliberately' — per #946 the wipe is the sanctioned design, and the fresh draft's voice remains recognizably Elena without the state, but the unguarded notebook read means what continuity the draft DOES show is partly contaminated rather than designed. UNVERIFIED SUSPICIONS: (a) /api/cycle_compare shows cycle 5 (genesis 07-12) with days_with_data=4 and +1.9 lb though cycle 6 began 07-13 — possible window-overlap bug, belongs to a data-integrity lens; (b) scattered empty daily coach summaries (Park/Reyes 07-15, Reeves 07-16) may be ADR-108 quality-gate holds — FE filters them cleanly, but 2-day holes in the by-coach dated trail are reader-visible; (c) Prologue I says 'Matthew is in his late thirties' — a decade band, not the chronological age the kill-rule and PhenoAge policy prohibit, but close enough to warrant the operator's explicit sign-off; (d) the draft title 'Day One, Again' acknowledges prior attempts, in tension with the stated presentation rule that only /data/cycles/ does — editorially I'd keep it (it's honest and a strong title), but the rule and the practice should agree. KILL-ON-SIGHT sweep: no causal claims stated non-correlatively on inspected surfaces (disclosures consistently present), no vice naming (Prologue I references 'a personal short list' without naming — compliant), no genome identifiers, no AI arithmetic claims (SoM disclosure explicitly deterministic-first).

### observability — Telemetry & alerting — **B**

**Rubric anchors** (re-apply verbatim on the next run):
- **A:** Every alarm state is honest (red = a real, actionable condition; nothing permanently red or structurally unclearable), every watchdog's producer is verified actually emitting (not just designed to), and the remediation/drift loop demonstrably closes or explicitly hands off every signal it sees — all within the $85 ceiling, urgent-vs-digest split intact, solo operator paged for what matters and only that.
- **C:** The alarm inventory is well-designed on paper but one or more watchdog producers are dead (IAM/emission failures swallowed as warnings), red alarms sit unactioned for a week-plus, or the self-healing loop completes triage only intermittently while reporting green — governance expressed but not achieved.
- **F:** Alarms missing or routed to nowhere (no live SNS subscription), DLQ pileup or a dead ingestion source discoverable only by manual audit, budget hard-stop silent, log groups unbounded — the operator learns about failures from the website being wrong.

**Confirmed findings:**

1. **[high/S/class-A]** coach-prediction-evaluator's IAM role lacks cloudwatch:PutMetricData, so the #727 scientific-liveness gauge (DaysSinceLastDecided/DecidedCount/GradableCount) has never successfully emitted — every daily run logs a swallowed AccessDenied WARNING, and the grading-stalled alarm has sat red since 2026-07-06 and can never clear (treat_missing=BREACHING with a permanently absent metric). The broken state is codified in the repo: role_policies.py compute_coach_prediction_evaluator() has no CloudWatchMetrics statement while the lambda calls put_metric_data — the exact class role_policies.py:824 documents fixing for coach_state_updater in May.
   - *Evidence:* aws logs filter-log-events /aws/lambda/coach-prediction-evaluator: '[liveness] metric emit failed (non-fatal): AccessDenied ... not authorized to perform: cloudwatch:PutMetricData' daily incl. 2026-07-16T16:00:39Z; get-metric-statistics LifePlatform/Predictions DaysSinceLastDecided 2026-06-15→07-16 returns []; lambdas/coach/coach_prediction_evaluator.py:1457; cdk/stacks/role_policies.py:788-817 (no PutMetricData grant); describe-alarms: grading-stalled ALARM since 2026-07-06
   - *Path-to-A step:* Add the same CloudWatchMetrics PolicyStatement coach_state_updater already carries (role_policies.py:838) to compute_coach_prediction_evaluator(), cdk deploy LifePlatformCompute, then confirm DaysSinceLastDecided lands and grading-stalled transitions per real state. Zero new machinery — a one-statement grant on an existing load-bearing alarm (ADR-103: monitoring is load-bearing).
   - *Regression guard:* A static test that pairs every lambdas/ module calling put_metric_data with a PutMetricData grant in its role_policies.py function (AST-scan both sides), so an ungranted emitter fails CI at PR time — turning the reference_iam_parity_codified_broken_state lesson into a gate.
1. **[high/M/class-A]** The self-healing loop is mostly not closing: 3 of the last 4 remediation-agent runs (Jul 8, 13, 15) ended with EVERY signal in 'untriaged' (needs_human=0, prs=0, auto_fixed=0) and a truncated _raw reasoning tail — the agent's structured triage failed to complete — yet every workflow run concludes 'success'. grading-stalled has been presented to 4 consecutive runs without ever being classified, fixed, or handed off, even though role_policies.py is on the automerge ALLOWLIST (this is precisely the fix class the agent exists for).
   - *Evidence:* s3://matthew-life-platform/remediation-log/2026/07/15/161053.json (mode=shadow, untriaged=4 incl. grading-stalled, needs_human=[], _raw cut mid-sentence), .../07/13/170248.json (untriaged=5), .../07/08/162305.json (untriaged=8); only .../07/10/164934.json completed (stale=5). gh run list --workflow remediation-agent.yml: all 12 recent runs 'success'. Shadow mode itself is sanctioned (ADR-129, docs/DECISIONS.md:3482) and is NOT the finding.
   - *Path-to-A step:* Make incomplete triage loud: after remediation/agent.py writes the report, fail (or red-annotate) the workflow step when the report retains untriaged items with a truncated _raw, and diagnose why the agent's output stops parsing (token/turn budget on the Bedrock loop is the likely culprit given the mid-sentence cuts). No new standing infra — hardening an existing load-bearing loop (ADR-064/103).
   - *Regression guard:* A workflow assertion (or the existing dispatcher-errors alarm's GH-side twin): exit non-zero when report['untriaged'] is non-empty AND '_raw' is present; plus a unit test that a truncated agent transcript yields a failed run, not a green one.
1. **[med/S/class-A]** The drift sentinel's flagship check is dead-on-arrival despite the 2026-07-12 'IAM-unblocked' claim: its first-ever run (2026-07-13) shows cfn_drift AccessDenied on ALL 9 stacks because the role grants cloudformation:DetectStackDrift but not DetectStackResourceDrift (which the drift operation fans out to). The failure is buried as status='degraded' inside a continue-on-error step, so it will stay dead silently. (Run history is non-empty, so this is not a #342 refile — it is one specific missing IAM action, filed with reference to #342.)
   - *Evidence:* s3://matthew-life-platform/drift-log/latest.json (generated 2026-07-13T17:01Z): every stack 'AccessDenied ... not authorized to perform: cloudformation:DetectStackResourceDrift'; aws iam get-role-policy github-actions-remediation-role shows DetectStackDrift/DescribeStackDriftDetectionStatus/DescribeStackResourceDrifts granted but NOT DetectStackResourceDrift; .github/workflows/remediation-agent.yml:71 (continue-on-error)
   - *Path-to-A step:* Add cloudformation:DetectStackResourceDrift to the remediation role (infra/iam), and change drift_sentinel.py to escalate an all-stacks-AccessDenied cfn_drift result to a needs-human signal instead of 'degraded' — a dead capability must not report as a soft state (the IAM-parity lesson: verify the capability, not the parity).
   - *Regression guard:* A sentinel self-check: if every stack in cfn_drift errored with AccessDenied, write status='error' and emit it as a first-class agent signal; unit test in tests/test_drift_sentinel.py planting the AccessDenied response and asserting escalation.
1. **[med/S/class-B]** A stale unmanaged twin of email-subscriber is live in us-west-2 (last modified 2026-05-30) while the managed copy deploys in us-east-1 (modified 2026-07-13) — the drift sentinel correctly flagged it as an orphan (101 live vs 100 managed), but the shadow-mode agent dismissed it as 'FALSE POSITIVE — defined in web_stack.py' without checking regions, so the sentinel's one real catch was triaged away wrongly; the us-west-2 email-subscriber-errors alarm (last transitioned 2026-03-29) watches this dead copy.
   - *Evidence:* drift-log/latest.json orphan_functions=['email-subscriber']; aws lambda get-function-configuration email-subscriber: us-west-2 Modified 2026-05-30 vs us-east-1 Modified 2026-07-13; remediation-log/2026/07/15/161053.json _raw: 'This is a FALSE POSITIVE. The email-subscriber Lambda is defined in web_stack.py'; monitoring_stack.py:800-802 documents email-subscriber as us-east-1
   - *Path-to-A step:* Human-verify the us-west-2 copy has no live triggers (SES receipt rules / event sources), then delete it and its orphaned us-west-2 email-subscriber-errors alarm via the existing cloudwatch_retire_orphans.sh pattern; teach the agent's orphan triage to compare regions before declaring a false positive.
   - *Regression guard:* The drift sentinel's orphan check already catches this class — the guard needed is on triage: a test in the agent's triage prompt/harness asserting a live-in-region function absent from that region's stacks cannot be dismissed by a cross-region CDK definition.
1. **[med/S/class-A]** life-platform-alert-digest is the single point through which every digest-routed alarm (the majority of the 67) becomes visible, and it is itself completely unwatched: no Errors alarm, no DLQ, and no depth/age alarm on life-platform-alerts-digest-queue — if it starts failing, most alarm notifications pile up in the queue silently (partially mitigated: the M/W/F agent reads alarm states directly, and urgent-topic alarms bypass it).
   - *Evidence:* aws cloudwatch describe-alarms FunctionName-dimension map: no alarm for life-platform-alert-digest; lambda list-functions: alert-digest has no DeadLetterConfig; describe-alarms contains no queue alarm for alerts-digest-queue (only life-platform-ingestion-dlq-messages); sns list-subscriptions-by-topic life-platform-alerts-digest → sole subscriber is the SQS queue; queue holds 3 messages, drained 1×/day (Invocations=1 daily)
   - *Path-to-A step:* Add two alarms in monitoring_stack.py routed to the URGENT topic (the digest path can't announce its own death): alert-digest Errors >= 1/day, and ApproximateAgeOfOldestMessage on the digest queue > 48h. ADR-103: the notification path is load-bearing; cost ~$0.20/mo.
   - *Regression guard:* Extend the existing integration alarm-inventory test (tests/test_integration_aws.py I-series) to assert the digest consumer and digest queue each have a live alarm — the same pattern that already pins the ingestion DLQ alarm.
1. **[low/S/class-A]** Doc drift on the remediation cadence: CLAUDE.md (and the ADR-064 section) say the remediation agent runs '~07:45 PT daily', but the workflow cron is '45 14 * * 1,3,5' — Mon/Wed/Fri only, confirmed by the run history. Small, but cadence determines how long a red alarm waits for triage (up to ~3 days, not 1), and doc accuracy is this platform's stated moat.
   - *Evidence:* .github/workflows/remediation-agent.yml:13 'cron: "45 14 * * 1,3,5"' vs CLAUDE.md 'Self-healing Remediation Agent' section '~07:45 PT daily'; gh run list dates land only on Mon/Wed/Fri (Jul 15, 13, 10, 8, 6, 3, 1)
   - *Path-to-A step:* Correct CLAUDE.md/DECISIONS.md/RUNBOOK to 'Mon/Wed/Fri ~07:45 PT' (or restore daily if that was the intent — pick one and make docs match live).
   - *Regression guard:* A check_doc_facts.py rule whose ground truth is the parsed cron in remediation-agent.yml, failing on any 'daily' claim about the agent — the same generalized stale-fact gate PR #1189 just built.

**Path to A:**
1. Grant cloudwatch:PutMetricData to compute_coach_prediction_evaluator() in role_policies.py (mirror coach_state_updater's CloudWatchMetrics statement) and verify grading-stalled can actually clear — the only red alarm on the board becomes honest again (S).
1. Add a CI test pairing every put_metric_data call site in lambdas/ with a PutMetricData grant in its role-policy function, so a silently-AccessDenied emitter can never ship again (S).
1. Make incomplete agent triage a red outcome: fail the remediation workflow step when the report retains untriaged items with a truncated _raw, and fix the underlying output-truncation so 4/4 runs complete triage instead of 1/4 (M).
1. Add cloudformation:DetectStackResourceDrift to github-actions-remediation-role and escalate an all-AccessDenied cfn_drift result to needs-human — the weekly drift sweep's core check goes from dead-but-degraded to alive (S).
1. Alarm the digest path's own death: alert-digest Errors + digest-queue oldest-message-age → the URGENT topic (it cannot announce its own failure through itself), and extend the integration alarm-inventory test to pin both (S).

**Lens notes (coverage):** INSPECTED: full monitoring_stack.py (67 live metric alarms enumerated, urgent-vs-digest routing verified per alarm); live alarm states (66 OK / 1 ALARM / 0 INSUFFICIENT_DATA — the missing-data heartbeat design deliberately avoids INSUFFICIENT_DATA limbo, which is genuinely good); coverage math for the ~95 real lambdas (71 with DLQs feeding the depth-alarmed ingestion-dlq, 20 with direct alarms, plus 8 absence-heartbeats on the watchdog producers — the residual silent-fail set is small: alert-digest [filed], traffic-digest and insight-email-parser [low value surfaces, not filed]); both SQS queues (DLQ depth 0); the full SNS paging chain end-to-end (urgent topic has a live email subscription to awsdev@ + the remediation dispatcher; digest topic → SQS → daily alert-digest drain confirmed by invocations); log retention (0 of all log groups unlimited, 30d on all four sampled; evaluator logs are structured JSON with level/source/correlation_id); MCP EMF (emission path works — ToolInvocations datapoints as recent as Jul 6 under ToolName dims — but MCP usage is ~zero for the last 10 days, so any #395-style EMF-based pruning decision right now would run on an empty window: data-maturity caveat, not a defect); qa-smoke (invoked daily incl. Jul 12-15) and freshness-checker (its heartbeat alarms OK, which structurally proves it emitted within 2 days); drift sentinel run history (non-empty — ran Mon Jul 13, so per instructions nothing refiled to #342; the specific DetectStackResourceDrift gap is new) and 12 remediation-agent runs with 4 report payloads read from S3. NOT COVERED: us-east-1 alarms (web_stack — site-api-ai/subscriber alarms there not enumerated); SES delivery of the digest email itself (verified the lambda runs, not the inbox); per-threshold calibration against personal variance (ADR-105) for the token/duration alarms; the Fresh-Eyes and golden-brief eval workflows. UNVERIFIED SUSPICIONS: (1) the coherence sentinel's two live warns (100%-qualitative predictions 'extraction may be drifting', and experiment_arc_week_count=4 vs genesis ~1 on day 4 of cycle 6) look like a real post-reset counter bug plus a prereg-extraction gradability question — both belong to the intelligence/reset lens, but note the telemetry DID catch them and the broken triage loop is why they sit unactioned; (2) grading-stalled red state is 'honest by accident' — red for the right macro-reason (no science signal) but for the wrong micro-reason (IAM), and once fixed it may re-fire legitimately early each cycle since the last-decided marker is cross-cycle system-state — worth watching after the grant lands. DATA-MATURITY: day 4 of cycle 6 means near-zero gradable predictions and near-zero MCP traffic are expected; neither was graded as a defect. OVERALL: the alarm ARCHITECTURE is A-caliber for a solo $85/mo operator (absence-is-failure heartbeats, watchdog-watchers, documented noise retirement with rationale, urgent/digest discipline); the grade is B because the live truth diverges from the expressed design in exactly the way the platform's own July critique named — the newest liveness signal never worked, the one red alarm can never clear, the drift sweep's core check is dead, and the self-healing loop completes triage in 1 of 4 runs while reporting green.

---

## Ledger to A (per-area, ≤5 actions each)

The consolidated remediation ledger — each area's ranked path from its current grade to an A, as written by its grader.
Actions referencing already-tracked issues cite them; nothing here has been filed or shipped yet.

**a11y (A- → A):**
1. Override --alert in both light-mode token blocks to an AA-compliant oxblood (>=4.5:1 on #F4EFE4) — the one confirmed AA text failure on the site (tokens.css:271-301).
1. Add a stdlib token-contrast pytest asserting the documented ink/ember/alert pairs meet AA in BOTH themes, wired into the existing CI pytest job — converts the hand-written AA comments into a gate (zero standing cost, ADR-103-clean).
1. Close the #1010 tap-floor gap on .wave a.bar (route touch through the shared data-cpts readout or add the #1017-style invisible hit expansion) and add the selector to visual_qa's TAP_TARGET_SEL, promoting that audit from advisory to gating.
1. Migrate the home .dx-tab strip from the aria-pressed loop (story.js:593) to the existing wireTabList module, matching dispatches.js/coaching.js.
1. Expose theme-toggle state (aria-pressed or name swap) in theme.js and assert it in the Playwright sweep.

**dataviz (A- → A):**
1. Fix the home waveform's two encoding lies (findings 1+2): neutral 'mid' tier when spread is below threshold (or honor the API's served per-day color), and extend the meaningfulSpread guard from color to bar height — this is the front-page hero and the only live contradictory encoding found.
1. Move constellation edge r/n/significance onto the shared data-cpts readout so the evidence behind the home page's strongest visual is reachable on touch and keyboard (one interaction system, as #583 intended).
1. Harden the two leaky primitives at the source: correlationChip renders an explicit n-absence marker instead of a bare coefficient, and sufficiencyBars prints the real pct while clamping only the bar width.
1. Reserve lineChart's area fill for zero-based domains (one conditional), retiring the last non-zero-baseline area encoding.
1. Add a tiny node-run charts honesty test file (no deps, offline — ADR-103: zero standing infra) locking the contracts that make this area near-A: <4-point refusal, n display on every correlation mark, clamp-label truth, tier defaults; wire it into the existing offline suite.

**designer (A- → A):**
1. Ship the shared SVG-text floor-scaling helper (the #1017 sizeConstellation pattern generalized in charts.js) and apply it to the autonomic 2x2, the pillar radar, the tier emblem, and the body-map captions; add the effective-px SVG-text audit to tests/visual_qa.py so the floor is measured, not asserted by comment.
1. Retokenize the two LIVE off-palette accents: head-coach lead markers -> var(--coach)/ember (story.css:639-654), vice-hold green -> the §4 ember/muted-ink grammar (evidence.css:240-250).
1. Extend scripts/check_css_tokens.py to sweep all 8 sheets and police raw hex colors + the §10.1 nine-number breakpoint set (the doc already contains the exact grep — promote it to the test), fixing story.css:582's rogue 520px in the same PR.
1. Delete the five dead off-palette status selector families (.pl-*, .cr-confirmed, .sc-confirmed, .hb7-*) before any renderer can adopt the dormant red-for-lost encoding.
1. Re-scope the fs-ok sanction policy so 'SVG viewBox units' is no longer a self-service exemption from the type floor.

**devex (A- → A):**
1. Make the experiment anchor machine-synced everywhere it appears: add CLAUDE.md:145 to sync_doc_metadata RULES (source: lambdas/constants.py) and a genesis_date fact to check_doc_facts.py, with a planted-violation test — kills the class where the restart pipeline updates code+docs/ but the top agent brief silently keeps the old cycle.
1. One pass over .claude/commands/deploy.md: rewrite Mode 1 (site) as a one-line pointer to CONVENTIONS §2, correct LifePlatformOperational→LifePlatformServe, fix the CLAUDE.md command signatures (<source-file> args) — the meta-rule already mandates pointers over restatements; this doc just predates it.
1. Close the tombstone-reflex gap for non-count retirements: #750 (approval-gated site path) and #793 (Operational owning site-api) each retired a concept without adding its tombstone rule — add both rules now, and add a 'ship-path or ownership changed? → tombstone rule in this PR' line to the PR template's existing Docs-impact checklist (zero standing cost, extends the CONVENTIONS §8 contract already in force).
1. Add memory-index hygiene (orphan + broken-link one-liner) and 'main tree back on main' to the /wrap skill checklist — both are two-line process gates on machinery that already exists.
1. No new standing infrastructure is needed: all four deploy paths verifiably stage through build_bundle.py, CI pins match requirements-dev.txt, the gate ordering doc matches ci-cd.yml, and the deploy-critical lane runs green offline in 3.79s — the remaining gap is purely that drift gates were taught counts but not process/ownership/date facts.

**principal (A- → A):**
1. Ratchet the coverage floor 25→40 in ci-cd.yml, fix the stale '~28% baseline' comment (measured 45.58% on green main run 29270686464), and add a slack-warning line to the existing gate (S, extends ADR-080 — no new machinery).
1. Consolidate the ~10 divergent _to_decimal forks onto lambdas/numeric.floats_to_decimal, adding character_engine's NaN/Inf→None guard to the canonical, with a structural test banning new local definitions (M, closes the deferred R13-F10 for real).
1. Delete the 269 verified-dead lines in intelligence_common.py and the 2 archive-only character_engine fetchers (S, shrinks the every-bundle module ~14%).
1. Move site_api_data.py's vitals-cluster and intelligence-cluster handlers to their existing sibling modules and regenerate its stale docstring endpoint list (M, finishes the decomposition the module itself documents).
1. Fix the advisory-vs-ENFORCED mypy comment contradiction in mypy.ini:2 and requirements-dev.txt:23, and add a gate-status rule to check_doc_facts.py (S).

**security (A- → A):**
1. Fix client-IP extraction to use the CloudFront-appended last X-Forwarded-For hop in a shared helper across site_api_social + email_subscriber, with a unit test — this is the one enforcement boundary currently spoofable, and closing it makes the DDB-backed rate limits actually bind.
1. Apply the PHY-01 mitigation (band published PhenoAge to nearest 5 and/or drivers direction-only) to close the age-inversion residual and make the chronological-age absolute truly non-deducible.
1. Add a standing privacy-regression test that curls the live public JSON endpoints and greps for chronological age / birth year / genome IDs / substance names, so any future field addition that leaks an absolute reds the build.

**ai-quality (B+ → A):**
1. Guard the three unguarded latest-DATE# public readers (state_of_matthew, forecast, scenarios) with the #946 tombstone/phase predicate and add the post-reset 'no tombstone:true on any public payload' smoke assertion — this closes the one live reader-facing honesty breach (S effort).
1. Add the deterministic band-adjective contradiction class to grounding_guard so 'strong recovery — 44%' fires a finding: value→band mapping from the platform's own computed vocabulary, thresholds from personal baselines (ADR-105), no new AI calls.
1. Generalize the chronicle's date allow-list cross-check into grounding_findings as a fabricated_date token class, killing the invented-date hallucination surface the number gate structurally cannot see.
1. Extend the allow-list gate to the 8 ungated internal email narrators by reusing _ground_legacy_output — pure reuse of the bundled harness, keeping ADR-104's 'every AI narrative surface' claim true rather than aspirational.
1. Make gate coverage structural: a wiring test that every model-calling module imports a grounding gate or sits on an audited exemption ledger, so the next new narrative surface cannot ship ungated by omission (the same reflex test_wiring_coverage.py gives MCP tools).

**cost (B+ → A):**
1. Make the public inference receipt read the effective ceiling + surge state from the /life-platform/budget-breakdown SSM param instead of the hardcoded 75, and fix its '$75 ceiling' note text (lambdas/web/site_api_data.py:4002; S, reuses existing machinery).
1. Replace the hand-maintained monthly_cost '~$60' in PLATFORM_STATS with a breakdown-derived figure (or '~$80' pinned by a truth test) so the public cost page matches the ~$80 actual run-rate (site_api_common.py:112; S).
1. Sync cost_governor's _TIER_LABELS and docstring tier table to budget_guard's ADR-125 ladder and pin them with a test against _FEATURE_CUTOFF, so tier-change alert emails describe what actually paused (S).
1. Sweep the remaining stale literals in one pass: bedrock_client.py:221 '$75' message, the three 'hourly' cadence claims (governor docstring, budget_guard comment, /method/build editorial), and the chronicle lambda's allow('chronicle') revert per its own TODO (S).
1. Extend scripts/check_doc_facts.py's budget ground-truth rule to also scan lambdas/ source string literals (not just docs) for stale dollar ceilings and governor-cadence claims — the structural fix that prevents this class recurring (B; no new standing infra, extends the existing gate).

**cpo (B+ → A):**
1. Fail predict-the-week closed on week mismatch (server guard in site_api_social._predict_subject: week_id must equal the current ISO week) and give site/config/current_challenge.json a real lifecycle — cleared by restart_pipeline.py, re-seeded weekly — with a nightly qa_smoke assertion; the flagship cockpit hook must be alive-or-honestly-hidden, never mis-live (ADR-103: hardening an existing load-bearing loop, no new infra).
1. Slash-normalize extensionless viewer paths in the already-deployed v4-redirects CloudFront function so /data, /cockpit etc. 301 to their slashed doors, plus a bare-URL assertion in deploy/smoke_test_site.sh — shared links are the Reddit-newcomer front door and today they 404.
1. Ship the early-cycle 'start six vs the five before it' beat on Home/cockpit from the already-live /api/cycle_compare (self-hides after ~day 21) — turn the reset from a legible fact into the season-premiere hook it deserves to be.
1. Add a deterministic cadence line to the Chronicle and Podcast lists ('the next installment lands Wednesday' — derived from the cron schedule, honest-pending when withheld) so a returning follower knows exactly when to come back; today returnability leans entirely on subscribe/RSS.
1. Regenerate the Prologue Part II read-aloud episode against the re-anchored article date and add the date-join parity check, so curated prequels kept across a reset never strand their audio.

**cto (B+ → A):**
1. Grant cloudwatch:PutMetricData to compute_coach_prediction_evaluator() and deploy LifePlatformCompute, then confirm a DaysSinceLastDecided datapoint lands within 24h (capability check, not parity check); seed the last-decided marker at reset so each new cycle doesn't open with a guaranteed false 14-day ALARM.
1. Ship the put_metric_data↔IAM lockstep test (test_site_api_write_scope.py pattern) — third occurrence of this exact fail-soft AccessDenied class; ADR-103 frame: deploy-guardrail, load-bearing, earned by a real incident.
1. Add alarm-aging (>72h in ALARM = named needs-human digest line) to the existing remediation agent triage — closes the 'digest's only consumer is one human' single point of failure with zero new standing infrastructure.
1. Run the ARCHITECTURE.md accuracy pass on the six verified stale claims, then convert the compute/email cron table into a sync_doc_metadata-generated artifact and add a docs-vs-CDK cron-diff rule to check_doc_facts.py so this recurrent class (R18-F01, R19-F02, now again) becomes structurally impossible.
1. Reconcile the two stale ADR-103 ledger rows (MCP prune executed #395; Panel live per ADR-135) with dated one-line edits and add their phrasings to the tombstone scan.

**data-architect (B+ → A):**
1. Fix the wipe's re-stamp clobbering (deploy/restart_intelligence_wipe.py): if_not_exists on cycle/tombstoned_at or skip already-tombstoned items in 'all' mode, with a double-reset idempotency unit test — one small diff, closes the highest-severity provenance bug.
1. One-off backfill (dry-run first) re-deriving true cycle stamps on archived records by date-inference against the known cycle genesis dates, restoring the 'archive navigable by reset generation' property that live data currently violates.
1. Resolve the write-time-stamping doc-vs-behavior drift: either ship the shared phase+cycle stamp helper into the COACH#/ENSEMBLE#/NARRATIVE# writers, or amend phase_taxonomy.py's docstring to state that provenance is reset-time-only — and pin whichever answer wins with a check_doc_facts.py rule.
1. Add a pk-family census preflight to restart_pipeline dry-run (one cheap 32k-item scan, reset-time only — ADR-103: extends existing load-bearing tooling, zero standing cost) so a new non-SOURCE pk family fails loud before a reset instead of silently surviving it; prove it catches a planted unknown family.
1. Correct the raw-path pattern lines in CLAUDE.md/SCHEMA.md/MEMORY.md and add a filename facet to raw_layout, gated by check_doc_facts.py.

**growth (B+ → A):**
1. Wire the share surface in one PR: add og:image/og:title/canonical to /cockpit/ and point each /data/ topic page + /story/chronicle/ at its already-generated daily card (og-sleep, og-glucose, og-chronicle, ...) — S effort, zero new machinery, immediately upgrades every external share.
1. Make capture measured: add a subscriber-funnel section (attempts / confirms / pending-expired / sub_count trend) to the existing weekly traffic digest, reading the subscribers partition the platform already owns — this is the last unmeasured joint of the flywheel.
1. Stop silent signup death: one 24h confirmation-reminder branch in the existing subscriber-onboarding lambda for pending records with live tokens (2 of 3 all-time signups died this way).
1. Establish the distribution beat as process (fix_class B): every published chronicle/dispatch gets one tagged external share (?src= is already persisted by handle_subscribe) with the result read back in the next digest — the flywheel needs inbound flow, not more machinery.
1. Honest-numbers cleanup on growth surfaces: compute the og-home '25 data sources' claim from source_registry/public_stats, and emit full-content RSS (<content:encoded>) for the feed-reader audience.

**integrations (B+ → A):**
1. Make the public freshness read cross-phase: include_pilot=True in _latest_date_str and the same-basis pulse read (site_api_data.py:553-604), restoring real last_update/age/days_dark after every reset — one-line class of fix, no new machinery, matches the RAW_TIMESERIES cross-phase precedent already in the same file.
1. Add the post-reset regression test: seed a pilot-only source partition and assert handle_source_freshness returns non-null last_update + days_dark — this is the test that would have caught the current live gap and will catch it again at cycle 7.
1. Under epic #342, delete or cdk-import the two orphan EventBridge rules (pipeline-health-check-daily, subscriber-onboarding-daily) and add a read-only ENABLED-rule-vs-CDK-template parity assertion to the I-series integration tests (zero standing cost — it's a test, not infrastructure; ADR-103: strengthens the existing load-bearing parity gate rather than adding a subsystem).
1. Disclose the dead macrofactor_export honestly in the public vacation_fund payload or drop it from extra_sources_enabled.
1. Optional polish: when an HAE datatype's last_seen exceeds the liveness scan window (CGM currently shows dark:true with last_seen:null), render 'dark >Nd (beyond scan window)' instead of a bare null — still computed, never fabricated (ADR-104).

**qs (B+ → A):**
1. Grade-or-void at reset: restart_pipeline writes a cycle-stamped 'voided_at_reset' row to the cross-phase calibration ledger for every open hypothesis and coach prediction before tombstoning, closing the survivorship hole in 'every forecast graded' (highest-leverage — it protects the platform's central claim through the very resets the story is about).
1. Citation integrity sweep of the supplement registry: replace the 4 PubMed search-URLs with real PMIDs or honest 'no direct study' labels, and apply a citation-or-cut rule to unhedged population claims like '80% of Americans are magnesium deficient'; add the schema lint so it can't regress.
1. Resolve /method/benchmarks: build the small weekly writer for SOURCE#benchmarks or retire the page and its chronicle hook (ADR-103 retire-candidate call), and fix the empty-state copy that promises re-population no code performs.
1. Surface the existing graded record on the calibration scoreboard: fold forecast interval-coverage (n=23, 82.6% vs 80% target — already computed and honest) into /api/calibration so the honesty-moat page is never falsely empty in the post-reset window.
1. Complete the justification contract: hoped_outcome/measured_by on the 3 cognitive supplements and dedupe the 4 near-identical experiment-library pairs, with schema lints for both.

**reader (B+ → A):**
1. Apply a word-boundary+ellipsis truncation helper at the three generator sites (wednesday_chronicle_lambda.py:1795, site_api_lambda.py:831-832, intelligence_common.py:1693) — kills the only 'site looks broken' moment a first-time reader hits (S).
1. One coherence pass on the hero weight surface: single rounding policy, delta computed from displayed values, 'since the start (one weigh-in)' phrasing instead of 'in 4 days', and a non-negative-ambiguous 'to goal' label (S).
1. Complete #787 on the coaching digest: plumb each coach read's analysis date through /api/coaching-dashboard and stamp the cards; make the daily line's 'yesterday' scope a permanent kicker instead of a dismiss-once explainer (M).
1. Extend the existing nightly reader-truth lane (lambdas/reader_truth_qa.py / qa_smoke — ADR-103 load-bearing, no new infra) with two deterministic rules: no reader-facing string ends mid-word, and displayed weight arithmetic must be internally consistent (S).
1. Annotate pre-genesis 'as of / last prescribed' dates on carried-forward levers via a restart-pipeline verify rule, so cycle N+1's Day-1 reader never sees unexplained stale dates (S).

**narrative (B → A):**
1. Close the tombstone seam in one pass: guard the three unguarded singleton endpoints (state_of_matthew/forecast/scenarios, site_api_data.py:2904/2938/2977) and the three PERSONA#elena read paths (chronicle notebook, Margaret callbacks, between-chronicle stance) with the existing singleton_visible pattern — all S-effort, zero new machinery (ADR-103: extends load-bearing ADR-077 discipline).
1. Before approving the held 'Day One, Again' draft, regenerate it post-fix or hand-edit: remove the phantom 'documented in the prologue' Sunday-walks callback and fix the Sunday/Monday weekday error — the approval gate exists precisely for this.
1. Reconcile Prologue Part I with Part II via a dated Margaret editor's note (302→314, 1800→1500, 190→170) instead of silent revision — preserves the honesty posture while removing the binge-reader contradiction.
1. Add deterministic calendar facts (genesis weekday, date→weekday map) and plan constants to the chronicle's ADR-104 grounding bundle so date and plan-figure errors fail the gate instead of reaching drafts.
1. Give the coaches their voices on the flagship prereg artifact: one in-voice rationale sentence per prediction (claim text stays verbatim for gradability), and feed prereg passages into the voice-fidelity harness's blind sample when its first cycle-6 run lands.

**observability (B → A):**
1. Grant cloudwatch:PutMetricData to compute_coach_prediction_evaluator() in role_policies.py (mirror coach_state_updater's CloudWatchMetrics statement) and verify grading-stalled can actually clear — the only red alarm on the board becomes honest again (S).
1. Add a CI test pairing every put_metric_data call site in lambdas/ with a PutMetricData grant in its role-policy function, so a silently-AccessDenied emitter can never ship again (S).
1. Make incomplete agent triage a red outcome: fail the remediation workflow step when the report retains untriaged items with a truncated _raw, and fix the underlying output-truncation so 4/4 runs complete triage instead of 1/4 (M).
1. Add cloudformation:DetectStackResourceDrift to github-actions-remediation-role and escalate an all-AccessDenied cfn_drift result to needs-human — the weekly drift sweep's core check goes from dead-but-degraded to alive (S).
1. Alarm the digest path's own death: alert-digest Errors + digest-queue oldest-message-age → the URGENT topic (it cannot announce its own failure through itself), and extend the integration alarm-inventory test to pin both (S).

