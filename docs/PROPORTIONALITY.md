# The Proportionality Ledger — does this complexity earn its keep?

> **Status:** canonical · **Owner:** Matthew · **Verified:** 2026-08-22

> **The maintained home of the complexity-posture ledger** (ADR-103 established it;
> ADR-144 made it legible and moved it here). Consult BEFORE adding or removing
> machinery. Posture changes are one-line edits with a dated note; the quarterly
> review re-reads the whole table. **#1666 / epic #1648.**

## The standard (ADR-144)

A one-person platform runs systems at home in a mid-size org — deliberately: the
platform is both an instrument and a public demonstration of engineering practice
(the ADR-078 wedge-B frame). Proportionality is not "have less"; it is **every
standing subsystem names what it costs, what it earns, and what would demote it.**

- **Postures:** `Load-bearing` (product or safety depends on it) · `Portfolio`
  (publicly-demonstrated pattern, justified even at low utilization) ·
  `Retire-candidate` (named removal trigger, never an open-ended "someday") ·
  `Retired` (dated, with the removal record).
- **Rent classes** (what a standing subsystem pays — honest categories, not
  invented dollar figures; the platform's total AI+infra spend is governed
  separately by the ADR-063/133 ceiling): `$` AWS/AI spend is measurable on it ·
  `CI` it runs in or gates the pipeline · `attention` it emails/pages/asks a human ·
  `surface` it adds deploy/IAM/public attack surface · `mind` a successor must
  understand it to operate the platform.
- **The landing rule** (from ADR-103, unchanged): new enterprise-pattern
  infrastructure names its frame in its PR/ADR or it does not land. "Cool to
  have" is not a frame.
- **The demotion rule** (the ADR-129 worked precedent): a posture is earned by
  *measured output*, not design quality. The remediation agent — exemplary
  safety design, zero merged safe-class PRs in ~6 weeks — was demoted to
  `shadow` with a numeric re-promotion bar (10 consecutive clean runs) — retired,
  with the demotion made permanent, when a further 8 weeks produced zero to
  count (#2833, 2026-08-30). That is the template: demotions cite a
  measurement, promotions cite a bar, and a bar nothing ever approaches is
  itself a finding.
- **Cadence:** quarterly re-read — owned by the `proportionality-reread` entry of
  the Platform Operating Calendar (#2832, `scripts/operating_calendar.py`), whose
  dead-man probes the dated lines in the Re-read log below. Any review that finds
  a subsystem findings-empty twice consecutively proposes a demotion row here.

## Re-read log (#2832 — the calendar's probe reads these lines)

Each completed quarterly re-read appends one line, dated the day the whole table
was actually walked: `- Re-read: YYYY-MM-DD — <one-line verdict>`. The `Verified:`
stamp in the header is refreshed by automation and therefore proves nothing about
a re-read (the stale-behind-a-fresh-timestamp class); only a line here advances
the calendar's clock. First calendar-driven re-read due within one quarterly
window of the 2026-08-22 adoption.

- Re-read: 2026-08-27 — first calendar-driven re-read (the entry had NEVER produced its artifact; #3273 made never-run a verdict). All 73 active rows walked; ~60 verified unchanged, 11 corrected against live evidence. **Two triggers had already fired unnoticed:** fresh-eyes' "revisit at 3+ runs" fired 2026-07-26 (7 runs since, all green), and the reader-truth ledger hit ZERO entries 2026-08-26, starting its one-month demote clock (decision 2026-09-26). Two more had their premise expire: the privacy-registry row's demote trigger points at a #2797 target state that epic closed `realized` WITHOUT (no privacy facet in `source_registry.py`), and the deferred 13th grant gap's stated blocker (#3037) is merged. Biggest mis-pricing: the privacy-tier registry's rent read "3 fields, 4 discovered pairs" and measures **31 fields / 42 pairs / 15 modules** — 10x, via #3055's registry port; the row's *conclusion* (small) survives, its stated *reason* (small vocabulary) does not. Ceilings re-derived live: base $215 / surge $252 from 2026-09-01, Aug window $200/$235, AWS Budgets backstop live at 215.0 — all one derivation, none hand-typed. Gate census 563 re-derived from source and matched the doc exactly. <!-- drift-ok: the $200/$235 pair here is named as the August window's OWN value, which becomes history on 2026-09-01; the current claim on this row is the $215/$252 base -->

## EMF series census log (#2837 — the calendar's `emf-series-census` probe reads these lines)

The CloudWatch custom-metric estate is inventoried in `deploy/emf_namespace_ledger.py`
(one row per namespace: owner, consumer, verdict, series budget) and graded live by
`python3 deploy/emf_series_census.py`. Each monthly cost close appends one line here —
`- EMF census: YYYY-MM-DD — …` — and **only such a line advances the clock**, for the
same reason the re-read log exists: the count that matters is the one somebody actually
looked at. `live-orphan` counts namespaces with live series that nothing in the tree
writes; they need no action and age out of CloudWatch's window by themselves.

Read the two series numbers as different things. `list-metrics` returns everything with a
datapoint in the last **14 days**; `--recently-active PT3H` returns the last **3 hours**.
CloudWatch prorates a custom metric by the hours it receives data, so the second is far
closer to the invoice — on 2026-08-23 the estate was 703 series by the first measure, 102
by the second, against ~67 billed metric-months in Cost Explorer. Multiplying the 703 by
$0.30 gives ~$211/mo against a measured ~$19; the naive count is wrong by an order of
magnitude **and points at the wrong namespaces** (`LifePlatform/SiteAPI`: 288 series, 2
active — `LifePlatform/IngestLiveness`: 27 series, 22 active).

- EMF census: 2026-08-23 — 703 series / 30 namespaces live (14d), 102 active (PT3H sample, n=1); 0 over budget, 0 unregistered, 2 live-orphan
- EMF census: 2026-08-31 — 590 series / 30 namespaces live (14d), 204 active (PT3H sample, n=1); 0 over budget, 0 unregistered, 2 live-orphan

## Active ledger

| Subsystem | Posture | Rent | Earns its keep by / demote trigger |
|---|---|---|---|
| Cost-bearing-surface baseline ratchet (#3374 R1: the `cost_surface` plane in `model/platform_model.json` + the hand-owned `model/cost_surface_baseline.json`, exact-pinned by `tests/test_platform_model_drift.py`) | Load-bearing (**small**) | **CI: ~0s marginal** — five integer comparisons inside a drift gate that already regenerates the whole model every run; the three new counts are AST lifts from registries other gates already hold true (`emf_namespace_ledger.LEDGER` set-equality #2837, `KNOWN_SECRETS` SR1, `_FEATURE_CUTOFF` ladder tests). No new job, no schedule, no AWS call, no runtime, $0. **Ops: one JSON count bump per deliberate surface growth.** | The platform's recurring-cost floor is the product of five populations (schedules, alarms, EMF namespaces, secrets, AI features), and until now every one of them could grow in a merged PR with nothing asking whether the growth was chosen — the +16% Jul→Aug floor creep was found by a quarterly panel, after the money was spent. This ratchet moves the question to DIFF TIME: growth reds `test_cost_surface_baseline_is_exact` until the baseline is bumped in the same diff, shrink forces the baseline down (it can never rot above the estate), a zero count RAISES in the extractor rather than serializing (the absence-read-as-success dead-man), and the mutation controls fail all four directions. Mutation-proven through the REAL gate 2026-09-01: a planted 29th secret redded the gate naming the surface (`secrets GREW: 28 -> 29`); reverting returned it green. Scope is honest: it counts POPULATIONS, not dollars — a count that holds while one member's unit cost balloons is R2/R3's job, not this one's. **Demote trigger:** two consecutive quarterly re-reads in which every baseline bump was rubber-stamped in the same commit with no PR sentence naming the surface (a stamp, not a decision) — then fold the five counts into the model's meta and retire the baseline file; or the #2845 drift gate itself is ever demoted, which takes this rider with it (row added 2026-09-01) |
| Per-feature AI budget ledger (#3374 R3: `scripts/ai_budget_ledger.py` + `tests/test_ai_budget_ledger_3374.py`, close-graded by `scripts/monthly_close.py` step [5/5]) | Load-bearing (**small**) | **CI: ~0.1s** — set-equality + rule checks against two committed files. **Close: $0 marginal** — grades the attribution run `monthly_close.py` already makes for its stability check; standalone form re-runs the instrument once (read-only CloudWatch GetMetricData). **Ops: one budget edit per deliberate feature growth**, one ratchet-down edit whenever attribution improves. **Mind: one rule** — budget = round(max($1, 1.15 × founding Aug actual), 2), reusing R2's derived threshold, recomputed by `validate()` so no stamped budget can drift from it. | The `unknown` Bedrock bucket reached **$33.19/8,524 calls** in August 2026 before #2888/#2892 closed the attribution gap, and no per-feature ceiling existed anywhere — the ADR-063 governor sees only the total. Now: a NEW `_FEATURE_CUTOFF` feature cannot merge without a ledger row (set equality, the #2837 discipline), 10 budgeted rows covering ~88% of August's stamped dollars (incl. `unknown`) red the close on overage, and the `unknown` budget is DOWN-ONLY from its founding $33.19 — shrinking is the success state and never reds; raising it is a two-file act (ledger + the test's pin). Attribution honesty per ADR-104/105: the dimension is per-lambda, so features sharing an emitter carry `budget=None` with the gap stated in the row, never a manufactured number. Live proof at build time: grading the founding month itself reds by $0.02 ($33.21 re-read vs the 08-31 $33.19 — late-arriving datapoints), i.e. the close gate demonstrably CAN fail on real data; September is the first governed close. **Demote trigger:** three consecutive closes where no budgeted row trips AND the `unknown` ratchet was never advanced down — the ledger is then a snapshot of a bill that neither grows nor improves; fold the unknown ratchet into R2's close clause and retire the per-feature rows. Or the other way: if a tripped row is answered by a same-day budget raise three closes running, it is a stamp, not a control — the budgets need re-founding on a fresh measured month (row added 2026-09-01) |
| MoM close delta clause (#3374 R2: `doc_facts_ops.monthly_close_driver_hits`, wired into `scripts/check_doc_facts.py`) | Load-bearing (**small**) | **CI: ~0s** — one regex pass over `docs/COST_TRACKER.md`'s Monthly Actuals table inside a gate that already reads every doc; no new job, no schedule, no AWS call, no runtime, $0. **Ops: one token per tripped month**, and only on a month that already grew >15% — both facts (top-growth CE service, dominant `CallerClass`) are pulled by the #1354 close ritual's own steps 1 and 4, so the clause adds writing-down, not measuring. **Mind: one number, 1.15**, and it is derived rather than chosen (see the demote column). | Nothing governed cost GROWTH structurally: the platform floor grew +16% Jul→Aug 2026 ($43.00 → $49.77 non-AI) with no gate noticing, and the Bedrock `unknown` attribution bucket reached $33.19/8,524 calls in August before #2888/#2892 closed it. Both were found by a once-a-quarter diligence panel — an audit of the absence of a control, not a control. **The threshold is calibrated on its own founding incident:** 49.77/43.00 = 1.158, just over 1.15; a ratchet whose origin story would have passed it is decoration, and `test_the_threshold_catches_its_own_founding_incident` is that claim as an executable one (with the just-below case, 1.140x, asserted quiet). Mutation-proven through the REAL gate, both directions: a synthetic 1.48x Sep row reds `check_doc_facts.py` (exit 1, named by line); removing it returns exit 0. Scope is honest and narrow: it checks a `driver:` token is PRESENT, never that it is CORRECT — verifying the named driver needs live Cost Explorer, which a docs gate must not call. Rows before 2026-09 are grandfathered because the `CallerClass` a token names only went live 2026-08-23. **Demote trigger:** two consecutive quarterly re-reads in which no month tripped the clause AND no close was found to have grown >15% unexplained — the clause is then measuring a bill that does not move, and the honest fold is back into the ritual's prose. Or the other way: if it fires on three consecutive closes and the `driver:` token is written each time without changing any decision, it is a stamp rather than a control — replace it with the R1 surface baseline (which fails at diff time, before the money is spent) and retire this row (row added 2026-09-01) |
| Per-door audience instrumentation (#3376: the bounded `Door` dimension on `LifePlatform/Traffic::PageViews7d` + `SyndicatedReferrals7d`, emitted weekly by `lambdas/operational/traffic_digest_lambda.py`) | Load-bearing (**small**) | **AWS: cents/mo** — 7 new weekly-emitted series; CloudWatch prorates by data-receiving hours, so 7 × $0.30 flat (~$2.10) is the ceiling, not the bill (#3376 budgeted +~$1.5/mo; a once-a-week datapoint bills a fraction of that). **CI/runtime: zero new jobs, zero schedules** — the series ride the Monday digest's existing CF-log parse and its one existing PutMetricData call. **Privacy: zero new collection** — same first-party CloudFront logs, no cookies, no client JS, IPs hashed-then-discarded; the only new retained bit per request is a boolean "query carried utm_source" (the query string itself is dropped at parse time). **Mind: one mapping** (URI prefix → door, a code literal mirroring `docs/SITE_MAP_AND_INTENT.md`; the six doors are the closed dimension set, so cardinality is bounded by construction, never by traffic). | The 2026-08-31 financial-diligence finding: instrumentation was inversely proportional to cost — the platform could defend its cheapest features' audiences and its most expensive features' almost not at all, and two retire-triggers on this very ledger (voice notes, syndication) were unfalsifiable because the series they would read did not exist. This makes each door (and via the rewritten triggers, each named feature) gradeable against a measured audience series; the pattern it catches is a door with attempts and zero successes/readers — the #3419 class (a checkbox 429'ing every reader for 12 days), visible as a flat-zero series instead of silence. Registered in `deploy/emf_namespace_ledger.py` (Traffic budget 5 → 12, dated note) and graded by `emf_series_census.py` like every other series. **Demote trigger:** two consecutive quarterly re-reads in which no posture, retire, or close decision cites a door or syndication series — then the audience evidence is decoration; fold back to the 2-series site-wide pair and ratchet the Traffic budget back to 5 (row added 2026-09-01) |
| Reject-only production-gate lease steward (#3422, carried by the #3021 janitor: `scripts/check_deploy_wedge.py --janitor --apply` on `deploy-wedge-watch.yml`'s existing 15-minute cron, credentialed by `DEPLOY_GATE_JANITOR_TOKEN`) | Load-bearing (**small**) | **CI: ~2s riding a run that already exists** — a handful of GitHub Actions API reads per sweep + one POST per rejection; no new job, no new schedule, no AWS state, no model call, $0. **Surface: one fine-grained PAT** (required reviewer of the `production` environment, Actions read/write, this repo only — set 2026-08-30). That credential COULD approve; the reject-only bound is therefore held in CODE, not in the grant: no approval path exists in the module or workflow (structural per #2833/ADR-129, negative-controlled by `tests/test_reject_only_steward_3422.py`). **Attention: zero when healthy** — a sweep that cannot do its job reds the scheduled run (the existing paging channel), never silently reverts the class to manual. | Disposing superseded ancestor leases was live manual toil on every merge-heavy session (Session Q: 4 by hand; Sessions R/S: a hand-rolled per-session steward loop), and a leave-waiting lease holds the deploy-group slot hostage (#2467 — the 2026-08-09 all-day wedge was two 8-day-old gate-parked zombies). The sweep rejects only PROVABLY superseded leases: strict-descendant proof against RUN-BEARING shas only (a `[skip-reconcile]` tip mints no run and can supersede nothing — the Session R rule on #3422), the newest run-bearing lease structurally never touched, unknown ancestry never rejects. Its rejection comment is a CONTRACT with `scripts/check_main_green.py` (#2590): a steward-rejected run reads back "rejected-and-superseded, not a red main", with the reason verbatim (wire-fixture tested in the same file). Approval remains a human act by structure, not flag. Honest status: the apply path has issued ZERO live rejections as of 2026-09-02 (hand stewards have beaten the 15-minute cron to every disposal; every observed live rejection is human-authored) — the first unattended live rejection is this row's outstanding live proof, and until it exists the row earns rent on offline mutation evidence only. **Demote trigger** (#3422's own): a quarter with zero rejections issued — the merge cadence then no longer mints ancestor leases and the sweep is measuring a queue that stays empty; fold the step out of `deploy-wedge-watch.yml` and retire the PAT (row added 2026-09-02, #3422) |
| Co-owned computed_metrics write contract + ACWR dead-man (#3443: `compute/computed_metrics_contract.py` registry, read-before-put carry in `store_computed_metrics` + the sick-day rebuild, `qa_smoke_lambda.check_acwr_liveness`) | Load-bearing (tiny) | **$: one extra get_item per daily-metrics store (~2 writes/day)** · CI: three offline test families in the unit suite · attention: the liveness check rides qa-smoke's existing nightly email/alarm channel, no new topic or schedule | ACWR was destroyed nightly 2026-08-24→09-01 — 9 days dark, zero alarms — because two writers co-own one record and the from-scratch rebuild erased the other writer's merge (trigger: the #2811 PT-clock correction re-aimed the evening re-put onto the record acwr had merged onto; the wrong-record latent bug had been accidentally protecting it). The field set is declared once; a derivation guard pins the acwr writer's UpdateExpression to it, a contract test proves survival through both rebuild paths (red-then-green both directions), and the dead-man reds qa-smoke when `acwr_computed_at` ages past 48h — this incident would have paged on day 2 instead of being found by a review pass on day 9. **Demote trigger: acwr-compute and daily-metrics-compute merge into a single writer of the record** — the contract then guards a seam that no longer exists; retire the registry with the merge (row added 2026-09-02, #3443) |
| Additive-IAM CI gate (#2834: `deploy/iam_additive_gate.py` + the Plan/Deploy wiring in `ci-cd.yml` + the `remediation-log/iam-additive-gate/` ledger) | Load-bearing (small) | CI (one extra `cdk synth` + ten read-only `GetTemplate`s per Plan run, ~6s measured), **surface** (CI now *exercises* `cdk deploy` for admitted stacks — through the CDK bootstrap chain the deploy role already held; the honest residual is in the ADR-065 amendment 2026-08-30: the bootstrap cfn-exec role is AdministratorAccess, so this gate guards against mistakes and drift, not against a push-to-main adversary), mind (a reviewer must know the forbidden shapes; they are data at the top of the gate) | Replaces the R8-ST6 prose grep that stranded the code pipeline on every IAM grant (~14h on 2026-08-15) and left a P1 privacy no-op unfixable through CI for five days (2026-08-14). Earns its keep by admitted deploys that would otherwise have been owner touches — the S3 ledger is the count, and the five primitives are all present (registry / derivation guard / ratchet / wire-shape contract / dead-man — named in the PR). Demote trigger: two consecutive quarterly re-reads with ZERO `ALLOW-ADDITIVE` ledger entries (the gate only ever said NO-IAM-CHANGE / OWNER-REQUIRED — then it is the old grep with more code; fold it back to the destruction check + the advisory warning), OR any admitted deploy later judged out of shape (an escape — fall back to option (c), status quo priced explicitly, per the #2834 review condition) (row added 2026-08-30) |
| CDK cfn-exec permissions boundary (#3340: `infra/iam/cdk-cfn-exec-boundary.boundary.json`, derived by `deploy/derive_cfn_exec_boundary.py` from `deploy/iam_additive_registry.py`; asserted by `deploy/verify_oidc_iam.py --strict`) | Load-bearing (small) | **AWS: $0** — a permissions boundary is free and adds no request cost. **CI: 0s** — the assertion runs inside the existing read-only verifier, and the offline half is 73 tests in ~0.2s. **Ops: three API calls to apply, three to roll back**, plus one `create-policy-version` whenever `deploy/bucket_policy.json` gains a protected prefix or `cdk/app.py` gains a stack (the derivation catches it; the committed-JSON-equals-derivation test is what makes the catch a gate). **Mind: one — the 6,144-character managed-policy limit.** The document renders at 5,675 compact, so ~470 characters (about ten protected prefixes) of headroom; the test refuses to let it fall below 200. | The 2026-08-30 ADR-065 amendment said in writing that the additive-IAM gate is not a permission control and that the real perimeter — an `AdministratorAccess` cfn-exec role with no boundary — was unchanged. This is that perimeter, and it is the cheapest control on this ledger: no runtime, no schedule, no alarm, one file. It earns its keep the first time the gate's parser is wrong about a template, and it is the only thing that still holds if the workflow step is edited away. Demote trigger: **two consecutive quarterly re-reads in which the boundary has refused nothing AND the additive gate has admitted nothing** — the pair is then over-built for a single-operator account, and the boundary (one static document) is the half to keep over the gate (a parser with a CI job). A legitimate deploy blocked by one of its Denies is NOT a demote trigger: it is a one-clause widening, regenerated and re-applied with `create-policy-version` (row added 2026-08-31) |
| CloudWatch custom-metric estate (#2837: `deploy/emf_namespace_ledger.py` + `emf_series_census.py` + the pre-merge drift guard) | Load-bearing (small) | $ (~$19/mo MetricMonitorUsage, the platform's largest non-AI AWS line), CI seconds (one AST sweep), attention at the monthly close | The estate grew 9x in three months (7.4 → 66.9 billed metric-months) with no inventory, budget or owner — the only detector was the invoice, three weeks late. Each namespace now names its consumer (alarm / dashboard / code reader / dated ritual) or carries a retirement verdict, and the guard reds at PR time on an unregistered namespace in BOTH directions. Honest limit, stated: the audit found **no large prune** — 9 unread namespaces totalling ~32 series, nearly all sparse; the value is the growth detector, not the cut. Demote trigger: two consecutive closes where every over-budget red is resolved by raising the budget rather than examining the cardinality — then the budget is a rubber stamp, derive it from the driver population instead (row added 2026-08-23) |
| Phase machinery (ADR-077 taxonomy + restart pipeline) | Load-bearing | mind, CI | The experiment's reset semantics; coverage-asserted. 11 cycles of worked use |
| Coherence sentinel + canonical-facts contracts | Load-bearing | CI, attention | The honesty moat's enforcement layer (ADR-104/105) |
| Permanence archive (nightly public snapshot + continuity switch, #2572) | Load-bearing | S3 (~GBs), one nightly writer | The stolen-laptop/continuity contract in writing — the site survives its operator. Demote: if the nightly writer fails silently >7d with no alarm catching it, the machinery is theater — alarm it or fold it |
| Coach voice notes (Telegram sendVoice, #2552) | Experiment | Google TTS $ per note, budget-gated | Persona texture measured against real chat use. Demote (rewritten 2026-09-01, #3376 — the old "<1 voice note/week actually played" trigger was unfalsifiable: the Telegram Bot API exposes no play receipt, so no series could ever grade it): if `LifePlatform/Telegram::TelegramVoiceNoteSent` (summed across the `Coach` dimension, emitted at each send since #2494) records <1 send/week over a month, retire the path. Sends bound plays from above — zero sends proves zero plays, which is exactly the retirement case the trigger exists to catch |
| Outbound initiative pings (celebration + soft-concern, #2527) | Experiment | Bedrock $ within the 2/day outbound cap | The coaches text FIRST when the data moves — the #2490 priority ladder bounds volume. Demote: if the owner mutes/ignores >50% over a month, the initiative reads as notification spam and the classes retire |
| Gate census + can-it-fail proofs (epic #2578) | Load-bearing · **ratchet, down-only** (#3329) | CI seconds, session attention; one ratchet line per new gate | 591 declared gates with measured error bars; the week's cannot-fail finds (#2746, #2754) came from it. Count is DERIVED (the `gate_census_count` fact — registered into `sync_doc_metadata.RULES` at runtime but LIVING in `deploy/sync_census_fact.py`, split out under the module-size ceiling; the earlier citation of `sync_doc_metadata.py` sent a reader to a file that does not contain it) rather than hand-typed — the prior "425" had drifted 13% behind #2639's widened derivation with nothing to notice. Demote trigger: census derivation cost exceeding one CI minute, or two consecutive sessions with zero acted-on findings. **Re-read 2026-08-27:** neither fired — the derivation re-ran from source at **7.8s** (vs. the 60s trigger) and returned 563, matching the doc exactly; #3220's ten name-matched non-gates are the session's acted-on finding. The census's "measured error bars" claim is real and verified: it prints both-direction floors (22 exit-only false negatives, 48 unadjudicated steps) and hand-sampled FP rates with 95% CIs. **Posture, restated 2026-08-31 (owner decision on #3329, option B — epic #2578's box 2 amended the same day):** this is a RATCHET, not a proof programme. Every gate entering after 2026-08-24 arrives proven under #3000 (this session: #3336, #3318, #3315, #2834 — four for four); the installed unproven base is tracked by `BASELINE_UNPROVEN_GATES` in `tests/test_gate_census_lane_3000.py`, which may only move DOWN and shrinks opportunistically — the census prints `RATCHET DOWN AVAILABLE` with the number whenever the committed ceiling sits more than the stated slack (16) above the live pile. The three verdicts are proven / unproven / not-applicable, and since #3329 all three are INSIDE n: the six name-matched rows that can never fail carry a recorded one-line reason each instead of being excluded from the denominator. **Rent:** one ratchet line per new gate — its verdict, or the reason nothing in it can fail. **Demote trigger (superseding the derivation-cost one above, which has not fired):** a gate recorded `can-fail (proven)` that is then found NOT to fail in the field — at that point the proof record is measuring the mutation harness rather than the gate, and the honest move is to retire the verdict layer rather than keep a number nobody can trust. |
| Proportionality-ledger wrap gate (`check_proportionality_ledger.py`, #2380/#2761) | Load-bearing (small) | session attention (~1s per wrap) | Keeps THIS ledger a live inventory, not a snapshot — the prose-only #2380 version produced zero ledger commits in its first week while four standing systems shipped. Demote trigger: two consecutive quarterly reviews find the `**Ledger:**` line rubber-stamped (`none` while unledgered machinery shipped) — then the script measures nothing, redesign or fold it into the census |
| Handover line assertion (`check_handover_lines.py`, #3006) | Load-bearing (small) | session attention (<1s per wrap, offline) | Eight of /wrap's eleven gate lines were prose-only; measured cost 20 missing lines in a 25-handover window, ALL in 4 truncated wraps. Markers are derived from wrap.md's own contracts (a hand-list drifts on the twelfth gate), with a min-count floor so a parse regression errors instead of passing an empty set. Demote trigger: a quarter with zero missing-line reds AND zero derivation changes — then fold it into `wrap_gates.py --verify` as an inline check rather than a standing script |
| Batched wrap-gate runner (`wrap_gates.py`, #3007) | Load-bearing (small) | session attention (~10–15s per wrap, parallel; no CI, no $) | Replaces ~12 sequential gate runs + ~10 re-edits of one file with gather → write-once → verify; the sibling measurement shows wrap truncation is a remembering failure, so the runner turns "remember eleven things" into "correct a draft". Preserves every gate's own exit code and degrade path (surfaced, never swallowed). Demote trigger: if the battery's composition drifts from wrap.md's steps (a gate runs in neither phase) twice, the registry belongs in wrap.md itself — regenerate, don't hand-sync |
| Closure contract — registry + two detectors (`scripts/closure_contract.py`, `closure_sweep.py`, `check_pr_closing_set.py`, #3318) | **Advisory** (warn posture; flip bar in the registry docstring — 10 consecutive clean session sweeps AND 25 watched merges with zero overridden closing-set findings, re-measured at flip time) | session attention: one GraphQL sweep per wrap (~3–6 issues, ~2s) + one `gh pr view` + label reads per merge-eligible watcher verdict (~3s); no CI job, no $, no new schedule; mind: one registry a successor reads to learn what a valid close is | Priced from a measurement, not a hunch: Session K's audit (2026-08-30) found **5 escapes in ~60 closes** — a stray-`Fixes` false close whose author had written "stays OPEN" 15 min after `closedAt` (#2848, unnoticed 3 days), a post-close scope assertion (#2670), three residuals named with no carrier (#2938/#2921/#3208, the #2845 shape) — while the handover's residual queue, which HAS the `not-work — <home>` rule, audited clean the same night. The first live run over the same window (257 closed since 08-16): detector B named PR #3226 (`declared-target-mismatch`, the #3222 false close) and PR #3253 (`partial-acceptance-close`, the #2848 one) and passed the two clean controls; detector A's structural leg found every dispositioned escape by timestamp alone. Honest limit, stated: the dominant finding class live is `no-outcome-verdict` (~100 of 122 hits — the known (e8) silence, going-forward-only per ADR-099), so the sweep is session-scoped at wrap and a 30-item sample in `/sdlc-review`, never a standing ritual. Charter check: registry ✓ · derivation guard ✓ (doc block byte-equal to `--render`; `not-work` grammar imported from `check_residual_queue.py`) · ratchet ✓ (dated `DISPOSITIONED_ESCAPES`, reason-length asserted) · contract test ✓ (fixtures are captured wire nodes; both detectors mutation-proven with negative controls) · dead-man **partial** — the sweep is a gate in `wrap_gates.py`'s gather battery (the wrap cannot complete if the script is missing or crashes) and its verdict rides the `**Closures:**` line `check_handover_lines.py` already asserts; the watcher always prints a `CLOSING-SET` line (UNAVAILABLE when it cannot run). Nothing fires if a session skips `/wrap` entirely — that gap is the wrap's own, not this row's. **Demote trigger:** two consecutive monthly `/sdlc-review` samples with zero hits outside `no-outcome-verdict` — then the contract has become habit and the sweep folds into (e8)'s listing command; or, the other way, if the flip bar is not met within one quarter because the driver keeps overriding closing-set findings, the grammar is wrong — fix the parser or retire detector B rather than arming a gate people route around (row added 2026-08-30) |
| Operating calendar + ritual dead-man (#2832: `scripts/operating_calendar.py`, daily `operating-calendar.yml`) | Load-bearing | CI (one 5-min daily run + a docs-ci step), attention when a ritual lapses | The filing-rate lever: review cadence is what finds defects, and every prior cadence statement was prose that drifted (craft-review NEVER ran; sdlc's obligations died with it). One registry, artifact-dated probes, misses scream via the remediation triage. Demote trigger: two consecutive quarters where every OVERDUE red is resolved by re-anchoring/exempting instead of running the ritual — then the calendar is theater and the honest fix is deciding the rituals themselves are dead |
| Prediction-gradeability contract (#3046: `prediction_emission.py` gradeable_by stamp, `GradableShare` metric + `prediction-gradable-share-low` alarm in `monitoring_prediction_alarms.py`) | Load-bearing (small) | one metric emit + one alarm evaluation per evaluator run; attention when the share drops | Closes the DIL-007 class structurally: #715's "zero ungradeable-by-construction" criterion was prose until 56% of the pending corpus was qualitative and nothing noticed — the emission contract makes it a regression test and the ratio alarm makes a regrowth visible. Expected to fire ~3d post-deploy on the legacy corpus, clearing as window-end retirement drains it. Demote trigger: a quarter at GradableShare ≈ 1.0 with zero observational emissions — then the observational class is extinct at source and the alarm is vestigial, fold it into grading-stalled (row added 2026-08-23) |
| Kernel conformance guard (#2844) | Load-bearing | CI seconds (one AST sweep per unit-test run), mind (the ledger discipline) | The charter's standing rule 1 made executable — closes the missed-consumer class (the elite review's WS-A trace: SOCIAL_CHANNELS, _BROADCAST_SOURCES, ALL_LAMBDAS-at-40) by construction; the ledger (37 dated entries at birth, 2026-08-17) only shrinks. Demote trigger: two consecutive sessions where a red is resolved by loosening thresholds instead of deriving a site — that's the guard mis-tuned, re-scope its vocabularies |
| System model + drift gate (#2845) | Load-bearing | CI seconds (~30s regenerate+diff per unit-test run) | The kernel's second primitive artifact: one generated source of truth (lambdas/schedules/alarms/partitions/edges) with blast-radius lookup; subsumes the hand-prose DEPENDENCY_GRAPH that misled on the critical path (#2839). Demote trigger: two consecutive sessions where drift reds are resolved by regenerating without reading the diff — then the model isn't informing anyone, fold the doc rendering |
| Pre-merge full unit suite + duration instruments (#3025, folding #2692: `full-suite` job, per-test 90s warner, 1500s aggregate ratchet) | Load-bearing | CI (~16 min/PR, parallel, free minutes), attention (a `::warning` per >90s test via the e11 triage) | Closes the platform's #1-by-frequency incident class (4 full-suite-only main reds in 5 days, 1-2.5h TTR each, always misattributed): a PR that would red main cannot read green pre-merge. Budget raised WITH the CI durations measurement as rationale, not blind (the #2692 bar). Demote trigger: if the job's own flake rate exceeds one false red per two weeks, or the suite crosses ~25 min and starts gating session throughput — then re-decide against merge-queue/impact-selection with fresh numbers, don't quietly widen timeouts |
| Deploy-critical lane import guard (#2758) | Load-bearing | ~2s of every premerge + lane run | The #2699/#2732 collection-crash class (redded main twice in 48h) now fails premerge; self-protecting (deploy_critical-marked) with an in-suite mutation proof. Demote trigger: two consecutive quarters with zero catches AND the lane's dep set going unchanged — then fold into the census |
| expert-gate-infra-hold alarm (MetricFilter, #2763) | Watcher | CloudWatch ~$0, attention when red | Pages when the analyzer's grounding gate cannot RUN and holds (reader analyses stop refreshing; nothing wrong is served). Fire-proof: the twin-pinned filter literal. Demote: fold into a consolidated AI-hold alarm if a second hold class ships |
| telegram-coach-hold alarm (MetricFilter, #2823) | Watcher | CloudWatch ~$0, attention when red | Digests when a Telegram coach reply's grounding gate HOLDS 3+ times in an hour — the operator signal a held reply previously had none of (the 2026-08-10 P2 held every reply for ~9h with one log line as its only trace). One shared token/filter covers the primary reply hold and the three structurally-routine unsolicited-outbound holds (referral/checkin/event); threshold derived above both the empirical single-hold baseline (1/38 turns, 30d) and the daily-ledger's structural 2/day unsolicited ceiling (see monitoring_silence_alarms.py). Fire-proof: the twin-pinned filter literal. Demote: fold into the same consolidated AI-hold alarm noted above if a third hold class ships |
| between-chronicle scrub-failed alarm (MetricFilter + `between-chronicle-scrub-failed-closed`, #2654) | Watcher | CloudWatch (~$0), attention when red | Pages when the outbound privacy scrub fails CLOSED (nothing leaked; the digest went dark and silence must not be the only tell). Filter fire-proof recorded on #2654. Demote: if the fail-closed abort never fires in 12 months AND the vocabulary channel gains its own end-to-end canary, fold into the canary's alarm |
| 8-coach board + stance engine + orchestrator | Load-bearing | $, mind | The COACHING pillar — the product. Public-audience evidence since 2026-09-01 (#3376): `LifePlatform/Traffic::PageViews7d{Door=coaching}` (weekly, from CloudFront logs) — the coaching door's readership is now a measured series, not an assumption; owner-side consumption evidence (brief opens, chat use) remains unmeasured and any future demote argument should say which audience it means |
| Coach feedback loop (nudges #1382, docket #1386, dossier #1387, review pack #1698, calibration ADR-141) | Load-bearing | $, attention | The coaching layer's learning loop; first real-data cycle starts 2026-07-27 — findings-empty rule applies from Q4 |
| Budget governor + budget_guard | Load-bearing | attention, `$` (small — **the governor pays to measure**: 1 Cost Explorer call per run × 3 runs/day ≈ $0.90/mo, priced in its own `operational_stack.py` schedule comment; the ledger listed `attention` only until the 2026-08-27 re-read) | Enforces the ADR-063/133 ceiling; the reason rent class `$` stays bounded. **Re-read 2026-08-27, verified live:** the whole family is ONE derivation out of `cost_governor_lambda.py` (`scripts/budget_ceilings.py`) — base **$215** / surge **$252** effective 2026-09-01, August window $200/$235, and `aws budgets describe-budgets` returns **215.0** on `life-platform-monthly-75`, so the backstop moved WITH the base rather than lagging it as in the $85-vs-$150 era. No consumer hand-types a ceiling (`tests/test_budget_ceiling_registry_2898.py`) | <!-- drift-ok: the $200/$235 pair here is named as the August window's OWN value, which becomes history on 2026-09-01; the current claim on this row is the $215/$252 base -->
| Freshness / ingest-liveness / reconciliation detectors | Load-bearing | CI, attention | The silent-failure coverage class (the 44-day-Garmin lesson) |
| Character engine + sheet | Load-bearing | $, mind | Public flagship page |
| Derived-artifact registry + lane guard (`tests/derived_artifact_registry.py`, epic #2986) | **Load-bearing (small)** | one build-time AST scan of `scripts/` + `deploy/` plus a `git ls-files` call; one classification line per discovered generator — **29 today, 34 registry entries** with 5 acknowledged scanner blind spots (re-derived 2026-08-27 from `discover_generators()` / `ARTIFACTS`; the row read 28/33) | Priced before building. The rent is a single pre-merge test file and a classification line per new generator; what it buys is a class the platform had been paying for one incident at a time. Three generators, three *different* broken triples: #2975's guard sat in a lane a `docs/**` change cannot trigger (so every wrap's red landed on an innocent later commit), `generate_platform_model`'s guard ran post-merge only, and `generate_mcp_tool_catalog` had a `--check` wired into nothing and drifted a month **behind a timestamp the reconcile bot refreshed daily** — the doc read `Total tools: 76` two lines above `## All 72 Tools`. A checklist cannot find that; only a both-directions derivation can. The lane assertion is the novel half — "is there a guard?" already answered YES for two of the three. **Demote when** a full quarter passes with no registry entry changing and no new generator appearing — i.e. the generator population has stopped moving. (**Re-read 2026-08-27: NOT fired, measured** — the population moved 28→29 generators / 33→34 entries since the row was written.) Never by deleting the guard while unwired generators remain: shrink the DERIVED set by wiring artifacts, not by un-declaring them. **Amendment 2026-08-26 (#2838's fold, the epic's last residual):** the generic re-stamp rule — *a sync/apply run may only advance a freshness date on content it regenerated or verified* — now lives in `deploy/doc_restamp_guard.py` instead of being re-implemented per literal (it had been written three times: `doc_facts_ops` CHECK D3, `sync_doc_secret_inventory`, and the catalog's own body). Added rent: ~155 lines and one 0.5s pre-merge test file; `deploy/sync_doc_metadata.py` shrank by the block the module absorbed, so the ceiling was paid, not raised. Its guarded surface derives from `sync_doc_metadata.RULES` (every `{date}` template), so a new stamped literal enrols by existing. **Demote that half when** the freshness stamps stop being machine-written at all. |
| Deploy guardrails (clobber guard, postflight, drift checks, one-bundle rule, **site-rollback scope check**) | Load-bearing | CI | Each earned by a real incident (see CONVENTIONS.md). **Amendment 2026-08-31 (#3352):** the site auto-rollback's scope check (`tests/visual_qa_verdict.py` + one `if: always()` classifier step + a bash guard in `rollback-site-on-failure`) rides THIS row — it is not new machinery, it is the existing rollback learning what it can reach. Rent: ~1s of CPU per site deploy, one job output, and one extra ~20s runner job that closes the auto-filed issue on the next green deploy (without it the #1447 body's close policy would be a claim nothing implements). Earned by two measured incidents where the rollback reported success and reached nothing: the 2026-08-31 P1 (it re-ran the sync script that had just stamped the whole `/data/*` door `application/json`) and 2026-08-27 Session G (it reverted a wanted published build beat over a DynamoDB-sourced defect). **Demote trigger:** a quarter in which every declined rollback was later judged a misclassification — i.e. the classifier is costing reverts that would have helped. Never by widening the decline set silently: an unrecognised failure classifies `site-shell` and rolls back exactly as before. **Amendment 2026-09-01 (#3395):** the SMOKE leg joined the same scope check after the 2026-08-31 P3 (an `/api/vitals` data-plane red reverted PR #3392's innocent content) — surfaces are DECLARED at the call site (`deploy/lib/smoke_verdict.sh`, `SMOKE_SURFACE` = site/api/infra per section of `smoke_test_site.sh`), zero new jobs or steps; same negative control (unknown surface = site, rollback runs) and the same demote trigger. |
| Weekly Panel podcast pipeline | Load-bearing (STORY) | $, surface | Live no-touch pipeline (ADR-135); #1737 performance epic in flight. **Dated re-check: TTS/dialogue-vendor landscape due 2026-10-27, owner Matthew** (ADR-087 amendment 2026-07-27 / #1741 — the previous monitor trigger carried no date and went two months unnoticed; record the result even when nothing changed) |
| Reading pillar (2 GSIs, tools, page) | Load-bearing (small) | mind | The owner's real instrument |
| MCP server (76 tools post-#395 prune — re-derived 2026-08-27 from `sync_doc_metadata._auto_discover_tool_count()`; the row read 75) | Load-bearing | surface, mind | The instrument itself; tool removals ride the MCP_TOOL_AUDIT ratchet |
| Conversation channel (chat-journey ADR-141/142, journal quotes, intake) | Load-bearing | $, mind | The fourth ingestion surface; consent machinery is the fence |
| Paging channel (ADR-143, ≤5-alarm P1 set) | Load-bearing (small) | attention | Guard-tested ≤5 cap; 2 false pages/quarter reopens membership |
| Stats/forecast machinery (stats_core, hypothesis tester, calibration ledger) | Load-bearing | mind | The credibility moat (ADR-105) |
| personal-baselines monthly compute | Load-bearing | $ (small) | Bands from own variance (ADR-105 r4); retire only if banded verdicts stop |
| AI QA gates — vision (`visual_ai_qa`, Bedrock semantic screenshots) + prose (`reader_truth_qa`) | Load-bearing | $ (measured 2026-08-30, #3251: vision $0.0455 → $0.0475 per deploy run, n=16/11, + $0.53 per Sunday full-surface pass; prose $0.195 per daily fire, n=3), CI | Vision gating CI per-deploy since 2026-06-05 — holds the only recorded catch (2026-08-27). **Re-priced 2026-08-30 (#3251, option C1):** the prose judge was 81% of a $0.239/run pair and its most recent recorded output was two reproducing false positives that rolled back a healthy deploy, so it left the per-deploy copies for the daily standalone (non-gating: advisory issue + SNS; accepted ~24h exposure; ~$39/mo given up at the measured 6.7 deploys/day). Demote trigger for the daily prose fire: a quarter with zero true findings; re-promote to per-deploy if it records a catch a deploy-window pass would have held. |
| Required pre-merge lane (ADR-148 + the structural-gate extension, #1662/#2372-adjacent) | Load-bearing | CI | Rent: ~3 min CI per PR. Earned same-fortnight: caught I5/SR1/I3/type-hints on the Telegram surface pre-merge and 3 union/size breaches on 2026-08-09 alone. Demote trigger: a quarter with zero pre-merge catches (2026-08-09) |
| Module-size ratchets ×2 (1200-line hard ceiling + BASELINE no-grow, #1665) | Load-bearing | CI | Self-justified by four same-day catches at BASELINE enforcement start (2026-08-09, incl. two on this very ledger's fortnight PRs); the #2373 coupling assertion is its named follow-up. Demote trigger: per the guard's own text — baselines only shrink |
| Recall corpus writer + freshness watcher (semantic_recall, #1384/ADR-150) | Load-bearing (small) | $ (embeddings, small), CI | Publish-time indexing + nightly link/existence QA; corpus froze 18 days when it had no automated writer — the watcher is the anti-recurrence. Demote/scope trigger: #2347's corpus-scope decision (2026-08-09) |
| The review estate — one `/review <lens>` spine, seven lenses (full, accuracy, craft, sdlc, site, platform, journey) | Load-bearing (rotating) | $ (~2–3M tokens/run), attention | Nine commands folded to one spine + per-lens rubrics on 2026-08-30 (#3250) — the shared phases and the filing contract now exist once. Demote any lens findings-empty twice consecutively (standing rule above); cadence, exemptions and the set guard are `scripts/operating_calendar.py` |
| golden-brief-eval workflow | Load-bearing | CI | Deterministic canary gating every push |
| Remediation agent (ADR-064; triage-email-only shape since #2833) | **Portfolio** — as a *triage instrument*, shadow permanently (ADR-129 amendment 2026-08-30) | `$` **measured:** `LifePlatform/AI::EstimatedCostUSD{LambdaFunction=remediation-agent}` = **$1.60 over 2026-08-24→30, n=9 emitting runs of 11 (3 scheduled Mon/Wed/Fri, 8 urgent-dispatch/manual)** ≈ $0.18 per run; at that rate the Mon/Wed/Fri leg is ~$2/mo (~13 runs) and all triggers ~$7/mo. The series only exists since #3070 (2026-08-24) and is the Agent SDK's self-reported cost, which under-counts Cost Explorer (the `CostMetricDriftRatio` caveat); the `CallerClass=remediation` copy the governor splits on had **no datapoints yet** on 2026-08-30 (first emission expected at the 2026-08-31 run) — re-cite from it at the next re-read. Plus CI (one job/run), attention (one curated email/run), mind. The reshape removed `surface` (no self-merge path exists to price) and one workflow step; Bedrock spend is unchanged by it — triage was always the whole bill | **Re-priced 2026-08-30 (#2833):** the owner decision closed the "undecided middle" (full cost, zero self-healing). Measured for the decision: SSM `remediation-mode` = `shadow` since 2026-07-06; `auto-fix-safe` PRs merged in either mode since June = **zero** (`gh pr list --label auto-fix-safe --state all` returns none — none was ever opened); `remediation-log/automerge/` = **0 objects**; `auto_earn_marker.json` untouched since 2026-07-06 17:31Z (54 bytes). Retired with the mode: `remediation/automerge.py` + its tests, the earn marker + `earn_or_shadow_check`, the #1337 10-clean-run bar, the workflow's gate step. Earns its keep by the needs-human email being acted on and by proposed PRs merged by a human. **Demote trigger:** two consecutive quarterly re-reads in which the agent's proposed PRs merged by a human = 0 AND the needs-human lines it raised were all already known from another channel (alarm email / ops pack) — then it is duplicating the digest at $7/mo + a job and moves to `Retire-candidate` with a dated deletion |
| fresh-eyes weekly survey workflow | Portfolio | $ (small) | **This row's own revisit trigger FIRED on 2026-07-26 and nobody noticed for a month** — found by the 2026-08-27 re-read, and the cleanest instance on this ledger of a trigger with a bar but no clock. Measured now: **7 runs, 7 successes** (2026-07-12 → 2026-08-23), each having landed its board at `remediation-log/fresh-eyes/…` (verified in S3), so the machinery is not the question. "Too new to grade" is retired as of this re-read. **The grade that is still owed is findings-acted-on, not runs** — deliberately NOT manufactured here. New trigger, dated: at the next quarterly re-read (**due 2026-11-27**), count boards whose findings reached an issue or a PR; zero across a quarter of weekly runs retires the workflow, since a pre-computed board nobody consumes is a session-prep artifact the session is not reading |
| Personal/Product deliberation boards (BOARDS.md) | Portfolio | none (no runtime) | Decision-quality tooling demonstrated in public |
| Social syndication poster (#1629, gated/unbuilt, ADR-140) | Portfolio | none yet | Dated retire trigger (made falsifiable 2026-09-01, #3376 — "no measured referral traffic" previously named a measurement that did not exist): 90 days live with `LifePlatform/Traffic::SyndicatedReferrals7d` (utm_source-attributed page views from the site's own CloudFront logs, emitted weekly by the traffic digest) summing 0 → delete the lambda, keep `scripts/post_social.py`. Corollary the poster inherits: every link it projects MUST carry `utm_source=<channel>`, or its own retire-trigger cannot see the traffic it earns |
| Daily-fingerprint broadcast payload (#1402, `content/fingerprint_broadcast.py` + the `og_moments` fingerprint sweep) | Portfolio | S3 (one dated PNG + shell/day) | No new service: the card is the unchanged #1379 render and the sweep is one more class in the existing moments pass. Human-post-only by ADR-140 rule 5. Retire trigger: **90 days with no manual fingerprint post** (`post_social.py --report`) → drop the sweep class, keep the page card. **Re-read 2026-08-27: not fired, and now it has a clock.** The trigger shipped without a start date, so "90 days" was unresolvable; the postable path landed **2026-08-06** (`fingerprint_broadcast.py`, PR #2167), which dates expiry at **2026-11-04**. Live `post_social.py --report` reads *"No posts logged yet. Days used in the last 30: 0/30"* — 21 days in, zero posts. On present behaviour this trigger fires |
| Social membrane inbound (youtube RSS ingestion, #1668) | Portfolio | surface (small) | Keyless by design; retire if no captures used by a public surface in 90 days |
| /legacy preserved v3 site | Portfolio (archive) | none | Zero maintenance; retire only if storage/privacy cost appears |
| State of Mind subsystem (HAE How-We-Feel) | Kept (load-bearing-pending-data) | mind | ADR-121: habit restart chosen over prune; flips to retire if habit not resumed by 2026-Q4 review |
| Privacy-tier field registry + consumer gate (`lambdas/privacy/field_tiers.py`, `tests/privacy_tier_wiring.py`, #2803) | **Load-bearing (small)** | one build-time AST scan of `lambdas/` + `mcp/`; one policy line per discovered (module, field) pair — 4 today | Priced against the ADR-103/144 caveat before building, as #2803 required. The rent is genuinely small because the vocabulary is small: 3 fields, 4 discovered pairs, and the scan is pure AST with no runtime cost or deploy surface. What it buys is the one thing a checklist could not — #2782's careful consumer sweep declared "all consumers are field-selective" and #2809 measured it FALSE, because a hand sweep missed `mcp/tools_data.py`. Highest blast radius in the platform (public exposure of medical data). It also RETIRED a duplicate: `TIER2_STRIP_FIELDS` now derives from the registry instead of restating it. **Re-read 2026-08-27 — this row was the ledger's worst mis-pricing, and in the way that matters.** The rent above still read "3 fields, 4 discovered pairs"; measured from `tests.privacy_tier_wiring.CONSUMERS` it is **31 distinct fields across 42 (module, field) pairs in 15 modules** — roughly 10x, banked by #3055/ADR-155's full registry port and never re-priced. The row's *verdict* (Load-bearing, small) survives the correction; the *argument for it* does not. The rent is small because the check is a build-time AST scan with no runtime, deploy or `$` cost — **not** because the vocabulary is small, which is what the row claimed and is no longer true. A posture defended by a stale reason is a posture nobody can re-derive. **Demote trigger, re-pointed:** the old trigger named "a per-field privacy column in `source_registry.py` (the #2797 target state)", and **#2797 closed `realized` on 2026-08-22 without ever landing one** — `source_registry.py` carries `qa_tier` and no privacy facet, so as written the trigger had no owner and could never fire. It is therefore re-stated as a condition rather than a project: **demote when the tier vocabulary is enforced at the field's point of definition** (a privacy facet on the source registry, or an equivalent single declaration every consumer must read) such that this sweep is asserting something already structural — not before, and never by deleting the gate while the fields stay Tier-2. |
| Reader-truth debt ledger (`tests/truth_baseline.json`, #2956) | **Load-bearing (small)** | one triaged entry per standing finding; an UNTRIAGED entry reds the unit suite | It is the only thing separating "this deploy broke truth" from "the site carries truth debt" — without it the armed gate rolled back 3 healthy deploys. **Demote when** the ledger reaches zero entries and stays there for a month: at that point the gate can go back to bare. **Re-read 2026-08-27 — the first half HAS FIRED:** `tests/truth_baseline.json` carries `pages: {}` (zero entries) as of commit `138673275`, **2026-08-26**; the commit before it held 2. The month clock is therefore RUNNING and the demote decision falls due **2026-09-26** — recorded here because the trigger as written had no clock, so the condition could have been satisfied indefinitely with nothing to notice. Re-check the count before demoting: a single new baselined finding restarts the month. **2026-08-30 (#3251, C1):** the gate this ledger de-noises no longer holds a rollback — the prose judge left the per-deploy copies for the daily standalone, so the ledger's rent is now paid against an advisory issue, not a deploy. The demote clock is unchanged; the case for demoting on 2026-09-26 got stronger, not weaker. |
| Chronicle / Weekly-Signal delivery dead-men (#2820) | Load-bearing (small) | 2 alarms + one metric emission per send | The subscriber promise is a *promise*; a silent no-send was previously invisible on every channel. **Demote when** delivery moves to a platform that reports its own failures. |
| Alarm flap detector in the wrap citation gate (#2912) | Load-bearing (small) | one `describe-alarm-history` read per wrap | Caught 3 invisible fired-and-cleared episodes on its first live run. **Demote when** alarm periods stop producing sliding-window flap (i.e. the Period=86400 alarms are re-cut). |
| Producer↔gate cron mirror check (#2818) | Load-bearing (small) | one AST diff per `cdk/**`/`lambdas/**` PR | A moved cron silently drifted the QA window for a full winter. **Demote when** QA windows are read from the schedule at runtime rather than mirrored. |
| chronicle-podcast season-1 lambda (unscheduled — but see below; "zombie" is now the wrong word) | **Retire-candidate** | surface | **Re-read 2026-08-27 (this IS the 2026-Q3 review the trigger named).** Unscheduled: confirmed — `list-rule-names-by-target` returns nothing, so the #310 cron really is gone. But the row assumed dormancy, and live telemetry says otherwise: **12 invocations, 0 errors, across 5 days (2026-08-03 → 2026-08-16)** in the trailing 30. So the "one further back-catalogue re-render window" the trigger was waiting on **has happened**, and it worked. Two honest readings follow and the choice is the owner's, not this re-read's: the condition is literally satisfied (delete), or the function is a working manual operator tool for feed repair and should be re-postured `Kept (manual tool)` with the schedule-absence as the whole point. **Recorded rather than silently rolled to "Q4 review", which is what a re-read exists to prevent.** Decide by **2026-09-30**; a further quarter at zero invocations makes deletion unambiguous |
| Nightly edge-429 observation (#3058: qa-smoke `ratelimit:edge_429`, `lambdas/operational/qa_check_edge_429.py`) | Load-bearing (small) | 6 zero-model-cost POSTs/night + one qa-smoke check slot; attention on RED | The only standing observation of edge enforcement — the 2026-08-14 class (in-Lambda limiter silently broken, all CI green) had no detector but a manual habit. First live run observed a real 429. Two AST lockstep pins keep the probe premise honest. **Demote when** a WAF rate rule lands at the edge (the 2026-10-15 revisit) — then pre-invoke rejection makes the nightly trip redundant — or if the charge-before-validation premise breaks and the probe is redesigned |
| Budget-integrity fail-closed channel (#3059: `FAIL_CLOSED_FEATURES` in budget_guard + `budget-tier-unreadable` alarm, `cdk/stacks/monitoring_budget_alarms.py`) | Load-bearing (small) | one metric filter + one alarm evaluation; one ERROR log line per failed tier refresh | DIL-036's structural close: an IAM/SSM regression used to silently disable the monthly ceiling for the PUBLIC surface (bare except → tier 0); now public inference denies and the operator is paged with the failure class named. Proven immediately: the split logging surfaced the CI diagnosis role's missing budget-tier grant the same day it deployed (#3064). **Demote when** #2824's generalized grant-enumeration set-guard covers the SSM consumer set — the alarm then watches a provably-granted path and can fold into the digest tier |
| Grant-enumeration drift sweep (#2824: `tests/grant_enumeration.py` + `tests/test_grant_enumeration_drift.py`) | Load-bearing (small) | ~5s of one CI job (offline halves); one read-only `iam:GetRolePolicy` per OIDC role on the integration lane; three dated ratchets to prune | Generalizes the #1196 per-action lockstep to the whole fail-closed-channel SET (SSM `/life-platform/*`, Secrets, `config/` objects, SES config-sets) across 103 wired Lambdas and every workflow job that assumes an OIDC role — the n=16 incident class (P1×2: the 2026-03-10 role deletion, the #2503 missing `config/` grant that no-op'd the privacy scrub for 5 days with `Errors=0`) whose four existing guards each covered exactly one resource. First run found **13 real, verified gaps** (3 social-ingest Lambdas cannot read the content-filter vocabulary; the golden-eval OIDC role cannot read `budget-tier`, so its weekly Bedrock spend is outside the ADR-125 ceiling — the third member of the #2959 class after #3059's). **12 were closed the same day** (2026-08-23) in `role_policies_{ingestion,operational,serve,compute}.py` + the golden-eval permissions doc; the 13th (chronicle-sender ↔ `config/personas.json`) was deferred behind #3037's concurrent rework of `role_policies_email.py` — **and the 2026-08-27 re-read finds that blocker gone: #3037 is MERGED, while `_OPEN_GAPS` still carries the entry and its now-expired reason.** The gap is unblocked, not fixed; it is one additive S3 statement in `role_policies_email.py::email_chronicle_sender`. The repair is the payoff, not the finding: nine additive least-privilege statements, no resource replacement in synth. Consumers are derived by import-scoped call graph, not import closure (closure invented 35 phantoms of 46), and grants include the `create_platform_lambda` baseline as well as `role_policies_*` (omitting it invents ~30 more). (**Re-read 2026-08-27, ratchet state measured:** `_OPEN_CI_GAPS` and `_PENDING_LIVE_APPLY` are both **empty**; `_OPEN_GAPS` holds the single unblocked entry above. `_DYNAMIC_REFERENCES` is at **13** against the ~20 immediate-demote clause — not fired, but it is the number to watch, and it grew by one when #2846 pulled HAE into `create_platform_lambda`.) **Demote when** two consecutive quarters pass with zero new gaps caught AND all three ratchets are empty — the population is then stable enough that the four narrow per-resource guards suffice; or immediately if the dynamic-reference whitelist grows past ~20 entries, which would mean the tree has moved to runtime-computed resource ids and static enumeration no longer describes it. |
| Public-claims registry (#3042 Phase D2: `tests/public_claims_registry.py`, `tests/test_public_claims_registry_3042.py`) | **Load-bearing (small)** | CI (one pre-merge sweep of `site/**`, `scripts/**`, `CONTRIBUTING.md`, `README.md` — 34 surfaces today, ~0.3s); one registry entry per public behavioural claim — 3 today (4 until #2833 retired the auto-merge gate on 2026-08-30 and the essay's verbatim cap quotes became past tense with it) | The external diligence review's false positives were **manufactured by this platform's own prose**: public sentences stating behavioural claims with no checkable source. The rent is small because the vocabulary is small — a claim is registered by its whole assertion, not a keyword, so the sweep is 3 phrase sets rather than every sentence on the site. What it buys is the thing a proofread cannot: the remediation-mode claim lives in ONE generator editorial and ships to **31 pages**, and the org-chart essay quoted two integers out of the (now retired) gate module verbatim in a different tree. Both already drifted once (ADR-129's demotion corrected by hand; #2611 removed the IAM family from the ALLOWLIST while the essay stayed put); since #2833 the mode claim is derived from the SHAPE (no gate module, no merge step, `gh pr merge` disallowed, live modes off|shadow) rather than from a gate's precondition. Runtime-state claims follow the `v4_build_permanence_terms.py` pattern — recorded value offline, live SSM reconciliation opt-in with a loud SKIP — so no comparator needs AWS in CI. **Demote when** every registered claim's stated-where is generator-derived rather than hand-authored prose (at that point the generator IS the derivation and the comparator is redundant), or if the registry stops growing while public claim surfaces do — a registry of 3 against a site of hundreds is honest only while it is being extended. |
| No edge WAF — deliberate absence (#2828/DIL-014, owner decision 2026-08-23) | **Priced acceptance (dated)** | $0 (the declined build was a minimal CloudFront rate rule, ~$8/mo at ~400k req/mo) | The accepted residual is the 2026-08-14 class (in-Lambda enforcement silently broken) COINCIDING with actual abuse — never observed in ~6 months public. Compensating stack, named: fail-closed `client_ip.py` (AST-guarded, wire-proved), DDB atomic counters, fan-out pricing, budget-guard hard ceiling (fail-closed for public inference since #3059), `site-api-invocation-spike` detection, and the **nightly `ratelimit:edge_429` observation (#3058)** which shrinks a silent limiter break to ≤1 day. **Revisit 2026-10-15** (owner-set), or immediately on: any observed abuse, a non-self-inflicted invocation-spike fire, or a commercialization decision. |
| Owner-data deletion-propagation machinery — deliberate absence (DIL-050, priced 2026-09-01) | **Priced acceptance (dated)** | $0 (nothing built) | The subscriber half is SHIPPED (#3044: deletion promise, retention behavior, tokenless unsubscribe); the absent half is propagation machinery for the OWNER's own data across raw/ archives, derived partitions and replicas — a single-subject platform where the data subject is the operator, so no external erasure obligation exists today. **Revisit:** an actual erasure obligation · a second user · commercialization · an ADR-155 consent-surface change |
| No manual WCAG audit — deliberate absence (DIL-051, priced 2026-09-01) | **Priced acceptance (dated)** | $0 (nothing built) | The automated half is standing and gating: `tests/a11y_audit.py` (axe) inside the visual-QA CI lane + light-theme and landmark guards; the recorded blind spot (color-mix contrast) has a written discipline (prove by arithmetic; read CI's report.json). A full manual WCAG audit is deliberately not performed for a single-operator personal site. **Revisit:** measurable reader-audience growth · any accessibility complaint · commercialization |
| Architect operator leg (#2849 leg 1: cloud routine `architect-operator-2849`, `docs/OPERATOR_SUBSTRATE_SPIKE.md`) | **Load-bearing (small)** | ~1 opus cloud session/week — most weeks a no-op calendar check (the routine exits after one curl + one script run); a ritual week costs one review session. Gates itself on the public `/api/inference_receipt` budget tier (≥1 = stop, ADR-125) and is bounded to ONE ritual per run by prompt contract | The #2832 operating calendar was the platform's only dead-man estate whose EXECUTION still required the owner's laptop — a ritual overdue while the owner is away was previously unrunnable, which is how craft-review and proportionality-reread sat NEVER-RUN for weeks. This leg runs the most-overdue due ritual weekly, report-only (artifact lands as a PR the owner batch merges — the merge, not the routine, resets the clock; a reviewer that can fix things stops being a reviewer). Zero MCP connectors, no AWS credentials, repo checkout only. **Demote when** the calendar retires, or if two consecutive quarters show every ritual still being run from owner sessions before the routine reaches it (the leg would be measuring nothing) — then delete the routine, not the calendar. |
| **No independent evidence coaching changes behavior** — DIL-039/040 (2026-08-29, Fable re-grade; the row the D5 finalization missed) | **Priced acceptance (dated)** | $0 built. The declined build is an intervention-evidence loop: pre-registered coaching interventions with graded behavioral outcomes and adverse-effect tracking | The public product claims coaching happens; it does not claim coaching WORKS — the D0.5 sweep already deleted the overstatements (DIL-042). What remains priced is the positive evidence loop the §12 "Product" criterion asks for. It was structurally impossible before #3046 (nothing gradable existed); it becomes mechanically possible as prediction windows mature. **Revisit trigger: the first graded-prediction cohort (≥10 graded outcomes; earliest window 2026-08-31 — this trigger is weeks out, not aspirational)** · any public sentence claiming coaching caused an outcome · commercialization. **2026-09-02 sharpening (#3450):** the first cohort is in and the calculation-proof pass re-derived it exactly — read as **8/37 confirmed, Wilson 95% CI [11.4%, 37.2%]**, not the bare "21.6%" a percentage alone implies; `calibration_core.score_pairs` now serves that interval (`accuracy_ci95`) on every surface it feeds, so this row's own number carries its uncertainty going forward. |
| **Single-subject validity of all scoring/calibration** — DIL-049 non-cheap half (2026-08-29, same walk; D4 shipped the cheap transparency half 2026-08-24) | **Priced acceptance (dated)** | $0. The declined build is external-validity machinery (a second subject, cross-validation against population priors, formal generalization bounds) | Every threshold is derived from personal variance by design (ADR-105) and every scorecard says n=1 out loud after D4's sweep. What cannot be built at this headcount is evidence the numbers generalize — and the platform makes no such claim, which is the compensating control. **Revisit triggers: a second subject · any external claim of generalization · a published methods artifact.** |
| Idempotency census + send ledger (DIL-025: `docs/IDEMPOTENCY.md`, `lambdas/common/send_ledger.py`, `tests/test_idempotency_census_dil025.py`) | **Load-bearing (small)** | One DDB Query per guarded send (bounded `Limit=8` on a partition the sender already writes — no new table, no new row, no migration); ~5s of CI; one census row per new sender | Third of the three send gates, and the only one that answers "did this letter ALREADY go out": `send_guard` (#2222) answers operator intent, `daily_brief_lock` (#2860) answers concurrency, and a 1200s lease cannot see a `rate(6 hours)` DLQ redrive that re-invokes with the original payload. The census earns its own keep by **correcting the premise**: the SES set is 28 handlers, not the 7 the external review assumed, and 22 of them still have no replay guard — a number that only exists because the set is AST-derived from the same walk #2222 uses rather than hand-listed. Rent stays small deliberately: the guard checks a row's PRESENCE, not its prose, so it costs one line per new sender and cannot itself rot into a second source of truth. **Demote when** every sender in the set is guarded by the shared primitive AND #3113/#3114/#3115/#3118/#3119 are closed — at that point replay-safety is a property of the paved road (`send_ledger` is the only way to send) and the census becomes a generated artifact rather than a maintained one; or immediately if the census starts carrying rows nobody updates while senders keep shipping, which would mean the guard is being satisfied rather than read. |
| S3 lifecycle declared-vs-live drift assertion (DIL-026/#2799: `deploy/s3_lifecycle.json`, `deploy/drift_sentinel.py::check_s3_lifecycle`) | Load-bearing (small) | one `get-bucket-lifecycle-configuration` read in the weekly remediation sweep; one JSON file that is now the SAME source both `apply_s3_lifecycle.sh` and the sentinel read (no second hand-maintained expectation to drift against the first) | The bucket is imported into CDK (`Bucket.from_bucket_name`), so lifecycle rules live entirely out-of-IaC in a shell script nothing ever verified against live state — `imports/` sat with zero `NoncurrentVersionExpiration` coverage (2.07GB, unbounded age) for as long as the prefix existed, found only by hand during #2642's pass and explicitly left out of scope. The only standing signal was `life-platform-s3-bucket-size-high`, a post-hoc SUM across every prefix that cannot name which one drifted. Fixed at the source (`imports/` now covered, same shape as `raw/`) and closed the class (a future uncovered prefix, or an out-of-band console edit to any declared rule, reds within a week — mutation-proved missing/extra/changed-rule, both directions). Live sizing also found the threshold itself was stale for legitimate growth (deploys/ 7-day noncurrent churn now runs ~41-44GB under routine multi-agent deploy cadence, not a defect) — re-derived 50GB→65GiB from the measured ~52-55GB steady state rather than silently bumped. **Demote when** the bucket is re-imported under full CDK ownership (lifecycle becomes template-diffable and `cdk diff`/CFN drift detection already covers it — this sentinel becomes redundant with check #1) |

| Isolated `raw/` backup — CRR to us-east-2 + its weekly assertion (#3042 DIL-027: `cdk/stacks/backup_stack.py`, `deploy/s3_replication.json`, `deploy/sentinel_replication.py`) | **Load-bearing (small)** | $ **$0.015/month measured** (0.504 GiB replicated storage + 563 PUTs + transfer, from the live 2026-08-24 inventory — one-time backfill $0.49, RAN 2026-08-30: 41,175/41,175 replicated, sentinel clean); one bucket, one IAM role, one out-of-IaC script row, one weekly check | The rent is a rounding error and the thing it buys is the only irreversible loss on the platform. `raw/` — original wearable/API captures — is the sole unrecomputable data: DDB metrics, every derived artifact and the whole site rebuild from it. Its only protection was same-bucket versioning, which survives an overwrite and nothing else; `docs/DISASTER_RECOVERY.md` scored bucket deletion **NOT RECOVERABLE** and a us-west-2 outage **hours-days, no DR region**. Deliberately NOT us-east-1 (LifePlatformWeb's control plane is already there). The check is not ceremony: a replication configuration is replaced wholesale by one `put-bucket-replication`, and nothing raises when it stops — so the sentinel probes real objects on the wire and reds on five independent causes including the easy-to-forget one (**replication is not retroactive** — the pre-existing history is unprotected until the S3 Batch Replication backfill runs). **The residual is account isolation, not cost**, priced and dated in `docs/reviews/DILIGENCE_2026-08-23_RESPONSE.md`. **Demote when** a second AWS account exists and the destination moves cross-account (this build becomes the fallback leg, and the weekly check narrows to the cross-account edge), or if `raw/` ever stops being the platform's unrecomputable root. **Never** by deleting the check while the replication stands — an unasserted backup is the thing this row exists to prevent. |
| Compute source-completeness contract (#3049/DIL-024: `lambdas/common/input_manifest.py`, stamped at `common.compute_metadata.tag_record`, `tests/test_input_manifest_contract_3049.py`) | Load-bearing (small) | 8-10 extra 1-item DDB queries per compute run (~50/day fleet-wide, well inside free-tier RCU noise, one pass cached per (Lambda, PT day)); ~1KB of manifest on 8 output partitions; one declaration tuple per compute Lambda to maintain | The five compute Lambdas ran blind on fixed UTC crons whose ordering was held by a wall-clock comment, and a late connector produced an **unqualified** complete-day score — the only detection was a post-hoc `ComputeOutputsMissing` alarm, which watches for a missing OUTPUT and is structurally blind to a present output built on absent INPUT. The manifest makes the blindness visible without touching the schedules (#3049 scope guard: no event-driven rebuild). Rent stays small because it is ONE helper at ONE chokepoint, not five copies: `tag_record` already stamps `run_id`/`computed_at`/`phase` on every compute write, so the five Lambdas carry **zero** call-site plumbing, and thresholds are read from `source_registry.stale_hours` rather than restated (#2003). **Demote when** either (a) the crons are replaced by real arrival-gated triggers — at that point the manifest describes a condition the scheduler already enforces and collapses to an assertion — or (b) two consecutive quarters pass with zero `partial` runs observed on `/api/character`, which would mean the connectors are reliable enough that the qualification is noise. Raise the posture if a downstream consumer starts BRANCHING on `input_status` (it is provenance today, not control flow — a gate would need its own ADR). |
| **Absent commercial control plane** — DIL-033/034/037/043/044/045/046 (2026-08-25, D5) | **Priced acceptance (dated)** | $0 built. The declined set: multi-tenant isolation, customer-facing SLAs and status page, a support/on-call rotation, contractual data-processing terms, a formal change-advisory board, vendor/subprocessor management. Realistic build+run cost is measured in engineer-months and hundreds of dollars/month, against a platform whose AI ceiling is $215/mo | The external review graded these as gaps because it graded an *acquisition target*. This is a **single-user platform with one operator and no customers**, so each control would govern a relationship that does not exist — the classic case of buying machinery whose rent is certain and whose benefit is hypothetical (ADR-103/144). The compensating position is that the platform's actual obligations (its own data, its own reader-facing claims) are covered by controls that DO exist: the privacy tier registry (#3045/ADR-155), the deletion contract (#3044), the public-claims registry (#3072), the budget ceiling (ADR-133), and the disaster-recovery posture (DIL-027). **Revisit trigger: a commercialization decision — a second user, any paid tier, or any contractual/regulated data obligation.** Any one of those flips this row from "priced" to "must build", and the seven findings become the first sprint of that work, not a surprise. Not date-based: waiting does not make a customer appear. |
| **No clinician-reviewed hazard register (full clinical operating model)** — DIL-031 (2026-08-25, D5) | **Priced acceptance (dated)** | $0 built for the full model (a named clinical reviewer, a maintained hazard/mitigation register, an incident-to-clinical-review path). D4 builds the **clinical-lite** half instead: deterministic refusal/escalation phrase contracts on the coaching surfaces | The platform gives one adult health-adjacent coaching about his own measured data; it is not a medical device and makes no diagnostic claim. A full clinical operating model presumes a clinician in the loop, and there isn't one to review anything — commissioning that review is an owner decision with real recurring cost, not an autonomous build. What IS built is the part that protects the actual user without a clinician: deterministic escalation copy on the paths where a real hazard could surface, plus ADR-104's grounded-generation gate and ADR-105's uncertainty-and-n requirement, which together stop the narrative surfaces from manufacturing confidence. **Revisit triggers:** a second user of any kind · any claim on the public surface that crosses from description into diagnosis or treatment advice · a real adverse event traced to platform output · commercialization. |
| **Key-person concentration / no segregation of duties** — DIL-047/048 (2026-08-25, D5) | **Priced acceptance (dated)** | $0. The declined controls (a second operator, dual-control on production changes, a rotation) all require a second human | Structural to a solo operator, and honestly the review's sharpest non-commercial finding: one person holds every credential, every decision and all the context. It cannot be *fixed* at this headcount, only **mitigated and disclosed**, and the mitigation is real: the re-entry hardening path (`scripts/check_reentry_hardening.py` + the estate kit, with the ⚠️ UNDOCUMENTED rows deliberately loud), stolen-laptop resilience at ~90%, secrets held in Secrets Manager rather than in a head or a laptop, and — the load-bearing one — the fact that this platform's operating knowledge is written down continuously (CHARTER, CONVENTIONS, ADRs, runbooks, this ledger) rather than resident. **The honest residual is recovery TIME, not recoverability.** **Revisit triggers:** a second operator exists · the owner-handoff drill runs and finds the written record insufficient · commercialization. **Explicitly NOT closed by the DIL-027 backup** — that protects data, not decision continuity. |
| **Self-approvable production gate** — DIL-004 residual, folded under #2834 (2026-08-25, D5) | **Priced acceptance (dated)** | $0. The gate itself is live and blocking (verified 2026-08-25 by `scripts/diligence_verify.py::control_production_approval_gate` — `required_reviewers`, 1 reviewer); the residual is that the sole reviewer is the sole author | The review filed this as "no production approval gate", which is **WRONG live** — but the correction is not that the finding was worthless. A one-reviewer environment where author and approver are the same person is a *pause*, not an *independent check*, and calling it the latter would be the kind of self-flattering claim the D0.5 sweep exists to delete. Its real value is the deliberate stop before a deploy and the audit record of who clicked; that value is genuine and this row records it at its true size. Compensating controls do the work an independent reviewer would: CI's gates (lint, full suite, deploy-critical subset), the auto-merge deterministic ALLOWLIST/DENYLIST with IAM held OUT since #2611, post-deploy smoke + visual QA with auto-rollback, and the drift sentinels. **Revisit trigger: a second operator exists** (then required_reviewers goes to a genuine second party and this row retires). Not date-based — time does not make a solo operator two people. |
| Merge train (`deploy/merge_train.sh`, #3104) | Load-bearing (small) | driver attention per train; ~24 offline tests in CI | N green PRs verified ONCE by name, reconciled onto one accumulating tip (ONLY `platform_counts.py` by regeneration — any other conflict is a loud drop), validated as a stack, merged back-to-back with verdict-read and merge in separate commands (#3103). Motivating measurement: ~8 serial rebase+regen+recheck cycles at ~20 min each on a 9-PR train, the night after #3101 killed the wider literal class. Dogfooded the night it merged (its live run also became the event-swallow incident's instrument — 7 honest drops with named reasons beat 7 silent hangs). **Amended 2026-08-26 (#3200/#3209):** phase 1 now consumes a CLASSIFIED verdict — `wait_pr_green.sh` exits 4 `GREEN-WITH-RECONCILE-OWNED-RED` when the sole red is the Wiki-drift gate and every path in its own ❌ list is reconcile-owned (the sanctioned set DERIVED at runtime from `agent_commit.sh`'s refusal list, never hand-copied); any other red, or one non-reconcile path, still exits 1. Rent unchanged (3 fixture tests). **The rent that surprised us:** it shipped verdict-closed with 60 green tests and NEVER FIRED — the enrichment read the job log via `gh run view --job --log`, which omits the failing step for some jobs and not others; fixed to the raw jobs/logs endpoint (#3209) and live-proved at exit 4 before being trusted. A fail-closed path degrades silently to the status quo, so it carries a standing obligation: **re-prove it live after any change to its log source or GitHub's log API**, because its failure mode is invisible. **Demote when** the single-writer `test_count` literal itself is retired (a served-at-request-time derivation or a merge-queue) — the train then has no conflict class left to manage |
| Clinical-lite hazard gate (`lambdas/ai/safety_contract.py` + `_req.hazard_gate`, #3050/DIL-031-lite) | **Load-bearing** | zero runtime $ (pure regex, no I/O, budget-tier-immune); 123 offline tests; a benign-corpus false-positive review whenever patterns widen | The only deterministic layer between reader free-text and health-adjacent model output: 5 hazard classes answered with fixed copy INSTEAD of a model call (the platform cannot hallucinate advice it never generated). Found its own justification while being wired: board_ask's opening turn had NO input filter of any kind. Wire-proven on production the night it shipped, both directions (recall AND the exertion-idiom precision that keeps a fitness site usable). The full clinical operating model above this floor is the dated DIL-031 priced row above. **Demote: never while the doors accept free text** — but re-review the pattern corpus if the benign suite's false-positive rate ever forces a widening war |
| Pair-seam conformance guard (`tests/pair_seam_guard_lib.py` + residue ledger, #2847 box 4 / charter rule 3, PR #3194) | Load-bearing (small) | 22 pre-merge tests (~7s); the shrink-only seam ledger (**285 rows on 2026-08-27**, down from the 286 seeded 2026-08-25 — the re-read's direct check that "shrink-only" is a behaviour and not a slogan); one prune per enrolled contract | The at-birth half of the pair-contract story: a NEW must-agree seam (partition × module × direction, derived LIVE from the #2845 model's edge plane — never a committed file, whose batched regeneration would be dark on exactly the PR that adds the seam) either gets a contract or takes a dated ledger row; an enrolled contract FORCES its rows out (the countdown fired 3× for real within hours of landing: #3193's enrollment → main red by design → prune). Known limitation pinned: model resolves 824/1138 edge sites, ≥65% floor asserted. **Demote when** the pair-contract framework itself demotes, or a quarter passes with zero countdown firings and zero new seams — then fold the sweep into the #2844 conformance report |
| DynamoDB TTL declared-vs-live parity (`check_dynamodb_ttl` in `deploy/drift_sentinel.py`, #2799 fold, PR #3191) | Load-bearing (tiny) | one read-only `DescribeTimeToLive` per weekly sentinel sweep; a registered can-fail proof (#2578 census 15→16) | The #951 class (a TTL attribute silently unset ≠ the config the code assumes) had recurred once and had ZERO repo-wide `describe_time_to_live` references before this. Same declared-vs-live idiom as the S3-lifecycle check; the role grant rode in the SAME PR (the #2824 grant-with-consumer discipline). **Demote when** the sentinel itself demotes, or the check fires zero findings for two quarters AND the TTL surface stops changing — then it is one assertion inside the integration checks, not a sentinel line |
| Security-tier log-retention declared-vs-live parity, every region (#3278: registry `cdk/stacks/constants.py::SECURITY_TIER_LOG_FUNCTIONS`, `deploy/sentinel_log_retention.py::check_log_retention`, writer `deploy/apply_log_retention.py`) | Load-bearing (tiny) | one `DescribeRegions` + ~120 `DescribeLogGroups` reads per weekly sentinel sweep (free-tier API calls); one new IAM action (`ec2:DescribeRegions`) on the remediation role; ~1 MB more log storage per security function per month at 90d vs 30d (cents); a registered can-fail proof (#2578 family 6, both halves) | `docs/DATA_GOVERNANCE.md` promised 90 days for the security tier since 2026-05-17 and NO log group in any of 17 regions carried it: CDK set 30 uniformly, and the two Lambda@Edge auth gates — never CDK-created — were 30 in the two regions someone once hand-set and NEVER_EXPIRE in the five replica regions nobody had, holding real auth-gate bytes. The class is *a governance number that exists only as prose*: the fix declares it once, derives the CDK tier from the registry by construction, and asserts the doc row against the same constant, so the table and the config are one edit. The weekly sweep is the only mechanism that can cover a Lambda@Edge replica group in a region that starts serving tomorrow (CDK owns one region per stack; CloudWatch Logs has no account-level retention default) — honestly detect-then-apply, not prevention. **Demote when** the edge auth gates are retired (PRIVACY_MODE off is not retirement — the functions still execute and log) or Lambda@Edge log groups become centrally ownable; then the check collapses to the three CDK-owned groups, which CFN drift detection already covers |
| Producer/consumer pair-contract framework (`tests/pair_contract*.py`, #2847) | Load-bearing (small) | ~41 tests in the pre-merge lane; one registry entry per enrolled pair; the floor ratchet | The fixture-must-be-the-wire class, industrialized: each enrolled pair round-trips REAL producer output through the REAL consumer and asserts BOTH sides red on an injected disagreement (a one-sided contract is visible). Seeded with 6 real pairs; found 2 live mismatches while seeding (#3172) — the framework paid on day one. Enrollment floor is pinned and grows-only; full discovery honestly deferred to #2797's per-field registry. **Demote trigger:** two consecutive quarters where no enrolled pair reds and no new pair enrolls — then it is a museum, fold the harness into the census |
| Deploy-race convergence gate (`deploy/deploy_convergence.py`, #2978) | Load-bearing (small) | one poll loop per post-deploy check run (measured 0.6s, one poll, zero sleeps on the green path); 3 EMF disposition counters (#2837-registered) | The deploy-race false-positive class gets an owner: post-deploy checks WAIT on the convergence observables (viewer-path fingerprint, invalidation, API-before-frontend) instead of sleeping and hoping; a failure inside a known race window is `raced — reran after convergence`, a failure past convergence is REAL and still fails hard (the 20:43Z mixed-state incident is pinned as a fixture and still fails under the gate). The raced-vs-real rate ('1 per 1.5 days' at filing) is now measurable. **Demote when** a quarter's disposition counters show zero `raced` — the pipeline ordering will have made the window extinct |
| Lambda enrollment kernel (`cdk/stacks/lambda_enrollment.py` + the outside-constructor ratchet, #2846) | Load-bearing (small) | synth-time assertions (no deploy cost); 24 tests; three shrink-only ledgers (raw constructions, alarm stories, grant refs) | 'We added a Lambda but forgot registry X' dies by construction: create_platform_lambda() raises at synth on a non-literal name, a mis-rooted handler, or a non-UTC schedule, and records every construction; the AST ratchet reds a raw `_lambda.Function` outside it. Exemplar migration (HAE) auto-enrolled it in the platform model + grant lockstep — the thesis demonstrated. Its G4 ledger tightened the same night it merged (#3161's og-image alarm retired a row on the spot). Named debt, not hidden: og-image stays raw (non-Python), five GENUINE-GAP watch rows. **Demote: never while stacks construct Lambdas** — but the ledgers must keep shrinking; a quarter with zero ledger movement while Lambdas ship means the ratchet is being routed around |
| Reader-audience first-red alarm escalation (`scripts/platform_model_alarms.py::READER_AUDIENCE_ALARMS` facet + the conditional bar in `scripts/check_alarm_citations.py`/`remediation/agent.py::aged_alarm_escalations`, #3423) | Load-bearing (tiny) | **CI: $0** — no new alarm, no new SNS topic, no new schedule; both consumers were already reading live CloudWatch state and their own 72h constant, this only lowers that constant to 0h for 11 curated alarms. **Ops: marginal escalation noise**, bounded by how few `reader`-audience alarms exist (11 of 119, all serve_stack `site-api-*` + the `ai-canary-*` public-AI-canary family) — each one now surfaces into the `/wrap` citation gate and the Mon/Wed/Fri remediation-agent needs-human email on its FIRST red instead of after 72h. | `ai-canary-overall` went ALARM 2026-08-31 09:22 PT (the #3413 `/api/board_ask` 504 P1) and sat lit ~31h across launch day with zero escalation — under the 72h bar, so no wrap's citation check and no scheduled remediation-agent run ever named it. "Immediate detection, ~31h escalation. The canary worked; nothing read it." (docs/INCIDENT_LOG.md). The facet is curated, not derived from stack/routing/name (`ai-canary-overall` itself is declared in `monitoring_stack`, not `serve_stack` — a structural stack-keyed derivation would have missed the very alarm that motivated this row), and every tagged alarm carries its blast-radius ruling in the registry plus a mirrored table in `docs/DEPENDENCY_GRAPH.md`. Non-reader alarms, the 14-day mandatory-issue tenure, and the #2912 fired-and-cleared flap window are all explicitly unchanged (`tests/test_check_alarm_citations.py`/`test_remediation_agent.py` regression-pin all three). **Demote trigger: a quarter in which every first-red escalation on a reader-audience alarm was a false positive** (the alarm cleared with no real reader impact every single time) — then widen the bar back toward 72h for that alarm, or drop its `audience: reader` tag with the ruling for why. |

## Retired (the prunes are the proof)

| Subsystem | Retired | Record |
|---|---|---|
| ~105 unused MCP tools + 64-entry orphan allowlist | 2026-07-08 | #395/ER-04 — pruned 143→60 against 30-day EMF telemetry; `docs/MCP_TOOL_AUDIT.md` |
| apple_health XML import path | 2026-07-04 | #474/D-5 — latent clobber; lambda + role deleted |
| sleep-reconciler / sleep_unified merge | 2026-07-05 | #487, ADR-113 — promised merge never existed; removed with its API + panel |
| Eight Sleep bed-temperature pipeline (5 surfaces) | 2026-07-05 | #489, ADR-118 — 404'd every run for 4+ months; reactivation lead recorded |
| hevy-webhook FunctionURL lambda | 2026-07-06 | #756 — parked URL serving nothing; source kept in git for revival |
| /platform-review `sdlc` lens | 2026-07-18 | #1341 — superseded by /sdlc-review in depth |
| WAF | 2026-06 | Rate limiting moved entirely in-Lambda (DDB-backed) |
| Remediation agent `auto` mode | 2026-07-06 | ADR-129 — the worked demotion precedent; still runs in `shadow` |
| Remediation auto-merge gate (`remediation/automerge.py`), the `auto_earn_marker.json` streak + `earn_or_shadow_check`, the #1337 10-clean-run re-promotion bar | 2026-08-30 | #2833 / ADR-129 amendment — retired on the owner decision: shadow permanently. Zero `auto-fix-safe` PRs ever merged (none ever opened), `automerge/` audit prefix empty, marker untouched since the demotion — a streak counter would have counted nothing. Agent retained as a triage instrument (see the active row) |

## How to read this as a CIO

The platform prunes on measurement (every Retired row cites telemetry or a dead
fetch, not taste), demotes on yield (ADR-129), fences growth structurally
(guard-tested caps like the ≤5 paging set, ratchets like MCP_TOOL_AUDIT), and
prices every standing system in the rent classes above. The surface is large
because the demonstration is the product; it is right-sized because every row
has a trigger someone can pull.
