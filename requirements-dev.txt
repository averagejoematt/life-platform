# Dev dependencies — pinned for reproducible CI
# Install: pip install -r requirements-dev.txt
# Runtime deps are in lambdas/ (stdlib only) or cdk/requirements.txt

# Testing
pytest==9.0.2
pytest-cov==6.1.0

# Visual QA — Playwright browser automation (tests/visual_qa.py).
# After install: `playwright install --with-deps chromium`
playwright==1.58.0

# Linting & formatting (config in pyproject.toml; baseline reformat sequenced post-merge)
flake8==7.3.0
ruff==0.14.0
black==25.9.0
isort==6.1.0

# Type checking (advisory — see tests/test_mypy_clean_modules.py + mypy.ini)
mypy==2.1.0

# AWS SDK (for tests that call boto3 directly)
boto3==1.42.59
botocore==1.42.59

# CDK (synth + diff)
aws-cdk-lib>=2.170.0
constructs>=10.0.0
