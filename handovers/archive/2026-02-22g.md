# Handover — 2026-02-22g

## Session Summary
Full architectural review across 5 dimensions (Security, Performance, Cost, Schema, Architecture). Fixed all actionable findings. No version bump — no feature code changed.

## Current State
- **MCP Server:** v2.3.2, 30 tools, deployed and live (unchanged)
- **All 9 sources active:** Whoop, Withings, Strava, Todoist, Apple Health, Hevy, Eight Sleep, Chronicling, MacroFactor (mock)
- **DynamoDB:** 12,668 items, 19.3MB, deletion protection ON, PITR ON (new)
- **AWS cost MTD:** ~$0.00013 (essentially zero)

## What Was Done This Session

### 🔐 Security fixes
1. **Removed `dynamodb:Scan`** from `lambda-mcp-server-role` — was never used in code, now gone
2. **Scoped SES Resource** in `lambda-weekly-digest-role` from `"Resource": "*"` to `arn:aws:ses:us-west-2:205930651321:identity/awsdev@mattsusername.com`

### 🔒 Reliability fix
3. **Enabled PITR** on `life-platform` DynamoDB table — 35-day rolling recovery window, ~$0.004/month

### 💰 Cost fix
4. **Added S3 lifecycle policy** on `raw/` prefix — STANDARD_IA at 90 days, GLACIER at 365 days

### 📚 Documentation fixes
5. **ARCHITECTURE.md** — corrected tool count (25→30), version (2.0.0→2.3.2), fixed incorrect IAM description (claimed separate warmer role; in reality MCP Lambda is both server and warmer), added PITR status to DynamoDB table row
6. **SCHEMA.md** — added `macrofactor` and `chronicling` source sections, `source_of_truth` profile block with example JSON, `CACHE#matthew` partition documentation, enriched_name search behavior note, updated sources list
7. **PROJECT_PLAN.md** — removed closed `search_activities enriched_name` bug (was already fixed in v2.3.1 per CHANGELOG)
8. **CHANGELOG.md** — added architectural review entry

## Key Findings (no action needed)
- `search_activities` enriched_name bug was already fixed in v2.3.1 — the audit found the closed bug still listed as open
- `CACHE#matthew` partition has only 1 item (expected 5) — warmer may not have fired yet today or EventBridge timing. Worth checking CloudWatch logs for the 03:00 UTC warmer run
- All 11 CloudWatch alarms in OK state
- Zero Secrets Manager rotation configured (acceptable for personal system; OAuth tokens self-heal; manual rotation for MCP API key once/year is sufficient)

## Outstanding Backlog (post-review)
1. **MacroFactor real data** — export CSV from app, drop in `~/Documents/Claude/macrofactor_drop/`
2. **Weekly Digest refinements** — week-over-week comparison, CTL/ATL/TSB snapshot, notable PRs, MacroFactor section
3. **Monarch Money** — financial pillar setup
4. **Monitor CACHE warmer** — check CloudWatch logs to confirm nightly warmer is writing all 5 items

## Files Changed This Session
- `ARCHITECTURE.md` — corrected
- `SCHEMA.md` — expanded with 2 new sources + cache + profile SOT
- `PROJECT_PLAN.md` — closed bug removed
- `CHANGELOG.md` — review entry added
- `handovers/2026-02-22g.md` — this file
- AWS live changes: PITR enabled, 2 IAM policies updated, 1 S3 lifecycle policy created
