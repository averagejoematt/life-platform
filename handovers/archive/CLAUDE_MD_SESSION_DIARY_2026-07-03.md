# CLAUDE.md session diary — archived 2026-07-03 (#365, epic #343)

The chronological 'Verified / Prior verified' session log that lived at the tail of
CLAUDE.md (84% of the file, loaded into every session). Moved here verbatim — the
newest block at the time of the move is first. Newer session history lives in the
individual handover files; CLAUDE.md keeps only the single latest status block.

---

**Verified:** 2026-07-02 (**the `/uplevel` era begins — driver built + 3 runs: truth repair, the machine's bets surfaced, returnability slices. 8 PRs #305–#310 ALL MERGED; #305/306/307 DEPLOYED + LIVE-VERIFIED (Matthew authorized deploys in-session); #308/309/310 need one `sync_site_to_s3.sh` (a later session's sync may have carried them — check `/version.json`).** **The driver (`.claude/commands/uplevel.md`, in #305):** orient on the v5 brief → multi-agent Workflow survey (5 lanes + fresh-eyes audiences, every finding adversarially verified — 42 agents, 35 confirmed) → rank vs the north star → ship the flagship slice in a worktree; `/uplevel <idea>` = directed mode. Scoreboard + remaining board: memory `project_uplevel_driver_and_board`. **#305 truth repair (6 verified honesty leaks on skeptic surfaces):** correlations `p 1.000·FDR✓` (`or 1` coerced stored p=0.0; strength now |r|-derived; front-end `<0.001`, n<5 wall collapsed) · field-notes "zero minutes" (gatherer read per-activity fields off Strava DAY rollups → the published note accused its own pipeline) · home hero hardcoded "week one" in week 3 + no weight as-of (journey serves `last_weighin_date`) + a quiet-stretch hero line · pipeline board missing strava/macrofactor/hevy (hevy added to freshness_checker+BEHAVIORAL too; mirror test) · protein 190-vs-170 dual truth (floor served + graded from the SAME profile keys as daily_metrics_compute; contract test) · PLATFORM_STATS provably stale (303 tests vs ~1,290) → `sync_doc_metadata.py --apply` now REWRITES the literal + drift test (⚠️ the pre-commit hook runs it but leaves `site_api_common.py` UNSTAGED — `git add` it or main reds). **#306:** the regenerated W26 note flagged ITSELF ("20 nights of sleep/week") — whoop `DATE#<day>#WORKOUT#<uuid>` sub-records counted as nights → `_query_day_records` day-key filter on all six gathers; W26 regen recipe = `REMOVE ai_generated_at` + `{"manual_week":...}` (no force flag). Bundled-lambda deploys via the playbook's surgical `update-function-code` zip, verified file-list-identical to the CDK asset. **#307 "What the machine suspects":** the hypothesis engine (weekly Sun, falsifiable bets, citation-required verdicts) had ZERO public surface — now live bet cards on /protocols/discoveries (status badge: ember earned by confirmed ONLY, refuted muted never red; verdict trail `last_checked`/`last_evidence` newly served; founding evidence in a details toggle; expired-undecided honesty count; template fallback for the 30-day-expiry/post-reset empty windows) + "open bets" fig + cross-link on /method/intelligence; cold-start state-derived (⚠️ watch the first real Sunday grades ~Jul 5 render the mock-proven decided-state UI). **Returnability:** **#308** "since your last visit" cockpit strip (`/api/changes-since` — v3-era, zero non-legacy callers, EPOCH-SECONDS ts; fire-and-forget self-hiding, ≥12h gap on the legacy `amj_last_visit` key, stamp advances only after a successful read; live-verified with a seeded 4-day stamp) · **#309** waveform days are doors (scored home bars → anchors into `/now/?date=`; click-through to the dated sheet + chronicle cross-link proven; role img→group) · **#310** chronicle-listen cycle-safe join (was the SEASON-1 feed by bare week number — current Week 5 would have inherited a May episode; now panelcast + ±14d window; Week 1's missing listen link surfaced; the zombie season-1 weekly cron flagged → retired by a later session, #312). **Durable lesson — the AI-accuses-its-own-pipeline bug CLASS:** any `len(_query_source(...))` over a partition with sub-records, or per-activity reads off day rollups, makes a note/coach publicly "flag" phantom data errors — audit counts before trusting them. 30 new tests across 7 files, each replaying its live bug; render-QA via route-mocked/live-proxied Playwright (SW blocked, domcontentloaded — networkidle hangs; `page.route` handlers take `(route, request)` positionally, don't use a kwarg-default second param). See `handovers/HANDOVER_2026-07-02_UplevelTruthAndReturnability.md`. Prior session below.)

**Prior verified:** 2026-07-01 (**Presence / "the quiet stretch" — give a human logging-gap a voice; SHIPPED + DEPLOYED LIVE + verified; PR #301 merged, `main == live`, layer v93, fleet uniform (70/70). Matthew authorized the deploys in-session.** **Live-proof:** the first `adaptive-mode` bootstrap wrote Matthew's REAL current state — `engagement_state STATE#current` = **`dark`, gap 5 days** (last food log 2026-06-24), wearables still flowing — exactly the disengagement he described; `/api/presence` serves it (`in_lull:true`); a live orchestrator invoke confirmed the coach brief is injected with the real signal (`gap_days:5`, channels_quiet `food`+`journal`). Coaches voice it on the next daily run.** **The trigger (owner):** went to a friend's 50th Fri, and "as always when routine gets disrupted" stopped logging food/workouts/habits + started DoorDashing — but with minimal inputs the site/coaches noticed *nothing*; wanted it to feel like real coaches ("it's day X with no feeds… concern about falling off… and when I start logging again, observe what changed — probably weight regain"), NOT a big red PAUSED sign. **The reframe (4 Explore agents):** the platform ALREADY detects the gap (`freshness_checker`, `source_state`, `/api/source_freshness` behavioral-stale, `adaptive_mode`) but deliberately stays SILENT — `BEHAVIORAL_SOURCES` excluded from paging, `adaptive_mode` returns a neutral 50 for missing data ("don't penalise"). So this is NARRATIVE, not detection: give the gap a voice. **The lever:** wearables keep syncing passively when Matthew goes quiet, so coaches can name the silence + its *measurable* consequences (rough sleep, elevated RHR) and observe the return (weight up) WITHOUT fabricating the cause. **Owner decisions:** coaches react **in-character/varied**; public = **cockpit line + Story "quiet stretch" beat** (no banner); **full 3-phase arc**. **Built:** **(P1)** new pure `lambdas/engagement_core.py::compute_presence` — days-since-last-log per MANUAL channel (macrofactor/hevy/habitify/notion; food = primary anchor), **lag-adjusted** (logged today OR yesterday = present, respecting the 24h nutrition lag), presence class `present→light→quiet→dark`, **return detection** + weight-regain-over-gap, **sick/travel planned-pause suppression**; written by `adaptive_mode_lambda` (its own instrument — leaves the neutral engagement_score untouched) into `engagement_state` (`DATE#` + `STATE#current`), classified `EXPERIMENT_SCOPED` in `phase_taxonomy`. **(P2)** injected at the SAME deterministic seam as `current_stance`/`site_protocols` in `coach_narrative_orchestrator` (`_gather_all_state` read → `_engagement_for_brief` trimmer, omits when present → inject at the ~887 seam + surfaced in `_build_user_message`) + an **ENGAGEMENT/PRESENCE** clause in `ai_calls.py` (acknowledge in your own voice; ground the day-count; NEVER invent the cause — it's not in the payload; note the return supportively). Reaches BOTH the site coaching + daily email. **(P3)** fail-closed `/api/presence` (`site_api_data.handle_presence` — explicit allowlist, never spreads the record; no per-channel/passive_read leak) + a **self-hiding cockpit presence line** (`site/now`, `cockpit.js::renderPresence`, calm `.presence` CSS, no red) + a **Story/timeline "quiet stretch" beat** (`dispatches.js`, `.tl-quiet`). **⚠️ Caught in lint:** a literal `{gap_days}` in the ai_calls system-prompt **f-string** would `NameError` at runtime (ruff F821) → reworded braceless. **Tests:** `test_engagement_core` (13), `test_engagement_coach` (8), `test_presence_endpoint` (4, fail-closed privacy); **full suite 2480 passed** (5 pre-existing live-AWS failures — `test_coaches_api` S3-config + `test_i16` ingest — confirmed failing on clean origin/main via stash), black/ruff green. **Deploy surface (gated on Matthew):** ⚠️ `ai_calls.py` is a LAYER module → **layer dance** (`build_layer` → `LifePlatformCore` → bump `SHARED_LAYER_VERSION` → redeploy all consumers for fleet uniformity; bump MUST land on main or reverse-squash-drift reverts the fleet) + `LifePlatformCompute` (adaptive_mode/taxonomy/orchestrator, bundled — `cdk diff` first) + `deploy_site_api.sh` (`/api/presence`, NOT `cdk LifePlatformWeb`) + `sync_site_to_s3.sh` (cockpit+Story) + bootstrap-invoke `adaptive_mode` to write the first `STATE#current`. Plan: `soft-baking-toast.md`. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-07-01 (**Reading/Mind pillar — consolidated into the Data door + made content-rich; DEPLOYED LIVE + verified; `main == live`, 0 open PRs. PRs #298 + #299 merged.** Follows the audit session (below); all its PRs #295/#296/#297 also merged → main green. **The trigger:** Matthew found `/data/reading` (which the audit's PROD-01 fix #298 had added as a thin tile) too thin — "just the book I'm reading, not in the spirit of the data page" — and clicking "open the full library" jumped to the standalone `/mind/` page whose chrome differed ("the rest of the site looks odd"). **Root cause (2 Explore agents):** reading rendered **twice, in two chromes** — the thin `/data/reading` (evidence.css) and the richer `/mind/` (cockpit.css+mind.css: no active-door highlight, truncated footer). **Decision (owner: "I just want it to look like the same experience/website" + "full depth"):** consolidate into ONE surface in the Data door, retire `/mind/`. **Built (PR #299, 3 phases, all live):** **(P1)** `renderReading()` in `site/assets/js/evidence.js` grew into the full async readout — reading-now (cover+facts), the shelf, roundedness wheel, idea constellation — ported from `mind.js`, restyled `.rdg-*`/`wh-*`/`cst-` in `evidence.css`; **retired `/mind/`** → repointed the 2 inbound links (home constellation node `story.js:45`, `/now/` cockpit reading line) to `/data/reading`, made `site/mind/index.html` a **`noindex` redirect stub** (bookmarks don't 404; proper CloudFront 301 = safe follow-up), regenerated sitemap. **(P2)** `lambdas/web/site_api_reading.py::_public_shelf_item` now joins each book's **public notes** via `reading_visibility.project_public_list` (fail-closed — a note serves only when its `public` flag is truthy). **(P3)** the notes render as the loudest type — **"Why this book"** (intention) / "The takeaway" (synthesis) / "Reflections". **⚠️ KEY INSIGHT (owner: "I don't know my intention, I'm going off your recommendation"):** the "why" belongs to the **coach/recommender, NOT the reader** (the brief's anti-black-box reason-string rule) — forcing the reader to write an intention was backwards. So the queue was generalized from bare spines to `readingBookList` (cover + facts + why per book), and I **wrote 6 coach-authored recommendation reasons** (Dark Matter + the 5 queued) as public `intention` notes via `reading_store.add_note` (the exact fn the MCP uses; stable `noteId="coach-why"` → idempotent/editable — rewrite in place to change framing). **NO new field/tool/allowlist:** `intention` is just a public note type; `project_public` gates purely on the `public` flag (type-agnostic); the capture path (`manage_reading add_note`) already existed. **Verified LIVE:** `/data/reading` renders the full experience in Data-door chrome (active door, full footer); `/mind/` forwards to `/data/reading`; all 6 books show their "Why this book"; `/api/reading_shelf` serves `notes[]` (busted the 300s CDN cache to show immediately). **Privacy held:** only public notes leak; retention/mood/recall never served. **Gotcha:** the local render-QA harness (http.server + Playwright) hung on `goto` — the **service worker** stalls headless local loads; verified instead against **live prod + a Playwright route-mock** (inject a sample note into `/api/reading_overview` to prove the populated render without fabricating owner content). 101 reading tests + black/ruff green. **Deploys:** `deploy_site_api.sh` (notes serving) + `sync_site_to_s3.sh` ×3. **Outstanding:** the **auto-recommender-reason path** (wire `reading_recommender`'s reason string to persist as a `RECOMMENDATION#` on add so future books auto-get a "why" — the deferred Phase-3-optional, now clearly wanted); the proper `/mind/` CloudFront 301 (vs the noindex stub); rename the Data door's "Mind & inner life" tile → "Mood & journal" to de-collide with the reading pillar. From the audit: **DEVOPS-02** (OIDC) + the **doc-truth batch** (CQ-02/CQ-03/PRIV-03). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-07-01 (**Full platform deep-dive audit → Tier-0 + Tier-1 remediation, DEPLOYED LIVE + verified. 9 PRs (#289–#296); DEVOPS-02 deferred.** [NB — this entry's "STATE AT WRAP" is SUPERSEDED: #295/#296/#297/#298/#299 all merged since; main green, `main == live`, 0 open PRs.] Matthew asked for "everything a product team / CIO / CTO / consultant would do" as a **token-only** audit (no Bedrock/AWS spend), then authorized "you do all the deploys". **AUDIT:** an 80-agent `Workflow` — 10 expert lenses (security/arch/cost/reliability/correctness/code-quality/privacy/devops/product/frontend), each finding **adversarially verified** by a skeptic reading the real code → **23 confirmed, 45 refuted** (the ~50% FP filter working). Report: `docs/reviews/PLATFORM_AUDIT_2026-06-30.md`. **Verdict: fundamentally healthy — 0 P0, 0 P1, 4 P2, 19 P3.** 7 systemic themes: UTC-vs-Pacific date selection · silent-failure detectors that can go silent · multiple-source-of-truth drift · doc-drift-behind-a-fresh-façade · IAM-known-but-not-applied · CI gate-coverage/masking · built-but-stranded surfaces. **TIER 0 (deployed + live-verified):** **BUG-01/02/03** (#289) — circadian (7 PM PT) + evening-nudge (8 PM PT) + nutrition-MCP derived "today" in **UTC** while data is keyed by the **Pacific** day, so the evening crons (02:00–03:00 UTC = tomorrow PT) scored an **empty future day** on the public cockpit / cried "not logged"; fix = one canonical `lambdas/pacific_time.pacific_today()` (bundled into Compute+Email → **no layer dance**) + MCP mirror `mcp.core.pacific_today`. Live-proof: at the real bug window (UTC 07-01/PT 06-30) circadian wrote `DATE#2026-06-30`. **FE-01** (#290) dead `role="button"` on the cockpit band → removed. **BUG-04** (#290) `/api/vitals` trend ran on a `key=lambda _:0` no-op sort → explicit ordering, output unchanged. **COST-01/CQ-04** (#290) stale docstring/comments. **PRIV-01** (#291) the chronicle's only deterministic vice gate missed **"edible/edibles"** (already in `content_filter.json`) → added + a **superset drift-guard** test; `privacy_guard` is a layer module but bundled into every asset → shipped via the Email bundle, no layer dance. **TIER 1:** **DEVOPS-01+CQ-01** (#292) — CI push-`paths` excluded `cdk/ci/config/workflows/tooling`, so IAM/alarm/layer changes hit main **un-gated** AND the auto-merge ALLOWLIST's on-main re-run never fired for its own files → added the paths + fixed the comment + unified dev/CI black/ruff/playwright pins (+guard test). **SEC-01** (#293, deployed) — public `site_api` role had **unconditioned PutItem/UpdateItem on the whole table** → scoped (`LeadingKeys`) to the 6 real interactive partitions, enumerated from the code + a write-call-site canary; **verified via `aws iam simulate-principal-policy`** (real partitions `allowed`, `…SOURCE#whoop`/`labs` now `implicitDeny`). **REL-01** (#296, deployed) — the 4 silent-failure detectors (ingest-liveness/strava-reconcile/interior-gap/coherence) had only "≥1 problem" alarms with `treat_missing=NB`, blind to their OWN producer dying → added a `_heartbeat_alarm` companion each = **BREACHING when the daily gauge is absent 2 straight days** (`SampleCount<1`, mirrors `panelcast-no-episode-7d`; 2 days dodges the in-progress-UTC-period false fire that blocks flipping problem alarms to BREACHING at eval=1). +4 digest alarms 52→56; **all 4 live + `breaching`.** **⚠️ DEVOPS-02 (OIDC trust `repo:*`→tighten + role split) DEFERRED** — rewrites the live IAM trust gating ALL CI/CD; wrong = Actions locked out of AWS, needs a session where a real CI run validates it. P3, `matthew-admin`-bounded. **CI-MASKING CASCADE (real finding):** merging the audit exposed 2 pre-existing red-mains masked by the sequential Lint gate — **#294** (black: `test_editorial_image.py` unformatted since #280) then **#295** (`test_iam_secrets_consistency`/`test_secret_references`: `life-platform/pexels` never in `KNOWN_SECRETS`; `test_s4` 20→21). Ran the full suite + all gates locally creds-blanked → **after #295, main fully green (2427 passed; black/ruff/mypy clean).** **DEPLOYS (each behind a `cdk diff` — bundle re-hashes / +4 alarms / one IAM condition; zero destroys):** Compute, Email, MCP, Operational ×2 (COST-01, SEC-01), Monitoring, `deploy_site_api.sh` (BUG-04+PRIV-01 floors), `sync_site_to_s3.sh` (FE-01). **⚠️ DEPLOY-FROM-BRANCH TRAP CAUGHT ([[reference_deploy_from_main_not_worktree_branch]]):** first ran Compute/Email while the worktree was on the `priv01` branch → shipped only that PR's code, missing the pacific-date fixes; **the tell: `cdk diff LifePlatformMcp` = 0 differences when I expected a change.** Recovered via `git checkout origin/main` + re-verify + redeploy. **⚠️ STATE AT WRAP: `main` == #294; #295 (pexels) + #296 (REL-01) OPEN → main RED until #295 merges, and #296 is deployed-but-not-on-main (reverse-drift). MERGE ORDER: #295 then #296.** Doc `alarm_count` 52→56; ARCHITECTURE.md Secrets table separately stale (deferred doc-truth batch). **Outstanding:** merge #295+#296; then DEVOPS-02 + the doc-truth batch (CQ-02/CQ-03/PRIV-03). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-30 (**Reading/Mind pillar — broken covers fixed + persona reconciliation, both SHIPPED + DEPLOYED LIVE + verified; `main == live`, 0 open PRs.** Matthew authorized the deploys this session and merged #286/#287. **(1) Broken book covers (real bug):** Matthew saw broken-image icons in his queue — the `reading-cover-pipeline` writes real JPEGs to `generated/covers/<bookId>.jpg` and the `/mind/` front-end requests `/covers/<bookId>.jpg`, but **no CloudFront behavior routed `/covers/*` to `S3GeneratedOrigin`** → every cover fell through to the site origin and **404'd**. Phase C shipped the page+pipeline but missed the one edge route. Fix: a `/covers/*` cache behavior in `cdk/stacks/web_stack.py` (mirrors `/assets/images/editorial/*`, 30-day TTL; the S3 objects already existed). `cdk diff` clean (one behavior, no IAM/destroy) → `cdk deploy LifePlatformWeb` + invalidate. **All 6 covers now serve `200 image/jpeg`** (curl + a Playwright `/mind/` screenshot — shelf renders Dark Matter + 5 queued covers). **Durable lesson:** any new generated-content URL path needs its OWN CloudFront behavior (the ADR-046 prefix-stripping pattern) — a file in `generated/` is invisible at the edge until a behavior routes its viewer path. (PR #286, which also reconciled the A–E reading work that was deployed-but-not-on-main.) **(2) Persona reconciliation → Dr. Cora Vance:** the reading coach is now **Dr. Cora Vance** (`cora_vance`), recast from the placeholder "Lena Marsh". Registered in `config/personas.json` + `config/board_of_directors.json` as `type: board`, **`operational: false` / `active: false`, features-gated to nothing** → a defined-but-dormant persona generating zero email/chronicle content until the reading-coaching surface ships (like Elena Voss / Eli Marsh sit inert). All 13 `tests/test_persona_registry.py` invariants green (incl. the gate that a non-operational persona gets **no `config/coaches/*.json` voice file** — that set matches operational coaches only). Counter-voices recast to the **real roster** (`READING_CALIBRATION.md` §9): Coach Maya Rodriguez (on-ramp), Dr. Amara Patel (longevity-vs-pleasure), Mara Chen (restraint gate); orphan archetypes **Priya/Nadia/Crowe/Theo dropped** (Priya's pleasure-stance folded into Cora's mandate). **⚠️ The rename touched LIVE code, not just docs** (first `git grep` missed it; a repo-wide plain `grep` caught it): the onboarding LLM **system prompt** ("You are Dr. Cora Vance"), the `/mind/` empty-state, the MCP **tool description** (`registry.py`), track-record docstrings → renamed + deployed via `cdk deploy LifePlatformMcp` (clean code re-hash, no IAM/layer) + `sync_site_to_s3.sh`. **NB — the separate pre-existing Product-Board "Lena/Priya" personas are a different cast — DO NOT touch them** (renaming the reading coach to Cora disambiguates from them). (PR #287.) **Process:** #286 squash-merged mid-session → the clobber guard correctly blocked the site sync (origin/main had a `site/` commit the branch lacked) → reconciled by branching fresh off `main` + cherry-picking the persona delta into #287 (the net-delta pattern). Also **deleted the stale `feat/site-polish-reading-layer` branch** — forked long ago (a merge would have reverted ~9,600 lines of coherence/SS/serial work), its 2 commits already shipped via #232. **Outstanding:** only the gated Phase-E backlog (journal-resonance, mind-body bridge, voice debrief — earned on real reading data; Matthew has 6 books, no finishes yet). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-30 (the SS self-sustainability tail — **SHIPPED + DEPLOYED LIVE + verified (3 items, 2 PRs; Matthew merged all and authorized "work through them" for the deploys)**. These are counterweights to "fully automatic" content + a flat-day-still-shows-motion view; lower-priority maintenance that finishes the documented backlog. **SS-09 + SS-11 → PR #280; SS-08 → PR #281 — both MERGED + DEPLOYED.** **SS-11 — editorial-image guardrail** (`editorial_image.py`): a fail-closed quality/denylist gate before an auto-picked Pexels cover ships — `_acceptable(photo)` requires a usable landscape (≥1200×600, w>h) AND a description that reads as atmospheric texture (rejects people/face/text/brand via a **word-boundary** denylist so "woman" doesn't trip on the "man" substring); `_search` scans candidates from the seed offset and ships the FIRST that clears the gate, or **NO image** if none qualify (a missing cover beats a wrong one). Bundled with `lambdas/` (chronicle + panelcast), NOT the layer → no layer dance. **SS-09 — podcast format rotation** (`coach_panel_podcast_lambda.py`): a deterministic per-week entry-point lens (`_episode_angle(week)` over 6 angles) injected into the writer prompt so the show doesn't feel formulaic by ep 26 — the bet/Split/scoreboard scaffold (the show's identity) stays; only the LENS rotates. **SS-08 — monthly "what changed"** (`weekly_correlation_compute_lambda.py` + `site_api_data.py` + `cockpit.js`): the `/now` cockpit's "Month" scope button was a placeholder; SS-08 fills it with a REAL view so a flat day still shows monthly motion. **Fill-in-the-blank, not greenfield** (the planner's key find): the series + FDR correlations are already computed in the weekly correlation lambda → piggyback there (**zero new DDB queries, no new lambda/schedule, no layer dance**). `compute_month_deltas` — trailing-30d vs prior-30d averages for 8 headline metrics, emitted ONLY when both halves have **≥10 real (non-None) days** (never zero-filled), higher-is-better-aware direction. `diff_newly_unlocked` — a **first-seen LEDGER** (`what_changed` partition: `STATE#first_seen` + `SNAPSHOT#current`): a correlation is stamped the first run it crosses FDR significance and surfaced only while that stamp is within 30 days → announced ONCE, a flickering pair never re-announced. `honest_null` when nothing moved → the front-end shows a calm "steady month", **never fake motion** (the whole point). `handle_what_changed` + `/api/what_changed` (shaped-empty 200 pre-first-run); `renderMonth()` replaces the `showScopeSoon` stub. `what_changed` = `EXPERIMENT_SCOPED` in `phase_taxonomy.py` (a test enforces every writer is classified). **Low-fabrication across all three — only real FDR verdicts + real deltas + fail-closed image gating.** **Tests:** `tests/test_ss_tail.py` (11) + `tests/test_what_changed.py` (11); both suites + all related green; ruff + black clean; cockpit.js valid. (Updated 3 pre-guardrail mock photos in `test_editorial_image.py` + one rigid mock.) **DEPLOYS DONE + LIVE-VERIFIED (each behind a `cdk diff` — all benign shared-bundle re-hashes, zero destroys/IAM/layer):** `LifePlatformEmail` (SS-09/11, both bundle there); `LifePlatformCompute` (SS-08 + taxonomy; no IAM — table-wide PutItem already granted) + site-api (`deploy_site_api.sh /api/what_changed`, verified 200) + `sync_site_to_s3.sh`; then bootstrapped `what_changed` via a `{"force":true}` invoke of **`weekly-correlation-compute`** (NB: the real fn name — NOT `life-platform-…`). **VERIFIED LIVE:** `/api/what_changed` serves `honest_null=False, deltas=[], newly_unlocked=1` — `deltas=[]` is CORRECT/honest (genesis 2026-06-14 → the prior-30d half is empty, so no fabricated month-over-month motion), and the 1 unlock (`habit_pct↔day_grade`, stamped `2026-06-30` in the ledger) proves the FDR-rigor: the run logged "3 significant (|r|≥0.3)" but SS-08 correctly surfaces only the **1 BH-FDR-significant** pair, not the loose-|r| heuristic. SS-09/SS-11 are passive (take effect on the next weekly panelcast / next chronicle-with-cover). **main == live, no constants/layer/CDK change → nothing left behind.** **🎉 THE FULL BACKLOG IS CLEARED + DEPLOYED** — the backend serial arc (phases 1–4) + the SS tail (SS-08/09/11), all live; 0 open PRs; only genuinely-deferred items remain (SS-10 coach-grounding "its own session", PRE-13, AWS-support-gated infra). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-29 (backend serial phase 4 of 4: historical-window APIs — SHIPPED + DEPLOYED LIVE + verified end-to-end. THE BACKEND SERIAL ARC IS COMPLETE (all 4 phases live). Matthew merged #278 and authorized the deploys. Let a reader arriving months in see the platform **AS OF a past date** — extends the `?date=YYYY-MM-DD` time-travel pattern `handle_character` already used (`site_api_vitals.py`) to the data/waveform surfaces. **1 feature PR: #278 (MERGED + DEPLOYED).** **Two endpoints get `?date=` (both already `DATE#`-keyed → ZERO new compute):** (1) **`/api/observatory_week?domain=X&date=Y`** (`site_api_data.handle_observatory_week`) — the flagship 7-day waveform, all 6 domains in one edit; the window anchors to the date (top-block change), `include_pilot=bool(date)` threaded into all 8 `_query_source` calls (a local-var-name collision meant two of them lived in `handle_changes_since` — caught + reverted, since `ip` is out of scope there); (2) **`/api/vitals?date=Y`** (`site_api_vitals.handle_vitals` + a new `/api/vitals?date=` dispatch branch mirroring character's) — the cockpit; new shared `site_api_common._latest_item_asof(source, date)` gives the latest weigh-in **on-or-before** the anchor. **As-of semantics mirror `handle_character` exactly:** most-recent-on-or-before · future dates **clamp to today** · pre-genesis **honest-null 200, never 503** · `include_pilot=bool(date)` so prior-cycle history is visible when time-travelling while the live view stays phase-clean · `time_travel` flag · **immutable-past day cache (86400s)**. **Read-only — serves stored records verbatim, gaps stay gaps, never interpolated/fabricated.** **Front-end (`cockpit.js`):** the date-scrubber time-travel mode **used to HIDE the vitals band** ("no raw vitals on the dated sheet → hide it" — because no historical vitals endpoint existed); now it fetches `/api/vitals?date=` and shows the **REAL readings from that morning**, plus a **chronicle cross-link** ("Read Week N →") via a `postFor`-style `nearestPost()` lookup (reuses `/journal/posts.json`; `renderReadiness` self-hides if that date has none). **⚠️ Plan correction:** the planner assumed `evidence.js`'s silhouette scrubber was the time scrubber — it's **WEIGHT-keyed, not date-keyed** (drag to morph the body at weight W), so it's correctly NOT wired; the real time-scrubber is the cockpit's `/now/?date=`. **Tests:** new `tests/test_historical_window.py` (10 — dated window+flags, dateless-is-live, bad-date 400, future-clamp, honest-empty 200, vitals anchoring + as-of weight, dateless-unchanged, honest-null, `_latest_item_asof` on-or-before); **10/10 pass; ruff + black clean; 270 site-api tests green** (updated ONE rigid `_query_source` mock in `test_vitals_frame.py` to accept the new `include_pilot` kwarg — the rest use `*a, **k`). **DEPLOY (site-api only — NO layer dance, NO CDK):** `deploy/deploy_site_api.sh` (verified 200) + `sync_site_to_s3.sh` (clobber guard passed; CloudFront invalidated). **VERIFIED LIVE (6 curls):** historical vs current divergence proves real past records served — vitals weight **305 lbs on 2026-06-20 vs 301 now**, recovery **60 vs 84**; observatory_week historical period `[06-14→06-20]` vs live `[06-22→06-29]`; pre-genesis (2026-04-01) → **HTTP 200**; future (2099) → **clamps to today**. **main == live, no constants/layer/CDK change → nothing left behind, no reverse squash-drift trap.** **🎉 THE SERIAL VISION IS COMPLETE:** phase 1 (coach stances) + phase 2 (coaches react to protocols) + phase 3 (Elena recaps) + phase 4 (historical windows) — each composed on the last. **Outstanding:** only the **SS tail** (SS-08 monthly "what changed" · SS-09 podcast format rotation · SS-11 editorial-image guard) — lower-priority self-sustainability maintenance; plus the phase-3 spelled-number recap watch. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-29 (backend serial phase 3 of 4: Elena "previously on" recaps — SHIPPED + DEPLOYED LIVE + verified end-to-end; Matthew merged both PRs (#275 phase-2 wrap docs + #276 phase-3 feature) and authorized "you run the deploys" this session. A serial-TV cold-open so a reader arriving months in catches up fast: **Elena Voss** (the embedded narrator who writes the weekly Chronicle — NOT the PI; that's Dr. Eli Marsh) produces a "previously on" recap grounded ONLY in **published** chronicle installments + the narrative arc — never raw vitals, never invented events. **1 feature PR: #276 (MERGED + DEPLOYED).** **Low-fabrication by construction:** generate-at-draft / commit-at-publish — the recap is built as a `draft_recap_json` when the weekly Chronicle is drafted and only written to `RECAP#latest` when that week actually publishes (approve OR the auto-publish sweep), so it can never run ahead of the history it summarizes. **5 guards in `build_recap()` (`wednesday_chronicle_lambda`):** (1) the strongest — a **deterministic date cross-check**: any beat whose date isn't a real published-installment date is DROPPED (never trust an LLM-emitted date); (2) raw-vitals strip on beats; (3) raw-vitals reject on the headline paragraph; (4) privacy gate (fail-closed — no real public figures/vices); (5) thin-history blanking (<2 published → `story_so_far` only). All wrapped **fail-soft** — a recap error never aborts the chronicle. Uses the chronicle's LOCAL `call_anthropic` (Sonnet), NOT the `ai_calls` layer module → **no layer dance**. **The loop:** `chronicle_approve._commit_recap()` writes `RECAP#latest`+`RECAP#{date}` on BOTH the approve path and the sweep (cleans up `draft_recap_json` on publish); new `/api/recap` (`site_api_coach.handle_recap`) honest-nulls before the first recap and withholds a stale record (`experiment_day > current day`) like `ai_analysis`; `dispatches.js` leads the `/story/timeline/` "story so far" with Elena's recap (story + dated beats linking into the chronicle), **falling back to the existing front-end stat aside** when `/api/recap` is null (no regression). New `{"recap_only": true}` invoke mode bootstraps/regenerates the recap from existing published history without forcing a new installment. `RECAP#` lives under `USER#…#SOURCE#chronicle` → already `EXPERIMENT_SCOPED`, wiped on reset (**zero taxonomy change**, asserted). **Tests:** new `tests/test_chronicle_recap.py` (14 — shape, date cross-check, raw-vitals guard, thin-history, privacy gate, fail-soft, fence-parse, approve commit, endpoint honest-null/payload/stale-withhold, taxonomy); **14/14 pass; ruff + black clean; 368 related tests green** (the `NARRATIVE#arc` inline-pk literal tripped `test_ddb_patterns` until moved to a variable, the same way the orchestrator's helper avoids it). **DEPLOY (lighter than phase 2 — no layer dance):** `cdk diff`+deploy **`LifePlatformEmail`** (chronicle + approve; diff = benign shared-bundle re-hash, zero destroys/IAM/layer change) · **site-api via `deploy/deploy_site_api.sh /api/recap`** (verified 200) · front-end via `sync_site_to_s3.sh` (clobber guard passed; CloudFront invalidated). **VERIFIED LIVE:** bootstrapped the first recap (`recap_only` invoke → `RECAP#latest`, `as_of 2026-06-20`, day 7, 3 beats); `/api/recap` serves it in production; **all 3 beat dates are real published-installment dates** (cross-check working); the story faithfully summarizes the published prologue (recovery-12 morning, immaculate-infrastructure/empty-journal, the June-14 launch) and even the surname "Walker" traces to source content (appears 2× in published installments — not fabricated). **main == live, no constants/layer change → no reverse squash-drift trap.** **⚠️ Honest watch item:** the raw-vitals guard is DIGIT-based, so spelled-out numbers ("recovery score of *twelve* … climbed to *twenty*") pass through — here they're grounded (faithful to the published installment) so it's correct, but it's the same spelled-number gap the stance engine has; watch across future weekly recaps, don't over-engineer now. **Outstanding:** backend serial phase **4** (historical-window APIs — `/api/character?date=` already time-travels, extend to data/waveform; data→chronicle cross-links), SS tail (SS-08/09/11), the spelled-number watch. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-29 (backend serial phase 2 of 4: the coaches-review-the-site loop — SHIPPED + DEPLOYED LIVE + verified end-to-end; Matthew merged both PRs and authorized "do all the deploys" this session. Coaches now **react to the challenges/experiments Matthew has committed to on the site** instead of treating those protocol surfaces as separate from the coaching. **2 PRs, both MERGED + DEPLOYED:** **#273** (the feature) + **#274** (the `SHARED_LAYER_VERSION` 91→92 hygiene bump). **main == live, fleet uniform on layer v92, 0 open PRs, no drift.** **Built on the SAME seam phase 1 laid:** the stance engine made `coach_narrative_orchestrator._gather_all_state` the place coaches read evolving context from; phase 2 adds one gather step there. New `_gather_site_protocols(coach_id)` reads active **challenges** (clean `domain` field) + **experiments** (routed by `tags`), filtered per-coach via a deterministic `COACH_DOMAINS` map (explorer = all; an experiment whose tags match no domain falls through to explorer ONLY — never mis-attributed, never silently dropped). Reads go through `_query_begins_with` → **ADR-058 phase-filtered**, so the coach sees exactly the active set the site/MCP surface (confirmed: challenge writes set **no** `phase` attr → active items pass `attribute_not_exists`; stale pre-genesis `pilot` candidates stay hidden). `_protocols_for_brief` trims/caps (≤5 per surface, drops nulls); surfaced in the planning message and **injected into the brief deterministically on every path** (success/fallback/default) — the exact seam `current_stance` uses. **`ai_calls.py`** gets one ACTIVE PROTOCOLS steering line: acknowledge commitments BY NAME, **never invent** progress/streak/adherence numbers (ground in real data or say it's not visible yet); N=1 / decision-class ceilings still apply. **Scope held tight (board recommendation):** challenges + experiments only — **habits DEFERRED** (the 65-item registry is ongoing behavior whose adherence already reaches coaches via domain_data; re-surfacing risks number-fabrication); no persisted protocol-read record (that's phase 3/4). No site-api / read-path changes. **Folded-in stance polish:** the labs_coach persistent `grounding_flag` — the stance no-numbers rule (`coach_history_summarizer.STANCE_SYSTEM_PROMPT`) now explicitly covers **TARGETS and GAPS-TO-TARGET, even accurate ones** ("running short of the protein target", NOT "26% short of 190g"); keeps the one-invariant guard (a stance is an opinion, never a readout); takes effect on the next weekly summarizer run. **Tests:** +10 in `tests/test_coach_stance_engine.py` (domain routing, active-only, tag routing, explorer-catches-untagged, trim/cap, brief surfacing + deterministic injection, omit-when-empty, fail-soft) — **30/30 pass; ruff + black clean; 198 coach tests green** (creds-blanked per the CI lesson). **DEPLOY = the full layer dance** (`ai_calls.py` is a layer module; the orchestrator + summarizer are standalone Compute assets): `build_layer.sh` → **`cdk diff`+deploy `LifePlatformCore`** (publishes **v92** — diff was ONLY the layer content hash) → bump `SHARED_LAYER_VERSION` 91→92 → **redeploy ALL 5 consumer stacks** (Compute + Ingestion + Email + MCP + Operational) for **fleet uniformity** (the v89 lesson — a mixed fleet trips the Plan gate). Every `cdk diff` read first: layer re-point + benign shared-asset re-hash, **zero destroys, zero IAM**; the Operational site-api churn was the known-benign `Code.from_asset` re-hash (`lambdas/web/` matched origin/main exactly, so the CDK asset == live — NOT a code revert). **VERIFIED LIVE:** fleet histogram **71/71 functions on v92**; orchestrator + summarizer fresh code on v92; **live smoke invoke** of `coach-narrative-orchestrator` → `200`, no error → the new gather path runs clean against the real phase-filtered partitions, **correctly omits `site_protocols`** (nothing active right now → the block surfaces the moment Matthew activates a challenge), and `current_stance` intact (no phase-1 regression). **⚠️ The reverse squash-drift trap handled:** the `SHARED_LAYER_VERSION` bump HAD to land on main (#274) or a future `cdk deploy` from main would revert the fleet to `:91` — main was one constant behind live until #274 merged. **Outstanding:** backend serial phases **3–4** (Elena "previously on" recaps; historical-window APIs — `/api/character?date=` already time-travels, extend to data/waveform), SS tail (SS-08/09/11), watch labs_coach's flag across weekly runs. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-29 (the coach-opinion engine: an evolving, evidence-derived STANCE per coach — SHIPPED + DEPLOYED LIVE + verified end-to-end; Matthew merged both PRs and authorized the deploys. The first **backend serial phase** (of four). **2 PRs, both MERGED + DEPLOYED:** **#270** (engine) + **#271** (the pre-existing compression bug it surfaced). **The problem:** each coach's public "read of Matthew" was resolved from a hand-authored **weight-band ladder** (`config/coaches/*_stance.json` + `coach_stance.py` + `site_api_coach._stance_block`) — so a *sleep* coach "graduated" Matthew foundation→architecture because he lost weight, not because his sleep changed (incoherent-but-green on the most-visited coach surface). **Board decision** (asked "what would the product board recommend" — 4 lenses: Product/North-Star/coaching-expert/Coherence) **= "replace, enriched":** the evidence-stance becomes the single public read AND carries a domain-appropriate stage; the ladder is demoted to a **silent fallback** (pre-data / engine lag), never a parallel read. **Low-fabrication by construction:** a stance is the coach's *epistemic state* (focus / track record / how the read changed), grounded ONLY in the coach's own validated artifacts (`LEARNING#`/`CONFIDENCE#` + `COMPRESSED` positions/corrections), instructed to speak to patterns not raw vitals — sidesteps the daily-narrative fabrication frontier. **The loop (all live):** (1) **stance engine** — `coach_history_summarizer._generate_stance` (weekly, after compression) writes a grounded `STANCE#{date}` + `STANCE#latest`; a `_RAW_VITAL_RE` guard self-corrects ONCE then sets an internal `grounding_flag`, a `how_my_read_changed` claim survives only with a real signal (a logged correction or a stage shift) else blanked, first runs blank it; fail-soft `_run_stance` never aborts compression and skips a `_fallback` compression; (2) **generation** — `coach_narrative_orchestrator` reads `STANCE#latest` and injects `current_stance` into the brief **deterministically** (not via the LLM) so it reaches the coach verbatim on every path; `ai_calls.py` lets the stance lead framing over the static 185-lb goal; (3) **render** — `_stance_block` prefers `STANCE#latest` in a **normalized shape** both the coach page and My Team consume (ladder mapped into the same keys as fallback, canonical `stage_id` kept for the team's all-same check), `handle_coach` adds `stance_history`; (4) **front-end** — `coaching.js` + `dispatches.js` lead the coach page with the stance, "how this read has evolved" prefers the dated `STANCE#` trail, new `.cs-evolve` CSS. `STANCE#` is under `COACH#*` → already `EXPERIMENT_SCOPED` (no taxonomy change). New tests `tests/test_coach_stance_engine.py` (20). **⚠️ THE BUG #271 SURFACED:** bootstrapping, all 8 stances came back skipped — **compression itself was falling back for every coach**: as `THREAD#`/`PREDICTION#` accrued since genesis (sleep_coach: **52 threads, 39 predictions**), the compressed JSON outgrew `max_tokens=1500`, **truncated before its closing ```json fence**, failed to parse → structural stub. This had been **degrading the orchestrator's `COMPRESSED#latest` context for weeks** AND blocked the stance engine (which correctly won't ground on a fallback). Fix: compression 1500→4000, stance 900→1400 (same class + fix as the orchestrator's earlier 2000→6000 bump; memory `reference_llm_json_maxtokens_truncation` — a 200 + a `_fallback` flag is the tell). **DEPLOYS** (each behind a `cdk diff` read — asset re-hashes only, no destroys/IAM, no squash-drift): `LifePlatformCompute` ×2 (engine + fix); **site-api via `deploy/deploy_site_api.sh /api/coach_team`** (full `web/` package + route verify — **NOT** `cdk deploy LifePlatformWeb`; the site-api lambda lives in Operational and ships via the script — codified gotcha; the Web stack only carries EmailSubscriber + OG); `sync_site_to_s3.sh` (the clobber guard correctly blocked until `origin/main` was merged in); bootstrap invoke → all 8 `STANCE#latest` live. **VERIFIED LIVE:** all 8 serve `source=stance` with distinct evidence stages; 7/8 `grounding_flag=False`, labs_coach flagged on a derived "26%" (guard working; internal-only, page reads fine). **Outstanding:** backend serial phases 2–4 (coaches-review-the-site loop — same `_gather_all_state` hook; Elena "previously on" recaps; historical-window APIs), SS tail (SS-08/09/11), watch labs_coach's flag. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-29 (the Self-Sustainability (SS) A-tier: built, deployed live, verified; Matthew authorized all deploys. The 6-month-hands-off foresight's highest-leverage backlog is done. **5 items, 3 PRs (#266 MERGED; #267 + #268 open + mergeable):** **SS-03 — budget hard-stop alarm** (#266, `LifePlatformMonitoring`): `budget-tier-hardstop` (BudgetTier≥3 → **URGENT** — all Bedrock off, daily brief data-only). **Verified-DOWN** (the discipline paid off): on inspection 4 of SS-03's 5 proposed monitors ALREADY exist — Garmin-staleness → `ingest-liveness-unhealthy` (ER-01), podcast-HOLD → `panelcast-no-episode-7d`, budget≥2 → the existing `budget-tier-escalation` digest; only the tier-3 *urgent* escalation was the real gap (token-expiry-7d deferred as noisy, Dependabot-PR-age → SS-04). **SS-06 — gradable-predictions write-time metric** (#267, `LifePlatformCompute`): the C-3 fix already eliminated the `machine`/threshold=None spec, so the residual gap was *visibility* — `coach_state_updater` now emits `LifePlatform/Predictions::PredictionGradableShare` per run (leading indicator, ahead of the Sentinel's ≥8-closed check). **SS-05 — experiment continuity** (#267, `LifePlatformOperational`): **DECISION = runs continuously** (a reset is a manual `restart_pipeline.py` act, never automated); added Sentinel `check_experiment_continuity` invariant that ALARMs only when a surfaced week DISAGREES with genesis (the ADR-077 stale-pre-reset leak). **SS-02 — podcast HOLD-aging escape** (#267, `LifePlatformEmail`): holds tagged `hold_class` safety|quality (default safety, fail-closed); a Mon+Wed sweep `{"sweep_holds":true}` RE-GENERATES a *quality*-held current week through every gate (publishes only if a fresh attempt clears the bar — never ships the flagged draft), bounded by review-window/max-days/retry-cap. **Safety/sensitivity holds NEVER auto-release** (overrode the foresight note that lumped sensitivity as soft — an acute-crisis week stays human). **SS-04 — Dependabot safe-auto-merge** (#267, merge-activated, no deploy): `dependabot-validate` (`on: pull_request`, read-only — runs the enforced gates on the *bumped* versions; the main CI/CD only runs on push-to-main so PRs had no Python validation) + `dependabot-automerge` (`on: workflow_run` of a successful validate → approve+squash). Safe-by-construction: self-gated on validate success (no `allow_auto_merge`/branch-protection dependency), no PR-code execution under a write token, dev-tooling group only, leaves the PR for a human if a review gate blocks. **⚠️ THE SQUASH-DRIFT BIT AGAIN — and `cdk diff` caught it:** while deploying SS-02 I found **SS-01 (last session, deployed live) was NEVER on `main`** — #265's squash dropped the whole changeset (sweep lambda + `ChronicleApproveSchedule` rule + autopublish env + IAM); deploying `LifePlatformEmail` from a main-based branch would have **DESTROYED the live schedule + reverted the sweep**. Caught ONLY by reading the `cdk diff` (`[-] … ChronicleApproveSchedule … destroy`). Recovered the live SS-01 source from `feat/serial-reader` (12 commits ahead, verified a clean SS-01-only delta), folded it into #267 (deploy preserved SS-01 + added SS-02 — verified live: both rules present, sweep dry-run `swept:[]`); #268 lands the matching wrap docs. **Durable reflex: `cdk diff` before EVERY deploy; never trust "main == live" after a squash-heavy session** (memory `feedback_squash_merge_drops_unpushed_commits`). New tests: budget-tier-alarms · prediction gradability metric · panelcast hold-aging · experiment-continuity invariant. **Outstanding:** merge #267 + #268 → main == live; the SS tail (SS-08/09/11, lower priority) + the backend serial phases (coach-opinion engine, coaches-review-the-site, Elena recaps, historical-window APIs). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-29 (LONG session — **website visual uplevel + editorial imagery + 6-month foresight + serial reader + SS-01**, all SHIPPED + DEPLOYED LIVE; Matthew authorized the deploys as we went. **(1) Graphic identity (#260 + #261 merged to main `b35c8c68`; #262/#263 SUPERSEDED — close them):** a code-drawn SVG identity — `site/assets/icons/icons.svg` + `icons.js` (8 domain + 5 door line icons, currentColor `<use>`), `sigils.js` (a deterministic geometric mark per coach, FNV-1a→mulberry32, vivid per-coach `--coach` colour) wired into the board grid / coaching headers / team roster / popover, pillar+door identity across cockpit/data/nav, constellation earned-glow, an intensity pass (icons muted-not-faint, ember page-title mark, bolder sigils), and the OG card reskinned to v5 + `instrumentMark()` (**the live OG generator is `lambdas/og_image_lambda.mjs`, NOT the legacy `web/og_image_lambda.py`**). Codified in `DESIGN_SYSTEM_V5.md §8`; memory `project_visual_identity_system`. **(2) Editorial imagery (Pexels):** `lambdas/editorial_image.py` (fail-soft, kill-switch `EDITORIAL_IMAGES` now ON) — atmospheric covers on chronicle/podcast/blog ONLY (never data/meal). Two live fixes: the **Cloudflare 403 "error 1010"** (Pexels bans the default urllib UA → added a real User-Agent) + **Matthew's image-dedup catch** (chronicle Week N & podcast Week N shared a photo → a per-kind seed offset `_KIND_SEED_OFFSET`, re-fetched podcast covers, deployed for future posts). Pexels secret created; `LifePlatformWeb` (OG + `/assets/images/editorial/*` CloudFront behavior) + `LifePlatformEmail` deployed; posts backfilled (covers render live). **(3) 6-month "hands-off except data entry" foresight (#264 backlog PR open):** the engine self-sustains but several failures go dark/decay silently — recorded as the **SS-series** (11 items) in `docs/BACKLOG.md`; full report in plan `soft-baking-toast.md`. **(4) Serial "walk-backwards" reader (#265 / `feat/serial-reader`):** `/story/timeline/` recut into a serial spine (recap + month-grouped + "Read Week N →" into the chronicle), a "previously" rail in the chronicle reader, a coach "how this read has evolved" trail (renders `recent_outputs`), and the podcast↔chronicle cross-link — front-end, reads existing endpoints. **(5) SS-01 — the chronicle can't go dark (in #265, DEPLOYED + VERIFIED):** a daily EventBridge sweep on `chronicle-approve` auto-publishes drafts unapproved past a review window via the SAME approve publish path, **bounded** `[CHRONICLE_AUTOPUBLISH_HOURS=48, MAX_DAYS=10]` so it catches a forgot-to-click draft but never resurrects an abandoned one (the dry-run caught a real 3-week-old pre-genesis draft → now correctly skipped). 5 unit tests; verified live (`{"sweep":true,"dry_run":true}` → `swept:[]`). **⚠️ OUTSTANDING (next session):** Matthew's PR housekeeping (close #262/#263, merge #264/#265 → `main` == live); the rest of the SS-series (SS-02 podcast HOLD-aging · SS-03 loud decay alarms · SS-04 dependabot auto-merge · SS-05/06); the backend serial phases (coach-opinion engine + coaches-review-the-site loop, Elena "previously on" recaps, historical-window APIs). Squash-merge/branch-stack drift recurred — the `git merge origin/main` + `git checkout --ours` reconcile pattern worked each time. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Verified:** 2026-06-28 (Phase 4 of the Self-Management & Coherence Program — **self-healing gets eyes on content. SHIPPED + DEPLOYED + LIVE-VALIDATED**, 3 PRs #250–252 merged, Matthew authorized "you do all deploys this session". The 4-phase program is now COMPLETE. **The insight:** the remediation agent already ingested the `coherence-overall` alarm for free (it triages every ALARM-state alarm), but that alarm only carries "OverallAlarm>=1" — it doesn't say WHICH invariant failed. So Phase 4 = give the agent the Sentinel's actual findings + teach the taxonomy to triage content **without ever letting content auto-merge**. **#250:** Coherence Sentinel persists its findings to `s3://matthew-life-platform/coherence-log/{latest,<date>}.json` every run (`build_record()` pure, fail-soft) + new scoped IAM `s3:PutObject` on `coherence-log/*` (cdk deploy LifePlatformOperational); ALSO folded in the grounding↔detection follow-up — `_gather_facts_and_narratives` now grounds on `canonical_facts.build_canonical_facts` (the SAME schema the coaches get; semantic pass now sees protein avg/target/floor distinctly). **#251:** `agent.py::_coherence_findings()` reads the artifact as a `coherence` signal (NO new IAM — role already has s3:GetObject); `docs/REMEDIATION_TAXONOMY.md` "Content & coherence signals" section routes every invariant to **Bucket B/C only, NEVER auto-merge**; content/AI surfaces on the hard denylist; **a test ASSERTS the auto-merge ALLOWLIST has no content path** (the enforced safety invariant). Agent runs from the repo → merge=live, no deploy. **#252 (fix):** the first live run exposed a gap in the wiring itself — the AI semantic pass flagged a coach narrative incoherent while all deterministic invariants were green; `_emit_overall` fires the alarm on that (semantic_bad) but `build_record` set `status` to the deterministic verdict only ("ok") → alarm-with-no-detail. Fixed: `status` mirrors `_emit_overall` (alarm when det-worst is alarm OR semantic incoherent) + `deterministic_status`/`semantic_incoherent`; redeployed. **LIVE VALIDATION (the headline):** first run after deploy, the semantic pass (grounded on canonical_facts) **caught a REAL content bug the platform is serving** — a coach narrative claims "RHR dropped to 53" vs authoritative `rhr_bpm=64`, plus self-contradictory weight numbers ("13.8 lbs/4 wk" AND "15.8 lbs/15 d" in one essay) → `coherence-overall` is a TRUE-POSITIVE ALARM; the auto-mode agent will surface it as needs-human, can't auto-fix. ⚠️ **OPEN follow-up:** that content bug is real + live (likely a stale served `EXPERT#` record or a coach ignoring grounding) — decide re-run ai-expert-analyzer (operational, likely clears it) vs grounding-prompt fix. **CI LESSON (recurring):** #250/#251 main runs went RED on the ENFORCED **ruff I001** gate — pre-existing since Phase-2 #246 (`coach_state_updater` measurable_metrics import un-sorted), masking Tests/Plan/Deploy on every push; the local flake8 fail-loud subset (`E9,F63,F7,F82`) does NOT run ruff isort → **run full `ruff check` before merge.** #252 folded the `ruff --fix` → main now GREEN through Plan (Deploy = manual prod gate, intentionally pending; cdk deploys done directly). New test `tests/test_remediation_agent.py`. **CONTENT-BUG FIX (#254):** the RHR-53 finding was a real correlated **hallucination** (Whoop 7-day RHR 55-64; 53 nowhere; canonical 64 right). Root cause — the Phase-3 backstop only LOGGED + the layer validator's RHR regex misses the "RHR" abbreviation. Fix (self-contained in ai_expert_analyzer, no layer dance): `_hard_canonical_contradictions(text, facts)` (RHR/recovery/HRV) + the backstop now **self-corrects** (regenerates ONCE on a hard contradiction, keeps only if it drops) + a no-invent-trends prompt rule. **PROVEN** (logs: training recovery-73→fixed, glucose, sleep). **PRECISION PASS (#255/#256/#257):** #255 Sentinel `facts_agreement` flags only if a wrong value is cited AND the canonical is cited NOWHERE (`_mentions_value` — kills trend false-fire) + email-subscriber CDK timeout 15s→30s (matches live, no deploy) + the detector adopts the same grounded-anywhere logic + the format prompt stops saying "lead with the number" unconditionally; #256 tightened the Haiku semantic prompt; **#257 (capstone) — `coherence-overall` now fires on the DETERMINISTIC invariant verdict ONLY**; the Haiku semantic pass is too unreliable to gate a daily-emailing alarm (lists items it concludes are FINE in `issues`, returns coherent=false) so it's **ADVISORY** (digest + `semantic_incoherent` flag; supersedes #252's semantic-drives-alarm). Result: Sentinel reports **OVERALL OK**; the CW alarm self-clears in ~24h (Maximum-over-1-day window). **POSTFLIGHT ASSET GUARD (#258):** while verifying, the new check FOUND the Sentinel **broken again** — the CDK asset glitch is **REPRODUCIBLE** (CDK skips re-uploading an asset whose hash-key already exists in S3 → a corrupt zip poisons every lambda on that hash; `cdk deploy --force` won't fix it; a 200 invoke is not proof — check for `body` / CodeSize). Fixed live via `aws lambda update-function-code`; added `session_postflight.check_asset_completeness()` (downloads each bundled-asset canary's zip, asserts imported root modules present). **OPEN follow-up from #252 is RESOLVED** (the content bug was re-run + the self-correcting backstop deployed). New tests: `test_grounding_self_correction`, `test_session_postflight`. **9 PRs #250–258, all deployed; main green; 0 open PRs; postflight fleet-consistent.** See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-28 (LONG session, 12 PRs merged #237–248, Matthew authorized the deploys — **coaching commentary-first + Phase-C backends, nutrition 24h-lag, and the Self-Management & Coherence Program Phases 1–3 (Phase 1 DEPLOYED + LIVE)**. **Coaching (#237–243):** `/coaching/` recut roster-first → **commentary-first** (The Read · By Coach · Scorecard · Team · lab-notes · Reader Q&A — what the board is *saying* about the data, not bios), then 3 Phase-C backends: **C-1** cross-week **experiment arc** (`generate_experiment_arc()` in ai-expert-analyzer, ADR-058 phase-filtered → `/api/experiment_synthesis`), **C-2** cardio-vs-lifts modality + per-muscle balance on the training coach (front-end off `/api/training_overview`), **C-3** **gradable predictions + a Scorecard** — root cause: 248/248 machine preds had `threshold=None` → 100 % inconclusive; fix routes metric+direction claims to the directional/EWMA evaluator (extractor emits `direction` + a deterministic keyword fallback) and `handle_predictions` now reads real `PREDICTION#` (was `OUTPUT#.predictions` → all-zero). **Decided AGAINST** backfilling the 296 legacy dead predictions (keyword-inference on old prose mis-directs ~⅓; the qualitative-cleanup alt freezes natural expiry — both worse than letting them expire). **Nutrition 24h-lag (#244, DEPLOYED):** MacroFactor is a manual end-of-day upload → **always ~24 h behind BY DESIGN**; the compute layer already read 'yesterday' but the serving/MCP/presentation layers assumed 'today' → "not logged today". Fixed every surface to treat the latest COMPLETE day as live (`as_of`+`today_pending` in nutrition_overview; vitals `page_freshness`; observatory_week complete-days; **7 MCP tools** default to the latest complete day; a by-design data-page caption; a coach guardrail). The `ai_calls.py` coach guardrail is a **layer module → rides the next layer rebuild**. **THE COHERENCE PROGRAM** (thesis: the platform proves it's ALIVE but not RIGHT — every silent-incoherence bug [predictions 100 %-inconclusive-for-weeks, 30-vs-86, arc 7-vs-3, `handle_predictions` all-zero] is *incoherent-but-green* from implicit contracts): **Phase 1 (#245, DEPLOYED+LIVE)** — `life-platform-coherence-sentinel` (daily 10:45 AM PT, read-only) runs 5 pure invariants (`lambdas/coherence_invariants.py`, each unit-tested by **replaying a past outage**) — prediction-health · computed-coherence · canonical-facts cross-surface agreement · endpoint non-degenerate shape · cross-surface count-agreement — emits `LifePlatform/Coherence` → the `coherence-overall` digest alarm, plus a **budget-gated Haiku semantic pass**. **Its first live run FOUND REAL BUGS** (the semantic pass caught coaches inventing weight-loss numbers + the protein-140/170/190 confusion still in narratives). **Hardened the deterministic facts check for PRECISION over 3 live iterations** (a noisy alarm gets ignored — weight plausibility band + time-word/thousands-separator/points lookaheads + tighter HRV-unit binding; the AI pass carries recall). **Investigated the prediction-health alarm → NOT a broken evaluator** (runs daily, correctly skips un-elapsed windows): legacy preds are dead machine-threshold=None (C-3 casualties, expire w/o deciding); the C-3 directional fix is deployed + **verified on real coach prose** (re-extraction → directional/steps); today's are still machine only because today's coach run *preceded* the deploy → directional accrue next run, grade ~2 wk. Refined prediction_health to count only **gradable** predictions → now correctly GREEN, alarms only on a real grading failure. **Phase 2 (#246/#247):** `measurable_metrics.py` (one un-driftable registry — MEASURABLE_METRICS *derived* from METRIC_SOURCES; kills the 504-inconclusive drift) + `canonical_facts.py` (one facts schema + units; `_load_canonical_facts` builds via it; a **producer-contract test** asserts daily_metrics_compute writes every field — kills the 30-vs-86 class). Both behavior-identical. **Phase 3 (#248):** **clobber guard** in `sync_site_to_s3.sh` (blocks a full-package sync when origin/main has site/ commits the checkout lacks — the parallel-branch clobber; `ALLOW_STALE_SITE=1` overrides) + `deploy/session_postflight.py` (layer uniformity + config drift; ran live, **found a real pre-existing drift: email-subscriber timeout cdk=15 s / live=30 s** — open `cdk deploy`/align decision). Deferred the scheduled config-drift *workflow* (can't run in a lambda; covered by postflight + CI Plan). **Phase 4 (self-healing eyes on content) NOT started** — touches the sensitive remediation auto-merge/kill-switch; the coherence-overall alarm may already be in the agent's alarm triage. New shared modules (`coherence_invariants`/`measurable_metrics`/`canonical_facts`) are **bundled with the lambdas/ asset, NOT the layer** (no layer dance). New tests: coherence_invariants · coherence_sentinel · measurable_metrics · canonical_facts · prediction_gradability. ⚠️ Open follow-ups: the Sentinel adopts `canonical_facts.build_canonical_facts` (closes the grounding↔detection loop, now both on main); email-subscriber config drift; the `ai_calls.py` nutrition guardrail rides the next layer rebuild. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-28 (accuracy remediation **Phase 3 + 4b — COMPLETE, DEPLOYED, live-verified**; the 5-phase truth-audit program is now fully done. `main` post-#227, layer **v91**, Matthew gave express in-session deploy permission. **Phase 3 (the canonical-record fix):** `daily_metrics_compute` stores the chosen day's recovery_pct/hrv_ms/rhr_bpm + protein_g_avg/target/floor on `computed_metrics` (units in the field names); `daily_brief`+`ai_summaries` read ONE `primary_whoop` (today-if-finalized else yesterday) for vitals+recovery+readiness+narrative → kills the 30-vs-86 split; `ai_expert_analyzer._load_canonical_facts()` injects an AUTHORITATIVE FACTS block into every coach + integrator (intake 140.7 labelled apart from target 190/floor 170, HRV pinned ms) + a `validate_ai_output(health_context=facts)` grounding backstop; nutrition target from facts not hardcoded 190. **Live-verified:** computed_metrics 2026-06-27 = recovery 30 / hrv_ms 25.18 / protein_g_avg 140.7; regenerated coaches → integrator+nutrition "140", labs "140.7g against a 170g floor" (170 now correctly the floor, not hallucinated intake), `/api/coach_analysis` cites 140+170 **not** 190 — the 140/170/190 contradiction is gone. **Phase 4b (site):** paused-supplement badge+dim, autonomic caption gated on HRV-up, honest rate-tempo span labels ("13-day" not a fake "30/90-day"), genesis-anchored chronicle labels (Prologue·Part I/II vs Week N — `posts.json` had shipped two "Week 1"; the raw `week` also collided the list-selection id → now id=unique post date). The 5th medium ("/story/journal Nothing published") was a **verified false positive** (that tab is Matt's own blog, `blog.json` genuinely empty). `accuracy_audit.py --live`: **0 HIGH**. **Deploy:** layer dance published **v91**, redeployed Compute/Email/Operational **+ Ingestion+MCP** (fleet uniform 70/70 — the Plan layer-consistency gate needs uniformity, same lesson as the 2026-06-24 v89 stall); site sync for 4b. **CI lesson (again):** #220 (Phase 1) had left main's ENFORCED **ruff** gate red (`site_api_ai_lambda.py` import-sort) — masked, silently skipping Unit Tests/Plan/Deploy on every main push; surfaced by #226, cleared by **#227**. Verify the *main* CI/CD run is green after a merge, not just PR checks. **Privacy:** deleted leaking at-rest chronicle records `DATE#2026-04-07` + `DATE#2026-03-10` (Layne-Norton fabricated-quote twins, tombstoned cycle-3, not live); left the two "Goggins cycle" metaphor records (legitimate reference, nothing live). **Did NOT** re-send the daily-brief email (the day-selection is unit-tested + the canonical record verified) or run the full 50-agent `/accuracy-review full` (verified the 15 findings directly). New `tests/test_phase3_grounding.py`. Memory `project_truth_audit` updated to COMPLETE. **Follow-up RESOLVED (next-session continuation, same 2026-06-28, #230 merged `5de962f0` + deployed; Matthew gave express in-session deploy permission again):** greening the ruff gate had **unmasked the gating Visual+AI-vision QA** (skipped since #220 behind the red lint), which then failed on ONE **pre-existing** v5-era bug — **Home `/` 16px overflow at 390px** (also 31px at 768px). Real root cause (bisected + browser-verified, NOT a content element or inline CSS as first guessed): the decorative **`.hero::before` warm-bloom** in `site/assets/css/story.css` (`inset: -6% -4% auto -4%`) bleeds the glow ~4% past each side; at full-width mobile the right bleed pushed document width. `body{overflow-x:hidden}` never contained it because visual-QA measures the `<html>` box (`documentElement.scrollWidth`). Fix = **`overflow-x: clip` on `.hero`** (one line; `clip` keeps overflow-y visible so the bloom still bleeds DOWN into the arc, no scroll-container/sticky side-effects). Deployed via `sync_site_to_s3.sh`; live overflow **0 at 360/390/414/768px**, `visual_qa.py --page /` 1 passed, and the red `ci-cd.yml` run's Visual-QA job **re-ran → success → whole pipeline green**. Memory `project_home_overflow_followup` → RESOLVED. **Main CI now fully green (all jobs).** See `handovers/HANDOVER_2026-06-28_AccuracyPhase3_Done.md`. Prior session below.)

**Prior verified:** 2026-06-27 (v5 "Coherence" redesign **shipped + reconciled**, then a privacy-clean **traffic-measurement** pipeline built & **deployed live**; `main` @ `431ba4cd`, **suite green (2052 passed), deployed + live-verified** (Matthew authorized the two infra deploys: "you run them now"). **The redesign** (the four-doc v5 brief at the top of this index — `PLATFORM_NORTH_STAR` / `SITE_MAP_AND_INTENT` / `DESIGN_SYSTEM_V5` / `SITE_UPLEVEL_PLAYBOOK`) split Evidence into **Data / Protocols / Method** pillars on a base-aware archive engine, added a 5-door nav, a motion/interaction layer, **zero axe a11y violations** (AA contrast via light ember `#A34E13`), **CLS 0.4→0.17** (font preloads), OG/SEO, and surfaced `/ask`. **Traffic digest (ADR-095):** `lambdas/operational/traffic_digest_lambda.py` (cron Mon 9 AM PT) parses **CloudFront standard access logs** → weekly email of views / unique / returning / top pages / referrers; **IPs sha256-hashed in memory then discarded** (a test asserts no raw IP survives), bots/assets/api/non-200 filtered. New CDK in `LifePlatformOperational`: bucket `matthew-life-platform-cf-logs` (**BUCKET_OWNER_PREFERRED** so CloudFront's log-delivery account `c4c1ede6…` can be granted the write ACL — the grant that silently fails if ACLs are off; 90d lifecycle, RETAIN). **Both deploys live-verified:** `cdk deploy LifePlatformOperational` (clean diff — new role/bucket/fn only, **no existing IAM modified**; the `[~]` Lambda churn was the benign shared-`Code.from_asset` re-hash), CloudFront standard logging **enabled** on dist `E3S424OXQZ8NBE` → bucket prefix `cf/` (delivery account holds FULL_CONTROL ✓), Lambda smoke-tested (clean `no traffic` no-op on the empty window). **Key discovery — main was behind production AND red:** #216's squash merged only **15 of 24** v5 commits, so the tail (a11y/CLS/SEO/briefing-docs/bindings-fix) was **deployed live but never reached main**, and `test_site_review_bindings` had been **failing on main since #216** (it had the new `/protocols//method` QA pages but not their bindings). **#217** reconciles main to the live state via a conflict-free net-delta commit (`git reset --hard origin/main && git checkout <tip> -- . && commit`) — main **1 failed → 2052 passed**. **Accuracy note (this wrap):** the digest's `alarm_name` was a **dead param** (`alerts_topic=None` → `create_platform_lambda` creates no alarm; same trap as key-rotator) → removed it; alarm count correctly stays **51** (no false alarm claim). New memories: `project_traffic_digest_measurement` (+ the CF-logging ACL gotcha), `feedback_squash_merge_drops_unpushed_commits`. See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-24 (inbox-noise triage → **CI-health excavation** + Strava/alarm fixes; `main` @ `c91add72`, **Lint + Unit Tests green; deployed + live-verified** (Matthew authorized the deploy this once). The "Run failed: CI/CD" email stream was **one red gate masking three more** — CI's Lint job runs gates *sequentially* (flake8 → black → ruff → mypy) and `Unit Tests` `needs` Lint, so a single unformatted file stopped everything before ruff/mypy/tests ran; each fix exposed the next layer. **6 PRs merged:** **#208** removed 2 dead one-off scripts (`deploy/_publish_week1.py`, `_prologue_rewrite.py`, accidental #186 commits) failing **black** — the whole email source; **#211** fixed 9 latent **test** failures unmasked once Unit Tests could run (cost_governor ×7: test wasn't aware of the June-2026 tier headroom #169 [auto-reverts Jul 1] → autouse fixture pins normal thresholds; coach_panel_podcast sensitivity: refit #166-182 made the gate AI-adjudicate, test expected pre-refit auto-hold AND called live Bedrock → rewrote to new contract + mocked; hevy_common: `BUCKET` binds `$S3_BUCKET` at import time → order-dependent → pinned); **#212** cleared 14 **ruff** violations (12 I001 import-sort via `--fix` with sys.path/`# noqa: E402` guards preserved, F841 dead var, S324 sha1 → `usedforsecurity=False`); **#213** stubbed `routine_title.build_title_context` in 2 hevy `dry_run` tests that hit DynamoDB → **`NoCredentialsError` in CI** (passed locally on ambient creds — found by running the suite **creds-blanked**). Full suite creds-blanked: **2045 passed, 0 failed**; live CI Lint+Test both **success** (run 28125725760). **Two real platform bugs also merged:** **#209** Strava late-sync drop — the ADR-092 reconciler caught 5 afternoon walks (Jun 22-23) never reaching DDB: gap detection is **presence-based** (`_find_missing_dates`; `refresh_today` only re-pulls today), so walks that **sync late** land on an already-present date and are stranded → `refresh_trailing_days=3` re-fetches the trailing window, `transform()` rebuilds the day from all activities, **auto-heals the 5 gaps on next run** (distinct from the #180 tz-window fix); **#210** ai-tokens alarm 33333 → 150000 (steady-state ~59k output tokens/day; not a cost issue — $75 guard + `ai-daily-spend-high` intact). **DEPLOY DONE (Matthew authorized "you run it this time"):** merging #209 had surfaced a *pre-existing* blocker — layer **v89 published Jun 22** (hevy-commit-hardening) + constants bumped to 89, but the **15 consumer Lambdas were never redeployed to attach it** (on v87/v88); the `Plan deployments` layer-consistency gate (also masked) failed → blocked #209's auto-deploy and was the new "Run failed" email source. Resolved by `cdk deploy` of the 5 changed stacks (Ingestion/Compute/Email/Operational/Monitoring, `--require-approval never`; `cdk diff` had verified **no IAM changes**). NOT `build_layer.sh` (v89 already published — consumers just needed re-attaching). **Live-verified:** all 15 consumers + strava on **v89** (0 behind → Plan gate now passes), `ai-tokens-platform-daily-total` → **OK** (threshold 150000), and a manual Strava ingestion **auto-healed the 5 walks** (Jun 23 record 2→5 activities, Jun 22 dedup-correct; reconcile `missing_count 0`). `ingest-reconciliation-strava` self-clears within ~24h (its daily-`Maximum` window still holds the pre-fix datapoint). The next push will show CI fully green end-to-end. New memory `reference_ci_masking_and_creds`. **Continuation (same session, +2 PRs = 8 total):** **#214** Whoop late-sync trailing-refresh (`refresh_trailing_days=2` — same sub-record drop class as Strava; **DEPLOYED + live**; per-source policy pinned by test, Garmin deliberately excluded as 429-fragile); **#215** chronicle `_FALLBACK_ELENA_PROMPT` privacy fix (real public figures Attia/Huberman/Norton/Walker → fictional Reyes/Nakamura/Webb/Park; merged, **`LifePlatformEmail` deploy STAGED, low-urgency** — fires only on S3 config-load failure); Dependabot #198/#200 merged + #199 closed (0 open PRs). Verified the `get_freshness_status` interior-gap parity was **already done** (B3, 2026-06-21 — stale follow-up, don't re-chase). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-21 (Strava walk-ingestion bug → DI-2 silent-gap detection; PRs #180 + #184 merged + deployed + live-verified, `origin/main` @ `b519257d`. **#180 root cause:** `strava_lambda.fetch_day` fetched a same-day **UTC** window but keyed records by the activity's **local** date (`start_date_local`) — an evening-PT walk (≥17:00 PT → UTC start on the *next* day) fell just past its own day's window AND was rejected by the local-date filter on the next day's, so it dropped through both passes **silently** (no error; freshness stayed GREEN because midday Hevy lifts kept the high-water mark advancing). Fix = bracket the UTC fetch window **±1 day**, keep the `start_date_local` filter (tz-agnostic, no double-count, no framework change). Backfilled Jun 15–19 → **5 distinct walks** Jun 14–20; the "6th" (Jun 16 0mi GPS-drop, 17s from the real 3mi walk) is correctly `_dedup`-collapsed, not a drop. **#184 (ADR-092) — why nobody noticed:** *every* freshness/health check (`freshness_checker`, `get_freshness_status`, `qa_smoke`, ingest-liveness) reads **only DDB** → blind to a gap behind the high-water mark. Added **(A)** daily Strava **source-of-truth reconciliation** *inside the existing lambda* (`{"reconcile":true}`, trailing-14d API-vs-store diff, dedup-aware so the GPS-drop twin isn't a false gap → `LifePlatform/IngestReconciliation::MissingActivityCount` → digest alarm `ingest-reconciliation-strava`, `cron(20 17 * * ? *)`) — the ONLY thing that catches a deterministic silent drop (a trailing-refresh would re-drop it); **(B)** **interior-gap detection** in `freshness_checker` for `DAILY_SOURCES`={whoop,apple_health,eightsleep,habitify} only (sparse sources excluded so rest days don't false-fire → `LifePlatform/Freshness::InteriorGapCount` → digest alarm `freshness-interior-gap`). Pure cores unit-tested (`test_ingestion_transforms.py` + new `test_freshness_interior_gaps.py`). **Live-verified:** reconcile → api14/store13/missing0; freshness scan clean; both metrics emitting 0.0; both alarms OK. ⚠️ `cdk deploy LifePlatformIngestion` re-uploaded the shared `Code.from_asset("../lambdas")` bundle to ALL ingestion fns (same handler code — benign reconciliation to main). Docs synced: **51 alarms** (+2), ADR-092. Open follow-up: `get_freshness_status` MCP tool is still high-water-mark only; reconciliation for Whoop/Garmin is a separate opt-in. Panelcast work from the earlier session is safe on `reconcile/panelcast-quality` (local+origin). See `handovers/HANDOVER_LATEST.md`. Prior session below.)

**Prior verified:** 2026-06-21 (long chronicle-truthfulness + podcast-quality session — PRs #166–#182 merged + deployed, `origin/main` reconciled. **Chronicle:** the 2 origin chapters are now a pre-genesis **Prologue** (Part I "The Body Votes First" June 7 / Part II "The Empty Journal" June 11), rewritten to drop the stale "Week 1 / platform went live / 302→301" framing while preserving the prose. Week numbering is **genesis-anchored** (June 14) so today's installment is **Week 1** ("The Week That Decided to Begin", published) — killing the "9 lbs in three weeks" error (it was one week). `publish_to_journal` labels by date-vs-genesis (Prologue vs Week N); URLs stay sequential (`week-NN`) so links don't break. New **SUBSTANCE & VICE PRIVACY (ABSOLUTE)** chronicle-prompt rule — never name marijuana/porn even from journal data (caught + deleted a draft that named marijuana) + nutrition-logging-integrity (low cals = real deficit, not "not logging") + cold-reader grade-context rules. **Privacy:** Layne Norton (real person) → fictional **Dr. Marcus Webb** across the board config, both `web/` board-ask lambdas, the measurements MCP tool, and the chronicle interview-routing prompt. ⚠️ the legacy `_FALLBACK_ELENA_PROMPT` still carries real surnames (Attia/Huberman/Walker/Norton) — only fires if the S3 board config fails to load; tracked follow-up. **Podcast (panelcast) — major refit to Matt's bar (the transcript must pass a read-aloud Turing test):** voice now sourced from the persona registry (`config/personas.json` `tts_voice`), fixing gender-flipped voices (Dr. Marcus Webb was on a female voice) — regression test `tests/test_panelcast_voice_gender.py`; new `_WEEKLY_RUBRIC` read-aloud QA gate + **self-correcting revision loop** (feed the judge's exact failures back to the writer, ≤2 revisions, then human HOLD) = the autonomy mechanism; sensitivity gate now **AI-adjudicates crisis-vs-backstory** (was auto-holding strong weeks that merely referenced past grief); editor judge robust (retry + fail-OPEN on bad JSON, not a hard HOLD); ER-03 digit-matching **removed from the weekly gate** (over-dropped spoken numbers → holey transcripts/unanswered questions — grounding is the LLM judge's job now); `_published_posts` reads the live `generated/journal/posts.json` (was the dead `site/chronicle/posts.json` → week-drift). **Episode 1 published** ("Week 1 — Monday was a 49…", 4:28, Elena + Dr. Sarah Chen) after clearing QA via the revision loop — proper guest intro, grounded, gender-correct voices; Episode 0 (intro) also live. **Still human-in-the-loop (E)** — review 2–3 clean episodes before podcast autonomy. **SW:** JSON feeds (`posts.json`/`episodes.json`) are now **fresh-first**, not cache-first (new chronicle/podcast content was hidden until the SW version rolled); version rolled to `88b99f29`. Docs auto-synced (Tools 135, Lambdas 81). One-off publish scripts `deploy/_prologue_rewrite.py` + `deploy/_publish_week1.py` are untracked local records. See `handovers/HANDOVER_2026-06-21_ChroniclePodcast.md`. Prior session below.)

**Prior verified:** 2026-06-19 (EOD — long multi-stream session, PRs #152–#158 all merged; BENCH-1 also deployed live. **BENCH-1 cut benchmarking & regain firewall (ADR-089, PRIVATE):** new `episode-detect` Lambda (weekly Sun, reads FULL withings/strava/hevy history bypassing the ADR-058 phase filter) writes two cross-phase computed sources `weight_episodes` + `training_reference` (keyed like `computed_metrics`, no `phase` attr → survive resets); new `get_benchmark` MCP tool (`pace`/`episodes`/`maintenance`, tool #134, module `tools_benchmark.py`). Backfilled (29 episodes, **0 ever held**). Smoke: `get_benchmark(view=pace)` → `behind`, walk_gap 11.59, run_gate_ok False. **The work order's pasted ZigZag `turning_points` was broken** (`direction=0` records 0 pivots — verified); replaced with the standard ZigZag, reproduces validated values exactly. Two follow-up pace fixes (#157/#158): curve-edge clamp in `_proven_rate_at` + cross-phase regression rate in `_current_weight_and_rate` (the phase filter had left only post-genesis water-weight days → bogus 12.75 lb/wk). **⚠️ `cdk deploy LifePlatformCompute` reconciled the whole compute backlog** — the deferred **precompute readiness sleep 30→25** change is now LIVE (page-by-page QA still pending). Also merged: **Hevy per-type folders** (#154, `commit()` files routines into Push/Pull/Legs/Engine), **coaching calibration §4a** (#156, continuity-over-calendar + night-before planning + all-source pull + ruck edge case, docs-only PRIVATE), readiness date-integrity + email-noise reduction (#152). Garmin alarms deleted earlier (known-dead); `ingest-liveness-unhealthy` now red on **Strava** alone (402-path doesn't record a liveness attempt — open decision). Docs all in sync (Tools 134, Lambdas 81, layer v85). See `handovers/HANDOVER_LATEST.md` + the two earlier 2026-06-19 dated handovers (ReadinessDateIntegrity, InboxTriage_NoiseReduction). Prior session below.)

**Prior verified:** 2026-06-18 (PM — Episode 0 voice-bleed RCA + CloudFront-path bug + git reconciliation. **Episode 0 "Eli ends as Elena Voss" actually fixed:** the live cut had been voiced `02:02` UTC, **28 min before** the deterministic Elena-sign-off fix (`fd5d69ed`) was committed (`02:30`) — so the morning handover's "live cut already meets it" was wrong. Fixed by deploying `LifePlatformEmail` (carries the fix + the `cloudfront:CreateInvalidation` grant) then re-voicing via `aws lambda invoke --payload '{"intro":true}'`; close is now `…Eli → Elena → Elena(INTRO_SIGNOFF)` so Gemini voice-bleed is Elena→Elena. **CloudFront-path bug (fixed in #151):** `_invalidate_cdn` invalidated `/generated/panelcast/*` (the S3 **key** prefix) but CF matches the **viewer path** `/panelcast/*` (the `generated/` prefix is stripped at the edge) → it never busted the edge cache. Now derives the public path; ships next `LifePlatformEmail` deploy. Manual unblock: `create-invalidation --paths "/panelcast/*"`. **Concurrent-invoke race:** the CLI's 60 s read-timeout retried the invoke 3× → 3 concurrent generations; last write won (`20:37:02`, a 5:46 cut, all artifacts consistent). New correct cut (5:46) ≈ old buggy cut (5:47) — **judge the ending by listening, not the duration timer**; next time use `--cli-read-timeout 0` / `--invocation-type Event`. **Git reconciliation:** **PR #151** = clean net-delta of all post-#150 live work off `origin/main` (57 files, 1 commit) — merge when CI green, then delete the old `feat/temporal-frame-honesty-2026-06-17` branch. Matt's coaching docs (accidentally on the reconcile branch) split to **`chore/coaching-docs-2026-06-18`** off main (his call: PR or keep in claude.ai). ⚠️ pre-commit hook bumps doc dates to 2026-06-19 and leaves them unstaged — cosmetic. Next: Matt's QA walkthrough. See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-18 (public-site overhaul + deep board profiles + build fingerprinting — all LIVE, driven by Matt's walkthrough. **Site fixes:** bio-age privacy (page + API — true age not derivable), lb/kg lb-first, workout "Set N", Evidence scroll-jump (desktop `scrollIntoView` fought manual scroll → mobile-only), nutrition panel (nested `d.nutrition` + field-name fix), challenges dedupe, vice-streak cards. **New panels:** discoveries surfaces REAL `ai_findings` (FDR correlations the API computed but never rendered); **Recent cardio** = merged Strava+Whoop `cardio_sessions` with mi·km; habits last-7-days color grid; supplement "evidence ↗" links; experiment source attribution. **Evidence IA:** split 19-topic "Credibility & the machine" → "How it holds up" (7) + "The machine" (12); cycles/post-mortems/survival → footer-tier "The Reset Log" (`scripts/v4_build_evidence.py` `_REGROUP`). **Architecture diagram** (inline-SVG) on `/evidence/build/`. **Deep board profiles** on `/story/coaches/`: `_character()` + `_working_hypotheses()` enrich `/api/coach` (config + DDB, zero new AI). **Build fingerprinting:** `sync_site_to_s3.sh` stamps git-SHA+UTC → `/version.json` (no-cache) + `<meta name=build>` + footer stamp (Cockpit/Evidence) + rolls SW `VERSION` (kills stale-cache "v451 vs v452"). Verified live: `/version.json`==HEAD. ⚠️ **PR #150 squash-MERGED (earlier batch); branch is 24 ahead — post-#150 site work `c16aa511..9850b1ad` is LIVE but needs a fresh PR off `origin/main` (cherry-pick, don't re-PR this branch).** Next: Matt's QA walkthrough → batch the feedback. Board-page intro font still flagged (needs visual pinpoint). See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-17 (cost-telemetry + Hevy-title + reliability/noise session. **Merged:** #137/#138 (cost-governor trailing-AI projection + CE cadence — deployed; budget tier was a phantom tier-2), **#142** (G1/G2: meter AI spend at the `bedrock_client.invoke()` chokepoint per-feature + `EstimatedCostUSD` + `ai-daily-spend-high` alarm), **#143** (Hevy `Phase-Type-N-Y` renderer is authoritative — performed-derived per-phase N + reset-epoch Y, dry-run truthful, `force_title` lockdown; **ADR-088** supersedes the 2026-05-31 ADR-067 amendment). **Open PRs:** **#144** (project non-AI from a trailing window too — finishes the governor honesty fix), **#145** (Hevy type via exact `hevy_routine_id` link), **#146** (`black`-format 9 files — CI's **ENFORCED black gate** was reddening main on every push = the bulk of the email noise), **#147** (ai-expert-analyzer 120s→600s timeout [was dumping failed scheduled-events into the DLQ → 3 alarms], **G1 PutMetricData IAM gap** fixed in `_compute_base`, Garmin auth alarm URGENT→digest). **Email-noise RCA:** CI-spam=black gate (#146); DLQ=ai-expert timeout (#147, DLQ purged live); budget alerts=expected June outlier. **Garmin RCA:** Garmin 429-blocks server-side OAuth2 refresh (datacenter-IP crackdown) → re-auth only holds ~48h → **best-effort degraded; do NOT re-auth expecting it to stick**; sleep/HRV/recovery covered by Whoop+Eight Sleep. **Secrets consolidation = NO-GO** (verified: easy wins already in `ingestion-keys`; rest is rotating-OAuth or actively-read; log-retention already 30d). Panelcast wk5 HELD = correct fail-closed on genesis Day-Zero thin data (self-resolves as cycle-4 data accrues); HOLD-path IAM grants exist in code but need `cdk deploy LifePlatformEmail`. Layer **v85**. Full suite 1884 (2 pre-existing live-AWS integration failures). See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-16 (AM — cockpit circadian tile + cost investigation. #136 **cockpit `/now/` "tonight's forecast" tile** (renders `/api/circadian`, fail-quiet, present-tense) — merged + live. An AWS ">$75 forecast" prompted a full cost dig: **the platform is NOT over budget** — true steady-state ≈ **$60/mo** (AI ~$29 = Haiku $22 + Sonnet $9 trailing-7d; CloudWatch $9; Secrets $7.6 = 18 secrets' storage; CE $4.6; tax). The real defect was the **cost-governor crying wolf** — it projected $115/mo off a lumpy MTD-average AI rate and auto-paused website AI at tier 2. **#137** fixes it (project AI from a trailing 7-day window; pure `_project_month_end` helper + 3 tests; safe — only reduces false escalation, actual-mtd cap/early-month guard/tier-3 hard-stop untouched); **#138** trims the governor's own CE polling 4h→8h. **Dropped on inspection:** Tier-3 AI cuts (unneeded), the alarm-prune (the 6 ingest-consecutive-failures alarms catch hard-failures, complementary to #131's graceful-skip coverage — not redundant), caching re-enable (D-01). ⚠️ **#137 + #138 OPEN, deploys pending** — `life-platform-cost-governor` + `LifePlatformOperational`; tier stays 2 (website AI paused) until #137 deploys. Secrets consolidation available (~$2-3/mo) but deferred — not urgent. Full suite 1860. See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-15 (PM — elite-review fix sprint: **6 PRs #129–#134** from the 89-finding review, each re-verified before edit. #129 **surfaced** two compute outputs never exposed — `/api/circadian` (predictive 0–100 score) + `/api/sleep_reconciliation` (Whoop+Eight+Apple merged) + Evidence→Sleep panels; #130 **public-write hardening** (vote `catalog_id` validation, checkin date-dedup, finding idempotency); #131 **fleet-wide ingestion auth-liveness** (`auth_breaker` emits `IngestAuthHealthy`, alarm `ingest-auth-unhealthy-24h` live+OK — closed the notion/dropbox silent-death gap); #132 **AI-endpoint hardening** (history-replay gating + zero-width/obfuscation scrub); #133 **circadian DST fix** (hardcoded UTC-8 → DST-aware `ZoneInfo`, was skewing the score #129 surfaces); #134 **nudge+finding rate limits → DDB**. **5 findings dropped/bounded** on re-verify (EventBridge-DLQ, weekly-correlation, ACWR-parse, SIMP-2-already-covered, short-term-scrub-residual). ⚠️ **#133 + #134 deploys pending** — `circadian-compliance` + full `web/` site-api. Full suite 1852. See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-15 (wearables reliability + privacy purge + deep elite review — PRs #124–#127. **Strava 402 graceful-degrade** + **Garmin auth-liveness alarm** (`GarminAuthHealthy`/`GarminTokenDaysLeft`, both live) closed a silent-ingestion-death gap (both pipes were dead ~2–5 weeks); Garmin re-authed; **Garmin→Strava auto-upload ON** as the durable activity backstop. A **multi-path vice-catalog privacy leak** was sealed across site + API + seed + a **git-history rewrite** (force-pushed `main`+tags; GitHub Support GC of the old SHA across all 123 PRs **in-flight — option B**); **ER-05/06** PII guard added (`deploy/pii_surface_guard.py`, fail-closed in `sync_site_to_s3.sh`). A **558-agent DEEP elite review** produced **89 verified findings** (`docs/reviews/ELITE_REVIEW_2026-06-15.md`); fix batch 1 = silent-failure `return 500`→`raise` (#126) + pipeline-health IAM describe-only (#127); the "cost-cache leak" was a **verified false positive** (D-01 intentional). Remaining findings = a re-verify-before-fixing backlog. See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-14 (genesis day, cycle 4 — baseline **re-anchored to the real genesis weigh-in (314.52)** via `restart_pipeline.py --genesis 2026-06-14 --apply`; the podcast is now an **autonomous, QA-gated weekly show** (`coach_panel_podcast_lambda`: deterministic gather → Sonnet write (bet/Split/scoreboard format) → Haiku editor → **fail-closed Compassion & Safety gate** → publish-or-HOLD; Fri 17:00 cron, 900s; `series_state` in DDB `PANELCAST#`; **ADR-087** records the audio-realism ceiling) + a **Panel bet-ledger** (`/api/panel_ledger`); **Episode 0** finalized (Elena interviews Dr. Eli Marsh, single-pass Gemini, deterministic cold-open, numberless); **installable Cockpit PWA** (`site/sw.js`, iOS meta, manifest) + a **mobile bottom door-bar**. PRs #110–#119. See `handovers/HANDOVER_LATEST.md`.)

**Prior verified:** 2026-06-13 (evening — Evidence-catalog restore after the cycle-4 reset purge (supplements/experiments/challenges/habits read from durable `config/`; habits sourced from Habitify + vice-filtered), PR #110; podcasts off Polly → Google **Chirp 3: HD** + new **"The Panel"** show (PR #109) and **Episode 0 as a single-pass two-person Gemini 2.5 interview** (`gemini_tts.py`); new **Dr. Eli Marsh — Principal Investigator** lead persona above the 8 coaches (non-operational, PR #111). Two Google keys in `life-platform/google-tts`: `api_key`=Cloud TTS (managed project), `gemini_key`=Gemini (personal account).)

**Prior verified:** 2026-06-08 (v8.5.0 — 2026-06-08 experiment reset run (genesis 2026-06-08, baseline 311.62, cycle 3); A-grade CI gates ADR-080 (mypy tier-1 + coverage floor + size gate); `ai_calls` god-module split; **all CLI Lambda orphans adopted into CDK — `list-functions ∖ CDK = ∅`, ADR-081**; `/og` handler bug + Whoop refresh-race + qa-smoke genesis-awareness fixed; CI now flags undeployed Lambda config changes)
