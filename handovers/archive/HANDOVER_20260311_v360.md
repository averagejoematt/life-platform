# Life Platform — Handover v3.6.0
**Date:** 2026-03-11
**Version:** v3.6.0
**Session focus:** CI/CD hardening — Technical Board consultation + pipeline fixes

---

## What Was Done This Session

### Technical Board consultation on CI/CD
Full 12-member board convened to assess the existing GitHub Actions pipeline (built at v3.2.1, MAINT-4). Board diagnosis: pipeline is architecturally sound but not being used as the primary deploy path, and has 6 specific fixable gaps. All high-priority fixes implemented.

### Changes to `.github/workflows/ci-cd.yml`

1. **Syntax check step** (Lint job) — `python3 -m py_compile` on all `.py` files. Catches broken syntax that flake8 misses.
2. **`lambda_map.json` validation step** (Plan job) — fails if any source file or layer module in the map doesn't exist in the repo.
3. **`aws lambda wait function-updated`** — replaces `sleep 10` after MCP deploy and each individual Lambda deploy.
4. **Layer version verification** — after shared layer rebuild, checks every consumer Lambda is on the new version. Would have caught v3.5.5 EmailStack incident.
5. **Structured smoke test + canary** — parse JSON and check `status` field instead of grepping raw output for "error".
6. **`notify-failure` job** (new Job 5) — `if: failure()` posts to SNS `life-platform-alerts` with commit SHA + run URL.

### Changes to `ci/lambda_map.json`
- Replaced stale `lambdas/freshness_checker.py` with `lambdas/sick_day_checker.py` in `skip_deploy`
- Bumped `_updated` to v3.6.0

---

## Platform State

- **Version:** v3.6.0
- **CI/CD:** Pipeline hardened. All 6 Board-identified gaps closed.
- **Hardening:** 34/35 tasks complete (SIMP-1 open, ~2026-04-08)
- **Everything else:** Unchanged from v3.5.8

---

## One Manual Check Required

Verify the GitHub `production` Environment is configured in repo settings (Settings → Environments). If it doesn't exist, the manual approval gate on the Deploy job is silently bypassed. Takes 30 seconds to check.

---

## Immediate Next Actions (Resume Here)

1. **Verify GitHub `production` Environment exists** in repo settings — 2 min
2. **Google Calendar integration** — highest-priority unbuilt feature (Board rank #2). ~6–8 hrs.
3. **Architecture Review #7** (~2026-04-08) — run `python3 deploy/generate_review_bundle.py` first
4. **SIMP-1 + COST-2** (~2026-04-08) — 30 days of EMF data ready for tool rationalization
5. **`life-platform/api-keys` permanent deletion** — 2026-03-17 (scheduled, recovery window closes)

---

## Key Technical Reminders

- **Deploy path going forward:** `git commit + push` → watch GitHub Actions → confirm green. Not `bash deploy/deploy_lambda.sh` for normal changes.
- **Emergency escape hatch:** `workflow_dispatch` → "Deploy ALL Lambdas" toggle for manual full deploy.
- **Layer v4 ARN:** `arn:aws:lambda:us-west-2:205930651321:layer:life-platform-shared-utils:4`
- **CDK:** `cd cdk && npx cdk deploy <StackName> --require-approval never` (still manual — not in CI/CD pipeline)
