# Handover: Git/GitHub Setup
**Date:** 2026-03-06
**Version:** v2.80.0

## What Was Done

Established proper Git hygiene for the platform:

- Confirmed existing `.git` repo (initialized at v1.8.0, never pushed since)
- Added `*.tar.gz`, `datadrops/`, `lambdas/backup_*/`, `lambdas/dashboard/data.json`, `lambdas/dashboard/clinical.json`, `lambdas/*.py.backup*`, `lambdas/*.py.broken` to `.gitignore`
- Validated no sensitive health data (PDFs, Apple Health XML, ECGs, clinical records, photos) was staged
- Rotated exposed GitHub token (was visible in terminal output)
- Set up SSH key authentication (`~/.ssh/id_ed25519`) — added to GitHub account
- Changed remote URL from HTTPS token to SSH: `git@github.com:averagejoematt/life-platform.git`
- Committed and pushed full platform catch-up: 555 objects, 1.91 MB
- Uninstalled `gh` CLI (not needed — SSH push works directly)
- Added Session Close Checklist to `docs/RUNBOOK.md`
- Updated memory to include `git add -A && git commit && git push` as final step of every session

## Session Close Ritual (going forward)

```bash
git add -A && git commit -m "vX.XX.X: <what changed>" && git push
```

## What's Clean in GitHub

All code, docs, deploy scripts, handovers, mcp modules, lambdas committed.
Excluded: datadrops (health exports, PDFs, photos), dashboard data JSON, backup dirs, env files.

## Next Session

- Reward seeding (prerequisite for Character Sheet Phase 4 / Weekly Digest integration)
- Google Calendar integration
- Flip BRITTANY_EMAIL env var once Matthew approves output
