# Handover — 2026-02-22e

## Session summary
Fixed `search_activities` enriched_name bug. Deployed v2.3.1.

## What was done
**Bug:** `name_contains` filter in `search_activities` only checked the raw Strava activity `name` field, not `enriched_name`. Since renamed activities (Mailbox Peak, Machu Picchu, etc.) store their human-readable label in `enriched_name`, searches for them returned zero results.

**Fix (2 lines changed in mcp_server.py):**
1. `flatten_strava_activity()` — added `enriched_name` to the flattened dict so it survives extraction
2. `tool_search_activities()` — `name_contains` filter now checks `name OR enriched_name`

**Verified working:**
- `name_contains=mailbox` → Mailbox Peak – New Trail (2025-05-04, 10.43mi, 4025ft)
- `name_contains=machu` → Inca Trail Day 4 – Sun Gate & Machu Picchu (2025-06-27)

**Deployed:** v2.3.1 via `deploy_mcp.sh` from Matthew's terminal

## Current state
- MCP: v2.3.1, 30 tools, all sources live
- MacroFactor: still mock data — real export pending
- search_activities: fully functional including enriched names

## Priority backlog (unchanged)
1. **MacroFactor real data** — export CSV from app, drop in ~/Documents/Claude/macrofactor_drop/ (launchd auto-processes every 5min) or manual: `python3 backfill_macrofactor.py path/to/csv`
2. **Weekly Digest refinements** — week-over-week comparison, CTL/ATL/TSB, notable PRs, MacroFactor section
3. **Monarch Money** — financial pillar, `setup_monarch_auth.py` exists, no Lambda/tools built yet
