{
  "_purpose": "#3374 R1 — the committed cost-bearing-surface baseline. HAND-OWNED, deliberately NOT written by scripts/generate_platform_model.py: the generator derives the live counts into model/platform_model.json's cost_surface plane, and tests/test_platform_model_drift.py exact-pins this file against them. A PR that grows a cost-bearing population (a new schedule, alarm, EMF namespace, secret, or budget-guard AI feature) must bump the matching count HERE, in the same diff — that hand edit is the ratchet's deliberate act. A shrink must ratchet the count down the same way, so the baseline can never rot above the estate.",
  "counts": {
    "ai_features": 18,
    "alarms": 127,
    "emf_namespaces": 31,
    "schedules": 89,
    "secrets": 28
  },
  "measured_on": "2026-09-13",
  "_last_change": "#3741 (2026-09-13): alarms 126 -> 127 — recap-card-no-invocations-24h, the daily recap card's absence heartbeat. The card generator first took a dated EXEMPT on the heartbeat-completeness ledger, reasoning that a missing card is self-evident to its only consumer the same morning; that premise is not true yet (delivery is off by default and the distribution path is undecided), and an exemption whose premise is not yet true is indistinguishable from ‘someone would notice’. A standard CloudWatch metric alarm (~$0.10/mo) on an existing AWS/Lambda metric — no new namespace, no new schedule, no new function."
}
