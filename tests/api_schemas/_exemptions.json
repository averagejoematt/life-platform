{
  "/api/board_ask": {
    "category": "write-path",
    "reason": "POST-only AI panel endpoint; dispatched by a SEPARATE lambda (site_api_ai_lambda.py) — this router's ROUTES entry is a reservation placeholder only, never itself invoked"
  },
  "/api/board_question": {
    "category": "write-path",
    "reason": "POST-only — board follow-up turn (write)"
  },
  "/api/challenge_checkin": {
    "category": "write-path",
    "reason": "POST-only — records a challenge check-in (write)"
  },
  "/api/challenge_follow": {
    "category": "write-path",
    "reason": "POST-only — follow/unfollow a challenge (write)"
  },
  "/api/challenge_vote": {
    "category": "write-path",
    "reason": "POST-only — votes on a challenge (write)"
  },
  "/api/cohort_submit": {
    "category": "write-path",
    "reason": "POST-only — #1394 one-tap weekly cohort single-number submission (write)"
  },
  "/api/experiment_follow": {
    "category": "write-path",
    "reason": "POST-only — follow/unfollow an experiment (write)"
  },
  "/api/experiment_suggest": {
    "category": "write-path",
    "reason": "POST-only — reader-submitted experiment suggestion (write)"
  },
  "/api/experiment_vote": {
    "category": "write-path",
    "reason": "POST-only — votes on an experiment (write)"
  },
  "/api/membrane": {
    "category": "capture-failed",
    "reason": "#1679 (epic #1668 S11) — the bidirectional membrane dashboard's read-only endpoint, newly added and not yet deployed, so there is no live instance to capture a snapshot from. Takes no parameters. Remove this exemption and commit the real captured snapshot once this ships and `python3 deploy/capture_api_schemas.py` runs against the deployed endpoint."
  },
  "/api/nudge": {
    "category": "write-path",
    "reason": "POST-only — evening nudge action (write)"
  },
  "/api/replicate_certify": {
    "category": "write-path",
    "reason": "POST-only — #1393 engagement-ladder Replicator self-cert; deduped per source, increments the VOTES#ladder_replicator aggregate counter (write)"
  },
  "/api/ritual_log": {
    "category": "write-path",
    "reason": "GET-verb but MUTATING: one-tap evening-ritual write gated by a signed HMAC token (ADR-124, #769) — no read-only variant exists; there is no safe way to probe it that doesn't attempt a write"
  },
  "/api/social_context": {
    "category": "requires-path-param",
    "reason": "#1674 — newly added, not yet deployed, so there is no live instance to capture a snapshot from. Requires ?route=training|mind; a sample query-param entry is already wired in deploy/capture_api_schemas.py's DYNAMIC_PARAM_LOOKUP (\"training\", no live lookup needed). Remove this exemption and commit the real captured snapshot once this ships and `python3 deploy/capture_api_schemas.py` runs against the deployed endpoint."
  },
  "/api/submit_finding": {
    "category": "write-path",
    "reason": "POST-only — reader-submitted finding, writes to S3 (write)"
  },
  "/api/verify_subscriber": {
    "category": "requires-path-param",
    "reason": "requires ?email= — a real subscriber address; probing an arbitrary address would leak that address's subscription status (privacy), and there is no safe synthetic value"
  }
}
